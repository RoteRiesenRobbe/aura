# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current Migration Status

- **Last completed:** **Mob-depth chunk 7 — TAUNT / ANTI-TAUNT EFFECT TYPES — DONE + VERIFIED IN-GAME 2026-07-11 ("works as intended"; full backend suite green 22 pkgs, binary rebuilt, tsc + webpack green, TDD red-first — 13 new tests; ZERO wire changes, one frontend file touched (Skills.ts entries only); committed by the user's workflow — no autonomous commit).** **Decisions (user, plan-first): (1) taunt = FORCE-TO-TOP via a new seam (not a large fixed credit); (2) NO separate target lock in v1 — pure threat manipulation, retention does the rest; (3) anti-taunt = a player "Fade" cooldown that drops the caster's OWN threat; (4) both delivered as cooldowns; (5) Fade = single-entry removal.** **Accepted-as-v1 caveats:** the taunted mob's AURA keeps hitting nearest en route (aura targeting is selector-based, not threat-based — converges on close); taunt/anti-taunt on a COMPANION no-ops (followers never read their own threat table); attribution scoped to **player casts only** (no summon-taunt). **Mechanics — two mob threat seams (`model/mob/mob.go`):** `ForceThreatToTop(source, margin)` reads the current max living threat and sets `source = max + margin` (strictly exceeds → wins retention's lower-ID tiebreak — the handoff's "exceed, don't match"); empty table → source becomes the sole entry at `margin`; nil/allied/dead/`margin<=0` dropped (the `noteThreat` gates). Because the taunter lands ON the table, the chunk-6.6 `MayHarm` gate (`aggroSet ∪ HasThreat`) grants the mob the right to hit the taunter **for free** (verified). `DropThreat(id)` = `delete(m.threat, id)` — retention re-picks the next-highest next tick; if the table empties the current aggro target stays latched (Fade sheds to a tank, **no-op solo** — accepted v1); dropping the entry also drops the mob's dynamic harm right on that entity until it acts again (the point of shedding). **Effect types (`skills/definition.go`, Step-0 pattern):** `EffectTypeTaunt`/`EffectTypeDetaunt`, shared `ThreatParams{Margin}` payload (detaunt sets an empty struct, ignores Margin); allowlist = geometry + targetFlags (+ `threatMargin` for taunt only); validator hard-fails `threatMargin <= 0` (a zero margin is a silent no-op — the tiebreak lesson). **Apply-site (`sys/skills.go`):** new `fireCooldown` early-continue branch → `applyThreatEffect` — a query circle (`InstantDamageMask`) of enemy mobs at the caster, eligibility through `eligibleByTargetFlags[threatManipulable]` so the faction/`mayHarm` gate applies (a player caster bypasses `mayHarm` and reaches any different-faction mob; the player is the threat source, itself a `model.Combatant`), per mob calling `ForceThreatToTop`/`DropThreat`; `threatManipulable` = one local capability interface with both methods (every mob implements both). **Content [ALL PLACEHOLDER]:** `api/skills/taunt.json` (id 25, cooldown 300 −20/lvl, `taunt` radius 2.0, targetsEnemies, `threatMargin` 50, maxLevel 3) + `api/skills/fade.json` (id 26, cooldown 300 −20/lvl, `detaunt` radius 2.0, targetsEnemies, maxLevel 3); milestone **level 8** unlocks BOTH (mirrors L5 = ImmolationAura+Ignite); cp-defs done; `Skills.ts` id 25/26 (ringless cooldowns). **Count pins: skills 21→23** (`registry_test` len + boot log `count=23`), **milestones 8→10** (no test pins the milestone count — file/boot log only), **mobs 8 unchanged**. **Pins:** mob seams (`taunt_test.go`: exceeds-max-and-becomes-target, empty-table-seeds-margin, gates-allied/dead/nil, grants-harm-rights, drop-removes-entry, drop-sheds-to-next-highest), definition parse (Taunt/Detaunt payload, zero-margin-fails, threatMargin-on-detaunt-fails), sys (`skills_behavior_test.go`: taunt-forces-caster-to-top, taunt-skips-allied, fade-drops-caster-threat). **Next action (user work order): chunk 8 (support mobs) plan-first in a NEW session — lift `healCaster`/heal-target capability (§3.7 / the two flagged Phase-6 limitations), seek-wounded-ally movement, healer-mob smoke content; gotcha #12 (no player-reward leakage — the chunk-7 threat ops never touched it, but mob heals must).** **Prior — Mob-depth chunk 6.6 — MOB FACTIONS & MOB-VS-MOB HOSTILITY — DONE + FULLY VERIFIED IN-GAME 2026-07-11 — round 1 “works as described” + same-day HARM-GATE fix from the two verification findings, fix RE-VERIFIED same day (“honestly, its perfect … groups fighting each other, killing each other, following me, getting distracted … feels very organic”; full backend suite green **22 pkgs** — the new `factions` package is the 22nd —, binary rebuilt, tsc + webpack green, red-first; ZERO wire changes, zero frontend code — frontend rebuilt only for the zone/mob JSON bundle).** **HARM-GATE FIX (user findings at verification):** (1) a tusker Mammoth chasing the player splashed a prey Dodo with its aura (“different faction = may harm”) → retaliation locked two NEUTRAL factions into a fight neither could start, ignoring the player; (2) the braziers (default hostile) legally dotted passing predator cats, which retaliated against the unreachable Viewport-only body and suicided (world mobs have no auraCanReach gate). **Fix (user: “can't aggro → can't damage”; upgraded to a CENTRAL SEAM after a scalability discussion): mob-cast harm follows the two-layer rule `mayHarm(A,B) = B.faction ∈ A.aggroSet (static) ∪ A.hasThreat(B) (dynamic)`** — retaliation keeps working (attacker lands on the threat table; passive prey still bites back at cats AND players), taunt (chunk 7) gets harm rights for free, encounter scripts (chunk 9) ride the dynamic layer config-free; bonus semantic (pinned): a passive mob no longer splashes bystander players it isn't fighting; players + owned summons unchanged (all-others aggro set = gate no-op). **Mechanics:** `model.HostilityGate` (`MayHarm(faction,id)`) implemented by `Mob` (`aggroMask ∪ HasThreat`); ONE sys seam **`mayHarm(caster, target)`** consulted by `eligibleByTargetFlags` (now takes the ACTING entity — the summon on owned casts via `applyPlayerDamageAura`, whose faction param dropped) and by `applyMobDamageAura`; damage/dot/instant/resist all route through it; **RULE: every new harmful effect type's enemy eligibility MUST go through the seam** (per-site copies = the AuraMaskFor resist-gap bug class); `applySlowAura` carries a NOTE (zero faction eligibility — pre-6.6 gap, harmless until a mob slow ships). **Fix pins (red-first):** `TestMob_MayHarm_DeclaredHostilityOrCombatLink`, sys `TestApplyMobDamageAura_NeutralFactionNeverSplashed` (finding-1 repro, verified red) + `_ThreatTableAttackerIsFairGame` + `TestApplyDotEffect_MobCasterRespectsHostility` (finding-2 repro, verified red). **Scalability record (banner):** hostileTo = sparse encoding of a stance matrix (`friendlyTo` later = additive); deferred friction: 62-faction bitmask cap (internal-only, ~3 places to widen), global faction namespace (content pass names e.g. `z1-farmers`), social aggro (future acquisition-side dynamic source), `THREAT <mob>` debug cheat (chunk 7 wants it), anti-taunt full-wipe also drops dynamic harm rights (chunk-7 handoff bullet added).** **Decisions (user, plan-first): hostility = per-faction `hostileTo` list** (acquisition-ONLY gate, asymmetry legal — wolf hunts rabbit, rabbit lists nobody; damage eligibility/threat retaliation/flee stay pure faction-inequality = "same aggro rules as vs players"); **authoring def-level only** (top-level mob JSON `faction` key, no per-spawn override — frontlines field two species); **ALL faction-flag masks widen to both combatant layers** (`model.LayerCombatants` = Player|Action; `factionLayers` + the auramask NOTE deleted; `AuraMaskFor(def)`/`InstantDamageMask(e)` lost the faction param; free fix, pinned: player ally auras — FireWard — finally reach the aligned companion); **kill rewards on ANY death** (`MobTouches` now calls `tryGrantKillRewards` — participants get full XP/drops when a mob lands the killing blow; **also closed a latent leak, pinned red-first: poking a mob-killed corpse used to collect full XP** since the death was never settled). **Default rule (deliberate deviation): absent faction = built-in `hostile` with aggro set {aligned} ONLY** — pre-factions behavior verbatim, so new factions never silently change legacy mobs, mob-hunting is always explicit content, and legacy mobs keep player-only sensor masks (**the perf knob: zero new broadphase pairs except opted-in factions; passive factions' sensors see NOTHING**). **Mechanics:** new **`pkg/berryhunter/factions`** registry (`api/factions/*.json`, one file per faction `{name, hostileTo}`, `_comment` tolerated; reserved undeclarable built-ins `aligned` ID 0 + `hostile` ID 1, both referenceable; declared IDs 2+ sorted-name-deterministic; hard-fails: missing hostileTo — `[]` = passive —, dup/empty/reserved name, unknown/self ref, >62; numeric `factions.Faction` mirrors `model.Faction`, boot seam converts — model→mobs→factions import chain); factions load BEFORE mobs, `mobs.RegistryFromFS(r, sr, fr, fsys)` resolves the def (absent → hostile default; `"aligned"` hard-fails — summon-only; dead `RegistryFromPaths` deleted); `MobDefinition.Faction/AggroMask` → `Mob.faction/aggroMask` (zero-value def → hostile guard à la defaultMobMaxHealth); `findAggroTarget` adds the aggro-set gate on top of equality+liveness; `aggroSensorMask` derives the sensor mask from the set (aligned bit→Player layer, mob bits→Action); `SetFaction` recomputes mask+set (all-others); **`applyMobDamageAura` gained the exact-faction gate** (the deferred 6.5 item: same faction rejected unless targetsAllies, BEFORE the cap — a pack mate never eats the nearest-1 slot; unfactioned structures keep riding the mask); `Faction.Bit()` helper; loaders: `contentSources.factions` + `loadFactions` + embedded `pkg/api/factions` + Makefile cp-defs + boot-log `Loaded faction definitions count=5`. **Content [ALL PLACEHOLDER]:** factions `prey` ([]), `predator` (["aligned","prey","tusker"]), `tusker` (["aligned","predator"]); Rabbit+Dodo→prey, SaberToothCat→predator, Mammoth→tusker (boss/totem/companion/brazier untouched). ⚠ Map-wide (accepted): cats hunt rabbits+dodos everywhere, cats-vs-mammoths fight on contact, dodos/rabbits stop attacking players unprovoked (retaliate only). **Frontline showcase:** 4 cats (y 9) vs 4 Mammoths (y 13) at x 41.5–48.5 east of the Sand Flats (clearance-checked; lines 4 u apart = inside both 4-u sensors → immediate engagement), respawn 300t ±30% = perpetual battle; proving-grounds spawns 381→389. **Count pins: skills 21 / milestones 8 / mobs 8 unchanged; NEW factions count=5 boot line; suite 21→22 pkgs.** **Pins:** factions registry (12 tests incl. all hard-fails + deterministic IDs), def faction resolve/absent-default/aligned-fails/unknown-fails/no-registry-fails, `mob/factions_test.go` (sensor-mask-follows-set, bare-def guard, wolf-acquires-rabbit VERIFIED RED, seen≠acquired control, passive-retaliates-and-flees, same-faction-no-threat, SetFaction-recompute, mob-killing-blow-rewards VERIFIED RED + poke-the-corpse leak pin VERIFIED RED), sys (same-faction-never-eats-slot VERIFIED RED, different-faction-hit, structure-still-hit, resist-reaches-aligned-mob), auramask pins rewritten to `LayerCombatants`, end-to-end loads factions + asserts ≥1 hunter & ≥1 passive in the roster. **Next action (user work order): chunk 7 (taunt) plan-first in a NEW session — read the chunk-7 HANDOFF BANNER in `plan-mob-depth.md` §5 first (incl. the new chunk-6.6 addendum: threat carries harm rights).** **Prior — Mob-depth fix chunk 6.5 — HAZARD BRAZIERS + COMPANION REACHABILITY — DONE + VERIFIED IN-GAME 2026-07-11 (“everything works”; full backend suite green 21 pkgs, binary rebuilt, tsc + webpack green, red-first; zero wire-FIELD changes — only the EntityType enum append; committed same day).** **Origin (user screenshot report):** at the Ember Ring the boss burned the hostile “braziers” (which never hit back) and the companion acquired a brazier it could never damage and died in its aura. **Root cause of BOTH:** the braziers were world-spawned instances of the player-summon **totem def**, whose PLAYER-layer body (the layer trick — correct for the aligned summon) breaks the faction≈layer assumption both ways: the hostile boss's enemy mask (player layer) sees it (`applyMobDamageAura` is deliberately mask-only), while aligned players'/companions' enemy mask (action layer) never does — exactly the `model/auramask.go` NOTE scenario. **Decisions (user):** braziers = **unkillable pure hazards** (not combatants); companion **refuses aura-unreachable targets**; mob-path faction gate **deferred into NEW chunk 6.6** (no same-faction friendly-fire path remains post-fix, and the factions rework rebuilds eligibility anyway). **Mechanics:** new **`Brazier` EntityType (ordinal 20**, appended to `server.fbs`, Go+TS regen) + `api/mobs/brazier.json` (id 8, hostile, speed 0, TotemAura L1, **collisionLayer 32 = Viewport ONLY — no damage mask in the game includes that layer, so its body is structurally unkillable/interaction-free with zero special-case code** while its aura still burns the player layer, since aura masks derive from the caster's FACTION, not its body layer; mask 16 = Border); proving-grounds: the 2 Ember Ring Totem spawns → Brazier (**totem def untouched** — the player summon keeps its layer trick); companion: **`Mob.auraCanReach(t)`** gate in `updateCompanionTargeting` acquisition — the prospective aura mask (**slot 0 + OWN faction**; the stored sensor mask is stale/hostile-derived while the aura is gated) must intersect the target's `Bodies()[0]` layer, and only PROVEN unreachability rejects (no body / no aura → acquire as before, keeping the bodiless test fakes green); frontend: the known 5-file path (`Mobs.Brazier`, BOTH `Game.ts` layer steps, `gameObjectClasses[20]`, `Graphics.ts`, stone-fire-bowl `brazier.svg` — deliberately not the totem face). **Count pins: mobs 7→8 (boot-log line only); skills 21 / milestones 8 unchanged** (TotemAura reused). **Pins:** `companion_test.go` `TestMob_FollowerIgnoresAuraUnreachableTarget` (verified red pre-implementation) + `_FollowerAcquiresAuraReachableBodiedTarget` (control). **Same session — NEW chunk 6.6 captured (user design): mob factions & mob-vs-mob hostility** — wolf-chases-rabbit aggro, rabbit flees, frontlines of two mob factions fighting each other (one may also attack players), same aggro rules between mobs; scope sketch in `plan-mob-depth.md` §5 chunk 6.6 (N-faction model, sensor+aura enemy masks widen to both layers with exact-faction eligibility everywhere = where the mob-path faction gate lands, reward-leakage check, perf at ~380 spawns). **Next action (user, work order): chunk 6.6 (mob factions) plan-first in a NEW session, then chunk 7 (taunt) plan-first in a NEW session.** **Prior — Mob-depth chunk 6 — COMPANION COOLDOWN — DONE + VERIFIED IN-GAME 2026-07-11 ("everything works"; full backend suite green 21 pkgs, binary rebuilt, tsc + webpack green, red-first; zero wire-FIELD changes — only the EntityType enum append; committed same day).** **⚑ §6.4 DECIDED (user, plan-first):** lifetime = **TTL totem-style** (1800+300/lvl ≤ cooldown 2400 [PLACEHOLDER]); catch-up = **teleport beyond ~15 u** (hold ring 1.5 u; gotcha #6's sanctioned exception — steering only handles convex blockers, a wall could strand it for its whole TTL); max-one = **TTL ≤ cooldown convention only, deliberately UNENFORCED — cooldown reduction beating the TTL yields multiple companions as the REWARD for that interaction** (no code cap, no owner→summon back-reference); new **`Companion` EntityType (ordinal 19**, appended to `server.fbs`, Go+TS regen, placeholder wolf-pup SVG). Leans recorded: follower = **owned + moving** (`owner != nil && velocity > 0`, no schema flag — YAGNI; totem is speed 0, zone mobs have no owner); **defend beats assist** at acquisition; combat tether ~10 u [PLACEHOLDER] measured from the OWNER (acquisition AND stickiness — the companion never strays); followers skip `returnPos` entirely (the chunk-5 handoff trap — follow IS their return behavior); **no self-retaliation** (§3.6 is owner-centric; hits on the companion build its threat on the attackers' tables unchanged — mobs turn on it — but the companion never reads its own table; calibration point for the in-game check). **Mechanics:** §3.6's "owner attacked X" signal became **owner combat stamps** — both acquisition conditions are O(1) reads off the owner, zero sensor/mask changes: `Mob.PlayerTouches` stamps the toucher via optional `model.AttackNotifier` when `Damage.Source == nil` (direct casts only — summon damage replaying through the owner never counts as "the owner attacked"), `player.MobTouches` stamps the attacker (any mob hit, resisted or not); both age over `combatSignalWindowTicks` [PLACEHOLDER 90 ≈ 3 s] in `ResetTickNumbers` and are read via optional `model.CombatSignals` (nil when expired or dead; `PlayerEntity` interface untouched — the mob asserts). New **`model/mob/companion.go`** (mirrors patrol.go): `updateIdleMovement` gained a follower branch FIRST → `updateFollow()` walks at FULL speed toward the NEAREST POINT ON THE FOLLOW RING (arrival clamp stops ON the ring, never under the owner; not the idle amble — the player at 0.05/tick outruns every idle pace), snaps to that ring point beyond the teleport threshold, stands inside the ring or when the owner is dead/absent (TTL cleans up); `updateAggro` gained a follower branch replacing sensor acquisition, threat retention AND the leash → `updateCompanionTargeting()` (sticky target held while it lives inside the owner tether, else acquire from the stamps defend-first, tether- and faction-gated); the chunk-3c aura gate rides `setAggroTarget`/`resetAggro` free (ring shows in combat, hides on follow); `noteCombatEntry` skips followers. **Spawn side was free:** `spawnSummon` already handles owner/faction/TTL/loadout-level/power/offset placement; owner XP + summon self-threat ride the chunk-1/3 `Owned` path (gotcha #9). **Content [ALL PLACEHOLDER]:** `api/mobs/companion.json` (id 7, maxHealth 60, speed 1.2 → 0.066/tick > player 0.05, **collisionLayer 160 = Viewport|Player** (player-layer trick: hostile masks hit it, ally auras buff it), **mask 17 = PlayerStatic|Border** (collides like its owner, goes wherever the owner goes), aggroRadius 0.1 dummy — sensor bypassed); `api/skills/mobs/companion-aura.json` (id 107, physical damage aura, nearest-1, 5+1/lvl, interval 20, r 0.8, maxLevel 3 = SummonCompanion's); `api/skills/summon-companion.json` (id 24, spawn, maxHealthPerOwnerLevel 2 + powerPerOwnerLevel 0.05 like the totem); milestone **level 7** (embedded JSON — cp-defs done). **Count pins now 21 skills (registry_test + boot log `count=21`) / 8 milestones / 7 mobs (boot-log line only).** **Frontend:** the known path — `Mobs.Companion` class, BOTH `Game.ts` layer steps (the invisible-mob trap), `gameObjectClasses[19]`, `Graphics.ts` entry, `companion.svg` (width/height=100%), `Skills.ts` id 24 (ringless cooldown). **Pins:** `model/mob/companion_test.go` (follows at full speed / holds + converges on the ring / teleports when hopelessly far / stands with dead owner; acquires on owner-attacked AND on-attacker-of-owner / defend-beats-assist / no-signals = pure follow + aura stays gated / dead + beyond-tether stamps ignored; sticky ignores new stamps / drops on target death + beyond-tether → aura gates off / own threat table never acquires; skips evade return; PlayerTouches stamps direct hits only), `player_test.go` (stamp + window expiry / re-stamp refreshes / dead reads nil / MobTouches stamps the attacker), `sys` `TestCooldown_SpawnMovingSummonFollowsOwner` (spawn → follower through the system, full-speed follow). **Next action: chunk 7 (taunt/anti-taunt effect types), plan-first — settle exact semantics against the 3a threat-table shape at chunk start.** **Prior — Mob-depth chunk 5 — PATROL ARCHETYPES + WANDER RESPAWN — DONE + VERIFIED IN-GAME 2026-07-11 ("everything works as described"), followed same day by the PACING REWORK (user findings; suite 21 pkgs green, binary rebuilt, tsc + webpack green, red-first; zero wire changes; pacing rework RE-VERIFIED IN-GAME same day — "pacing feels right now"; committed same day).** **PACING REWORK (user findings at verification):** full-speed patrol + 0.5×/1–4 s wander read as frantic → ONE idle pace for wander legs AND patrol marching with slower defaults + per-TYPE/per-SPAWN knobs + loop traversal returned (the circle-a-landmark case). User-confirmed: evade return + walk-home stay FULL speed. Mob-def `factors` gained **`idleSpeedFactor`** ((0,1], absent → 0.4 [PLACEHOLDER]), **`idleDwellMin/MaxTicks`** (absent → 90–300 ≈ 3–10 s [PLACEHOLDER]), **`wanderRadius`** (TYPE-default archetype, applied only via spawn points — summons unaffected; speed-0 type + default radius fails at registry load). **Dodo grazes by default: r 2.5, 0.25×, dwell 240–900 (8–30 s) [PLACEHOLDER]** — all 219+ proving-grounds Dodos wander with zero zone edits. Spawn overrides: **`wanderRadius` is TRI-STATE `*float32`** (absent = inherit, explicit 0 = stationary — roadmap's bridge-guard case —, >0 = override; explicit>0+waypoints still hard-fails), **`idleSpeedFactor`** per spawn, **`patrolMode: "pingpong"(default)|"loop"`** (wraps last→first; mode without waypoints hard-fails). Seams: `Spawn.EffectiveWanderRadius()`, `Mob.SetWaypoints(points, loop)`, `Mob.SetIdleSpeedFactor`, `Mob.rollDwell`. Editor: tri-state wander input (empty = inherit; inherited discs fainter), idle-speed input, Traversal select, loop preview closes the polygon; serializer exports explicit 0, omits inherited/defaults. Content: Henge route → `patrolMode: "loop"`. New pins: definitions idle parse/defaults/6 hard-fails; zone tri-state/factor-bounds/patrolMode fails; patrol-at-idle-speed/def-pace/spawn-override/dwell-band/loop-wrap/evade-return-full-speed; sys type-default-wander + explicit-0. **Plan-first decisions (user): ⚑ §6.6 CLOSED — inline per-spawn waypoint list + ping-pong-only traversal** (closed loop = author a polygon); **WoW-classic evade return (user amendment):** a mob leaving idle for combat records its position (`Mob.returnPos`, set in `setAggroTarget` on the nil→target transition); after the combat reset it walks back to that exact point FIRST, then resumes its archetype (next waypoint — index kept —, fresh wander pick, or stand); re-aggro during the return walk keeps the ORIGINAL point; this uniformly replaced walk-home-to-spawn for ALL mobs (a classic mob's recorded point IS its spawn spot — behavior unchanged, pinned); implied: **the aggro sensor now follows the body** (`SetPosition` moves `aggroAura` every call, was latched to spawn = territorial) — acquisition is mob-centered like the chunk-3 `targetWithinSensor` leash check already was, a patroller aggros what it walks past; **wander pacing = walk–pause at reduced speed** (`wanderSpeedFactor` [PLACEHOLDER 0.5×], dwell 30–120 ticks [PLACEHOLDER]). **Mechanics:** schema — `world.Spawn` gained `wanderRadius` + `waypoints []Waypoint{x,y}` (absent = stationary, pre-chunk zones stay valid; hard-fails: negative radius, both set, single-waypoint route, either on a speed-0 mob — the last in `resolve()` where Def binds); mob — new **`model/mob/patrol.go`**: `SetWander(anchor, radius)`/`SetWaypoints(points)` called by `MobSystem.spawnAt` AFTER `NewMob` (wander anchor = the AUTHORED point, never the rolled respawn position — gotchas #5/#7; `spawnPosition` still latches first `SetPosition` = stationary fallback home); Update's idle branch → `updateIdleMovement()` (evade return first, then waypoints → wander → walk home); wander picks uniform disc points from the mob's entity-ID-seeded RNG with a leg budget of 2× straight-line ticks + slack (a point rolled against a blocker expires into a dwell + re-roll, no orbit lock); patrol marches full speed, advances inside `waypointArrivalRadius` [PLACEHOLDER 0.3]; all movement rides `moveTowards`/new `moveTowardsScaled` (scale 1 = bit-identical old path) so chunk-4 steering + slows apply free; sys — wander points roll the (re)spawn position via `randomInDisc` (system RNG), waypoint/stationary spawns stay exact. **Editor:** wander-radius input + disc preview; selected spawn gains an **"Add on map click" waypoint toggle** (+ Remove last/Clear; numbered dots + polyline); serializer omits zero/empty (pre-chunk-5 zones round-trip diff-clean); mutual exclusion enforced in-panel. **Content [PLACEHOLDER] (moved to proving-grounds same day — see the zone-consolidation entry):** 21 archetype spawns on the proving-grounds map: 14 wanderers (4 hub Rabbits r3, 5 Sand-Flats Dodos r3, 2 Stone-Fields Mammoths r5, 1 Grove cat r4 whose disc contains trees, 2 Marsh Dodos r3) + 7 patrol routes (hub square loop, Grove steering-corridor lane, North Tree Line front→through-the-gap→back, Henge polygon loop, long east-trail route, Ember Ring arc, slow Marsh Mammoth) — every waypoint clearance-checked ≥0.35 against blocking props (a waypoint inside a blocker = eternal orbit; mid-segment blockers left in deliberately, they exercise chunk-4 steering). **Pins:** `zone_test.go` (parse + 4 hard-fails + nested unknown key), `patrol_test.go` (radius containment, authored-anchor not rolled spot, dwell + reduced speed, ping-pong order, full-speed patrol, evade return → resumes KEPT waypoint, re-aggro keeps original point, classic walk-home unchanged, sensor follows body), `sys/mob_test.go` (respawn roll within band + varied, anchored wander through the system, route spawn patrols; NOTE spawnAt + first mob Update share a tick — position asserts allow one step). **Same session — ZONE CONSOLIDATION (user call): proving-grounds is now the ONE canonical debug/test map** — `api/zones/scaffold.json` + `zone.json` DELETED (repo + embedded copies), `conf.default.json game.zone=proving-grounds` (a lone zone also loads without any flag; the local `conf.json` carries no zone key), `TestDiskContent_RepoApiLoadsEndToEnd`'s terrain pin re-anchored on proving-grounds + now also asserts it exercises ≥1 wanderer and ≥1 patroller; frontend rebuilt (zone bundle shrank; the editor's DEFAULT_STEM falls back to the sole zone). Proving-grounds spawns: 360 → 381. **Next action: chunk 6 (companion cooldown), plan-first — read the chunk-6 HANDOFF BANNER in `plan-mob-depth.md` §5 first (updateIdleMovement follow branch, returnPos skip for followers, follow speed ≠ idle amble, sensor mask caveat), then settle ⚑ §6.4 (lifetime, follow distance/catch-up, max-one).** **Prior — Mob-depth chunk 4 — OBSTACLE STEERING — DONE + VERIFIED IN-GAME (2026-07-11, committed same day; full backend suite green 21 pkgs, binary rebuilt; zero wire/frontend/content changes; TDD red-first — all behavior pins verified red at the exact jam points pre-implementation; verification ran in two rounds: round 1 produced 3 observations, 2 fixed same day red-first, round 2 confirmed — curves tight, no stuck mobs).** **In-game findings (2026-07-11):** (1) aura-ring-sized curves around props → `steeringLookahead` 1.5 → **0.6 [PLACEHOLDER]**; (2) perceived mob slowdown = pure path overhead of (1), step magnitude never changed (steer returns unit directions) — no code change; (3) **side-flip oscillation (real bug):** mob jittered in place against a tree+rock pair — the perpendicular-lean side pick is only stable vs a SINGLE blocker; between two (or in a wall corner, exposed by the tighter lookahead: `TestMob_FleeIntoCornerEscapesAlongEdge` went red) each sideways step flips the lean. **Fix: side latch `Mob.steerSide`** (+1/−1; picked on the first head-on tick from the combined vector's lean, exact line → left; held until FULLY clear of repulsion, rep=0 resets) — commits the mob to one way around a cluster/corner; pinned by `TestMob_TwoBlockerPocketNoSideFlipOscillation` (reproduced the report: mob frozen at the pocket mouth pre-fix). Mobs now deflect around blocking statics instead of jamming: **`model/mob/steering.go`** — `steer(desired)` bends the unit step direction of BOTH `moveTowards` and `moveAwayFrom` (chase, walk-home, flee — later patrol/companion — all steer; `stepLength` stays the magnitude home, physics resolution stays the non-penetration guarantee). Repulsion = `QueryCircleStatics` probe at `m.Radius() + steeringLookahead` [PLACEHOLDER 0.6, was 1.5 — first in-game round] **carrying the mob's own body mask** (a mob that walks through a static is never repelled — AngryMammoth ignores rocks for free, its Border bit keeps wall repulsion on); circles push radially, the **InvAABB border pushes axis-aligned inward per near edge** (corners = both axes; plan-first decision: the wall IS in repulsion, gotcha #10 closed); linear falloff 1 at contact/overlap → 0 at lookahead; dead-center fallback = heading. Compose: `desired + rep × steeringRepulsionWeight` [PLACEHOLDER 1.5] normalized; forward ≤ 0 (head-on) → deflect along the perpendicular component (leans to the freer side = tick-stable side choice), exact-on-line → always left (`Rot90`). Emergent by design: target-inside-blocker → mob orbits ("holds nearby, no jitter"); perpendicular flee into the wall slides along it; corner-pinned flee escapes along an edge. **Plan-first decision 2 (user): space access = `NewMob` param** — `NewMob(def, chaseIntoAuraMargin, space *phy.Space)`, nil = no steering (exact pre-chunk straight line; ~34 test callsites nil-padded — the old clamp pins `TestMob_FleePinnedInBoundaryCornerConverges`/`_FleeAlongWallSlides` now pin the nil-space fallback, comment updated); production sites: `NewMobSystem(g, seed, spawns, space)` ← `p.Space()` in `core/game.go`, `spawnSummon` passes `s.space`. **Pins (`steering_test.go`, all through the real `Space.Update()`):** `TestMob_SteersAroundBlockerReachesTarget`/`_NoBlockersPathUnchanged` (bit-identical step, empty space)/`_HeadOnBlockerDeflectsConsistently` (side never flips)/`_SpawnedOverlappingBlockerEscapes`/`_TargetInsideBlockerHoldsWithoutJitter` (distance band + keeps moving + no teleport steps)/`_FleeSteersAroundBlocker`/`_FleePerpendicularIntoWallSlidesAlongIt`/`_FleeIntoCornerEscapesAlongEdge`. Perf: one statics query per MOVING mob per tick (idle mobs never query) — fine at proving-grounds scale. **Next action: in-game verify chunk 4 (checklist in the banner; Grove steering corridor is the purpose-built venue), then chunk 5 (patrol 5a wander / 5b waypoints), plan-first — ⚑ §6.6 waypoint schema shape is open.** **Prior — `blocksAura` deletion sweep DONE (2026-07-11 — the LoS-cut leftover; full backend suite green 21 pkgs, tsc + webpack green, zero wire changes, zero behavior changes; committed same day).** Deleted the inert plumbing end-to-end: backend — `world.Prop.BlocksAura` schema field, `PropEntity.BlocksAura()` (`model/entity.go`; the interface is now just `Entity`), `model/prop.Prop.blocksAura` field + method (**`prop.New` is 4-param now** — `entityType, pos, radius, blocksMovement`), the `berryhunterd.go` callsite arg, test assertions/fixtures; content — all 500 `blocksAura` keys stripped from `api/zones/{zone,scaffold,proving-grounds}.json` via a scratchpad regex script (JSON re-validated) + `cp-defs` re-synced the embedded copies; frontend — `ZoneProp.blocksAura` + serializer line (`ZoneModel.ts`), the panel checkbox (`groundTexturePanel.html` + `blocksAuraToggle` wiring in `_ZoneEditorPanel.ts`), the inner-ring marker branch (`ZoneEditor.ts`). **Sequence was deliberately schema-first as the completeness proof:** removing the schema field first made `TestDiskContent_RepoApiLoadsEndToEnd` fail naming `blocksAura` as an unknown field (`DisallowUnknownFields`), so a missed zone file couldn't slip through; suite green after the strip. Docs flipped to done: roadmap item-6 banner + item-4 mention, tdd §4.2 bullet, `manual-zone-editor.md` (legend + props-mode text), CLAUDE.md deferred-debt entry. In-game spot-check (optional, cosmetic only): props mode has no "blocks aura" checkbox, markers show no inner ring, a downloaded zone JSON carries no `blocksAura`. **Next action unchanged: mob-depth chunk 4 (obstacle steering), plan-first — read the chunk-4 HANDOFF BANNER in `plan-mob-depth.md` §5 first.** **Prior — Mob-depth chunks 2+3 VERIFIED IN-GAME (2026-07-11, committed same day) — checklist PASSED except two findings, both root-caused + FIXED same day and the fixes re-verified in-game (backend-only, TDD red-first; full suite green 21 pkgs, binary rebuilt; no wire/frontend changes).** (1) **Chase aura flicker (~3 s cadence):** the 3b leash counted "out of aura reach + no damage" as out-of-combat, so a chased-but-unreachable target *inside the aggro sensor* expired the leash every 91 ticks → `resetAggro` (aura off + **threat table wiped mid-chase**) → sensor re-acquired next tick = 1-tick ring flicker. Fix: in-combat now also includes **target within the aggro sensor** (`Mob.targetWithinSensor`, geometric twin of the sensor overlap — no physics step needed); the countdown only runs with the target unreachable AND out of sight; "gives up after ~3 s" now means ~3 s *after leaving the sensor* (outrun behavior unchanged). Pinned by `TestMob_ChasingTargetInsideSensorNeverLeashes` (verified red at exactly tick 91 pre-fix); `plan-mob-depth.md` §6.7's quoted combat state updated (the sensor ignores walls too — wall-cheese analysis unchanged). (2) **Healer threat never fired on sensor-acquired combat (§6.3 AMENDED):** `creditHealerThreat` filtered on `HasThreat(healedID)` only — a tank holding boss aggro **without dealing damage** has no threat entry, so the healer was never credited no matter how much they healed (user repro: AngryMammoth). Fix: "in combat with the heal target" = threat table holds the healed entity **OR the mob's current aggro target is the healed entity** — new exported `Mob.TargetsEntity(id)`, `threatReceiver` seam widened. The healer's first landed heal then creates the only threat entry and retention swings the boss onto them. Pinned by `TestApplyHealAura_CreditsHealerThreatOnSensorAggroMob` + `TestMob_TargetsEntity`. Calibration note (by design, not a bug): once the tank deals damage, the healer only pulls when 0.5 × healed HP out-threats it — `healerThreatFactor` is the balance knob. Docs: plan chunk-3 banner (fix record + checklist status) + §6.3 amendment + §6.7 touch-up. **Next action unchanged: chunk 4 (obstacle steering), plan-first — read the chunk-4 HANDOFF BANNER in `plan-mob-depth.md` §5 first.** **Prior — Combat pacing & recovery — research + decision session + docs pass (2026-07-10, docs-only, no code).** Research deliverable **`docs/research-combat-pacing-recovery.md`** (placement/evaluation/implementation plan for the session's ideas A–F, incl. **tick-visualization forensics:** a forward-looking tick indicator NEVER existed — the aura ring has been a static sprite since the first aura commit `d21a2d1c`; what 2026-07-04's `a877e3f8` removed was the interval-1-era continuous `DamagedAmbient` flash, replaced by landing-only slash/fire VFX — restoring readable timing is new work, not a revert). **Decisions (user, walked through one-by-one; decision banner atop the research doc):** (1) **aura LoS CUT entirely** — auras pass through walls/objects; walls/props stay MOVEMENT blockers (untouched); roadmap item 6 dead, execution step 3 reshaped to "Atmosphere & recovery"; darkness/light (item 5) explicitly unaffected (area-based, never needed occlusion); **`blocksAura` plumbing decided for DELETION (sweep pending)** — `world/zone.go` schema field, `model/prop.Prop` field + `PropEntity` method, zone-editor inner-ring marker, authored values in zone/prop JSONs; **steering-first stands** (navmesh escalation trigger = wall-cheese in playtests); stationary-mob wall-cheese accepted via a GDD §8 placement rule; **no-progress leash rule PARKED, not scheduled** (user call; `plan-mob-depth.md` §6.7 — the designated wall-cheese leash mechanic: approach-with-no-progress for N ticks clears combat despite damage → leash → walk home → full regen; raise at chunk-4 plan-first or later — note the chunk-3 combat state "damage taken" keeps a wall-stuck mob pinned forever today). (2) **E1 recovery boundary: partial-instant OK** — instant effects never fully/near-fully restore; the L2 Heal cooldown (instant 20%+5%/lvl) stays as combat sustain; the out-of-combat reset is always time-based (~15–20 s at a fire) — recovery currency = time-at-fire, not cooldown availability; group cooldown rotation sanctioned. (3) **Feast aftereffect buff OPEN** with recorded constraint (no regen persisting into combat — GDD §12). (4) **Personal recovery-over-time cooldown early = decided, theme OPEN** (mini-campfire = cheapest candidate: totem machinery + heal aura legally heals the owner since the totem is the caster; waits on the chunk-7/8 mob-heal lifts). (5) **Passive OOC regen = classic declining-with-level model**; found gap: **player regen is NOT combat-gated today** (`model/player/update.go` regens mid-combat vs GDD §3) — gate scheduled step 3, prerequisite for harness thresholds. (6) **Respawn only at fixed WORLD campfires** — player-placed recovery points never respawn points (protects walk-back penalty + prevents boss corpse-zerg). (7) **Death state decided** — players persist as a body until actively pressing Respawn (the revive window; today `sys/state.go:114` removes instantly + spectator-at-deathspot), mobs leave a brief corpse (client-only fade may suffice — decide at scoping); bundled into step 3 (same `sys/state.go` surgery as the campfire tracker). (8) **Revive = rare high-level ability** (GDD App-A entry written; needs a new dead-player-targeting effect type, step 4/5). (9) Single-charge respawn fire → **backlog item 16** (NOT decided). (10) **GDD §1 design-pillars list added** (11 pillars, incl. first written record of "readable/relaxed combat except top-tier"); GDD §4 gained **"Combat Pacing: The Ring"** (held-decision stillness principle, solo-geometry ring analysis, Tempo/Fun recorded as #1 design risk + in the tdd §5 risk table) + the **two-zone aura decision** (special-occasion content, NO global falloff; per-effect radius already exists + sensor max-sizes — gaps: per-effect range check (lands with the step-3 campfire) + a second ring radius on the wire) + the **tick-readability requirement** (own AND mob auras; minimal indicator + wire BEFORE content-pass balancing — wire design shared with ⚑ buff-visibility + the Skills.ts debt; polish in the step-8 **aura VFX pass, now named** in item 8's checklist); **harness extended** (GDD §5/tdd §4.1: stand-still bot tiered per mob type ~90%/≤60%/boss-kills-bot [PLACEHOLDER], metric = sustainable kills/hour over chains incl. modeled regen+downtime, run per level bracket; explicit pre-step-6 gate bullet in roadmap). Docs touched: gdd.md (§1/§3/§4/§5/§8/§11/§12/App A), tdd.md (§1, §4.1, **§4.2 rewritten as cut record**, §5, §6), roadmap.md (items 4/5/**6=CUT**/7/8/12 + steps 3/4 + harness gate), backlog.md (items 8/9 + new 16), plan-mob-depth.md (§1.2 + new §6.7), plan-effect-foundations.md (§1 wall/barrier), research-doc decision banner. **Next action unchanged: mob-depth chunk 4 (obstacle steering), plan-first; chunks 2+3 in-game verification still pending.** **Prior — Mob depth chunk 3 — AGGRO & THREAT rework (3a–3d) — IMPLEMENTED (2026-07-10; full backend suite green (21 pkgs), binary rebuilt, tsc + webpack green; IN-GAME VERIFICATION of chunks 2+3 COMPLETE 2026-07-11: passed except the chase-flicker + healer-threat findings, both fixed same day + fixes re-verified in-game — see Last completed).** **§6.3/§6.2 settled at chunk start (user, 2026-07-10):** threat = **post-mitigation HP** actually lost per hit; **healer threat = heal-event crediting** — a landed heal credits the healer with healedHP × `healerThreatFactor` [PLACEHOLDER 0.5] on every mob whose threat table holds the healed entity (`SkillSystem.creditHealerThreat` iterates `s.entities`; self-heal cooldowns credit nothing); **reset-only decay**; plus a decision the chunk surfaced: **stationary mobs (speed 0) are exempt from 3c aura gating** — gating would have killed totems (dummy 0.1 aggro sensor; an ALIGNED totem's sensor masks the player layer and never sees a mob), and a hazard that can't chase has its aura as its whole behavior (zero content edits). **3a mechanics:** new **`model.Combatant`** (BasicEntity + Factioned + Position/Radius/HealthRatio; living = ratio > 0) replaces `findAggroTarget`'s `PlayerEntity` assertion (faction-aware — mobs now aggro totems/summons, no sensor/mask change) and retypes `Mob.aggroTarget`; per-mob **entity-keyed `threat` map** seeded in `PlayerTouches`/`MobTouches` from `takeDamage`'s now-returned actual loss; **`model.Damage` gained `Source Combatant`** (stamped by `applyPlayerDamageAura` on owned casts + `tickDots` alongside the owner replacement) so **threat credits the summon while XP rides the toucher** (gotcha #9); dead/absent Source falls back to the toucher (an expired totem's burn pulls threat to the owner). Retention: table non-empty → target = highest living threat (ties → lower ID; dead entries pruned on read); table empty → sensor acquires nearest enemy-faction — **a hit from outside the sensor acquires via threat (snipers get retaliated)**. **TTL expiry now zeroes health** so stale threat refs to a removed summon read dead (still no rewards — those only flow through PlayerTouches). Exported seams `NoteThreat`/`HasThreat` (healer threat now, taunt chunk 7). **3b:** the fixed territory-radius check is **GONE** — in combat (target within aura reach OR damage taken since last tick) there is no leash; combat clear starts a countdown (`leashCountdownTicks` [PLACEHOLDER 90 ≈ 3 s]) whose expiry runs `resetAggro` (target + threat cleared, aura off; walk-home + regen follow). Behavior deltas by design: a mob whose target outruns it gives up after ~3 s; the rabbit flees as long as it's chased instead of snapping at the territory edge. **3c:** moving mobs spawn with the aura **gated** (`ActiveAuraSlot -1`; the sensor is still pre-sized from slot 0 so the chase stop distance is right from the first aggro tick); `setAuraActive` flips only on transitions — **re-calling `SetActiveAura` every tick would zero the accumulator and the aura would never fire**. Wire: **`Mob.aura_radius:ushort` appended** (px, 0 = off; Go + TS regen; mirrors `Character.aura_radius`), `Mob.AuraRadius()` + `MobEntity.AuraRadius()`, codec sends it, pinned by `TestMobMarshalFlatbuf_AuraRadius`; frontend: `EntityManager` mob branch → new `Mobs.Mob.setAuraRadius` (lazy ring child, scaled per snapshot, hidden at 0) — **`damageAuraRadiusMeters` DELETED from Graphics.ts/Mobs.ts (standing hand-sync tech debt retired**; ring texture rasterizes at fixed [PLACEHOLDER 4 m]; the CHARACTER entry stays — it only sizes the player ring sprite). **3d fell out of 3a** (aggroTarget IS the highest-threat entity) — pinned by `TestMob_FleesFromHighestThreat`. `applyHealAura` became a SkillSystem method (~11 test callsites → `testSkillSystem()` helper). **Adapted pins (by design):** `TestNewMob_SkillLoadoutWiring` (slot −1 at spawn), `TestMob_FullOutOfCombatRegenClearsParticipants` (damage seeds threat → leash must reset before regen), `TestMob_TTLExpiryKills` (health 0 on expiry). New pins: threat crediting/attribution (`TestMob_ThreatCredits*`, `TestMob_PlayerTouches_Summon*`/`_DeadSource*`, `TestMob_MobTouches_OnlyEnemyFaction*`), retention/acquisition (`TestMob_Update_Retargets*`/`_ThreatFromOutsideSensor*`/`_DeadThreatEntryPruned*`, `TestMob_FindAggroTarget_AcquiresEnemyFactionSummon`), leash (`TestMob_LeashCountdown*`/`_InCombatHasNoLeash`/`_DamageResetsLeashCountdown`), gating (`TestNewMob_MovingMobSpawnsAuraGated`/`_StationaryMobAuraAlwaysOn`, `TestMob_AuraActivatesOnAggro*`), sys Source/healer-threat (`TestApplyDamageAura_OwnedCasterStampsSummonSource`/`_DirectPlayerCastHasNilSource`, `TestTickDots_OwnedDotKeepsSummonAsSource`, `TestApplyHealAura_CreditsHealerThreatOnMobsFightingTarget`). Docs: plan chunk-3 banner (incl. the 7-point in-game checklist) + §6.2/§6.3 flipped DECIDED; `manual-content-authoring.md` ring hand-sync retired. **Status: chunk 3 = DONE + VERIFIED IN-GAME 2026-07-11 (checklist passed; two findings fixed same day and re-verified). Next action: chunk 4 (obstacle steering), plan-first — read the chunk-4 HANDOFF BANNER in `plan-mob-depth.md` §5 first (seam layout moveTowards/moveAwayFrom/stepLength; Mob has NO phy.Space ref — getting QueryCircleStatics access in is the first design decision; body collisions are no lookahead; 3b's leash already killed the stuck-forever case, steering is pursuit quality).** **Prior — Mob depth chunk 2 — FLEE movement mode — IMPLEMENTED + standalone smoke VERIFIED IN-GAME (2026-07-09; full backend suite green (21 pkgs), tsc + webpack green; the FULL verification — chase + flee against the threat table — still rides with chunk 3 per plan §1.3).** **User-verified in-game:** rabbit flees at half HP; observed loop "flees to territory edge → leash drops aggro → out-of-combat regen snaps to full (~2 s shared placeholder constant) → re-aggros healthy" is **as designed** — the regen reset IS the flee exit (no hysteresis field); the abrupt leash feel is chunk 3b's job (state-dependent leash), flee re-points at the threat table in 3d. **Same session: frontend `npm audit fix`** — 27→5 advisories (critical `shell-quote` + webpack SSRF pair gone), lockfile-only change (no package.json edits, webpack 5.95.0→5.108.4), tsc + webpack green after; remaining 5 are dev/build-time only (`serialize-javascript` via css-minimizer needs a breaking major, `uuid` via sockjs/webpack-dev-server has no sane fix — npm proposes a *downgrade*) — deliberately left for the step-9 ops pass. Mechanics: `Mob.moveAwayFrom` = inverse chase vector sharing new `stepLength()` (velocity × strongest slow — chase/walk-home/flee all use it); zero-distance threat falls back to the current heading (never freezes); trigger = new mob-def **`factors.fleeBelowHealthRatio`** (flee while health ratio strictly below; 0/absent = never, 1 = whenever damaged, hard-fail outside [0,1]); in `Update`, flee wins over approach. **No exit hysteresis** — the territory leash drops aggro, walk-home regen recovers above the threshold (self-resolving). **v1 wall handling cost zero code:** the InvAABB per-axis clamp slides an angled flee along the boundary and pins a perpendicular one stationary — pinned through the real `Space.Update()` (`TestMob_FleePinnedInBoundaryCornerConverges`, `TestMob_FleeAlongWallSlides`; gotcha #10 closed for v1, full blocker repulsion stays chunk 4). **Smoke consumer (user call: full new-EntityType path, not a Dodo flag):** new **`Rabbit`** EntityType (ordinal 18, appended to `server.fbs`, Go+TS regen) + `api/mobs/rabbit.json` (id 6, DodoAura L1, fleeBelowHealthRatio 0.5, maxHealth 20, speed 0.8 → 0.044/tick, deliberately just under the player’s 0.05/tick so a fleeing rabbit stays catchable — all [PLACEHOLDER]) + 2 scaffold spawns (8,4)/(10,−3) @600t + the 5-file frontend path incl. BOTH `Game.ts` layer steps (chunk-1 invisible-mob trap avoided) + `gameObjectClasses[18]` + Graphics.ts ring 0.6 (= DodoAura, hand-synced). **Mob registry now 6 mobs** (boot-log count line; NO hard mob-count pin exists — only skills pin at 19). Other pins: `TestMob_FleesBelowThreshold_MovesExactlyAway`/`AtThresholdChasesAgain`/`FleeRespectsSlow`/`NoFleeThresholdChasesAtAnyHealth`/`FleeFromThreatAtOwnPositionUsesHeading` + `TestMapMobDefinition_*FleeBelowHealthRatio*`. Decision record: `plan-mob-depth.md` chunk-2 banner. **Prior — Mob depth chunk 1 — TOTEM: spawned-entity lifecycle (= effect-foundations Step 3) DONE — implemented + VERIFIED IN-GAME (2026-07-09; full backend suite green after `go clean -testcache`, tsc + webpack green; committed same day).** **Verification found ONE bug: the totem was invisible while fully functional — a new mob layer in `frontend/.../core/logic/Game.ts` is a TWO-step edit: `createNamedContainer` in the `layers.mobs` block AND `cameraGroup.addChild(...)` in the "// Mobs" block; the second was missing, so the sprite rendered into an off-stage container (fixed; trap recorded in `manual-content-authoring.md` step 9; totem.svg also gained the conventional `width/height="100%"` attributes).** **Same session — `api/zones/proving-grounds.json`, the all-assets test zone (user-praised):** 120×80, generated by a seeded scratchpad script (not committed) — **2423 terrain (all 16 types), 476 props (422 blocking, ~1/7 walk-through fakes), 360 spawns (219 Dodo / 83 SaberToothCat / 55 Mammoth / 1 AngryMammoth / 2 world-placed Totems)** at user-requested 5× density, center-biased. Layout: central Spawn Meadow hub with six terrain trails to themed regions — Sand Flats (E, Dodo farm), the Grove (NE: tree maze + a 4-tree "summon pocket" for placement-fallback testing + a parallel-row steering corridor), Stone Fields + rock-ring Henge (W), Puddle Marsh (S), the Ember Ring boss lair (SW corner, outside the central-80% player-spawn box: rock ring w/ two gaps, AngryMammoth + 2 hostile Totem "braziers"), North Tree Line (9-tree wall w/ one gap) — plus two mob rings around the hub, wanderers on every trail, and **~48 mixed herds** (templates: 5 Dodo + 1 Mammoth shepherd / 3 cats hunting 2 Dodos / Mammoth family / Dodo swarm / cat pride). Boot: `-zone proving-grounds` (conf default stays scaffold). **Doubles as the first load test** (~360 live mobs, ~880 physics bodies). **Deliberate quirk-exhibits:** world-spawned hostile Totems are UNKILLABLE by players (player-layer body, player damage masks the action layer — pure hazards) and respawn via their spawn point; the boss walks through rocks (mask 272 lacks MobStatic). **`TestDiskContent_RepoApiLoadsEndToEnd` now loads EVERY zone in `api/zones/` by stem** (a shipped zone can no longer break only when selected). Map reference page (schematic render + per-region test guide): claude.ai artifact e27af081. **Chunk-1 implementation record:** `SKILL SummonTotem` (or milestone at level 6) → the cooldown spawns an owned, player-aligned, stationary, TTL'd aura carrier whose kills credit the owner. **§8.4/1–6 settled at plan-first start — 1/2/3/5 as leaned (full `PlayerTouches` attribution path, stale-owner-ref accepted, killable, dedicated TotemAura JSON), 4 AMENDED (user): two scaling sources** — the SUMMON SKILL's level scales TTL (`ttlTicks`+`ttlTicksPerLevel`) AND the summon's loadout level (new `SkillComponent.RaiseLoadoutLevels`: raise-only, clamped per skill maxLevel); the OWNER's player level scales body+output — flat bonus max HP (`maxHealthPerOwnerLevel`) + a damage/heal **power** multiplier (`powerPerOwnerLevel` → `Mob.SummonPower`, frozen at spawn) that **never touches CC** (slow params ride only skill level; structurally guaranteed — `applySlowAura` never sees the caster) — **6 AMENDED (user): offset spawn** — random direction on the ring at casterR+summonR+gap [PLACEHOLDER 0.3], up to 8 candidates, blocked = summon body overlaps a blocking static OR pokes past the border (new **`phy.Space.QueryCircleStatics`** — statics twin of QueryCircle over `gridStatic`, works pre-Update; mask PlayerStatic|Border, the InvAABB only intersects circles leaving bounds so out-of-bounds rejection is free), all-blocked → caster position; rationale: the spawn must be instantly visible, never under the avatar. **Mechanics:** `Totem` EntityType appended (`server.fbs` — NOT common.fbs as §8 said — + Go/TS regen); `spawn` effect type (`skills/definition.go` payload pattern: `SpawnParams`, allowlist, validator — empty mob/TTL<1/negative owner-scaling hard-fail); spawnMob names cross-validated at boot in `mobs.RegistryFromFS` (skills load before mobs); `Mob` gained owner/`SetFaction`(first caller)/`SetTTLTicks` (decrement in Update AFTER the death check; expiry = normal removal path, no rewards)/`SetSummonPower`/`RaiseMaxHealth`; **`model.Owned`** (`Owner()` + `SummonPower()`, `model/owned.go`); `NewSkillSystem(space, game)` (game ref for Mobs()/Config()/AddEntity; ~26 test callsites updated); `fireCooldown` spawn branch (`spawnSummon` — a spawn never whiffs, mob-cast spawns work with nil owner/hostile faction for free); **owned-first dispatch** in `applyDamageAura` (before the MobEntity case → `applyPlayerDamageAura(owner, …, power)`; `outputScale` param, 1 for direct casts) and `tickDots` (owner resolved at tick time; `applyDotEffect` freezes power into the dot but keeps the summon as stored caster). **No respawn guard existed to write — §3.1 adaptation confirmed:** `TestSpawnPoint_TTLExpiredOrphanStaysDead` pins it (plus the existing no-point-no-respawn pin). **Content [ALL PLACEHOLDER]:** `api/mobs/totem.json` (id 5, maxHealth 50, speed 0, aggroRadius 0.1 dummy, **collisionLayer 160 / mask 16 — §8.2's 320/32 were STALE pre-sweep bits**, post-sweep: Viewport 32|Player 128, Border 16); `api/skills/mobs/totem-aura.json` (id 106, dot_aura fire 4+1/lvl ×3 events @ 1 s, nearest-1, interval 30, maxLevel 3 = SummonTotem's); `api/skills/summon-totem.json` (id 23, spawn, ttl 300+60/lvl ≤ cooldown 450 = one-totem-at-a-time by convention, maxHealthPerOwnerLevel 2, powerPerOwnerLevel 0.05); milestone **level 6** (embedded — cp-defs done). **Frontend:** `Mobs.Totem` class + `Game.layers.mobs.totem` + `Graphics.ts` totem entry (ring 1.5 = TotemAura radius, hand-synced) + placeholder `totem.svg` + `gameObjectClasses[17]` + `Skills.ts` id 23 (ringless cooldown). **Count pins now 19 skills / 5 mobs / 7 milestones** (registry_test updated; boot log expects `count=19`). **Pins:** `TestMap_Spawn*` (parse/validation), `TestRegistry_SpawnEffectUnknownMobFails`, `TestMob_TTL*`/`TestMob_SetFactionAndOwner`/`TestMob_RaiseMaxHealth`, `TestRaiseLoadoutLevels`, `TestSpace_QueryCircleStatics*`, `TestCooldown_Spawn*` (owned/aligned/TTL + all three scaling knobs + offset ring + blocked-skip + all-blocked fallback + mob-cast nil-owner), `TestTotemAuraDamage_CreditsOwnerXPAndKillRewards`, `TestApplyDamageAura_OwnedCasterScalesPower`, `TestApplyDotEffect_OwnedCasterScalesPower`, `TestTickDots_OwnedCasterCreditsOwner`, `TestTotem_KillableByHostileMobAura`. **Known interim (by plan):** hostile mobs don't AGGRO the totem until chunk 3a (their auras still hit it in range). **In-game checklist (all done 2026-07-09):** `XP 15000`ish → level 6 (or `SKILL SummonTotem`), equip the cooldown, fire near a Dodo → totem appears OFFSET beside you, burns the Dodo with fire numbers, **you gain the XP on its kill**, ring matches the burn range, expires ~10 s, stays dead; fire again wedged against the scaffold tree cluster → falls back to your position; boss aura kills the totem early. Decision record: `plan-effect-foundations.md` §8.4 + §4 Step 3 (flipped DONE); `plan-mob-depth.md` chunk-1 banner + §6.1. **Prior — Mob depth & totems — execution plan written (2026-07-09, docs-only, no code): `docs/plan-mob-depth.md`** — the plan + decision record for execution step 2 (roadmap item 7 remainder + effect-foundations Step 3 + a NEW **companion cooldown** added to scope this session). **Nine chunks, head order user-fixed: 1 totem → 2 flee → 3 aggro & threat (3a–3d), verified in-game together with 2 → then 4 obstacle steering → 5 patrol (5a local wander / 5b route waypoints) → 6 companion → 7 taunt/anti-taunt → 8 support mobs → 9 encounter-controller spine (9a–9f, user call to sub-chunk it).** Session decisions (plan §1.3): **mobs DO aggro totems/companions** — aggro acquisition becomes **faction-aware** in chunk 3a (a "living enemy-faction entity" interface replaces `findAggroTarget`'s `PlayerEntity` type assertion — the Step-1-deferred item lands here; no sensor/mask change needed, summons ride the player layer per §8's layer trick); **threat and XP attribution deliberately diverge** — the threat table is **entity-keyed** and summon damage credits the summon's own threat while XP rides `PlayerTouches(owner)` (pet-tanking); chunk 1 ships with a known interim (mobs ignore the totem until chunk 3 lands). **Companion cooldown** = second spawn-effect consumer: friendly mob follows the owner at a set distance, attacks the first mob the owner attacks or that attacks the owner (sticky until dead/out of range, then resumes follow). **Route patrol ships now** (both archetypes); **patrol-route validity is the level designer's responsibility** — obstacle steering (repulsion from `blocksMovement` blockers; decided steering, no navmesh) is only a safety net, placed directly before patrol. Terminology record: the user's "avoidance" in the ordering discussion meant **flee** (inverse of approaching the highest-aggro player), not obstacle steering — both exist as separate chunks. **⚠ The `plan-effect-foundations.md` §8 totem briefing predates the dead-code sweep — read it WITH plan §3.1's three adaptations:** (1) **NO respawn-guard field is needed at all** (the spawn-point MobSystem only respawns point-owned mobs — `sys/mob.go:86` — so a totem dies and stays dead; replace the planned `respawnBehavior:"None"` with a pinned test, do NOT re-add `respawnBehavior`/`generator`); (2) "never spawns naturally" is automatic post-sweep (only `zone.spawns` + the new `spawn` effect create mobs); (3) the signature is now `NewMob(def, chaseIntoAuraMargin)`. **Totem sub-decisions §8.4/1–6 are NOT pre-decided — present them at chunk-1 plan-first start (user call).** Open ⚑ (plan §6): aura-ring visibility wire for auras-off-until-aggroed (lean: wire-driven effective radius, 0 = off — also retires the `damageAuraRadiusMeters` constant tech debt), threat numbers/healer-crediting/decay, companion lifetime/follow-distance/max-one, 9f timed-world-state + dwell-capture cut (lean: slide to content pass), waypoint schema shape. Bookkeeping applied same session: roadmap item 7 + execution-order step 2 pointers, §8 adaptation banner in `plan-effect-foundations.md`. **Next action: chunk 1 (totem), plan-first, opening with the §8.4 questions.** **Prior — Dead-code sweep — circular-world & procedural-generation leftovers (2026-07-09, full backend suite green, `make gen` rerun, zero wire/frontend changes; committed 08e18c31, **verified in-game 2026-07-09** — AngryMammoth spawns/streams + collides after the layer-bit remap, mobs respawn at their spawn points, rectangular player spawn path live):** removed everything the world-foundation migration left inert. **Player spawn is now rectangular** — the last live `game.Radius()` consumer was `sys/state.go`'s spawn-in-circle; players now spawn uniformly in the **central 80% of `game.Bounds()`** (`randomSpawnPosition`, pinned by `TestRandomSpawnPosition_*` in `sys/spawn_test.go`; **interim by user decision — authored first-spawn/respawn points come later**). Deleted: `model.Game.Radius()` + `core.Radius`/`cfg.GameConfig.Radius` + the hardcoded 20 in `berryhunterd.go`; **`sys/respawn.go` (RespawnSystem)** + `model.Respawnee` (`model/respawn.go`) + `Resource.NeedsRespawn/ToRespawn` + `core/game.go`'s `addResourceEntity` and the now-unreachable `case model.ResourceEntity` (bare resources are never constructed; **PlaceableResource is the surviving consumer** of the resource model and registers via its own add-path); `gen.Generate`/`chunkRand`/`NewRandomEntityFrom`/`NewRandomPos` + the hardcoded trees/resources tables + `generator.go`+`generator_test.go` (**`gen` shrank to `resource_entity.go`**: `StaticEntityBody`/`NewStaticEntityBody`/`NewStaticEntityWithBody`/`deriveRadius`/`splitRandom`, kept for the live placeable embed); the whole **`wrand` package** (last consumer was the dead weighted choice); `NewMob`'s dead `rndPos`+`radius` params (every caller passed `false`; ~17 callsites); **`items.Generator` + `DepletionBehavior`** (no item JSON carried a `generator` block); the **mob-def `Generator` block** (Weight/Fixed/RespawnBehavior + enum — user decision: totem work re-adds a fresh respawn-guard field when it needs one; `generator` stripped from all 4 `api/mobs/*.json` + cp-defs); **`cfg.InitialMobCount`** (+ `initialMobCount` in `cmd/berryhunterd/conf.default.json`); **`LayerRessourceCollision`** (a layer bit nothing masked). **Gotcha caught during the layer delete:** the iota shift renumbers all higher collision bits, and `api/mobs/angry-mammoth.json` hardcodes raw ints — remapped `collisionLayer` 67→35 and `collisionMask` 544→272 (same semantic layers: PlayerStatic|Action|Viewport / Border|Placeable). **Raw collision ints in content are a standing hand-sync hazard** whenever `model/layers.go` changes (consider named layers at parse time someday); enumer regenerated via `make gen`. **In-game check done 2026-07-09** (mammoth spawn/stream/collision, mob respawn, player spawn — a first "missing mammoth" scare was NOT the remap but stale server content: edited `api/zones/scaffold.json` spawns weren't loaded because the server ran the embedded copy — editor markers bundle from repo `api/` while mobs are server-streamed, so markers show spawns the server doesn't have; the boot-log spawn count is the tell); suite + `TestDiskContent_RepoApiLoadsEndToEnd` cover the load paths. **Prior — World foundation (`plan-world-zones.md`) COMPLETE — Chunk 6 — zone-owned terrain + multi-zone selection (2026-07-09, full backend suite green, tsc + webpack green, **verified in-game via the checklist 2026-07-09**):** the world foundation is **done**. **§7.1 DECIDED → keep free-form terrain** (no snapped grid); **scope expanded mid-chunk (user call): terrain is now OWNED BY THE ZONE** (server-authoritative, one artifact per zone: bounds+terrain+props+spawns), NOT a client-only `ground-textures.json`, and the editor gained **multi-zone save/select**. **6a — schema+selection:** `world.Zone` gained `Terrain []TerrainTexture` (**parse-and-ignore** — the server declares it so `DisallowUnknownFields` accepts it + terrain typos fail by name, but never *uses* it; terrain is client-visual) + `ID` (file stem = the selection key + wire identity, distinct from human `Name`); `LoadZoneFS(fsys, name, mr, pr)` **selects by stem** — candidates enumerated *without parsing* (a broken WIP zone only breaks boot if selected), `name==""` loads the sole zone (backward-compat) or errors asking for `-zone`, unknown name errors listing available; `-zone <stem>` flag overrides a `game.zone` conf default. **6b — wire:** appended `Welcome.zone_name:string` (append-only; Go+TS regen), carried via `GameConfig.ZoneName`/`core.ZoneName(...)` ← `zone.ID`; only the *name* crosses the wire (the ~43 KB of visual data never does). **6c — client terrain:** `GroundTextureManager` stopped eagerly loading `client-data/ground-textures.json` (**DELETED**); `require.context('api/zones', …)` bundles every zone by stem; `loadZone(zoneName)` renders the **server-selected** zone's terrain × frontend `meter2px`(=120=backend `Points2px`, so terrain aligns with wire-positioned entities), called from `Game.startRendering(welcome.zoneName)`. **6d — editor:** `ZoneModel` gained a `terrain` slot filled at export from the live `GroundTextureManager` (terrain edit/render state stays there in px — no bidirectional state migration); `getZoneAsJSON` emits **name→bounds→terrain→props→spawns** (Go field order, server units); new `getTerrainServerUnits()` + `clear()`; `ZoneEditor` bundles all zones + `loadZone`/`newZone`/`rebuildMarkers`; panel got a **Load-zone picker** (+ "＋ New zone"), a **Zone id** field, one unified **Download `<id>.json`**; the standalone terrain export was removed (its popup is now a px preview); zone controls visible in all modes; manual rewritten (single-file flow + the **two reload loops**: backend restart for bounds/props/spawns, **plus a frontend rebuild for terrain** since the client bundles each zone's terrain). **6e — content:** hand-authored `api/zones/scaffold.json` (11 free-form terrain textures + 5 props (2 Tree/2 Rock blocking, 1 walk-through Rock) + 4 spawns, server units, 60×40); kept the Chunk-5 `zone.json` ("Test", no terrain) so **two zones prove selection**; `conf.default.json game.zone=scaffold`. **Follow-up fix (same day):** the editor defaulted to a hardcoded `"zone"` regardless of what the server loaded → on `-zone scaffold` you saw scaffold's terrain under Test's markers; fixed via `IGame.zoneName` (set from `Welcome` in `startRendering`) + `ZoneEditor.selectInitialZone(stem)` (aligns the editor to the server's zone **before** markers build, without touching the already-rendered terrain) called in the panel's `GamePlayingEvent` handler + `refreshPanelFromModel()`; markers + terrain now agree on join, the dropdown only *switches* zones. **Marker/entity clarification + toggle:** the editor renders **three layers from two sources** — real props/mobs are **server-streamed** (from `-zone`, unmovable by the client), while editor **markers** + **terrain** follow the dropdown; a dropdown≠server-zone shows one zone's markers/terrain over another's real entities, a match shows each prop/spawn twice (sprite + marker). Inherent to a client editor over a server-streamed world (no bug). Added a **"Hide editor markers"** checkbox → `ZoneEditor.setMarkersVisible` toggles a dedicated `markersLayer` sub-Container (bounds stays; persists across switches). **Panel overflow fix:** the shared dev panel (`#groundTexturePanel`) gained `box-sizing:border-box` + `max-height:100vh` + `overflow-y:auto` so tall mode combos (Terrain + zone controls) scroll internally instead of running off-screen. **Pins:** `world/zone_test.go` (select-by-name / unknown-name-lists-available / requires-name-when-multiple / ignores-unselected-malformed / terrain-parses / unknown-terrain-key-fails / ID-is-stem); `codec/server_test.go` (`Welcome` wire round-trip incl. `zone_name`); `loaders_test.go` `TestDiskContent_RepoApiLoadsEndToEnd` now selects `scaffold` + asserts terrain + every prop/spawn resolves. No frontend test runner → editor changes pinned by tsc + webpack + the in-game checklist. **Sandbox note:** live boot smoke tests aren't possible (the dev sandbox kills `berryhunterd` on port-bind) — the `Welcome` wire is covered by the marshal round-trip, browser paths by the checklist. Record: `plan-world-zones.md` §5 Chunk 6 + §7.1. **Prior — World foundation (`plan-world-zones.md`) Chunk 5 — in-game zone editor (2026-07-08, tsc + webpack green, **verified in-game 2026-07-08 incl. the full export→server round-trip**: authored in-editor → Download → `api/zones/zone.json` → `-content ../api` restart → world matches; zero backend/wire changes):** a full `zone.json` (name/bounds/props/spawns) can now be authored in-game. **§7.4 DECIDED → bundle directly from the repo `api/`** — the editor `require`s `api/zones/zone.json` + the `api/props/*.json` / `api/mobs/*.json` registries (webpack bundles outside `src/`, `Changelog.ts` precedent; **no duplicated `client-data/` copy**, unlike `ground-textures.json`), so dropdowns/loaded zone can't drift from what the server loads; webpack watches the file → replace → rebuild → reload = iterative loop. New feature `frontend/src/features/zone-editor/`: **`ZoneModel.ts`** (pure DOM/PIXI-free model mirroring `world.Zone`, **server units only**, `getZoneAsJSON()` in the hand-written field order, coords rounded to 2 decimals / angles 3), **`ZoneEditor.ts`** (editor-only PIXI marker overlay appended to `cameraGroup`: prop circles at true registry radius — red = `blocksMovement`, blue = decorative, inner ring = `blocksAura`, tick = rotation — green spawn diamonds + labels, yellow bounds rect with live preview; unit-space hit-testing; px↔unit ONLY via `meter2px`), **`_ZoneEditorPanel.ts`** (mode + DOM wiring). **One shared panel** (`?textures` + token, dev-only) gained an **Off / Terrain / Props / Spawns mode selector** (Off default = clicks play normally); click places with current control values + auto-selects, clicking a marker selects it (Update/Delete/Deselect; move = delete + re-place), **"Place at my position"** buttons + Mouse/You unit readouts in all modes; UI in degrees, JSON in radians. **Fixed in passing — map-click placement was TRIPLY broken:** (1) gated on a MysticWand hand-equip unsatisfiable since Block 2 (grant + `CharacterEquippedItemEvent` handler deleted); (2) registered as `click`, which `MouseManager`'s mousedown `preventDefault()` suppresses; (3) **attached to the canvas, which the full-screen `#inputAreas` joystick overlay covers — canvas listeners NEVER fire; world-click listeners must go on `document.documentElement` + `isMapPointerEvent` target filter** (accept only canvas/`#inputAreas` targets so panel/HUD presses can't place). `preventShortcutPropagation` gained the missing `text`-input case (zone-name field swallows all keys). No frontend test runner exists → `ZoneModel` factored pure but pinned by tsc + the manual round-trip; the backend's `DisallowUnknownFields` loader is the schema gate (proved in the round-trip). **User manual: `docs/manual-zone-editor.md`** (step-by-step incl. the edit→download→replace→restart loop). Record: `plan-world-zones.md` §5 Chunk 5 + §7.4. **Prior:** **Scoreboard removal (2026-07-08, `plan-rebrand-cleanup.md` Phase A.1 pulled forward; backend suite + tsc + webpack green, boot-verified; in-game check pending):** the periodic in-game scoreboard + chieftain-backed "Top 10" high scores are gone. Backend: deleted `sys/scoreboard.go`/`codec/scoreboard.go`/`model/scoreboard.go` + `core/game.go` registration, `cmd/berryhunterd/chieftain.go` + the `-chieftain` flag/handler/`dbUrl` plumbing, `cfg.Config.Chieftain` + `cfg.ChieftainConfig` + `chieftain` blocks in all `conf*.json`. Wire: `Scoreboard`+`ScoreboardPlayer` tables and the `ServerMessageBody.Scoreboard` union member removed from `server.fbs` (**`Obituary` kept — death flow**); Go + TS FlatBuffers regenerated. Frontend: `features/scoreboard/` + `ScoreboardMessage.ts` deleted; unwired from `index.ts`/`Backend.ts`/`Game.ts`/`HUD.ts`/`HUD.html`+`.less` (`#scoreboard`), start-screen "Top 10" link + dead `hideForLeaderboard` classes, `dbUrl` query param (`Urls.ts` `database` + `BasicConfig.DATABASE_URL`). **`pkg/chieftain/` + `cmd/chieftaind/` + `chieftain.fbs` are now fully orphaned** (decided: chieftain does NOT become the account service — deleted in the step-7 sweep, `plan-rebrand-cleanup.md` §4 A.3; roadmap item 3 ⚑ + tdd §4.3 flipped to decided). Rebrand/cleanup plan itself: docs written (`plan-rebrand-cleanup.md`), roadmap execution order gained **step 7 = rebrand** (accounts→8, ops→9). **Prior:** **World foundation (`plan-world-zones.md`) Chunk 3 — props as static entities (2026-07-08, full backend suite green (28 pkgs), embedded + `-content ../api` boot-verified (2 prop defs, zone loads 5 props/7 spawns both modes), **verified in-game + committed 2026-07-08** — props render at authored spots, blockers stop movement, the decorative rock walks through):** authored `zone.props` now spawn as static entities that collide (`blocksMovement`), carry `blocksAura` inert, and stream to the client with **zero wire + zero frontend changes**. **§7.2 DECIDED → dedicated prop registry as JSON content** (`api/props/*.json`, mirroring mobs): `world.PropDefinition` (name → `entityType` + `body.radius`) + `world.PropRegistryFromFS` (`world/props.go`) — hard-fails by name on unknown keys / unknown `entityType` / empty name / non-positive radius / duplicate names; `entityType` is a **name resolved against the FlatBuffers enum** (`BerryhunterApi.EnumValuesEntityType`), and the definition stores the FlatBuffers type because `world` can't import `model` (`model → cfg → world` would cycle) — the boot seam converts, like `gen`'s tables. **Prop entity = new lean `model/prop.Prop`** (`model.BaseEntity` + `blocksAura` field + `model.PropEntity` interface), NOT a reused `resource.Resource` — decision B's "new type only if reuse fights us" clause triggered (reuse would need a synthetic `items.Item`; its `Solid` is def-level while `blocksMovement` is per-instance; stock/replenish semantics are dead weight; `blocksAura` has no home). `prop.New(entityType, pos, radius, blocksMovement, blocksAura)` sets `Shape().UserData` (viewport streaming); layers: blocking → `PlayerStatic|MobStatic|Viewport` (the solid-resource bits minus generation-spacing `LayerRessourceCollision`, which nothing masks anymore), decorative → `Viewport` only (streams, never collides). **Zero `core/game.go` changes** — props fall through `AddEntity`'s existing plain `model.Entity` case (PhysicsSystem `AddStaticBody` + NetSystem; no respawn/update/status/decay systems). New codec case `model.PropEntity` → `PropEntityFlatbufMarshal` rides the existing `Resource` wire table with **capacity=1/stock=1** (the client's resource classes scale sprites by stock/capacity → full size) + an empty status-effects vector; scaffold prop types map onto existing EntityTypes (**Rock→Stone, Tree→RoundTree**), so the frontend's `gameObjectClasses` array already covers them — dedicated prop art/EntityTypes are content work (known 5-file path: server.fbs+regen, class, `gameObjectClasses`, `Graphics.ts`, SVG). **Rotation deferred:** parsed + stored on `world.Prop`, not on the wire (the `Resource` table has no rotation field; circle props don't need it — revisit when the editor places rotated props, chunk 5/6). **Wiring:** `zone.resolve` binds `Prop.Def` (unknown type fails at boot); `LoadZoneFS(fsys, mobs.Registry, world.PropRegistry)`; `contentSources.props` (embedded `pkg/api/props` + disk `sub("props")`, missing subdir hard-fails); `loadProps` boot-log count line; `Makefile cp-defs` copies `../api/props`; `berryhunterd.go` places props once after `NewGameWith` via `g.AddEntity(prop.New(...))`. **Content [PLACEHOLDER]:** `api/props/rock.json` (Stone, r 0.5) + `tree.json` (RoundTree, r 1.0); `api/zones/zone.json` authors **5 props** near the origin — 2 blocking Trees, 2 blocking Rocks, 1 **decorative** walk-through Rock at (2,5) — exercising both layer paths. **Pins:** `world/props_test.go` (valid load + all hard-fail cases), `zone_test.go` prop resolution + `TestZone_RejectsUnknownPropType`, `model/prop/prop_test.go` (layer flags per occluder flag, UserData, and **end-to-end through the real `Space.Update()`**: a player-masked circle overlapping a blocking prop is pushed out, a decorative prop leaves it unmoved), `TestDiskContent_RepoApiLoadsEndToEnd` extended (props registry + every zone prop resolved). Record: `plan-world-zones.md` §5 Chunk 3 + §7.2. **Prior — World foundation Chunk 4 — spawn-point system; mobs return (2026-07-08, full backend suite green, embedded + `-content ../api` boot-verified, **verified in-game + committed 2026-07-08**):** mobs now spawn at **authored `zone.spawns` positions** and **respawn at the same spot** after `respawnTicks ± respawnVariancePct` on death — the random/procreation population is gone. **This is the playable/commit point** the world work was driving toward. **Taken out of order** (jumped ahead of Chunk 3 props) to reach a committable playable state sooner. **`sys/mob.go` rewritten:** deleted `respawnMob`/`findMobSpawnPosition`/`findNearbySpawnPosition`/`randomMob` (+ `gen`/`wrand` imports); new `spawnPoint` state (def/pos/angle/respawnTicks/variancePct + `liveMobID`/`respawnAt`), `MobSystem.points []spawnPoint` + `initialized` flag. **Initial spawn runs on the first `Update` tick, not `New()`** — `game.AddEntity` only routes to systems registered *before* the call and SkillSystem is added *after* MobSystem, so first-Update is the earliest fully-wired moment; `spawnAt` keeps calling `mob.NewMob` so each spawn re-seeds its entity-ID RNG + rolls HP variance (gotcha #9 free). **Death → `onMobDeath`** links the dead mob to its point by `liveMobID`, schedules `respawnAt = Ticks() + rollDelay`; the respawn loop spawns each due point. **A mob owned by no point (future totem/owned entity) matches nothing → stays dead** — gotcha #7's totem guard falls out for free. **`rollDelay`** reuses `vitals.RollVariance` (`ticks × [1−pct, 1+pct]`, absent/0 = exact). **Wiring:** `GameConfig.Spawns []world.Spawn` + `core.Spawns(...)` option → `NewMobSystem(g, seed, spawns)`; `berryhunterd.go` passes `core.Spawns(zone.Spawns)` (no import cycles: `cfg`/`sys` → `world` → `mobs`). `radius`/`core.Radius` kept (unused NewMob `rndPos=false` path + `sys/respawn.go` resources). **Content [PLACEHOLDER]:** `api/zones/zone.json` authors **7 spawns** (4 Dodo @900t, 2 SaberToothCat @1800t, 1 Mammoth @2700t, 0.2 variance) inside 60×40 — scaffold to exercise the chunk, real placement in Chunk 6. **Pins:** `sys/mob_test.go` — a minimal `fakeGame` (implements `model.Game`, records add/remove, forwards to the system, settable tick); spawns-at-position (+ init-runs-once), respawn-at-same-spot-after-timer, variance-within-band, exact-delay-without-variance, no-point-no-respawn (totem guard); `MobTouches(nil, Factors{Damage:1e6})` is the lethal test path. **Obsolete-but-kept:** mob-def `Generator` block + `cfg.InitialMobCount` no longer drive anything (later cleanup). Record: `plan-world-zones.md` §5 Chunk 4. **Prior — World foundation Chunk 2 — `zone.json` schema + loader; resources stop generating (2026-07-08, full backend suite green, embedded + `-content ../api` boot-verified, **verified in-game 2026-07-08** — walked the empty bounded rectangle):** the server now loads a **server-authoritative `zone.json`** through the existing `-content` mechanism and the world's **procedural resource + random-mob generation is gone** — the world is a bounded empty rectangle until props (chunk 3) + spawn points (chunk 4) land. **New `pkg/berryhunter/world` package** (`zone.go`): `Zone`/`Bounds`/`Prop`/`Spawn` types + `LoadZoneFS(fsys, mobs.Registry)`, mirroring `RecipesFromFS` — walk `*.json`, `json.Decoder.DisallowUnknownFields()` parse (typos/stale keys fail **by name**), `validate()` (non-empty name, strictly-positive bounds), `resolve()` binds each spawn's `mob` name to its `*MobDefinition`. **Sub-decisions:** (a) loader **requires exactly one** zone (0 → "no zone file", ≥2 → "multiple zone files … not supported yet"; lifting that guard is all multi-zone later needs) — not a fixed filename; (b) spawn mob names **resolved at load time** (registry available; typos fail at boot), prop `type` resolution **deferred to chunk 3** (no prop registry yet). **Content + embed:** `api/zones/zone.json` ([PLACEHOLDER] `Scaffold` 60×40, empty props/spawns — real scaffold layout is chunk 6); `pkg/api/zones/zones.go` (`//go:embed *.json`, flat like `recipes.go`); `Makefile` `cp-defs` now also copies `../api/zones`. **Loader wiring** (`cmd/berryhunterd/loaders.go`): `zones fs.FS` on `contentSources`, embedded + `diskContent` `sub("zones")` (missing `zones/` hard-fails like the others), `loadZone` helper with a name/bounds/counts boot-log line. **Population removed** (`berryhunterd.go`): deleted the `gen.Generate` resource loop, the `initialMobCount` random-mob loop, the `Generator.Fixed` loop, and the now-dead `newRandomMobEntity`/`findMobSpawnPosition` helpers (+ 6 dead imports); **bounds now come from `zone.Bounds`** via `core.Bounds(...)` instead of the hardcoded `60×40` literal. **`radius`/`core.Radius` kept** — `sys/mob.go` + `sys/respawn.go` still read `game.Radius()` for the circular respawn paths until chunk 4. **Gotcha #6 swept:** no `gen.Generate` callers remain; the `gen` package stays (props reuse `resource_entity.go` in chunk 3; `NewRandomPos` still feeds the kept-but-now-inert respawn paths); `cfg.InitialMobCount` lingers unused (harmless). Pins: `world/zone_test.go` (valid load, empty props/spawns, unknown-key, non-positive bounds, missing name, unknown spawn mob, multiple/no zones); extended `TestDiskContent_RepoApiLoadsEndToEnd` asserts the repo `api/zones` zone loads. **Branch stays uncommitted until the world is playable again (mobs, chunk 4).** Record: `plan-world-zones.md` §5 Chunk 2. **Prior — World foundation Chunk 1 — rectangular world boundary (2026-07-08, full backend suite green (26 pkgs), tsc green, boot-verified, **verified in-game 2026-07-08** — walked all 4 corners/edges, wall holds):** the world is now a **rectangle** with a working physical wall. **`phy.InvAABB`** (`phy/inv_aabb.go`) mirrors `InvCircle` structurally — a static wall confining dynamic circles *inside* its bounds; only `intersectWithCircle`/`resolveCollisionWithCircle` carry real work (a static wall only ever meets circles and is never the querying shape, so the box/inv-circle dispatchers + `IntersectWith` panic — **no change to `Circle`/`Box`/`InvCircle` or the double-dispatch interfaces**, exactly how InvCircle was originally added). Resolution (`resolveInvAABB`) clamps the circle centre per-axis into the box shrunk by the radius, so **edges and corners fall out of the same three lines**; a box-narrower-than-circle guard mirrors InvCircle; `updateBB` doubles the half-extents (the InvCircle broadphase-overrun hack) so a body drifting just past a boundary still shares a grid cell with the wall. **Config:** `cfg.Bounds{Width,Height}` + `GameConfig.Bounds` (keep `Radius`), `core.Bounds(w,h)` option, `game.Bounds() (w,h)` + `model.Game.Bounds` (`Radius()` marked **Deprecated** — still drives the circular `NewRandomPos` spawn/gen paths until chunks 2/4); wall swapped to `NewInvAABB(VEC2F_ZERO, gc.Bounds.Width, gc.Bounds.Height)`. **Wire §7.3 = REPLACE:** `Welcome.map_radius` retired → `map_width` + `map_height` (float, ×`Points2px`); regenerated Go + TS FlatBuffers (flatc v24.3.25); consumers migrated (`codec.Welcome`, `WelcomeMessage.ts`, `Game.ts`, `EntityManager(width,height,…)`, `MiniMap.setup(w,h)`). **Frontend:** backdrop circle→rect (full-bounds `shallowWaterColor` + 240px-inset `landColor` land rect — water ring preserved rectangularly); **camera clamp rewritten circular→rectangular** (`Camera.ts` `keepWithinMapBoundaries` — a circular clamp would have fought the rectangular wall at the corners; dead `extraBoundary` removed). **§7.5 bounds = [PLACEHOLDER] 60×40 server units** (7200×4800 px), containing the radius-20 spawn circle so nothing spawns outside the wall during the temporary circle-spawn/rect-wall overlap (set in `core/game.go` + `berryhunterd.go`). Pins in `phy/inv_aabb_test.go`: per-edge/corner push-back, interior=zero force, narrow-box guard, and an **end-to-end confinement test** through the real `Space.Update()` broadphase+resolution pipeline. **Intended leftovers:** mobs still spawn in the old radius-20 circle (chunk 4 → authored spawn points); the 240px blue water ring frames the world (cosmetic; restyle in the terrain/content pass). Record: `plan-world-zones.md` Chunk 1 section + §7.3/§7.5. **Prior — Effect foundations Step 2 — status-effect framework (2026-07-08, full suite green, tsc green, boot-verified embedded + `-content ../api` (17 skills/6 milestones), **verified in-game 2026-07-08**):** **`skills.Buffs`** (`skills/buffs.go`) — ONE generic per-entity buff/debuff store with typed payloads, carried by every player + mob (field `buffs`, replacing `resistBuffs`). Inherited ResistBuffs semantics verbatim (entries keyed by source skill; per-strength streams age independently; strongest active wins within a skill; distinct skills stack; `Tick()` aging on the `ResetTickNumbers` hook), ported tests included; **`ResistBuffs` deleted**, `skills.ResistMultiplier` stays. **Slow migrated:** mob `slowFraction`/`slowTicks` fields + Update-decrement deleted; `ApplySlow(source, fraction, ticks)` now keyed by skill with the aura lifetime convention (interval+1 = the old hardcoded 2 for SlowAura's interval 1); `moveTowards` queries `SlowFraction()` (strongest active wins, cross-skill — unchanged); deliberate micro-improvement: a weaker slow's refresh no longer extends a stronger one's lifetime (the resist stream rule). **First NEW payload: dot** — effect types **`dot_aura`** (re-applies per effect tick: refresh resets duration → continuous burn in range) and **`instant_dot`** (one-shot on cooldown activation, shares the instant_damage query; `fireCooldown` dispatches both), sharing `DotParams` (`damageHP`+PerLevel, `damageTags`→physical default, `variance`, **`dotTicks` × `dotTickInterval`** — duration = count×interval+1 so "3 events over 3 s" is exact; dotTicks/interval/damage all-zero hard-fail like the stat guard; `AuraMaskFor` gained the dot_aura case — the resist-gap lesson applied preemptively). **The defining upgrade: dots outlive re-application** — they keep ticking after the target leaves the aura or the caster dies. Acting site `SkillSystem.tickDots` at the top of `processEntity` (aging at tick start, acting in the combat slice before serialization; dot entries keep a separate acting accumulator that is NOT reset on refresh, so per-tick aura refreshes can't starve a slower dot cadence); damage replays the stored caster ref (`any` in skills; sys type-switches) through **`PlayerTouches`/`MobTouches` — XP participation, kill credit, tags, per-event roll-then-mitigate and floating numbers all ride the existing paths**; each event stamps the fire `aura_hit_style`. **Zero wire changes** (visibility v1 = VFX only). `Cleanse()` (F10) in place; **buffs die with the entity** — respawn starts clean (pinned in `TestDeathRespawn_RetainsSpellbookAndProgression`). Root/mark deliberately skipped (root = slow-1.0 when a consumer exists; mark needs the §6 visibility decision). Content [PLACEHOLDER], both **milestone-unlocked at level 5**: **ImmolationAura** (id 5, aura, nearest-1 enemy, dot 5+1/lvl × 3 events @ 1 s, fire) and **Ignite** (id 22, NovaBurst-shaped cooldown 300 ticks −20/lvl, radius 1.5, uncapped, dot 6+1.5/lvl × 3 events @ 1 s ≈ 3 s burn, fire) — registry now **17 skills** (`registry_test` updated), `Skills.ts` entries added (ImmolationAura shows the default damage ring; Ignite is ringless). New pins: `buffs_test.go` (streams/stacking/expiry per payload + dot interval/duration/refresh/strongest-acts/caster-handoff/cleanse), `TestMap_DotAura` + validators, `TestApplyDotEffect_*` (level scaling, caster ref, no-friendly-fire), `TestTickDots_*` (PlayerTouches/MobTouches dispatch, fire VFX, per-event variance), `TestCooldown_InstantDotAppliesDotOnce`, slow-aura tests assert source+ticks. Decision record: `plan-effect-foundations.md` §7 banner (six sub-decisions) + §4 Step 2; schema tables in `plan-skill-system.md` (incl. fixing three stale tables: instant_damage/slow_aura still showed pre-Step-1 `targetsMobs`/`targetsPlayers`, stat_multiplier the pre-unification `additivePerLevel`). **Prior — Effect foundations Step 1 — binary faction/allegiance (2026-07-07, full suite green, boot-verified, **verified in-game 2026-07-08**):** new `model.Faction` (`FactionAligned`/`FactionHostile`, `model/faction.go`) with a `Factioned` interface on `PlayerEntity` + `MobEntity` — players return Aligned (no stored field), mobs store `faction` (explicitly initialized: Hostile is **not** the zero value; pinned by `TestNewMob_SpawnsHostile`); a setter waits for charm (YAGNI). **Content-facing schema change: `targetsMobs`/`targetsPlayers` → `targetsEnemies`/`targetsAllies`, relative to the caster's faction** — a mechanical per-caster-kind rename preserved every value across all 11 flag-carrying skill JSONs (player skills: mobs→enemies, players→allies; mob skills mirrored; FireWard `targetsPlayers:true`→`targetsAllies:true`), embedded copies synced via cp-defs; stale keys hard-fail via the Step-0 allowlist with a **rename hint** (`renamedEffectKeys` in `skills/definition.go`, covers targetsMobs/targetsPlayers/additivePerLevel/damageFraction/healFraction; pinned by `TestMap_RetiredTargetFlagKeysFailWithHint`, boot-verified against a stale disk copy). Masks are caster-faction-relative: `AuraMaskFor(def, faction)`/`InstantDamageMask(e, faction)` map enemies→opposing layer, allies→own layer via `factionLayers` (byte-identical masks for all current content; a future faction flip retargets via the per-tick re-derivation — but once factions cross entity kinds, enemy masks must **widen to both layers** with eligibility doing the exact check, noted in `plan-effect-foundations.md` §4). Eligibility: `eligibleByTargetFlags` now requires a `Factioned` target and gates on faction equality (targetsAllies) vs difference (targetsEnemies); heal-aura "ally" = same faction (+ the `PlayerEntity` vitals capability until item 7); the mob damage path deliberately stays mask-only (structures are unfactioned but must stay reachable via `targetsStructures`/`MobTouches`). **Deliberate behavior delta (improvement):** placeables satisfy `Interacter` but have no faction, so a capped player damage aura can no longer waste its `maxTargets` slot on a no-op structure hit (pinned by `TestApplyDamageAura_UnfactionedTargetNeverEatsTargetSlot`); `TestApplyDamageAura_SameFactionTargetExcluded` pins the charm-shaped case (aligned mob-like target protected by no-friendly-fire). Mob-vs-mob exclusion and no-friendly-fire are now one faction rule instead of two flag accidents. Docs: `plan-skill-system.md` schema + `tdd.md` §4.1 updated. Deferred to consumers: faction setter, mask widening, faction-aware aggro (`mob.go` aggro mask still hardcodes the player layer — item 7). **Prior — Effect foundations Step 0 — pre-refactors (2026-07-07, full suite green, disk-load verified by booting):** (1) **`EffectDef` per-type split** (`skills/definition.go`): shared core (geometry/cadence/targeting) + exactly ONE per-type payload pointer (`DamageParams`/`HealParams`/`SelfHealParams`/`SlowParams`/`ResistParams`/`StatParams`; scaling helpers became payload methods `HPAt`/`FactorAt`/`FractionAt`/`BonusAt`). **JSON stays flat** — parsing now checks every effect key against a per-type allowlist (`effectKeys`): unknown keys (typos, stale renames) and known-keys-on-the-wrong-type hard-fail **by name**, replacing the four hand-maintained `isXEffect` validators; adding a type = payload struct + allowlist entry + validator + dispatch case, no other type's code changes. **The net caught a real bug on landing:** the `items/mobs/definitions_test.go` fixture still used pre-item-11 `damageFraction`, silently dropped (= 0 damage) since the Phase-1 rename. (2) **Shared `eligibleByTargetFlags[Capability]`** predicate builder (`sys/skills.go`) replaces the copy-pasted flag gates of damage/resist auras (heal + mob-mask eligibility deliberately stay bespoke — different rules, not duplication). (3) **`-content <dir>` flag** (`cmd/berryhunterd`): loaders take `fs.FS`; `./berryhunterd -dev -content ../api` reads the repo **source of truth** directly — content edits skip **both** cp-defs and the rebuild (restart still required; `skills/milestone-unlocks.json` is code-adjacent and stays embedded); missing subdirs hard-fail at boot; the boot log prints the content source (strengthens the stale-server gotcha check). New pins: `TestMap_UnknownEffectKeyFails`, `TestMap_StaleAdditivePerLevelKeyFails`, `TestParse_ExactlyOnePayload`, `TestDiskContent_RepoApiLoadsEndToEnd` (**first load-validation of repo `api/` itself** — embedded-copy tests can't catch un-cp-defs'd edits), `TestDiskContent_MissingSubdirFails`; the old `effectDamageHP`/`effectHealHP` helper tests re-pin the payload methods. Verified end-to-end: booted with and without `-content` (identical counts 15 skills/4 mobs/1 recipe/10 items); injected a stale `additivePerLevel` into `api/damage-aura.json` → disk boot hard-fails naming the key, embedded boot unaffected (edit restored). No wire/frontend/content changes; gameplay identical. **Prior — Effect-system scaling decision + foundations plan (2026-07-07, docs-only, no code):** investigated growing the effect vocabulary from 8 to ~25+ types against a concrete ~35-idea candidate list (life steal, execute, thorns, DoT/CC, summons, shields, …). Outcome — decisions **F1–F10** in **`docs/plan-effect-foundations.md`**: **effect semantics stay hand-written Go effect types; no scripting engine for effects (off the table, not parked)** — the candidate list proved primitive-clustered (~5–6 missing engine primitives gate two-thirds of it; scripting would only relocate the cheap parse/dispatch third into a runtime-error domain), and composition semantics (pen × resist × shield × thorns × lifesteal ordering) belong in one audited Go pipeline. Constrained expression layer parked behind an explicit trigger (F2); encounter-controller Go-structs lean upheld (F3). Growth plan (F4/F5, sequenced in the doc §4): pre-refactors first (`EffectDef` per-type split — the fat struct is at its flagged ~30-field limit; shared eligibility predicate; dev-mode disk-load flag), then infrastructure in dependency order: **binary faction** (today "friendly" is a `PlayerEntity` type assertion — the hidden foundation under summons/charm/decoys) → **status-effect framework** (generalize `ResistBuffs`, whose `Tick()` lifecycle is already wired on player + mob) → **spawned-entity lifecycle** (totem ≈ velocity-0 mob + aura + `Decayer`-TTL; ownership/XP attribution is the gap) → **shield-as-buff-payload**; on-death/on-kill event hook with its first consumer. Design answers recorded: control effects **mobs-only in v1** (F7), faction **binary player-aligned vs hostile** (F8), stealth v1 = **transparency + aggro drop, no per-viewer info hiding** (F9), **everything cleansable** until proven otherwise (F10); damage-pipeline composition order needs its own decision record before two of {pen, shield, thorns, lifesteal} coexist (F6). Scripting research docs archived (`archive-scripting-audit.md`/`-options.md`, banners added); `tdd.md` §4.1 open-decision line flipped to decided; taunt/threat stays parked at roadmap item 7. **Prior — HUD bar text — absolute health + XP numbers (2026-07-07, backend suite + tsc/webpack green; in-game check pending):** the bottom-right HUD bars now render centered text — health `currentHP/maxHP`, XP `xpInLevel/xpForNextLevel` (**within-level** XP, same span the bar fraction shows; resets to `0/needed` on level-up **and on death** — no UI code needed for the latter, since `sys/state.go` calls `LoseCurrentLevelExperience()` *before* stashing the carried progression, so the first post-respawn tick already sends 0). **Wire:** appended `xp_in_level:uint` + `xp_for_next_level:uint` to `Character` (table-end, wire-compatible; regenerated Go + TS FlatBuffers). Backend: new `player.LevelProgressXP() (gained, required uint64)`; `LevelProgressFraction()` now derives from it (single home of the level-span math — this also replaced the old dead unsigned `fraction < 0` guard with an explicit `Experience < levelStart` check, closing a latent underflow); emitted once in `codec.characterCommonMarshalFlatbuf` (both Character encode paths share it). Frontend: `.barText` span in `#healthBar`/`#satietyBar` (`HUD.html`; styled in `vitalSigns.less` — centered overlay, z above the delta indicators, `pointer-events: none`), fed per tick via `HUD.updateBarTexts(...)` from `Player.updateFromBackend`. Pinned by `TestPlayer_LevelProgressXP_TracksLevelUpAndDeathPenalty` (0/100 fresh → 50/100 → level-up rollover 50/200 → death 0/200) and an extended `TestDeathRespawn_RetainsSpellbookAndProgression` (asserts 0/200 end-to-end through the death→stash→restore path). **Prior — Level-scaling unification (2026-07-07, code-quality items `research-code-quality.md` §3.2+§3.3):** ONE scaling convention system-wide — every leveled value is `base + (level−1) × perLevel`, expressed via generic `skills.Scaled(base, perLevel, level)` (`skills/scaling.go`; per-field floors stay at call sites), replacing 13 hand-written copies. `stat_multiplier`'s divergent single-field `additivePerLevel` (× level) became the paired `statBonus`/`statBonusPerLevel` — value-preserving migration of swift/tough-passive.json (`0.05` → `0.05/0.05` etc.); a `stat_multiplier` with both fields 0 **hard-fails at load** (catches stale `additivePerLevel` keys, which `json.Unmarshal` drops silently), and stat fields on non-stat effects hard-fail (mirrors the variance/resist guards). Pinned by `TestScaled`, `TestDerivedStats/base_and_perLevel_scale_independently`, `TestMap_StatMultiplierNoScalingFails`, `TestMap_StatFieldsOnNonStatEffectFails`. **This one-sentence rule is the item-12 authoring reference for scaling.** No wire/frontend changes. **Prior — Item 11 (deferred) Phase 3 — stat variance & damage ranges — COMPLETE (verified in-game 2026-07-06).** Decisions C1–C6 recorded in `docs/plan-item11-hp-resist-variance.md`: variance is a **percentage band around the programmatic center** (uniform roll in `[center×(1−v), center×(1+v)]`, valid `[0,1)`, absent/0 = static — the only representation, no min/max fields); **mob maxHealth rolls once at spawn** from the mob's entity-ID-seeded `m.rand` (**mobs only** — player max HP stays deterministic; a zero variance consumes **no RNG draw**, keeping seeded drop sequences intact); **damage + heal amounts roll per hit** — every target in a tick rolls independently — from a time-seeded, test-injectable `sys.SkillSystem.rng`; **roll first, then mitigate**: resistance multiplies the ROLLED value, min-1 rounding last (pinned by `TestApplyDamageAura_VarianceComposesWithResistance`); flat variance for all mobs (no tier scaling in v1); overhead shows the exact post-mitigation roll, **no crit styling**. Mechanics: shared `vitals.RollVariance(center, variance, rnd)`; `mobs.Factors.MaxHealthVariance` (JSON `factors.maxHealthVariance`); `skills.EffectDef.Variance` (JSON `variance` — only on `damage_aura`/`instant_damage`/`heal_aura`/`self_heal`, hard-fails on effects with nothing to roll); `selfHealHP` now returns the float center so the roll wraps the fraction-of-max heal cooldown too; heal-aura **self-cost stays static** by design (predictable build cost). **No wire/frontend changes** (per-mob `max_health` + literal per-hit HP numbers already existed). Content [PLACEHOLDER]: DamageAura `variance: 0.15`; Dodo + Mammoth `maxHealthVariance: 0.1` (SaberToothCat/AngryMammoth deliberately exact as control). **Prior — Item 11 Phase 2 — resistances & damage tags — COMPLETE (committed c0426e35, verified in-game 2026-07-06 incl. two in-game-found fixes: the `AuraMaskFor` resist case and the per-strength buff-stream expiry).** Damage/resist types are **arbitrary string tags** (never an enum). Decisions B1–B7 recorded in `docs/plan-item11-hp-resist-variance.md`: stacking is **keyed by source skill** (same skill from two casters → strongest wins, mirroring `ApplySlow`; **distinct sources — different skills, passive+aura — always stack multiplicatively**, so immunity is unreachable by stacking, only a single source granting 0 → content-design responsibility); untagged damage effects normalize to the reserved **`physical`** tag at parse (`skills.DamageTagPhysical` — armor equivalent, no "matches nothing" damage); **`Vulnerability` deleted everywhere** (struct+JSON+takeDamage; a `"*"` resist key can resurrect it if ever needed); bounds: multiplier ≥ 0, **0 = immune** (a fully immune hit is a **non-event**: no HP loss/number/status effect/VFX), **>1 = vulnerability**, no cap; heavily-resisted hits show 1 via min-1; **"RESIST" styling deferred** (needs a transient wire flag à la `aura_hit_style` — add when content ships a real immunity); ordering race accepted: **buff lifetime = tick interval + 1** survives one tick boundary (pinned by `TestMob_ResistBuff_ComposesWithBaseAndExpires`). Mechanics: hit payload `model.Damage{HP, Tags}` through `Interacter.PlayerTouches` (mob path: payload-only `mobs.Factors.DamageTags`); `mobs.Factors.Resistances map[string]float32` (JSON `factors.resistances`, validated ≥ 0/non-empty tags); `skills.ResistMultiplier(tags, sources...)`; transient `skills.ResistBuffs` on mob + player (keyed by source skill, `Tick()` rides the `ResetTickNumbers` lifecycle); new effect types **`resist_aura`** (`resistTags`/`resistFactor`+`PerLevel`/`targetsSelf` — self-buff outside the target cap; reuses the whole targeting pipeline via `sys.applyResistAura`) and **`resist_passive`** (folds into `DerivedStats.Resistances`, per-tag product across equipped passives); player damage = resist passives × buffs (untyped `damageReduction` unchanged on top), mob = base resistances × buffs. **New `SKILL <name>` cheat** (spellbook discovery + recipe cascade, same seam as real unlocks). Content [PLACEHOLDER]: `AngryMammothAura` deals `["fire"]`; new **FireWard** aura (ID 40, `api/skills/fire-ward.json`, fire ×0.6/0.5/0.4 L1–3, allies+self, tick 1) — registry now **15 skills** (`registry_test` updated); frontend: `Skills.ts` ID-40 entry + FireWard shows the **heal-style ring** (`FIRE_WARD_SKILL_ID` in `Character.setActiveSkill`); **no wire/FlatBuffers changes**. In-game check: `SKILL FireWard` → equip → boss aura numbers drop 4→~2–3, step out → full damage after ~1 tick. **Prior — Item 11 Phase 1 — absolute HP system — COMPLETE (committed, verified in-game).** Health + a new `maxHealth` are now **absolute integer HP** (was a single normalized 0..1 fraction of `vitals.Max`, identical for every entity). New primitives: `vitals.HP()` (round + **min-1 rule** — a real hit/heal never rounds to 0) and `VitalSign.AddCapped(n, maxHP)`. `EffectDef` fraction fields → `DamageHP` / `HealHP` / `SelfDamageHP` (+`PerLevel`); `mobs.Factors` gained `MaxHealth` (default-100 guard for direct construction) + renamed `DamageFraction`→`Damage`; new `PlayerConfig.BaseHealth` ([PLACEHOLDER] 100, `conf.default.json`). Player `MaxHealth() = round(BaseHealth × MaxHealthFactor)`; `takeDamage` is now **flat HP** (no more `/MaxHealthFactor`); heals clamp at maxHealth; **aura heals are flat HP** (a set value + per-level, **NOT** % of max — this is the aura-heal design; the heal **cooldown** instead heals a **% of max HP**, see review follow-ups); heal self-cost is flat HP; out-of-combat regen is absolute (player keeps a **fractional accumulator** because `healthGainTick` × maxHealth is <1 HP/tick). `HealthRatio = health/maxHealth` (selector contract unchanged). Wire: appended `max_health:uint` to `Mob` + `Character` (health / damage_taken / heal_received are now absolute HP); codec `Mob/CharacterAddMaxHealth`; **regenerated Go + TS FlatBuffers** (flatc v24.3.25). Frontend: overhead + HUD bars draw `health/maxHealth`; deleted `HEALTH_DISPLAY_SCALE` + `Mob|Character.MAX_HEALTH`; `vitalUnitsToDisplay`→`hpToDisplay` (identity) so floating numbers show **literal HP**. Content [PLACEHOLDER]: mob maxHealth Dodo 40 / SaberToothCat 60 / Mammoth 120 / AngryMammoth 400 (all `vulnerability→1`); every aura/cooldown converted fraction→HP (DamageAura 7 +1.6/lvl, HealAura heal 6 +3/lvl / self-cost 9, NovaBurst 25, Heal cooldown 20 +5/lvl, etc.); WildAura + boss aura gained tick intervals so fast ticks don't min-1-spam. Fixed a **pre-existing** stale test (`registry_test` expected 13 skills; PaladinAura made it 14 back in Phase 9). **Phases 2 (resistances & damage tags) and 3 (stat variance & damage ranges) have since landed — see above.** **Prior — Phase 9 (aura combinations) COMPLETE — tested in-game + committed** (see docs/plan-skill-system.md → Phase 9). Curated, secret, backend-only recipe system: `api/recipes/*.json` loaded via `skills.RecipesFromFS` (result/ingredient names resolved against the skill registry; hard-fail validation — unknown names, level `<1`/`>maxLevel`, empty ingredients, duplicate recipe IDs). `skills.ApplyRecipes(sc, recipes)` is a monotonic-cascade evaluator: discovers every result whose ingredients are all simultaneously at **≥** their level (pure threshold, nothing consumed), cascading until fixpoint; idempotent + cycle-safe (skips already-discovered results); no-op for mobs (nil spellbook). Player carries the registry (`p.recipes`, from `GameConfig.Recipes`); single seam `player.ApplyRecipeCascade()` is called at the three trigger sites — milestone unlock, mob kill-drop, and EquipSystem point **raise** (not unspend). **No wire footprint** — clients see combo results through the normal spellbook stream + existing 3.7 unlock glow. **First content: PaladinAura** (skill ID 30, `api/skills/paladin-aura.json`) unlocked by `DamageAura L5 + HealAura L5` — a **two-effect** aura (damage nearest enemy @ interval 20, heal lowest-HP ally @ interval 60), values a constant **70% of the base auras at every level** (dmg 0.126/+0.028, heal 0.042/+0.021, no heal self-cost — all **[PLACEHOLDER]**). **Required a tick-cadence fix:** `sys/skills.go` now uses a monotonic accumulator + per-effect `acc % interval == 0` (equip/`SetActiveAura` reset to 0), so a multi-effect aura runs each effect on its own cadence — replacing the old shared-max-interval reset that (latent bug) re-fired a short-interval effect every tick. Frontend: `Skills.ts` ID-30 entry + `PALADIN_AURA_SKILL_ID`; PaladinAura shows **both** aura rings (`Character.setActiveSkill`). **Prior — roadmap item 11 (aura targeting) COMPLETE:** Steps 1–3 as before (selector/cap machinery in `sys/targeting.go`; base auras single-target; floating damage/heal/XP numbers). **Step 4 — per-tick hit VFX (slash vs fire):** SkillSystem stamps an aura-hit style on each struck damage-aura target via `model.AuraHitNotifier.NoteAuraHit(style)` (separate from the `takeDamage` number recording); transient `aura_hit_style:ubyte` wire field on `Mob`/`Character` (0 none / 1 slash / 2 fire), reset on the `TickAccumulators` lifecycle. Style from `sys.auraHitStyleFor`: **per-effect `hitStyle` JSON override** (`slash`/`fire`/`none`) wins, else `auto` derives from cadence (interval ≥ `auraSlashTickThreshold` **[PLACEHOLDER 15]** → slash). Frontend `GameObject.showAuraHit` — single-instance sprite refreshed per hit tick: fast → sustained fire cluster over the avatar, slow → discrete slash streak sweeping across the model. **Replaced/removed the old `DamagedAmbient` white-flash** on mobs + characters. **Step 5** tick-interval verified. **Content compensation:** base auras retuned so slower ticks keep DPS/HPS (DamageAura int 20, MammothAura 20, HealAura 60/2s, DodoAura 24/0.8s, SaberToothCatAura 10/0.33s — all **[PLACEHOLDER]**); overhead health bars moved **below** the avatar (mobs + player in-world bar; HUD bar unchanged). **Prior:** Phase 8 complete (13 skills, 4 milestones); Block 2 survival removal complete.
- **Next:** **Execution order DECIDED 2026-07-08 — systems-first, content-last** (canonical list: `roadmap.md` "Execution order"; tech mirror: `tdd.md` §6). Consciously trades a fast playable build for author-once content. **Remaining sequence:** **(1) World foundation ✅ COMPLETE** [`plan-world-zones.md`, all 6 chunks done + in-game-verified: Chunk 1 (`phy.InvAABB` rectangular boundary) + 2 (`zone.json` schema/loader, procedural generation removed) + 4 (authored spawn points + same-spot respawn) + 3 (props as static entities: dedicated `api/props/` registry + lean `model/prop.Prop`) + 5 (in-game zone editor, §7.4 bundle-from-repo) + **6 (zone-owned free-form terrain §7.1 + multi-zone save/select, verified in-game 2026-07-09 — see Last completed)**. The world is a server-authoritative, selectable single Space; real zone *content* is deferred to the item-12 pass.] → **(2) Mob depth + totems ← IN PROGRESS (`plan-mob-depth.md`)** [9 chunks: **1 totem ✓ DONE, verified in-game 2026-07-09** → **2 flee ✓ IMPLEMENTED 2026-07-09** → **3 aggro & threat ✓ IMPLEMENTED 2026-07-10 (see Last completed; entity-keyed threat + faction-aware aggro + state-dependent leash + auras-off-until-aggroed; closes the chunk-1 interim — mobs aggro the totem; chunks 2+3 VERIFIED IN-GAME 2026-07-11 — two findings fixed same day + re-verified)** → **4 obstacle steering ✓ DONE + VERIFIED IN-GAME 2026-07-11 (see Last completed)** → **5 patrol ✓ IMPLEMENTED 2026-07-11 (see Last completed; ⚑ §6.6 closed: inline waypoints, ping-pong; WoW-classic evade return; wander-band respawn; in-game verify PENDING)** → **6 companion ✓ DONE + VERIFIED IN-GAME 2026-07-11 (see Last completed; ⚑ §6.4 closed: TTL totem-style, teleport catch-up, max-one unenforced convention; owner combat stamps + follower branches)** → **6.5 hazard braziers + companion reachability ✓ DONE + VERIFIED IN-GAME 2026-07-11 (fix chunk)** → **6.6 mob factions & mob-vs-mob hostility ✓ DONE + VERIFIED IN-GAME 2026-07-11 + same-day harm-gate fix (see Last completed; §6.8 decided+amended: per-faction hostileTo list, def-level authoring, masks widened to LayerCombatants, rewards on any death, mob-cast harm = aggroSet ∪ threat via the mayHarm seam; fix re-verified same day)** → **7 taunt ✓ DONE + VERIFIED IN-GAME 2026-07-11 (force-to-top Taunt + single-entry-removal Fade, both player cooldowns; 2 mob threat seams; zero wire changes; skills 21→23, milestones 8→10; see Last completed)** → **8 support ← NEXT (plan-first, new session)** mob-heal → 9 encounter-controller spine + threat integration (shaped by the doc'd lava-bridge boss)] → **(3) LoS + darkness/light** [roadmap items 6+5: LoS blob-perf spike→occlusion; `light_aura` effect type + campfires] → **(4) Skill-vocabulary fill** [effect-foundations Step 4 shield-as-buff-payload + cheap effect types: life steal, execute, crit, berserker] → **(5) Unlock-source systems** [roadmap item 9: world clue anchors + NPC teaching] → **(6) Initial content pass (item 12 — the prove-it gate)**: real zones, full mob roster (replace legacy Berryhunter mobs), boss scripts, skills/passives/cooldowns, combination recipes, first balance pass; assign real `damageTags`+`resistances` across the roster (every aura defaults to `physical`; only the boss aura is `fire`) **and real variance bands** (only DamageAura `0.15` + Dodo/Mammoth HP `0.1` carry placeholder bands today); more recipe content is a pure `api/recipes/`+`api/skills/` JSON edit (no code) → **(7) Rebrand to Aura & Berryhunter cleanup** [`plan-rebrand-cleanup.md`: dead features (rating, chieftain, survival/item scaffolding) → structural rename (module/`pkg/berryhunter`/`berryhunterd`/`BerryhunterApi`) in one atomic commit → branding; **scoreboard removal pulled forward, done 2026-07-08**; hard deadline: before ops] → **(8) Accounts & persistence + UI polish/avatar** [roadmap items 3+8, deliberately AFTER content; account service built fresh — chieftain decided against + deleted in step 7] → **(9) Ops & closed-alpha readiness** [`research-v1-readiness.md`]. Steps 0+1+2 of effect foundations ✓ done and verified in-game (2026-07-08). **Review follow-ups (DONE, committed):** (1) the **heal cooldown** now heals a **% of max HP** — new `self_heal` fields `healFractionOfMax` (0.20) + `healFractionOfMaxPerLevel` (0.05 absolute → 20% / 25% / 30% at L1/2/3), applied via the `selfHealHP` helper; when set it overrides flat `healHP`, so the heal **aura** stays flat by design. (2) the self-heal cooldown now **records the floating heal number** (`NoteHealReceived` in the `self_heal` path; frontend already rendered it). Verified: `HealFractionOfMax` scaling ×max-HP + heal number pinned by `TestCooldown_SelfHealFractionOfMaxAndNumber`. **Block 2 COMPLETE** (survival removal, roadmap items 1+2 ✓) — see docs/plan-block2-survival-removal.md.
- **Current state:** **The world is a rectangle with a hard physical wall, loaded from a server-authoritative `zone.json` (world foundation chunks 1+2):** `phy.InvAABB` border sized by the zone's `bounds` (via `GameConfig.Bounds`, [PLACEHOLDER] 60×40 server units); client draws a rectangular water-ring + land backdrop and clamps the camera rectangularly; world size travels on `Welcome.map_width`/`map_height`. **Procedural resource + random-mob generation is removed;** the world's population source is now `api/zones/zone.json`, loaded via `-content` (or the embedded copy). **Mobs spawn at authored `zone.spawns` positions and respawn at the same spot** after `respawnTicks ± respawnVariancePct` on death (world foundation chunk 4) — scaffold content is 7 spawns (Dodo/SaberToothCat/Mammoth) in the 60×40 bounds. **Props are placed from `zone.props`** (chunk 3): each resolves against the `api/props/` registry (Rock→Stone, Tree→RoundTree sprites) into a lean static `model/prop.Prop` — `blocksMovement` puts it on the player+mob static-collision layers, decorative props stream but never collide, `blocksAura` is stored inert until item 6; scaffold content is 5 props near the origin (2 Trees + 2 Rocks blocking, 1 walk-through Rock). **Players spawn uniformly in the central 80% of the zone bounds** (interim until authored spawn points); the circular-world/procedural leftovers (`game.Radius()`, RespawnSystem, `wrand`, item/mob `Generator` blocks, `InitialMobCount`, `LayerRessourceCollision`) are **deleted** (dead-code sweep 2026-07-09, verified in-game 2026-07-09). **Terrain is zone-owned and the world is multi-zone-selectable (chunk 6):** a zone is one server-authoritative file (`api/zones/<id>.json`) carrying **bounds + free-form terrain + props + spawns**; the server loads the zone chosen by `-zone <id>` (or `game.zone`; a lone zone loads without a flag, 2+ require selection), streams its `id` on `Welcome.zone_name`, and the client renders that zone's terrain (bundled by stem, scaled ×`meter2px`). Terrain is client-visual — the server parse-and-ignores it. Repo ships ONE zone since 2026-07-11: `proving-grounds` (120×80 all-assets debug/test map, 2423 terrain + 476 props + 381 spawns incl. the chunk-5 archetype content); `conf.default.json game.zone=proving-grounds` (a sole zone also loads with no flag). `scaffold`/`zone` are deleted. **The in-game zone editor is live (chunks 5+6):** `?textures` (+ valid token) opens the shared dev panel with Off/Terrain/Props/Spawns modes — click (or "Place at my position") to place, click a marker to edit/delete; a **Load-zone picker** (+ "＋ New zone") switches/creates zones and the editor **defaults to the zone the server loaded** (`Welcome.zone_name`) so markers + terrain line up; one unified **Download `<id>.json`** (server units, incl. terrain); iterate via `api/zones/<id>.json` + `-content ../api` restart (**+ frontend rebuild for terrain**). Editor data is bundled straight from repo `api/` (§7.4), manual in `docs/manual-zone-editor.md`. **Key frontend input lesson from chunk 5:** the full-screen `#inputAreas` joystick overlay covers the canvas — world-click listeners must attach to `document.documentElement` (with a target filter) and use `pointerdown`, never `click` on the canvas. new players start with DamageAura in slot 0 on spawn, server-authoritative from spawn. **Base auras are single-target (item 11):** DamageAura/WildAura hit the nearest one mob per tick; HealAura heals the lowest-%-HP ally a **flat HP** amount; NovaBurst/boss stomp remain AoE-all. **Health is absolute integer HP (item 11 Phase 1):** per-mob `maxHealth`, player `MaxHealth = BaseHealth × MaxHealthFactor`; overhead + HUD bars draw `health/maxHealth`; floating damage/heal/XP numbers render over mobs + players as **literal HP** (no more display-scale placeholder). The bottom-right HUD bars also show the absolute numbers as centered text: health `currentHP/maxHP`, XP `xpInLevel/xpForNextLevel` (within-level; wire `Character.xp_in_level`/`xp_for_next_level`; reads `0/needed` after level-up and after the death XP penalty). Damage auras stamp a per-hit VFX (slash for slow ticks, fire for fast — per-effect `hitStyle` override or cadence default); the old white damage flash is gone. Overhead health bars sit **below** the avatar (mobs + player in-world bar; the bottom-right HUD bar is separate). Unlock sources live: milestones (HealAura+Heal L2, SwiftPassive L3, NovaBurst L4, ImmolationAura+Ignite L5), kill drops (WildAura: SaberToothCat 20%/boss 100%; SlowAura: Mammoth 20%; ToughPassive: Dodo 5%). Skill points: earned per player level, spent/refunded freely in the spellbook panel, equipped skills scale live. All combat participants (incl. healers, ~10s window) get full XP on mob death. `XP <amount>` cheat for manual leveling. **Combinations (Phase 9) live:** maxing `DamageAura + HealAura` (L5 each) secretly unlocks **PaladinAura** (damage+heal two-effect aura); recipes are curated/secret/backend-only, discovered by hitting ingredient-level thresholds. **Resistances & damage tags live (item 11 Phase 2):** every damage effect carries string tags (`physical` default), mobs can declare base `factors.resistances`, `resist_aura`/`resist_passive` grant tag resistance (FireWard: fire ×0.6 to allies+self), and the `SKILL <name>` cheat grants any spellbook entry by name. **Stat variance live (item 11 Phase 3):** mob HP pools roll once at spawn (`factors.maxHealthVariance` percentage band), damage + heal amounts roll independently per hit (`variance` on the effect, rolled before mitigation); anything without an authored band stays exact. **Status-effect framework live (effect foundations Step 2):** `skills.Buffs` is the one per-entity buff/debuff store (resist + slow migrated in; dots are the first acting payload); `dot_aura`/`instant_dot` apply fire dots that keep ticking after the target leaves range — ImmolationAura (aura, single-target burn) and Ignite (burst, everyone in range, ~3 s) at milestone L5, damage riding the normal attribution/mitigation/floating-number paths with the fire hit VFX. **Threat & aggro are live (mob-depth chunk 3, implemented 2026-07-10, verified in-game 2026-07-11 incl. two same-day-fixed findings):** every mob holds an entity-keyed threat table (post-mitigation HP per hit; summon hits credit the summon via `Damage.Source`, XP the owner; heals credit the healer ×0.5 [PLACEHOLDER] on mobs in combat with the heal target — threat table holds it OR it is the current aggro target, §6.3 amended 2026-07-11); the aggro target is the highest-threat living enemy whenever the table is non-empty (sensor only acquires; out-of-sensor hits retaliate), the fixed territory leash is replaced by a state-dependent one (no leash while the target is within aura reach, inside the aggro sensor, or dealt damage since last tick; ~3 s [PLACEHOLDER] countdown once it is unreachable AND out of sight — the sensor half added 2026-07-11 to kill the chase flicker), and moving mobs run their aura — and show their wire-driven ring (`Mob.aura_radius`) — only while aggroed (stationary hazards always-on). **Obstacle steering is live (mob-depth chunk 4, implemented + verified in-game 2026-07-11):** every moving mob composes repulsion from nearby blocking statics into its step direction (`model/mob/steering.go`) — chase, walk-home and flee deflect around blocking props and the border wall instead of jamming (linear falloff within `steeringLookahead` [PLACEHOLDER 0.6, was 1.5 — first in-game round]; head-on cancel deflects perpendicular, tick-stable side, exact-line → left); the probe carries the mob's own body mask, so mobs that walk through a static (AngryMammoth vs rocks) are never repelled by it, and a target inside a blocker makes the mob orbit instead of grinding. Steering needs the `phy.Space` — `NewMob(def, margin, space)`; nil (tests) = the exact pre-chunk straight-line movement. **Patrol archetypes are live (mob-depth chunk 5, implemented 2026-07-11, in-game verify pending):** idle movement runs at the mob's idle pace (def `factors.idleSpeedFactor`, global default 0.4× [PLACEHOLDER], per-spawn override) — a spawn's tri-state `wanderRadius` (absent = mob-type default `factors.wanderRadius` — Dodos graze by default at 0.25× with 8–30 s dwells [PLACEHOLDER]; explicit 0 = stationary; >0 = override) makes its mob amble between random points in the disc around the AUTHORED spot and (re)spawn at a random point within it; `waypoints: [{x,y},…]` makes it march the route at the same idle pace, ping-pong or `patrolMode: "loop"` (explicit-radius+waypoints, mode-without-waypoints, ≥2 points, speed-0 mobs — all boot-time hard-fails); every mob now records where it left idle for combat and, after the leash/reset, RUNS back at full speed to that exact point before resuming (WoW-classic evade; walk-home-to-spawn is the stationary special case), and the aggro sensor follows the body (mob-centered acquisition — patrollers aggro mid-route). Zone editor: wander-radius input + disc preview, per-spawn waypoint authoring ("Add on map click" toggle + numbered route polyline); proving-grounds exercises both generously (14 wanderers + 7 routes across all regions). **Flee is live (mob-depth chunk 2, implemented 2026-07-09, fully verified in-game with chunk 3 on 2026-07-11):** a mob whose definition sets `factors.fleeBelowHealthRatio` runs from its aggro target while its health ratio is strictly below the threshold (inverse chase vector, slow-aware, wall-sliding via the InvAABB clamp); smoke consumer is the new **Rabbit** (EntityType 18, mob id 6, DodoAura, flees below half HP, 2 scaffold spawns) — registry is 6 mobs. **The companion cooldown is live (mob-depth chunk 6, verified in-game 2026-07-11):** SummonCompanion (milestone L7, id 24) spawns an owned, aligned, MOVING summon (Companion, mob id 7, EntityType 19) — owned + moving = follower: it trails the owner at full speed on a ~1.5 u hold ring (teleport catch-up beyond ~15 u), skips the evade-return machinery, and fights per §3.6 — it acquires the last mob that damaged the owner (defend, priority) or the last mob the owner directly damaged (assist), read as O(1) combat stamps off the owner (`model.AttackNotifier`/`model.CombatSignals`, ~3 s window [PLACEHOLDER], direct casts only — summon-sourced damage never stamps); sticky until the target dies or drifts >10 u from the OWNER, then back to follow; it refuses aura-unreachable stamp targets (`auraCanReach`: slot-0 aura mask vs target body layer — it never suicides into hazards, fix chunk 6.5); aura ring shows only in combat (chunk-3c gate); max-one is a TTL ≤ cooldown content convention, deliberately unenforced (cooldown reduction beating the TTL = multiple companions as the reward). **Spawned entities live (mob-depth chunk 1, verified in-game 2026-07-09):** the `spawn` effect type summons owned mobs — SummonTotem (milestone L6, id 23) places a player-aligned, stationary, TTL'd Totem (mob id 5, TotemAura fire dot) offset beside the caster (random unblocked point, fallback caster position); its damage credits the owner's full `PlayerTouches` path (XP/kill drops/recipes/floating numbers), summon-skill level scales TTL + loadout level, owner player level scales bonus HP + a damage/heal power multiplier (never CC); TTL expiry uses the normal death path with no rewards and no respawn; chunk 3a closed the interim: hostile mobs aggro the totem (faction-aware acquisition) and its damage builds its own threat while XP stays on the owner. **The Ember Ring braziers are their own `Brazier` mob (fix chunk 6.5, verified in-game 2026-07-11):** hostile, stationary, TotemAura, **Viewport-only body layer = structurally unkillable pure hazard** — no damage mask sees it, it interacts with nothing except burning the player layer; the player-summon totem def is unchanged. **Mob factions & mob-vs-mob hostility are live (mob-depth chunk 6.6, implemented 2026-07-11, in-game verify pending):** factions are content (`api/factions/*.json`: name + explicit `hostileTo` list; built-ins `aligned`/`hostile` reserved; mob defs opt in via a top-level `faction` key — absent = hostile default that attacks players and ignores all mobs, exactly the pre-factions behavior); the hostility list gates PROACTIVE sensor acquisition (aggro sensor masks follow it — legacy mobs keep player-only sensors, passive factions' sensors see nothing), and mob-cast harm follows the two-layer rule `aggroSet ∪ threat table` via the single sys `mayHarm` seam (harm-gate fix, same day: neutral factions never splash each other, hazards never burn mobs that can't hurt them back, bystander players aren't splashed by retaliating prey — while retaliation itself always works since the attacker is on the threat table; player-sourced damage incl. summons stays “different faction = may harm”; threat retaliation and flee remain faction-inequality), so prey fights back and flees per its own rules; ALL faction-flag aura/instant masks span both combatant layers (`model.LayerCombatants`) with exact-faction eligibility everywhere (`applyMobDamageAura` included — no same-faction splash, structures still reachable), ally auras reach the aligned companion, and kill rewards fire on any death (participants get XP on a mob killing blow; a pure mob-vs-mob kill settles with zero rewards, closing the old poke-the-corpse XP leak). Smoke content: Rabbit+Dodo=prey, SaberToothCat=predator (hunts prey+tuskers), Mammoth=tusker (fights predators) → cats hunt dodos/rabbits map-wide, plus a perpetual 4v4 cat-vs-mammoth frontline east of the Sand Flats (~x 44 / y 11, respawn 300t ±30%); proving-grounds spawns 389. **Taunt & Fade are live (mob-depth chunk 7, verified in-game 2026-07-11):** two player cooldowns operating on mob threat tables — **Taunt** (milestone L8, id 25) forces every enemy mob in range to the top of its threat table (`ForceThreatToTop` = current max + `threatMargin` [PLACEHOLDER 50], granting the mob harm rights on the taunter for free via the 6.6 `MayHarm` gate), so retention swings its aggro onto the caster (the mob's AURA still hits nearest en route — accepted); **Fade** (milestone L8, id 26) is a single-entry removal (`DropThreat`) of the caster's OWN threat, shedding aggro to the next-highest holder (a group tool — no-op solo, and no target-lock in v1). Both are cooldowns via `applyThreatEffect` in `fireCooldown` (query circle + `eligibleByTargetFlags`/`mayHarm`), player-cast only, zero wire changes; taunt/fade on a companion no-ops (followers never read their own threat table). New effect types `taunt`/`detaunt` share a `ThreatParams{Margin}` payload; skills 23 / milestones 10.
- **Deferred tech debt / known bugs:**
  - **FIXED — `blocksAura` plumbing deleted (sweep done 2026-07-11, decided at the LoS cut 2026-07-10):** `world/zone.go` schema field, `model/prop.Prop` field + the `PropEntity.BlocksAura()` method (`prop.New` is 4-param now), the zone-editor checkbox + inner-ring marker + `ZoneModel` field, and all authored values in `api/zones/*.json` (500 keys) — see Last completed. Re-adding later is a one-line additive schema change (`DisallowUnknownFields`).
  - **Player passive regen is not combat-gated** (`model/player/update.go` regenerates whenever `0 < Health < max`, in combat too — GDD §3 says out-of-combat only). Gate decided 2026-07-10, scheduled execution step 3 (needs a player in-combat flag / recent-damage window); prerequisite for the harness stand-still thresholds.
  - **FIXED — respawn now retains level + spellbook:** on death `sys/state.go` stashes both the progression *and* the whole `SkillComponent` (spellbook + loadout + active aura) keyed by client UUID (`carriedState`); re-join restores both via `SetProgression` + `SetSkillComponent`. Semi-permadeath removed except the existing partial-XP-within-level loss (`LoseCurrentLevelExperience` kept, by design). Pinned by `TestDeathRespawn_RetainsSpellbookAndProgression`.
  - **FIXED — mob aura ring is wire-driven (mob-depth chunk 3c, 2026-07-10):** `Mob.aura_radius` streams the active aura's effective radius (0 = gated/off); `GraphicsConfig.mobs.*.damageAuraRadiusMeters` deleted — no hand-sync remains.
  - **FIXED — `net_test.go` no longer hangs the suite:** `backend/pkg/berryhunter/net/net_test.go` (a manual `ListenAndServe` WebSocket smoke script, not a real test) now starts with `t.Skip`, so the full `go test ./...` runs and passes. The script is kept for manual debugging — remove the skip to run it explicitly.
  - **FIXED — equip level=1 gap:** the spellbook stores per-skill levels (`map[SkillID]int`) and `EquipSystem` equips at the stored level (Phase 7).
  - Frontend `Skills.ts` hardcodes skill ID → name, maxLevel *and* category, duplicating the backend registry — sync manually when skills change; revisit (wire or generated file) when the skill list grows.
  - **FIXED — dead character-variant code removed (2026-07-06):** the old Berryhunter variant system (`Character.variants`/`pickVariant()`/`hashCode(name)` — a no-op since only `player.svg` shipped) is gone. `Character` now preloads a single `Character.avatar` from `GraphicsConfig.character.file` (was `files: [...]`); the unused `hashCode` util and `static svg` field were deleted, along with 15 unreferenced `assets/characters/*.svg` (only `player.svg` remains). The future avatar selector (roadmap item 8) starts clean from here.
  - **FIXED + VERIFIED IN-GAME 2026-07-11 ("everything works, blue borders gone at all zoom levels") — Terrain "blue bleed" + fixed-FOV zoom (tdd §4.7):** the stale-devicePixelRatio bug (root-caused 2026-07-08: `Game.ts` snapshotted DPR once, Pixi `resizeTo` never re-applied resolution → hard reload at ≠100% browser zoom clipped the early terrain draw batches to a stale scissor while later batches — mobs, markers — escaped) is fixed by the proposed reconciliation: `Game.ts` owns resizing (`renderer.resize(innerWidth, innerHeight, devicePixelRatio)` on window resize + re-chained `matchMedia('(resolution: …dppx)')` DPR listener + one post-init rAF), `Game.width/height` read `renderer.screen` live (both FIXMEs gone), the waterRect redraws on the renderer resize event, `Camera.Corners` cache deleted. **Landed together with fixed FOV + 3 in-game zoom levels** (user request, same session): `camera/logic/Zoom.ts` defines visible world heights [PLACEHOLDER 6/7.6/9.5 m, width cap 18 m under the 20 m streaming viewport], `Camera` scales `cameraGroup` + screen↔map conversions + clamp by `viewScale` per frame; browser zoom now only affects sharpness + the DOM HUD (known caveat); ctrl+wheel is blocked in-game; HUD `#zoomControl` (magnifier icon, +/− buttons, level number 1–3, pointerdown) above the vital bars; `Spectator` pan speed zoom-aware. tsc + webpack green; zero backend/wire changes. **In-game checklist (PASSED 2026-07-11):** hard reload at 80/100/125% browser zoom → no blue borders; browser zoom mid-session → world view size unchanged; +/− buttons step 3 levels (buttons dim at the ends); world-edge camera clamp + zone-editor click placement correct at every level; night tint still covers the screen after a window resize.
  - **FIXED — movement micro-stutter every ~30 s (fixed 2026-07-05, verified in-game):** while walking, the character was set back one tick's distance against the walk direction, exactly periodic (~30 s), scaling with movement speed (more visible with SwiftPassive). Root cause (input-starvation beat): the client sends inputs on its own free-running 33 ms JS timer (`Tock`, `INPUT_TICKRATE`) while the server consumes one input per 33 ms Go tick; ~0.1% clock drift starves the server of an input once per ~30 s → one tick without movement. **Fix (candidate (a), server-side hold):** `core/input.go` `PlayerInputSystem.pickInput` bridges a single starved tick with the last applied movement, stored as a **movement-only** copy in `lastMove map[uint64]*PlayerInput` and **consumed on use** (`delete`) so a disconnected client halts after one tick instead of sliding forever. The copy sets `ActiveAuraSlot: ActiveAuraSlotNoChange` and drops `CooldownActivations`, so a bridged tick never replays a one-shot aura switch or cooldown. Also removed the dead `lastBuf`/3-buffer-history plumbing (`updateInput`'s `last` param was never read). Pinned by `TestPickInput_BridgesOneStarvedTick`. Not scheduled: candidate (b) (client sends input on GameState receipt, killing the beat structurally) / (c) (full prediction+reconciliation) remain available if the hold ever proves insufficient.
  - Frontend FlatBuffers toolchain migrated to flatc v24.3.25 in a dedicated commit.
  - `-2` `active_aura_slot` deactivate sentinel is a workaround for FlatBuffers omitting the `-1` default (an explicit `-1` is indistinguishable from an absent field). Decided in Phase 5: it stays. Paired constants: `model.ActiveAuraSlotDeactivate` (Go) / `DEACTIVATE_AURA_SLOT` (InputMessage.ts).
  - ⚠️ Testing gotcha: `go:embed` patterns don't include subdirectories (`*.json **/*.json`!), and disk-based registry tests can't catch embed gaps — pinned by `pkg/api/skills/skills_test.go`. Before manual tests: `pkill berryhunterd`, rebuild, and check the boot log (`Loaded skill definitions count=19` as of mob-depth chunk 1) — a stale server process silently masks new behavior.
  - **FIXED — `KILL` cheat no longer killed (found + fixed 2026-07-04, Block 2 testing):** one-shot zeroing of `Health` was reverted before death was detected. `KILL` sets `Health = 0` in `CommandSystem` (prio −50); `UpdateSystem` (also −50, runs after) regenerated any `Health != Max` via `updateVitalSigns`, bumping it to a tiny positive value the same tick, before `ConnectionStateSystem` (prio 10, `sys/state.go`) checked `Health == 0` next tick. **Fix:** `updateVitalSigns` now regenerates only when `0 < Health < Max` (0 = dead, no revive). Pinned by `TestUpdateVitalSigns_DeadPlayerDoesNotRegenerate`.
- **Doc map (`docs/`, naming: core docs unprefixed; `plan-` = execution plan/record per work item; `research-` = point-in-time investigation/assessment; `archive-` = resolved, kept for rationale):**
  - docs/gdd.md — game design truth: vision, mechanics intent, open design questions
  - docs/tdd.md — technical big picture: architecture, decided/open tech questions, risks
  - docs/roadmap.md — v1.0 scope + per-item status outside the skill system; prototype path (items 1, 2, 10, 11 ✓ — item 12 content pass is the remaining prototype gate)
  - docs/backlog.md — unscoped feature ideas with open-question catalogs
  - docs/architecture.md — runtime cost model, scaling limits, zones-as-Spaces & fluid transitions, hazard/encounter runtime cost
  - docs/plan-skill-system.md — skill system design + migration record (Phases 1–9, complete); combination system; wire protocol
  - docs/plan-block2-survival-removal.md — Block 2 (items 1+2) execution record, complete
  - docs/plan-item11-hp-resist-variance.md — absolute HP (✓), resistances/damage tags (✓), stat variance (Phase 3, open); decisions A1–A3, B1–B7
  - docs/plan-effect-foundations.md — effect-vocabulary scaling: decisions F1–F10 (stay Go, no scripting for effects; primitive-first growth) + the tackle-now sequence and candidate-effect cost map
  - docs/plan-world-zones.md — world & zones first slice (roadmap item 4 + placement/respawn half of item 7): in-game editor, rectangular single-Space world, server-authoritative zone.json (bounds/props/spawn points), resources demoted, movement-blocking occluders only; decisions A–D + six-chunk plan + pitfalls
  - docs/plan-mob-depth.md — mob depth & totems (execution step 2): 9-chunk plan (totem → flee → aggro/threat → steering → patrol → companion → taunt → support mobs → encounter-controller spine); decisions: mobs aggro summons via faction-aware acquisition, entity-keyed threat diverging from owner-XP attribution, companion cooldown in scope, route validity = level-designer responsibility; §8-briefing adaptations in §3.1; open ⚑ in §6
  - docs/manual-zone-editor.md — step-by-step user manual for the in-game zone editor (`manual-` = how-to prefix, first of its kind): setup, modes, placing/editing props+spawns, export→server round-trip
  - docs/manual-content-authoring.md — how-to for adding/replacing content by hand: new mobs (5-file EntityType path), new abilities (auras/passives/cooldowns), ability VFX (rings + programmatic hit fx), mob/player icons; wire-touch table + hand-sync points
  - docs/plan-rebrand-cleanup.md — rebrand to "Aura" + Berryhunter cleanup: footprint tiers, phased plan (dead features → structural rename → branding), scheduled as execution-order step 7; scoreboard removal pulled forward (2026-07-08); chieftain-deletion decision (does NOT become the account service)
  - docs/plan-content-zone1.md — CAPTURED (not scoped) content for Zone 1 + the 21-zone progression + mob-category taxonomy (content-pass / step 6 material): Zone 1 flow (farmer/turnip/wolves → tunnel guard/light → Zone 2/City), NPC/mob drafts, the two resolved conflicts (turnips=harvest-mobs, peasant onboarding), open content questions
  - docs/archive-scripting-audit.md + docs/archive-scripting-options.md — data-vs-Go audit and scripting/expression-layer options (decided 2026-07-07 → plan-effect-foundations.md)
  - docs/research-content-pipeline.md — designer-authoring pipeline gaps + preventive steps
  - docs/research-v1-readiness.md — prototype→live readiness assessment (ops/CI/observability gaps)
  - docs/archive-combo-questions.md — resolved Phase-9 combo question catalog (rationale record)


## Development Principles

These principles apply to all code written or modified in this project.

### KISS — Keep It Simple, Stupid

Prefer the simplest solution that works. Avoid clever abstractions, unnecessary
indirection, or premature generalization. If a function does one clear thing in
20 lines, that's better than a "flexible" version in 80. When proposing
architecture, start with the simplest design that satisfies the actual
requirements — not the imagined future ones.

### DRY — Don't Repeat Yourself

Knowledge should have a single source of truth. If the same logic, constant, or
configuration appears in multiple places, extract it. Watch for subtler
duplication: parallel switch statements, repeated validation patterns, copy-paste
between similar systems. But: don't deduplicate things that just *look* similar
— two pieces of code that happen to be identical today but represent different
concepts should stay separate.

### YAGNI — You Aren't Gonna Need It

Don't build for hypothetical future requirements. No "we might need this later"
parameters, configuration options, or abstraction layers. Add complexity only
when there is a concrete, present need. This applies especially to the aura
system: build what the current design requires, not what every possible future
combination might require.

### TDD — Test-Driven Development

For new features and bug fixes:

1. Write a failing test that captures the desired behavior
2. Write the minimum code to make it pass
3. Refactor if needed, keeping tests green

This applies to backend Go code (`go test ./...`) primarily. For exploratory
prototype work or UI tweaks, strict TDD may be relaxed — but any non-trivial
game logic (aura calculations, combination resolution, damage application)
should have tests before or alongside the implementation.

When fixing a bug: first write a test that reproduces it, then fix.

## Project Overview

**Berryhunter** (repo name: aurahunter) is a multiplayer browser survival game. Players gather resources, craft items, manage vitals (health, satiety, temperature), and fight mobs. The repo has three main parts:

- `backend/` — Go game server (`berryhunterd`)
- `frontend/` — TypeScript/webpack browser client using PixiJS
- `api/` — Shared FlatBuffers schemas and JSON item/mob definitions

## Build & Run

### Backend (Go ≥ 1.22)

```bash
# One-time: copy config
cp backend/conf.local-windows.json backend/conf.json   # Windows
# or use backend/conf.default.json as a template

# Build
make -C backend build          # produces backend/berryhunterd

# Run (dev mode serves static frontend too)
cd backend && ./berryhunterd -dev

# Run without build (go run)
make -C backend dev
```

> **Gotcha:** after backend logic changes, rebuild the binary with `make -C backend build`.
> `go build ./...` compiles/type-checks packages but does **not** refresh `./berryhunterd`,
> so a running `-dev` server keeps executing stale code.

> **Content iteration:** `./berryhunterd -dev -content ../api` loads items/mobs/skills/recipes
> from the repo `api/` directory directly instead of the embedded copies — JSON edits then skip
> both `cp-defs` and the rebuild (a server restart still applies them). The boot log prints the
> content source (`Loading content source=…`). Production/default stays embedded.

`backend/conf.json` controls server port (default `2000`), day/night cycle durations, and all game-balance tuning values. `backend/tokens.list` must exist with at least one token (e.g. `plz`) for in-game commands to work.

### Frontend (Node 20 / npm 10)

```bash
# Dev server (webpack HMR on port 2001) — no Docker
cd frontend && npm install && npm run start

# Production build
npm run build                  # output goes to frontend/dist/

# Docker-based alternatives (if local Node unavailable)
make -C frontend dev           # dev server via Docker
make -C frontend build         # prod build via Docker
```

### Opening the game

```
http://localhost:2001/?token=plz&wsUrl=ws://localhost:2000/game
```

Optional dev query params:
- `&develop` — opens the draggable dev panel
- `&start-cmds=GOD,GIVE BronzeTool,...` — runs server commands on spawn

### Backend tests

```bash
cd backend && go test -timeout 60s ./...
```

The full suite runs and passes. (`backend/pkg/berryhunter/net/net_test.go` is a manual `ListenAndServe` smoke script that used to hang the suite; it is now skipped via `t.Skip` — remove the skip to run it explicitly.)

The test runner requires generated files (`go generate ./...`). The Makefile `gen` target runs this automatically before builds.

### Code generation

```bash
# Regenerate Go enumer files and FlatBuffers bindings
make -C backend gen            # runs go generate ./...

# Regenerate FlatBuffers bindings (if .fbs schemas change)
cd api/schema && ./make.sh     # or make.bat on Windows
```

## Architecture

### Backend (ECS-based game loop)

The game server uses an **Entity-Component-System** architecture via `github.com/EngoEngine/ecs`.

- `backend/cmd/berryhunterd/` — entrypoint; wires config, game, HTTP server
- `backend/pkg/berryhunter/core/` — `game.go` constructs the ECS world and registers all systems; `Loop()` ticks at ~30 FPS (33 ms/tick)
- `backend/pkg/berryhunter/sys/` — ECS systems: physics, mob AI, day/night cycle, decay, respawn, scoreboard, status effects, heater
- `backend/pkg/berryhunter/model/` — interfaces and concrete types for entities (player, mob, resource, placeable, spectator)
- `backend/pkg/berryhunter/items/` — item and mob definitions loaded from `api/items/` and `api/mobs/` JSON files at startup
- `backend/pkg/berryhunter/codec/` — FlatBuffers encode/decode for the WebSocket protocol
- `backend/pkg/berryhunter/phy/` — 2D physics (circle/AABB collision, spatial hashing)

**Adding a new system:** implement `ecs.System`, register it in `core/game.go:NewGameWith()`, and add entity registration cases in the relevant `addXxx()` methods.

**Adding a new entity type:** implement the appropriate `model.*Entity` interface, update `game.AddEntity()`, and register in all relevant systems.

### Communication Protocol (FlatBuffers over WebSocket)

Schemas live in `api/schema/`:
- `client.fbs` — client→server: `Input`, `Join`, `Cheat`, `ChatMessage`
- `server.fbs` — server→client: `GameState`, `Welcome`, `Scoreboard`, `Obituary`, etc.
- `common.fbs` — shared types (`Vec2f`, `ActionType`, `AuraType`)

After editing `.fbs` files, regenerate bindings for both backend and frontend.

### Game Configuration (conf.json)

All numerical tuning lives in `backend/conf.json` (or `conf.default.json` for reference). The `game.player` block controls movement speed, aura radii, vital-sign drain/gain rates, and level-up scaling. Changes take effect on restart.

### Item / Mob Data (JSON)

`api/items/` and `api/mobs/` contain JSON definitions. The `make -C backend cp-defs` target copies them into `backend/pkg/api/` so the Go build embeds them. Run this (or just `make -C backend build`) after editing any JSON definition.

### Frontend

The frontend is structured as feature modules under `frontend/src/features/`:
- `backend/` — WebSocket connection, FlatBuffers deserialization, entity snapshot management
- `core/` — game loop, entity manager
- `player/`, `vital-signs/` — local player state and HUD
- `game-objects/` — rendering entities (resources, mobs, placeables) via PixiJS
- `input-system/`, `controls/` — keyboard/mouse/touch input
- `internal-tools/` — dev panel, console, overlay tester (only active with `?develop`)

**HUD event handling:** Use `pointerdown` (not `click`) for all interactive HUD panels. `MouseManager` (`input-system/logic/mouse/MouseManager.ts`) registers a `mousedown` listener on `document.documentElement` with `event.preventDefault()`, which suppresses the synthetic `click` event. `pointerdown` fires before this and is unaffected. `click` listeners on HUD panels silently never fire — this is not obvious from the source.

Webpack configs: `webpack.common.js` (shared), `webpack.dev.js` (HMR, port 2001), `webpack.prod.js` (minified output).

## Aurahunter Project Context

This fork is being transformed into **"Aura"** — a top-down MMO. The Berryhunter
survival systems (vitals, crafting, temperature, hunger) will be removed or
heavily reduced. The core loop revolves around the aura system described below.

The codebase still says "Berryhunter" in many places. That is expected. Do not
rename or refactor naming proactively — focus on building new systems on top of
the existing foundation. The full rename + legacy cleanup is **scheduled, not
abandoned**: execution-order step 7 (after the content pass), per
`docs/plan-rebrand-cleanup.md` — until then this rule stays in force.

### Vision

**Tagline:** MMO lite — resource vs. resource, as simplified as possible.

**Core principle:** Players and NPCs interact exclusively through **auras** —
circular effect fields that automatically apply to anything in range. No
targeting, no direct attacks. Positioning and cooldown timing are the only
skill expressions.

**References:** WoW Classic (progression, environmental storytelling), Gothic
1+2 (organic worldbuilding), Hotline Miami / Monaco / Rimworld (top-down art
direction — not isometric, not pixel art).

**Platform:** Browser-based.

### Core Loop

1. Player moves through a persistent shared open world
2. Encounters mobs / other players — own aura ticks automatically on anything in range
3. Damage, healing, buffs emerge from aura overlap; cooldown abilities modify temporarily
4. Combat ends → XP for all participants → possibly aura unlock
5. Level up → skill points → strengthen existing auras or unlock combinations
6. Explore world → find hints → unlock new auras / passives / cooldowns
7. Rearrange slots, adjust build, tackle harder content

### The Three Skill Categories

Players collect, level, and combine three categories of skills:

- **Active auras** — toggleable, have visible ranges in-world. **Exactly one
  active aura is on at a time**; the aura slots are a loadout (several equipped,
  one active, switchable mid-fight), not multiple simultaneously-active auras.
  Build variety comes from slot loadout, combination unlocks, and switch timing.
- **Passives** — passive bonuses, always on (these DO run in parallel)
- **Cooldowns** — active abilities with cooldown timers (triggered individually)

Mobs use the same aura system as players.

### The Resource

Every player and every NPC has exactly **one resource**. It represents HP, mana,
and everything else at once. Drops to 0 → death.

### Aura Combinations

- Combination unlocks trigger when specific skills reach specific levels
- Recipes are **curated, not algorithmic** and **not documented anywhere in-game**
  — the community discovers and shares them
- Combinations can cross categories (aura + passive + cooldown is valid)
- The result of a combination can itself be an ingredient for higher combinations
- **Variant auras** exist as rare world drops and are also combinable
- **Damage types** exist for mob resistances and build identity (fire, ice, physical, etc. — specifics TBD)

The combination system must technically support arbitrary combinations from day
one. Content (specific recipes) is added manually over time.

### Spellbook & Unlocks

The **spellbook** is the collection of all auras, passives, and cooldowns a
player has discovered. Five ways to obtain new entries:

1. **Milestone unlocks** — guaranteed at certain levels
2. **Monster kill unlocks** — certain mobs drop auras/passives on death
3. **World exploration** — clue anchor points throughout zones
4. **NPC teaching** — peaceful NPCs teach a specific aura on approach, often
   tied to nearby harvest-mobs that only that aura can damage (soft "profession"
   identity without a class system)
5. **Meta-progression** — sacrificing a max-level character unlocks new base auras account-wide

### World Design

Persistent shared open world, multiple connected zones for different level
ranges. Designed and built by hand — no procedural generation. Environmental
storytelling is central.

**Open-world dungeons** — no instances. WoW-Classic-style caves in the open world.

**Darkness & light** — certain areas (caves, tunnels between zones) are dark.
The tunnel between zone 1 and zone 2 serves as a natural tutorial for the role
concept (light aura forces a trade-off between light and damage; players can
support each other).

### Multiplayer

- Persistent shared world — everything visible, everything shared
- No formal groups in v1 — all combat participants receive XP
- No PvP initially (earliest 5 years out)
- **Players filling roles for each other is essential, not optional**, for all
  larger challenges (light support in tunnels, heal support at bosses, etc.)
- No griefing possible by design

### Numbers Are ALWAYS Placeholders

Every concrete number — max level, skill points at max, slot count, aura max
level, respec cost, drop rates, combination requirements, damage values, aura
radii — is a **placeholder** until explicitly marked as final.

Treat such numbers as examples for thinking, never as decisions made. When
numbers are relevant for an answer, ask first or propose concrete values for
discussion — never silently adopt them as set.

### Scope v1.0 (Must Have)

Accounts, aura system (base auras, cooldowns, first combinations), spellbook
with milestone and monster unlocks, progression (level, skill system, slots),
persistent world, 2–3 zones, mob types (normal/elite/boss), UI (resource bar,
XP bar, ability bar, aura panel, minimap, zone chat), line-of-sight for auras,
campfire system.

### Not in v1.0

PvP, formal group system, economy, mobile, endgame raid events, character sacrifice.

---

## Working Style

- **Always ask before modifying files or running commands.** Show the plan first.
- Keep changes small and confirm individually.
- For architectural decisions, propose options first — don't implement directly.
- Treat existing Berryhunter physics, collision, and the WebSocket/FlatBuffers
  protocol as stable foundations. Extend, don't rewrite.
- When in doubt about game design intent, ask — don't infer from the codebase.


## Implementation Workflow

The skill system migration follows `docs/plan-skill-system.md`. When working on
that migration, reference the phase and step you're implementing in commit
messages and explanations (e.g. "Phase 1.2: skill registry").

### Plan before code

For any non-trivial change (new file, new system, refactor, multi-file edit):

1. State the plan in plain text first — what files will change, what gets added,
   what the test strategy is.
2. Wait for confirmation before writing.
3. Then write the code.

This applies even when running with auto-edits enabled. Showing the plan is not
the same as asking permission for each file — it's about making the reasoning
visible so it can be corrected before code is written.

### Sanity checks after every step

After completing a step, before declaring it done:
- Run `go build ./...` from `backend/`
- Run relevant `go test` for affected packages
- Report the output

Don't claim "done" without these checks.
