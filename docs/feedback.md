# Feedback intake

The single landing surface for raw feedback: playtest reports, PO observations,
bug sightings, things said out loud during a session. Established 2026-08-24 as
the successor to `plan-playtest-feedback.md`'s intake role (that doc is
archived; its remaining open items were redistributed the same day).

## The pipeline

1. **Capture.** Every item lands here as a dated row (source, one line).
   Capture is cheap; nothing is lost because it seemed small.
2. **Triage.** Every item leaves this doc through exactly one of four doors,
   normally in the same session or the next one:
   - **→ plan** - it is scheduled work; it moves into an existing or new
     `docs/plan-*.md` (link it from the row before pruning).
   - **→ ruled** - the PO decides it on the spot; the ruling is recorded where
     it belongs (a CLAUDE.md standing lock, a plan doc, a backlog entry) and
     the row closes.
   - **→ watch** - not now, but with a NAMED re-open trigger; it becomes a
     `backlog.md` entry carrying that trigger.
   - **→ dropped** - closed with a one-line why.
3. **Prune.** A row that has exited stays only until the wrap of the session
   that moved it; the receiving doc is the record. This file stays short - if
   it grows past a screen or two, triage is overdue.

Standing rules:

- **Plan docs never double as intake.** Accumulating feedback rounds inside a
  plan doc is what grew `plan-playtest-feedback.md` to 3,400 lines and made
  its open items invisible.
- Content ideas (specific mobs, skills, lore) still go to the `content-*.md`
  catalogs; unscoped feature/system ideas to `backlog.md`. This doc is the
  funnel, not the archive.
- A watch item without a trigger is not a watch item - name the condition that
  re-opens it or pick another door.

## Open items

| Date | Source | Item | State |
|---|---|---|---|
| 2026-09-23 | PO, C3a look (chat) | **A DoT redraws its projectile on every tick.** The Giant Spider's spit, Immolate, the Bandit Pyromancer's ember and every other DoT draw their `on: hit` layer once per TICK, because a DoT tick is an ordinary hit on the wire and application/refresh emits no event at all. PO wants: the spit / fireball / projectile drawn ONCE on application and again on RE-application (after the debuff lapsed), ticks then just tick (the engine's mark only). Needs a wire vocabulary for "applied" vs "tick" (`HitKind` values, the server already knows ignite vs refresh) and a new trigger. Same session ask: the Giant Spider gets a general auto-attack (two big white FANGS clamping the player from either side) and the Pyromancer gets per-tick damage beside its DoT, because a DoT-only aura now draws exactly once per application. | → plan: `plan-skill-vfx.md` §12h (C3a-ii), PO 2026-09-23: draw on application AND every refresh, never on a tick |
| 2026-09-23 | PO, C3a look (chat) | **The bite is too long and reads as a crocodile**, because a `strike`'s length is the skill's reach (the C2b "weapon is HELD, length = reach" rule) and a bite has no head. Nine natural-weapon options were collected (rim bite, spectral head, attacker lunge, afterimage lunge, motion streak, attacker flash, number from the attacker's side, knockback nudge, staggered timing, facing notch); PO leans to "lunge the attacker token". Ranked by ease in the session answer; the pick is the PO's. Also noted: the boar's tusk draws as a spear and the bear's paw as a sword, which are the `thrust`/`swing` PLACEHOLDERS (§12g.3, `tusk` and `claw` are P1 on the wanted list) and the same natural-weapon question. | → plan: §12h, PO 2026-09-23 picked the RIM BITE only; the lunge stays unscheduled |
| 2026-09-23 | PO, C3a look (chat) | **Do cooldowns draw anything?** Today only `WhirlingAxes` (orbit) and `SummonSpider` (emitter); NovaBurst, DamageBurst, Envenom, Ignite, RimeBurst, Shockwave author no `visual` and show the engine's mark alone. NovaBurst and Shockwave are the obvious `wave` users (§12g.2 named them, "not without a PO pick"). | → plan: §12h, PO 2026-09-23: `wave` on both, and every skill gets a placeholder `visual` for testing |
| 2026-07-29 | playtest-feedback open question 8 | Does the pacifist healer's threat table have any consumer besides Taunt (`ForceThreatToTop`)? If not, the uniformity ruling still stands - but it should be said explicitly rather than by accident. | open question, awaiting ruling |
| 2026-08-24 | PO mockups (chat) | Animation wishes: ice-aura particle field, punch/sword strike at the hit target, fireball + slow-bolt projectiles with impact-timed damage numbers. PO rulings same day: dressing over normal aura ticks (no new targeting mechanic), own player first, one strike per victim (most auras are nearest-1 BY CONTENT), lane = prototype branch (since deleted). | → plan-skill-vfx.md |
| 2026-09-05 | PO, mid-session | **Content census tests must not go red just because content was added.** Three tests in `pkg/aura/items/mobs` hardcode a roster or a count and so fail on any new mob/NPC that is otherwise perfectly authored: `TestContent_ConversantCensus` (an explicit name list), `TestContent_AuthoredRoleCensus` (a creature COUNT), `TestContent_XPFactorZeroSpeciesAreNotPrey` (`assert.Len(free, 34)`). The collaborator's Martin NPC (`6c2e6d5c`) tripped all three and they stayed red at HEAD for weeks, which is the real damage - a permanently-red suite stops being read. PO call the same day: **martin.json deleted** (unplaced stub, TODO opening line, referenced by no zone or quest - suite green again), and the tests themselves are the thing to fix: they should assert the INVARIANT (a conversant resolves its grants; a creature is not a structure; an xpFactor-0 species is not prey) and not the population. | open - needs a door: the fix is a plan-sized pass over the three, or a ruling that a census is worth its churn |
| 2026-09-06 | PO, off the back of the world-scale Leg C in-game pass | ⭐ **Do a dormant mob's sleep conditions match a CREDIBLE world-simulation state?** Two cases the PO named; both checked against the code the same day and **both are reachable today** (`docs/plan-world-scale.md` §11 M1-F5). **A — an unobserved mob-vs-mob fight never ends:** mob-on-mob hostility is authored content already (`api/factions/human_army.json` has `"hostileTo": ["orc"]`, another faction lists `["aligned", "human_army"]`), and `Pristine()` refuses `InCombat()` plus any non-empty threat table — so if the player walks away **mid-fight**, both combatants stay awake and fight to the death unobserved, burning tick budget in exactly the region S3 exists to stop paying for and churning authored spawns off-screen. ⭐ The PO's own parenthetical is the other half and is correct: a mob that DID sleep is out of the physics space, so an awake mob's sensor cannot reach it — it is **effectively invulnerable**. **B — a mob can freeze mid-walk-home, off its route:** ⚑ there is **no "returning home" mode** (`combatMode` is only `modeIdle｜modeEngage｜modeSupport｜modeFlee`, `model/mob/support.go:44-51`) and the walk-home lives *inside the idle path* (`patrol.go:108-113`, `if m.returnPosSet { m.idleWalk(m.returnPos) }`). After a leash the mob clears threat → mode falls to `modeIdle` → health regens (~5 s) → **`Pristine()` goes true while it is still walking home**, so it sleeps clumped from the chase, off its patrol route, away from its spawn, and stays there until someone comes close. ⭐ **This is precisely the case S3's L7 ruling does NOT cover** — L7 reasoned about a patroller frozen mid-leg *on its own route* (benign); a mob frozen mid-return is parked somewhere the world never authored. | open — needs a door: **B** looks like a narrow D3 amendment (refuse sleep while `returnPosSet`, i.e. sleep only once home/on-route) and is cheap; **A** is a genuine design ruling (should unobserved mob-vs-mob combat run at all, freeze, or auto-resolve?) — both are gameplay-visible, so PO's call, not a perf tidy-up |
| 2026-09-06 | M1 Leg C, testability gap | **L2 (totem beside a sleeper) cannot be verified in-game today** — mobs kill a planted totem too fast to observe, and it times out anyway. Needs an **invulnerable, long-TTL totem**, which no cheat provides. Covered meanwhile by unit legs (`sys` leg 6) and D4's construction, so this is a testability gap rather than a known defect. | open — cheap fix: one cheat that plants a pinned totem, worth adding whenever someone is next in `sys/cmd` |
| 2026-09-08 | PO, mid-session | **Region atmosphere.** *"what if we introduce effects per region (we can call it region-atmosphere) … when a player is in a certain region like a dungeon, there is for example a dark fog surrounding them and limiting their view similar to the darkAreas we already have, but global and not placed in the level separately"*, extended in the same message to *"limit surrounding darken reveal only until we hit a cave wall or other object — this would give a 'line of sight' effect."* | → plan: `docs/plan-region-atmosphere.md`, designed the same day (11 proposals, 7 open PO calls; A/B split, atmosphere first). ⭐ **The A/B split became TWO PLANS on 2026-09-16** (PO ask): the air stayed put and SHIPPED (A0-A2 `5b57d0c4`, A4 `6d97a4fc`), while the line-of-sight half quoted above moved to `docs/plan-line-of-sight.md` and is unbuilt. Row prunes at this session's wrap. |
| 2026-09-11 | spell builder C1 wrap (session finding) | **The content editor's JS validator is stale on U4b's `travel_to` mode `anchor`**: `validate.mjs` `TRAVEL_MODES` knows only `home_campfire`/`caster`, so `cave-exit.json` + `cave-mouth.json` show as 2 errors on every editor open (and the NPC grant row cannot author the optional `anchor` key). The §B3 stale-port class `plan-content-editor.md` predicted for the mob tab. Two fixes: a one-word patch (+ the `anchor` key on that mode, refused on the other two, `interaction.go` ~979), or §B11 Q7 (Go validation on save for every tab, retiring the port). | → ruled 2026-09-11: PO chose the patch, fixed as a C1 rider (ledger `plan-content-editor.md` §B12 C1); Q7 stays the structural answer |
| 2026-09-11 | PO, spell builder C1 look | **Skill `_comment`s read as "nonsensical outdated babbling"**: 102 of 105 carried chunk retrospectives in the plan-doc ledger dialect (median 832 chars). → ruled the same day: a `_comment` is an authoring note (rule in `manual-content-authoring.md`), all skill comments rewritten as a C1 rider. **Open half**: the same dialect lives in mob, quest, recipe and faction `_comment`s (e.g. `api/recipes/barrier-home.json`); the ruling covered skills only. Extend the rule + a pass to the other kinds, or leave them? | skills ruled + done; other kinds awaiting a door |
| 2026-09-12 | PO, spell builder C4 look (`SummonSpider`) | **A summon follows only when its mob is authored `role: follower`, so every pet is a twin mob file and a wild species cannot be summoned as a companion.** Charm already proves a creature can be somebody's pet through a runtime link. → **plan**: `docs/archive/plan-summon-follows.md` (one chunk, `follows` on the `spawn` effect, UNIFORM by PO ruling; next chunk ahead of spell builder C5). | → plan |
| 2026-09-13 | PO, in-game (screenshot: "+271" heal over a red damage number on a Dire Bear) | **Damage and heal numbers draw on top of each other.** Every floating text spawned at one point with a random horizontal jitter narrower than a three-digit number, so two numbers on the same server tick (heal + damage, or the own character's damage + heal + cost) were unreadable. PO asked for the WoW-classic shape. → **ruled the same day: lanes + stacking, standalone fix now.** `FloatingNumberLayout.ts` (pure, vitest): losses left of the entity, gains right, each lane anchored at its inner edge; within a lane a free-slot search over the still-rising numbers, so a number never spawns over a live one and a steady stream keeps landing at the base. "Immune" takes the damage lane. Constants [PLACEHOLDER]. `plan-skill-vfx.md` C1's per-hit stream inherits the layout (the slot search reads the live texts' real positions, per spawn). ⚑ Rider: `immune-feedback.mjs`'s "no damage number over the wall" half was scoped to a 2.5 u radius, which admitted a wolf at melee with the bot (three red runs on the long-lived server); it now asserts over the bramble's own head. Two presentation notes for the PO's look: the base slot still sits ON the name plate (unchanged from before), and a lone number now hangs left or right of the centre line rather than centred. | → fixed 2026-09-13, PO look owed |
| 2026-09-23 | PO, C3a-ii look round 1 (screenshot, wolves) | **The rim-bite jaws are "still quite long, they should be shorter overall"**, and the spider's fangs should be **"two tusks gripping from either side, faced inwards, like the two front fangs of a spider"** rather than a jaw pair on one hinge. | → built the same session: `BITE_LENGTH_FACTOR` 1.4 → 0.8 with a 20 px floor; a fifth `strike` curve `pincer` (plan-skill-vfx.md §13 C3a-ii, "PO look round 1") |
| 2026-09-23 | PO, C3a-ii look round 1 | **"The hit VFX are hard to see with the still very prominent aura rings"**: reduce aura and cooldown ring intensity, outer-ring size, interior colouring, every element by 50 %. | → built the same session: every ring alpha / width / pulse halved in `AuraRings.ts` and the burst ring; the ring RADIUS kept (it is the range line, triage item 7); §13 C3a-ii "PO look round 1" |
| 2026-09-23 | PO, C3a-ii look round 1 | **Ignite and Immolate should be a beam**, "more fitting for fire DoT spells" (they were bolts on `applied`). | → built the same session: `beam` `extend` on `applied` on both; §13 C3a-ii |
| 2026-09-23 | PO, C3a-ii look round 1 | **Projectiles are too small and too fast**: "make the projectiles 30 % bigger in general and a bit slower as well". | → built the same session: engine knob `PROJECTILE_SIZE_FACTOR` 1.3, default speed 700 → 500 px/s, authored speeds 900 → 650 / 700 → 500; §13 C3a-ii "PO look round 1" |
| 2026-09-25 | PO, at the C3b/C3c wrap, after asking what an aura can do to its own caster | ⭐ **Auras with DRAWBACKS that offset a higher power level, and mobs that can SLOW players.** PO: *"I want to ensure we can author auras with drawbacks that can offset a higher power level, including self slow, more damage taken. I also want mobs to be able to slow players through cooldowns or through auras. To me it seems we will need the 'while active' self modifier for that."* Measured at HEAD before the ask: the only designed drawback is the per-application self-cost (`costFractionOfMax`); a self-vulnerability is expressible by accident (`resist_aura` + `targetsSelf` + a factor above 1, unauthored, untested); a self-slow, a cost multiplier and a while-active stat change are NOT expressible (a `speed_aura` refuses factors of 1 or below and excludes the caster, players carry no slow door at all so every slow on a player is inert, `stat_multiplier` is passive-only). | **→ plan**: `plan-aura-drawbacks.md`, DESIGNED 2026-09-25 (D1-D7 ruled, 3 chunks; the stun door is IN, D4). Scope to design: (1) a while-active SELF MODIFIER on an aura (movement, damage taken, max HP, a cost multiplier: new vocabulary + a buff kind tied to the active aura, off when switched off); (2) the PLAYER SLOW DOOR (players implement the slow the mobs carry; `slow_aura` and slow cooldowns on mobs then reach players), which decides the CC watch item "can a mob stun a player" for slows at least; (3) whether a `stun` on a player follows. Balance: the sim batteries cannot see a self-modifier today. |
| 2026-09-25 | PO, after the C3b/C3c look wrap, asked about Immolate's beam → `wave` change made in the editor | ⭐ **Any change the PO makes in the editor is INTENTIONAL, and a later change overrides an earlier ruling.** PO: *"it was a deliberate change. any change I make in the editor can be assumed intentional. It can be questioned but later changes override early assumptions. this is a live service game, balance changes and adoptions of visuals can also still change any time. balance is important still."* So Immolate is a `wave` on `fired` with radius 2 and two targets by ruling (the 2026-09-23 beam ruling stands for Ignite only), and the guardrail-forced `38bd586f` is the balance floor it lives under. | **→ ruled**: a standing working rule (memory `feedback_editor_changes_are_intentional`): record an editor-made content change as a ruling, flag a conflict with an older ruling in ONE clause, never revert it; the simharness guardrails stay the balance net after an editor session. |
