# docs/ — index

Naming convention: core docs unprefixed; `plan-` = execution plan/record per work item;
`research-` = point-in-time investigation/assessment; `manual-` = how-to guide;
`content-` = game-content catalogs + per-zone design intent (see Content section for the
conventions).

**`docs/` holds live work; `docs/archive/` holds finished work.** A doc moves to
`archive/` when its work has shipped (or was abandoned/superseded) and won't be resumed —
it stays readable and is still the rationale record, it just stops competing for attention.
Anything in `docs/` proper has something open. The older `archive-` filename prefix predates
the folder and is kept as-is on the six files that carry it, so existing references stay
literally correct — don't add the prefix to newly archived docs, the folder says it.

**Where status lives** (four layers, each with a distinct job — don't duplicate between them):

- **CLAUDE.md `## Status`** — the authoritative *current* state: last completed chunk, what's
  next, open PO calls, standing locks and placeholders. This is what you read first in a session.
- **roadmap.md "Execution order"** — the authoritative *sequence* + per-step outcome. Steps get
  sealed here when they complete; it is not a per-session log.
- **plan-\*.md banners** — the per-chunk ledger for one work item: what was decided, what landed,
  which commit.
- **MEMORY.md** — the cross-session index, so a fresh session can find the above.

**How feedback becomes work** (established 2026-08-24, replacing the rolling
collection docs): raw feedback of any kind lands in **`feedback.md`** as a
dated row, and leaves through exactly one of four doors - **→ plan doc**
(scheduled work) · **→ ruled** (PO decides, ruling recorded where it belongs)
· **→ watch** (a `backlog.md` entry with a NAMED re-open trigger) ·
**→ dropped** (one-line why). Plan docs never double as intake; `backlog.md`
is the idea/watch archive, not the funnel. The full contract is in
`feedback.md`'s header.

## Core

- **gdd.md** — game design truth: vision, mechanics intent, open design questions
- **tdd.md** — technical big picture: architecture, decided/open tech questions, risks, known bugs / standing gotchas
- **roadmap.md** — v1.0 scope + per-item status; **authoritative "Execution order"**
- **backlog.md** — unscoped feature ideas with open-question catalogs
- **feedback.md** - **the feedback intake** (2026-08-24): every playtest report / PO observation lands here dated, and leaves through exactly one of four doors - → plan doc · → ruled · → backlog watch (with a named trigger) · → dropped. Plan docs never double as intake; the pipeline contract is in its header
- **architecture.md** — runtime cost model, scaling limits, zones-as-Spaces & fluid transitions, hazard/encounter runtime cost

## Plans — live (something is still open)

Everything else that ever had a plan doc lives in `archive/` (see the Archive section).

- **plan-content-single-source.md** — **DESIGNED 2026-08-25, nothing built; PO bias to option A**: kills the `api/` ↔ `backend/pkg/api/` duplicate — **215 JSON files exist twice**, kept in sync only by `make cp-defs`, with nothing verifying it. The forcing constraint is proved rather than recalled: `//go:embed` rejects `..` outright (`invalid pattern syntax`), so an embedded single-binary server needs the content *inside* its own package tree. ⛔ **The survey found a live bug: a fresh clone of this repo does not compile** — `backend/pkg/api/mobs/.gitignore` hides `*.json` while `mobs.go` still embeds `*.json`, so `go build ./...` fails on a new checkout with `pattern *.json: no matching files found`, undocumented anywhere. That one directory is an inherited Berryhunter-era commit (`6317cce2`); the other eight are tracked, so **the repo runs two conventions nobody chose between** — and it is also the empirical case against the intuitive fix (gitignore the copy), whose *other* failure mode, silent staleness, is already logged in CLAUDE.md as the stale-`backend/pkg/api/mobs/` report. Four options weighed (A move · B generate · C drift-guard · D stop embedding); **C0 fixes the fresh clone first and independently (D4)**. ⚑ Schema NONE, and the bytes never change — which is the danger: a half-done path refactor loads *stale but valid* content and looks completely normal. Sized, not estimated: 124 code references, 73 doc files, and the `.gitattributes` LF pin must travel or a CRLF checkout reports ~14,600 phantom differences. §10 holds the real open question — *does authored content belong under `backend/` at all?* — which must be answered before C1
- **plan-ui-pass.md** - **ROADMAP SET 2026-08-25, not chunked; next session = the Phase 0 design-language mockups**: the single consolidated home for all UI work - the ui-polish §Deferred checklist (9 items), the tooltip maintenance debt (rulings restated in client English with no link back), round-9's cleaner-UI/dialogue-UI/journal-overlap items, the mobile items (☰-sheet nag, the two "J Journal" nodes, perf ceiling), map marker sizing, the FireWard-ring/band-width readability residue, and the boot-to-game surfaces (added 2026-08-25). §4 holds the phased path + rulings R1-R3 (R1: nothing hard-gates the release-map playtest, continuous process; R3: font PO-owned, non-blocking)
- **plan-ui-font.md** — **reference only, NOT SCHEDULED, nothing built** (2026-08-09): the home of the parked round-9 *"better font"* half. Records how fonts are implemented today (two declarations, `userInterface.less`'s `body` rule + `TextDisplay.ts`'s `defaultStyle()`), a live comparison artifact of five candidate faces shot in-game, and six findings that outlive the candidates. ⚑ Two are traps rather than tastes: `stone-age` is **4352 bytes, i.e. under webpack's 8 KB inline threshold**, while every candidate is over it, and **nothing in the client waits for a font** while PixiJS bakes glyphs at `Text` creation, so a naive swap can ship nameplates stuck in fallback serif on cold caches only. Also: a swap is always a swap **plus a size retune**, and the retune moves the ROOT font-size, which `html.mobile`'s clamp already owns
- **plan-playtest-deploy.md** — the live server (2026-07-21, `a7a2267d`): Hetzner VPS, `devops/deploy.sh`, server-only cheat token. **Now persistent** (step 8a, live since chunk 4 — restarts no longer wipe characters) and ⚑ **deliberately NOT backed up** (PO 2026-08-04). Live-ops reference; its §Ops & security posture still owns the unticked *security* items (firewall, DB bound to localhost, credential handling), which that ruling did not cover
- **plan-onboarding-cleanup.md** — **⏸ DEFERRED 2026-08-24 (trigger: a human coworker is actually about to join); created 2026-07-23, not started beyond workstream B:** documentation & repo cleanup for human coworkers across code/art/audio/design. Its Workstream B is what created `archive/`; Workstreams A/C–G are still open
- **plan-avatar-system.md** — design sketch, unscheduled: join-screen portrait picker + icon-unlock track, for step **8b** (its chunks 1–3 need no accounts; only chunk 4, avatar persistence, waits on 8a)
- **plan-entity-medallions.md** - **design 2026-08-10 · revised 2026-08-20 (PO mockup, D9–D15) · 2026-08-22 (artist feedback, D16–D18); nothing scheduled, nothing built beyond the shipped border pattern**: every non-prop actor drawn as a **token in a circular frame** (six `Token_*` layers; D14 predicate decides who), from PO concept art plus the ruling that this is *"a fundamental part of the visual identity"*. Supersedes today's stroked tier ring (`Mobs.ts:55`); "normal stays bare" survives inside the new art (D12). ⭐ Mechanically cheap but not free: **DB NONE**, **FlatBuffers ONE appended stance field** (D11 pulls the allegiance slice of §39 forward for the live disc tint), one catalog faction field (D9). ⚑ What it actually costs is elsewhere: the **asset contract** (now `art/medallion-asset-spec.md`), **fill rate** on mobile (~3-4 alpha quads per entity, and fill rate is the project's proven mobile failure mode), and the **child-index arithmetic** in `shape` that three consumers already disagree about. ⚑ D6's customizable player rings are structurally `plan-avatar-system.md`'s lane, a second cosmetic id beside `avatar_id`, so do not build a parallel mechanism. Eight open questions (§12), the sharpest being D15 sizing: the ring must correspond to the entity's collider, and the measurement table is C0's job
- **plan-entity-presentation.md** - **SCOPE CAPTURED 2026-08-24 (backlog §39 folded in; that entry is now a pointer), nothing designed, nothing built; owes its own design session**: the actor's *state* layer - buffs/debuffs **with durations**, allegiance, faction, cast bars, and the consolidation of the six independently-anchored overlays (nameplate, health bar, `AuraRings`, `EffectPips`, `AuraTickIndicator`, `InteractBadge`) around one frame. ⭐ **Ordering PO-ruled 2026-08-24: `plan-entity-medallions.md` executes FIRST** - the token is the anchor this plan's design attaches to, so it needs D15 sizing and C0's named sub-container refactor settled; the reverse dependency does not exist. The wire cost is the real cost (bits ride today; durations, stacks, sources, cast progress do not) and wants doing ONCE, bundled. Its §6 collects the whole queue waiting on it: the stun/slow wire conflation, §57 attack lines ("prototype freely, ship through §39"), per-hit damage numbers, stealth (F9 cosmetic), CC-immunity feedback, the FULL `aura_category` ubyte, D29's shared ascension ceremony, the §40 archetypes. Boundaries: the stance field stays medallions C2's (D11); HUD chrome stays `plan-ui-pass.md`'s. Until it runs, the moratorium stands: no per-effect overlay art
- **plan-server-performance.md** — **2026-08-02, chunk 0 built and COMMITTED (`00bd0549`, deployed — `devops/loadtest.md` run 2 records both ceilings roughly doubling), chunks 1–5 not started**: the execution order for raising the concurrent-player ceiling, cheapest first, off the measured profile in `devops/loadtest.md`. ⚑ **Its ordering premise has a second measurement it does not know about**: the plan sequences off *encoding 57 % / physics 24 %* at 50 clustered bots, but the 2026-08-05 scaling profile (`backend/cmd/aurad/scaling_profile_test.go`, added `d1f06a06`) measures **PhysicsSystem at 65→72 % of the tick, ~N^1.76**, sensor-inflated ×3–4, on the *entity-density* axis. Different axes, not a contradiction — but the "physics is 24 %, therefore chunk 4" reasoning is now half the picture, and **that profile is recorded nowhere in `docs/` or `devops/`**. Re-read the order before starting chunk 1. Chunk 0 (the XP curve — evaluated twice per character *per viewer* per tick) already **halved the tick** at 50 clustered bots. Then: share entity encodings across viewers (the O(players × entities) one, the big win) → parallelise `NetSystem` → **chunk 3, rewritten + PROMOTED 2026-08-30** → the broadphase → delta encoding. ⭐ **Chunk 3 now owns the BYTES half as well as the CPU half**: measured, the per-tick message carries **784 B per player of slow-changing owner-only state** (spellbook + slot vectors + quest_progress — 35 % of a quiet frame) plus a **4 264-byte conversation tree re-sent 30×/s while a dialogue is open** (125 KB/s for one player standing still talking). ⭐ It is the ONLY chunk that can touch owner-only fields — chunk 1 shares encodings between viewers and there is no second viewer for these, by construction — so it does not queue behind chunk 1. ⚑ Its blocking landmine is that **absent already means something, three different things**: `conversation` absent = CLOSE THE PANEL, `quest_progress` absent = empty journal, `discovered_campfires` absent = no change; D3 answers it with an always-sent `conversation_entity_id` scalar (its one appended wire field) and D1 rules **send-on-change over a second slower message**, because a fixed cadence would put up to a second of latency on every aura switch. ⚑ Chunk 2's prerequisite was found while writing the plan and closed the same day — the XP table is built eagerly in `New`, because encoding reads it for every character in every viewport and a lazy build writes to one player's table during another player's snapshot
- **plan-mob-voicelines.md** — **⏸ DEFERRED 2026-08-24 (PO; revive only if play-feel asks), planned 2026-08-02, not started**: a mob shouts an authored line when it latches onto an enemy (the WoW aggro yell). One backend-only chunk, **frontend zero-change** — every layer already exists and is proven by the Town Crier's ambient speech, so the work is an authored `aggroLines` field plus a drain on the aggro rising edge. 4 PO rulings (per-mob ~30 s cooldown · aggro only · sensor audience · elites/bosses + a few flavour). ⚑ Its L1 is the *third* appearance of the structural-assert silent-wiring class (R2/R3), and L2 is the per-mob RNG stream the drop roll shares
- **plan-npc-hails.md** - **⏸ DEFERRED 2026-08-24 (PO; revive only if play-feel asks), planned 2026-08-17, not started**: conditional, name-personalized walk-by greetings. `interaction.ambient` becomes an ordered variant list (conditions + `{name}` substitution, first match wins) and the condition vocabulary gains `talked_to`/`never_talked_to`, which dialogue nodes get for free. One chunk, backend + content, **frontend zero-change, schema NONE**. 2 PO rulings (public to the sensor · per-pair throttle ~180 s). ⚑ Its L1 is the reason the plan exists: the hail path must READ `HasTalkedTo` and never write it, because `NoteTalkedTo` advances talk-to quest objectives and would complete them by walking past
- **plan-region-primitive.md** — **designed 2026-08-23, 5 PO rulings, not started**: `zone.regions` — authored **polygons**, each naming a **profile** (a named bag of client-side presentation properties: ground colour, footstep sounds, music, later atmosphere). ⭐ **This is `tdd.md` §4.6 finally being built** — the 2026-07-09 decision that per-area music, darkness and per-area terrain *"all reduce to the same primitive: a named region inside a zone carrying its own properties"*. ⚑ **It is also the same field `plan-release-map.md` §8 (D6) specifies**, ruled and designed a day apart without either session knowing of the other; the reconciliation is its §2 — **D6 stays the ruling and the direction, this doc owns the mechanism**, and nothing was overturned because a coordinate square is a 4-point polygon. ⭐ **D0 is the load-bearing ruling**: ONE array, ONE rule applied *per property* — the last region in array order that contains the point AND whose profile declares that property wins — so a small bog blob inside a swamp zone can override the ground underfoot while the zone's music keeps playing, and neither has to know about the other. Also ruled: polygons over circles/rects · a named profile over raw hex (⭐ the ruling that made footsteps and music free later, instead of a content migration) · Tiled places them, the in-game editor only round-trips them. ⚑ **Schema NONE** — parse-and-ignore server-side, the `darkAreas` posture verbatim. ⭐ Its whole risk is **L1**: `ZoneModel.getZoneAsJSON()` is a hand-written whitelist, so shipping the field without teaching it means the first in-game save deletes every region silently with all tests green — the failure that already ate `spawn.level` once. There are now **THREE** such whitelists (zone.go · aura-convert.js · ZoneModel) and only the Tiled one is guarded; this plan is the **first customer of the tiled C5 completeness pin**, which goes red by design the moment `zone.go` grows the field. Map parity is non-negotiable (`MapTerrain` bakes its own copy, once, and never re-bakes); night tint comes free because the day-cycle filter set is derived; ⛔ atmosphere must never be built on the day/night filter machinery. ⭐ **D11: `resolve()` is TOTAL** — an unknown profile name, a profile omitting the property, and a profile naming a missing asset all fall back to the shipped default, never to silence, a blank or a throw (an authored `null` is the one deliberate exception, since it is the only way to ask for quiet). That ruling is also what makes it safe to leave profile names unvalidated server-side: a typo costs you one region's look, never the world's sound. ⭐ **And there is ONE check, not two**: `resolve(property, point)` is the whole interface, called with a point every consumer already holds — a remote player's footstep is the *identical* call to your own, since `PlayerMoved`/`CharacterMoved` both hand over the position already. 2 chunks + an optional PO colour pass
- **plan-region-audio.md** — **designed 2026-08-23, 2 PO rulings, not started**: the two audio consumers of the region primitive — **footsteps** (walking on swamp sounds like swamp) and **music** (the track changes when you cross into a region that declares one). One doc because they share every dependency and blocker; four chunks because their costs are nothing alike. ⭐ Footsteps are genuinely one lookup — `PlayerJuice.ts:77` already plays `step/step2/step3` through `spatialAudio` on a 400 ms trigger — and **the default profile is already authored**, since the shipped assets are named *footsteps-on-road*. ⭐ Music is 32 lines today (`Music.ts`: one hardcoded looping mp3, no track table, no transition) and ⚑ **the real cost is content — the repo owns exactly one track**. ⭐ **The find of the session:** `triggerMap.trigger('step', 400)` keys by that string alone on a shared map, so **every character in the world contends for one footstep token**, and `CharacterMoved` carries no id (`Events.ts:190`), making per-character throttling inexpressible today. Inaudible now (all steps sound alike); per-region sound turns it into your own steps going missing beside someone walking elsewhere — a pre-existing bug this feature would expose, so C1 fixes it standalone and can land any time. ⭐ **D6 PO-ruled: players only, local AND remote; mobs stay silent** — the widest of three scopes, and the ruling is what keeps that throttle bug in scope (either narrower reading would have left one emitter). ⭐ **D4 PO-ruled: ALWAYS fall back to the default audio** — covering four misses, not one (outside every region · profile declares no sound · unknown profile · **unknown sound id**). ⚑ The fourth needs writing on purpose: `SpatialAudio.play` opens with `if (!sound.exists(soundId)) return;`, so a mis-spelled id today is dropped with no sound, no warning and no throw — a region that mutes the game over a typo is exactly what the ruling forbids. ⚑ Blocked on backlog **§19** (~160 MB of eagerly decoded mp3s) in both directions: footsteps *add* ~18 decoded files, while music *wants* the HTML5 streaming §19 already prescribes for long tracks. ⚑ **Schema NONE**, client-only. 4 chunks (throttle · footsteps · music · **audio credits — `credits.html` attributes none of the 16 shipped sounds today**)

- **plan-localization.md** - **planned 2026-08-23, not started**: multi-language text, en (authored source) + de first, N-language by construction. Five chunks: C0 client i18n core + ~280 static UI strings · C1 per-locale content catalogs (`api/lang/de/` sidecar overlays, marshal-once-**per-locale**) · C2 wire keys+args for system messages + structured quest objectives · C3 the conversation surface (a public per-locale `/lang` text bundle) · C4 SkillTooltip templates + the German length sweep + PO playthrough. **DB schema NONE everywhere; wire schema YES in C2+C3** (appended fields, both binding regens - the immune-feedback landmine, twice). 5 PO rulings: keys+args on the wire (server stays locale-free) · client-side locale (localStorage, no DB column) · sidecar locale files · fonts deferred whole to `plan-ui-font.md` (de ships with fallback-serif umlauts, and that pass inherits glyph coverage as a hard requirement) · ⚑ the widened spoiler leak ACCEPTED (all authored prose becomes curl-readable, superseding the quests catalog's recorded minimal-projection philosophy). ⭐ Cheap where it matters: 0/61 mobs + 65/72 skills have no authored display name, and under the sidecar ruling `DeriveDisplayName` output *is* the English - no naming pass needed. ⚑ The relocated cost is grammar, not strings: D9's uninflected-template rule (the P21 dodge, extended) is what keeps German name refs from needing per-case inflection
- **plan-world-scale.md** — **M0 measured 2026-08-04; ⭐ S3 SHIPPED 2026-08-29 (uncommitted); S1 + S2 not started**: how big one `phy.Space` can get. ⭐ **M0's answer was ~3.5× today's area before S3, and terrain culling is dead** (§11). The tick cost was *dead linear in area with no bend* — ~3.4 ms per 1× of world — and at 4× the world burned **p95 16.0 ms with nobody connected at all**, which is F3 stated as a number. ⭐ **S3 (mob dormancy) then changed the cost's SHAPE, area-linear → player-linear**: a mob that nothing player-controlled is near stops thinking AND leaves the physics space. Measured at 10 dispersed players, **the 30× world (14 640 mobs) went 61.5 → 5.7 ms/tick, 10.8×** — from 186 % of the whole 33 ms budget spent on mobs alone to 17 %. ⚑ Its landmines are now the live ones: **`RemoveShape` must never be used for the sleep transition** (it purges by walking every shape in the space — ~2.6 ms per sleeping mob at 30×, and itself O(total mobs), which would hand the win straight back; `phy.SleepShape` is the sanctioned pair, and a real departure still uses `RemoveShape`) · **`m.SetWakeSources(s)` in `core.NewGameWith` is the ON-SWITCH**, a nil seam disables dormancy entirely (which is what keeps the sim byte-identical) and deleting it fails nothing · **spectators are wake sources** (they stream viewports, so omitting them empties the pre-join screen) · **D6's awake-count table was understated ~1.7×** — hysteresis puts the steady state at the SLEEP box, ~53 mobs/player not 32, moving M0's "~52 dispersed players" to ~32 · ⭐ L3 is CLOSED by D3 (a wounded mob is never eligible to sleep, so it always regenerates first — measured full at t=5.8 s vs asleep at t=14.8 s). ⭐ M0 rewrote S3's wake volume as **D6** — the **AOI box scaled** (`1.7 ×`), derived from `constant.ViewPortWidth/Height`, *not* a hand-set radius. The first draft's "wake 40 units" was a circle covering **21 × the AOI box**, which would have left 91 % of mobs awake and capped the server at ~7 dispersed players. ⚑ Zoom does **not** enter it: `Zoom.ts` is a fixed FOV hard-capped at 18 m to stay inside the 20 m stream. PO call open in §9: authored **mob density** is a free lever that competes with S3. ⭐ Read §2 for the survey that says **nothing in the engine caps world size** — `Bounds` is validated only `> 0`, wire positions are float32 with ~1000× headroom, the broadphase is density-bound, the network is already AOI-gated, and no coordinates are persisted. ⚑ **Read §0 first**: "map" means the *world* or the *map panel*, and conflating them made the first draft propose a whole wrong chunk. §10 keeps that adversarial review. **Still owed by S3:** the probe-ladder re-run on a real server and the §8 in-game pass. **Schema NONE**; parks the cross-zone `spawnpoint-N` id collision in §7. **S1 (stop bundling every zone into every client) is now the ranked next chunk** — and it has grown teeth: the PO's tiled `world.json` is **7.6 MB**, larger than the entire previous prod bundle, and it is still eagerly `require.context`-ed into the client

- **plan-release-map.md** - **direction set 2026-08-22 (PO session), nothing built, the map itself not yet designed**: the next map we build is the **first release-style map**, not another prototype. Carries the two rulings taken that day - **camps are content, not a system** (membership is a completed quest; §3 is the full mechanism, including the two ledger rules that force *one quest id per durable choice* and seal the exclusion at completion rather than acceptance) and **the prototype map is dropped**. ⭐ Everything the camp design needs is already authorable: exclusive teaching, camp-gated ascension rewards, a rival finale that renders *locked: complete "Pledge to the Ashfolk"*, and an ascension reroll that reopens all camps for free because the quest ledger is keyed `character_id`. **Schema NONE.** Inherits `archive/plan-test-world.md` §1 (the F1-F4 map bounds + measured densities) and explicitly refuses its rulings. §7 is what the map still owes a design session. ⭐ **§8 (D6, 2026-08-23) — one contiguous map, zones as coordinate regions carrying their own look, music and atmosphere.** Its MECHANISM is owned by `plan-region-primitive.md`, designed the same day out of a ground-colour question before either session knew of the other; D6 stays the ruling, that plan owns the field. ⚑ §8.2's "TWO touch points are mandatory" was corrected to **three** (the Tiled extension shipped the same day), and its music bullet now names `plan-region-audio.md` and the backlog §19 blocker it did not mention.
- **plan-mob-tether.md** — **designed 2026-08-05, not started**: a mob chased beyond a maximum distance from **where it entered combat** gives up, becomes immune and un-aggroable, and walks home. Reinstates the max-chase rule roadmap.md §7 shipped with and mob-depth 3b replaced — but the ask sharpened during the design pass into what it is *for*: ⭐ **stopping a high-level player from pulling mobs onto low-level players**. 6 PO rulings; **D5 reverses D4 in the same session** and the reason is measured, not argued — `SetActiveAura` zeroes `TickAccumulator`, so an *unprotected* tether makes a mob drop and re-latch aggro every ~3 s, restarting its aura cadence forever: a `tickInterval: 90` mob standing past its cap deals **literally zero** damage. Hence the WoW-style protected evade, taken with the PO's *"evading can be very annoying"* on record (C2 answers it). ⚑ **Anchor is `returnPos`, not the spawn point** — patrollers and the wander-radius spawn roll both break the literal reading. ⚑ **DB none, wire none**, one conf knob + one optional def field. Its §10 keeps the declined **territory clamp** (no evade, no flicker, drag impossible by construction) intact for the day the evade reads badly
- **plan-camps.md** - **⏸ DEFERRED 2026-08-22 (PO), not cancelled.** Camps ship as **quest content** instead - membership is a completed quest, exclusivity is a `quest_at_stage` gate on the offering/teaching/turn-in nodes, no new vocabulary and no schema; the design moved to **`plan-release-map.md`** §3. What this plan still buys and would revive it: a genuinely *hostile* standing, losing standing without joining, and server-enforced permanence (the quest design guards that invariant with a test pin instead). ⛑ Its §4.1 standing wipe and L0 hazard defer with it - nothing to wipe; the change to guard is now moving the **quest ledger** off `character_id`, since that keying is what reopens the camps after ascension. Original design, unretracted: **designed 2026-08-04** (the design pass backlog **§15** has been parked for since 2026-07-09, and the session `mobs/interaction.go` names in the boot error it raises today): a character joins one of **two rival human camps** at the Z2 front — permanent, teaches what the rival never teaches, and **closes the rival's questlines**. Hostility is **social, never attack-on-sight** (§4.4 prices that out: players are a compile-time `FactionAligned`, and the aggro mask is baked into the *physics sensor shape*, so per-player aggro is collision-path work). 8 PO rulings (world-parity power, reusing ascension D1 · permanent · per-faction friendly/neutral/hostile enum · teaching + quests + ascension rituals · `human_army` + one new player-safe rival · neutrality legal forever · free composition with silent recipe dead ends, ⚑ taken against recommendation · primitive anytime, content after ascension C1). ⭐ Cheap because the previous pass reserved the slot: the consequence vocabulary, its JSON shape and its boot refusal already exist, and **factions load before mobs** so the faction name resolves inside the existing loader — no cross-validation pass. 3 chunks + C0, **no migration** (a new `character_flags` key)


- **plan-leaving-the-world.md** — **designed 2026-08-04, nothing built**: backlog **§52**, absorbing and superseding **§48** (the deletable reconnect token — §48 becomes a *consequence*, not a companion, and its ⛔ block is lifted). Leaving the world stops being instant: a disconnect leaves your body in the world for a linger, a **deliberate** leave is refused in combat and costs a 5 s countdown when unsafe (D10), and *safe* means **inside a campfire ring AND not in combat** (D2). ⭐ **D11 was taken last and rewrote the plan — the reconnect park is DELETED, not reduced**, which dissolved an earlier ruling (D3) along with the two holes it had opened; re-adoption is in place (D5), one chunk (D8). ⚑ **Its line references are stale**: the banner says they were refreshed against `1ac8078e` and to re-verify if more landed — flight C2 (`bc01a45c`) has since touched `sys/state.go` and everything past ~line 1000 has drifted ~30 lines (`trackCampfireDwell` 1095 → **1127**, `Remove`/`removeFromPlayers` 1167/1224 → **1201**/**1258**). The design holds; the L-numbered landmines cite moved lines

- **plan-content-tooling.md** — **designed 2026-08-05 (D1–D6), RE-SCOPED 2026-08-09 (D7–D10), not started, queued AFTER ascension C1 (D6)**. ⭐ **D7 set the frame — bulk placement/authoring is AI-side (scripts + JSON), the human editor is for spot edits and tuning** — and cut half the plan: JSON Schemas (D8), the backlog-§22 standalone map editor (D9 — §22 closed; only **drag-to-move survives, in the in-game editor**), and the D1/D2 full quest+dialogue editor (D10 — shrunk to a **texts + reward numbers form**; D1–D3 → §10). What remains: **C0** `-validate` CLI + CI gate + registry lock/tombstones + reconciliation (D5 rules ratified: skill IDs forever, maxLevel never decreases) · **C1** the dev-only save endpoint + zone-editor retrofit, **no hot reload** (D4) · **C2** in-game drag-to-move · **C3** the text form. ⚑ **Schema impact NONE — no migration**; encounters (§17) and conf.json knobs deliberately stay as they are

- **plan-prop-scale.md** — ⭐ **COMPLETE 2026-08-23, all six chunks shipped** (C1 scale · C1b the visual/collision split · C1c the two content incidents + D8 · C1d the census pins · C2 rotation · C2b the rotated rect collider). ⚑ Still in `docs/` rather than `archive/` for one reason: the PO's in-game pass on C2b is owed. Designed 2026-08-22, broadened the same day to scale + rotation: per-placement prop transform, so a forest can have a big old tree and saplings instead of 573 identical ones. Opened by a PO observation while authoring in Tiled (scaling a tree did nothing, because `world.json` has no per-prop size). ⭐ Cheap because the expensive half already exists: props ride the Resource wire table, which **already carries a per-entity radius** (`codec/gamestate.go:610`) that the client **already** scales the sprite from (`Resources.ts:63`) — so **FlatBuffers NONE, client NONE, DB NONE**, and the work is one optional authored field reaching the two constructors at `aurad.go:171-179`. Design: a **multiplier**, not terrain's absolute `size`, because a prop body is either a circle OR a rect and only a multiplier scales both while keeping the authored aspect; tri-state, so all 778 existing props serialize unchanged. ⚑ **Collision scales with it** — the body IS the shape, so this is a gameplay knob, not a cosmetic one (scaling props along a path narrows the path). ⚑ Its L1 is the editor whitelist that already ate `spawn.level` once: a new field must be named in `getZoneAsJSON` in the SAME chunk, or scale authored in Tiled is silently deleted by the next in-game-editor save. ⭐ **Rotation, the "adjacent twin" the plan first excluded, shipped too** — it needed the one thing scale did not, a wire field, and cost **+16 bytes per snapshot flat** (measured; an unrotated prop costs zero, because the field is last in the table and the vtable trims). ⚑ **C2b then removed the one thing rotation did not fix**: the plan had priced rotated rect collision as "backlog §34 territory, far beyond this plan" — but §34 is *entity-vs-entity* collision, already decided against, and covers none of this. The real price was one chunk, because all of the shape's geometry funnels through a single `clamp()` and props are static
- **plan-play-bot.md** - **designed 2026-08-12, nothing built**: a headless character that actually *plays* (real kills, real XP, real dialogue, a real quest ledger, real deaths and respawns), as an on-demand **soak instrument** for the two blind spots nothing else covers: long-horizon accumulation bugs, and an empirical kills/hour to cross-check `simharness` against the real loop. 4 PO rulings (own binary `cmd/playbot` with the transport extracted out of `loadbot` · cheats for **transport and seeding only**, never inside the measured window · the first run answers soak + ledger integrity + economy, **reachability deliberately not selected** · **per-band sprints**, not one long climb). ⭐ Cheap because the wire already carries everything a bot needs to perceive *and* act - the **whole conversation tree** is streamed (`server.fbs:476`) and `Interact` echoes authored indices back, so questing needs no cheat and no browser. ⚑ **Schema NONE at every layer.** Its L1 is the tick-accumulator thrash `plan-mob-tether.md` D5 measured on mobs: a bot that re-asserts its aura slot every tick deals literally zero damage


- **plan-prototype-projectile.md** - **P1 (skeleton + bomb) BUILT 2026-08-19 on `main` (PO overrode the own-branch posture), PO in-game check PENDING; P2 drift + P3 mob thrower hang on the verdict (ledger: §11)**: throw an aura - a cooldown spawns a short-lived aura-carrying entity in the caster's last walking direction, in three feelable shapes (flying bolt · bomb with proximity-mine AND timed variants · frost ball that parks while overlapping someone). ⭐ Cheap because the projectile is an authored `role: structure` mob riding summon TTL, the dash aim, placeholder rendering, and the mob auto-fire path (consume-on-hit = a free proximity mine); mine vs timed is two authored numbers, not a flag; attackability (boss bombs burstable, player bombs not) is authored health. ⚑ **Schema NONE at every layer.** 11 decisions, 4 PO rulings; the recorded risk is D3's kiting caveat (last-walk aim throws *behind* a fleeing player - the cursor angle is already on the wire and server-dropped, the prepared fallback)

**The `plan-accounts-*` set has split**: roadmap **step 8a shipped and CLOSED 2026-08-04**, so schema + implementation + frontend moved to `archive/` (see the Archive section); 8b is `plan-ui-polish.md` + `plan-avatar-system.md` and is still open. Only the password-reset doc below is live.

- **plan-accounts-password-reset.md** — **split out of the three archived docs (2026-07-29), runs after them and is still unscheduled**: optional recovery email, forgot/reset flow, session invalidation, real client-side routing, and the outbound-email infrastructure aura has none of. ⚑ Until it ships, a forgotten password is unrecoverable — an accepted interim state the register form states plainly

## Content (catalogs + zone design intent)

Structure (2026-07-16): **type catalogs** hold the *what* — one file per category, every
entry with a status (`idea` → `designed` → `in-game`); **zone docs** hold the *where* as
**design intent only** — exact runtime placement lives in the editor-authored zone JSON and
is never mirrored into docs. Catalogs and zone docs cross-reference by name (one source of
truth per thing). In-game entries point to their authoritative definition under `api/`.
All numbers are [PLACEHOLDER] until the balance pass. Ideas go **directly into the
matching catalog** with status `idea` — there is no separate inbox. Features/systems still
go to `backlog.md`; this section is content only.

- **content-auras.md** — active auras
- **content-passives.md** — passives
- **content-cooldowns.md** — cooldown abilities
- **content-recipes.md** — combination recipes (curated, secret, community-discovered)
- **content-npcs.md** — friendly/teaching NPC roster (identity, speech, teachings, placement intent)
- **content-mobs.md** — mob roster + category taxonomy (animals → dragons)
- **content-lore.md** — cross-zone worldbuilding: history, factions, tone
- **content-story.md** — cross-zone story arcs + breadcrumb chains
- **content-world.md** — zone progression map (21+ zones), scope tiers, connections, unplaced locations
- **content-ability-matrix.md** - the **possibility space**: the effect vocabulary as a grid (category × effect type × damage type), which cells are filled, which are empty, what the vocabulary cannot express, and how many relevant abilities are authorable. Owns the empty cells and the count only; numbers and sources stay in the inventory, per-ability intent stays in the three catalogs above
- **content-skill-inventory.md** — **generated** from `api/` (not hand-maintained): every player skill with id, maxLevel, values and **unlock source**, plus the reachability sweep. The source of truth the three catalogs above point at; regenerate after any content chunk
- **content-zone1.md** — Zone 1 (village + forest) design intent
- **content-zone2.md** — Zone 2 (village + City Gates + the front) design intent

## Art (`docs/art/` — inventory for the art overhaul)

Generated 2026-08-15 from `api/` + the client, not hand-maintained prose. Regenerate
(or spot-fix) after any chunk that adds a mob, prop, ground texture or in-world VFX.

- **art/README.md** — the art brief **and** the worklist in one file, 108 tickable rows
  (`☐` → `☑`). Brief: **the Portrait Rule** (entities are front-facing busts, never
  rotated — GDD §10 + `manual-content-authoring.md` §4), the Gothic tone register, the
  120 px/meter scale rule, the ×2 renderer gotcha, rendering constraints (darkness
  overlay, damage flash, tier rings, rotation), and where the placement volume is
  concentrated. Lists: 58 live mobs/NPCs/summons/fixtures grouped by the `content-mobs.md`
  taxonomy · 29 environment (props, the 16 ground decals, terrain colours, lighting,
  world scale) · 21 player + code-drawn VFX + overlays + deferred UI. Legacy Berryhunter
  content is excluded to an appendix.
- **art/pipeline.md** — how art gets *into* the game: the bake-once/square-sprite
  mechanics and the `maxSize` resolution knob, drop-in file replacement (frontend-only,
  plus the shared-sprite and lying-filename traps), **SVG vs PNG** (webpack already
  handles raster; the Pixi game-object path is unverified — includes the 10-minute test
  that settles it), and the **four-layer medallion** (background/portrait/decor/border,
  which replaces the runtime tier ring) with per-layer selector availability and the
  open zone-selector question (⚑ superseded 2026-08-20: the border keys on faction
  family, not zone+level; `plan-entity-medallions.md` D9)
- **art/medallion-asset-spec.md** — the **artist-facing delivery contract** for the
  medallion layer set (2026-08-20, rev 2026-08-22 on artist feedback): 512×512 shared
  canvas, three shared circles (proportions artist-led, measured at the pilot and then
  frozen), rim at the bottom of the stack, per-layer rules (greyscale disc, per-family
  rings+rims, universal additions/decorations), naming, self-check list. Delivery is
  in-repo: the artist commits PNGs directly, keeps one master source file, no per-file
  PSDs. Written so art starts before implementation; design rationale lives in
  `plan-entity-medallions.md` (the feedback decisions: D16-D18)

## Manuals (how-to)

- **manual-zone-editor.md** — step-by-step user manual for the in-game zone editor: setup, modes, placing/editing props+spawns, export→server round-trip
- **manual-tiled-editor.md** — the OTHER zone editor: authoring the same `api/zones/world.json` in **Tiled**, which is the only surface that can select/move/rotate a placed *terrain* blob. Install once, open the project, Ctrl+S writes the file in place. ⭐ Opens with a which-one-to-reach-for table, since both editors write the file byte-identically and neither is retired. ⚑ Its two live traps have their own callouts: the spawn form’s **inherit sentinels** (`-1`/`0`/`pingpong`, and the two fields where `0` is a REAL setting) and patrol routes (**the first polyline click is the spawn**, and dragging node 0 changes nothing)
- **manual-content-authoring.md** — adding/replacing content by hand: new mobs (5-file EntityType path + `entityType` variant shortcut), new abilities, ability VFX, mob/player icons, scripted encounters/boss fights (§5); wire-touch table + hand-sync points
- **manual-db-migrations.md** — step-by-step user manual and runbook for database migrations: embedded `golang-migrate` architecture, up/down SQL authoring, testing, dirty-state recovery, and production operations

## Onboarding artifacts (human-facing, standalone HTML)

Point-in-time snapshots for humans, not maintained alongside the code — re-generate rather
than patch. PDF renders of the first two sit in the repo root.

- **developer-onboarding.html** — "Aura — Developer Onboarding" (2026-07-22)
- **feature-inventory.html** — "Aura — Feature Inventory" (2026-07-22)
- **process-overview.html** — "Aura: Process Overview" (2026-08-12). The work process
  explained for an outside reader: the PO + AI-implementer arrangement, the four-layer
  status model, the anatomy of a work item, how the process evolved, and what changed
  from the initial plan. PDF render in the repo root (`aura-process-overview.pdf`).
  ⚑ Same snapshot caveat as the two above — re-generate rather than patch.
- **accounts/chunk-1a-summary.html** — "Step 8a, Chunk 1a: the database foundation"
  (2026-07-31). Plain-language summary of what shipped, what it deliberately does not do,
  the three defects found, and where the code lives. ⚑ A *snapshot*, not a source of truth —
  the `plan-accounts-*.md` docs win on any disagreement. Written to be importable into
  Google Docs, hence HTML.

- **accounts/chunk-1b-summary.html** — "Step 8a, Chunk 1b: auth & sessions"
  (2026-08-01). Plain-language summary of the six auth primitives, the eight deliberate
  breakages that proved the tests can fail, the two architecture questions answered
  (login CPU vs the game loop; 100 concurrent players), the separate-accounts-database
  proposal and why it was rejected, and the three rules that keep a future machine split
  a deploy decision. ⚑ Same snapshot caveat as 1a's.

- **accounts/chunk-1c-summary.html** — "Step 8a, Chunk 1c: the eight endpoints"
  (2026-08-01). Plain-language summary of what a player can now do (create with no signup,
  come back, sign up without losing progress, log in, log out everywhere, delete), the
  character-name ruling and why `Barney Rubble` had to survive it, the two old permissive
  transport defaults that were closed, and the twelve deliberate breakages — eleven caught,
  the twelfth a wrong comment rather than a missing test. ⚑ Same snapshot caveat as 1a's.

- **accounts/chunk-2-summary.html** — "Step 8a, Chunk 2: the front door" (2026-08-01).
  What a player now clicks through, and the three decisions worth explaining: why Log in
  vanished from inside the game, why the delete countdown is not a security feature, and
  why the name suggester changed. ⚑ Same snapshot caveat as 1a's.

- **accounts/chunk-3-summary.html** — "Step 8a, Chunk 3: the authenticated door"
  (2026-08-02). How the play ticket works in plain terms, the three rules it makes true
  (one player one place, refresh proves identity, logout ends the game session), and what
  the PO hand-testing pass found — including the reconnect break that was hiding behind
  two unrelated-sounding reports. ⚑ Same snapshot caveat as 1a's.

- **accounts/chunk-4-summary.html** — "Step 8a, Chunk 4: your progress survives"
  (2026-08-02). Why saving and loading had to be one piece of work, how a snapshot reaches
  the database without the game loop ever waiting for it, when a save fires, and the three
  things that were nearly wrong — including the cleanup tool that broke while every test
  stayed green. ⚑ Same snapshot caveat as 1a's.

- **accounts/campfire-bind-fix.html** — "Bugfix: your campfire remembers you"
  (2026-08-02). The first reported bug against step 8a, and why it was a missing piece
  rather than a break: campfires had no name stable enough to write down. Covers why the
  bind is stored as an id and not a location, what happens to your bind when a campfire is
  deleted, and the second bug found while testing the first — a counter that kept counting
  the fire you had walked away from. ⚑ Same snapshot caveat as 1a's.

`docs/accounts/` also carries **session handovers** — working notes for picking a chunk up
cold, not snapshots. ⚑ **Disposable: delete one when its chunk lands**, so the folder never
accumulates stale instructions. Durable content belongs in the plan doc's chunk ledger.
There are none open: `chunk-4-handover.md` was deleted when chunk 4 landed on 2026-08-02,
per that rule.

## Research (investigations / assessments)

*Point-in-time when written; most are still forward-looking inputs to unbuilt work.*

- **research-code-quality.md** — code-quality items (level-scaling unification §3.2/§3.3 done); **§7 = 2026-07-22 re-assessment**: legacy layer + frontend/backend duplication closed, one new latent risk (`gameObjectClasses` positional array), and ⭐ three cheap recommended fixes (tests in CI · enum-keyed entity map · frontend typecheck)
- **research-content-pipeline.md** — designer-authoring pipeline gaps + preventive steps
- **research-v1-readiness.md** — prototype→live readiness assessment (ops/CI/observability gaps); feeds step 9
- **research-hosting.md** — hosting phases, load math, persistent-servers decision; Phase 0's runbook is `archive/plan-phase0-deploy.md` (superseded by the live `plan-playtest-deploy.md`), Phases 1+ still open

## Archive — `docs/archive/` (finished work, kept for rationale)

Not maintained, but **not dead**: several of these are still the only design record for a
system that is very much live, so read them for *why*, not for *current state*. Paths below
are relative to `docs/archive/`.

- **status-history.md** — verbatim `CLAUDE.md ## Status` entries that fell off the section's
  cap (last completed + 2 prior; see the `chunk-wrap` skill). Historical snapshots — their
  "open"/"next" claims may since be closed; the full ledgers are the plan-doc §-banners
- **plan-tiled-authoring.md** — **COMPLETE 2026-08-23 — all seven chunks shipped** (C0–C2 on 2026-08-22; C4, C5, C6 and C3 on 2026-08-23; first commit `0b8ba13f`) — world.json opens in Tiled WITH THE REAL GAME ART, saves back byte-identical, and now REFUSES a save the server would reject at boot, naming the Tiled object id (⭐ a prop dropped on the spawns layer is told which layer it belongs in, and `terrain.type` is validated for the first time anywhere); adding content is now `node tools/tiled/generate-palette.mjs` + a reopen (the extension carries no content and is installed once per machine), and ⭐ a **completeness pin** scraped from `zone.go`’s json tags stops a new FIELD being dropped in silence — proven red before being trusted; and ⭐ a spawn now shows a full TYPED form — mob from a dropdown, every knob present — while world.json stays byte-identical, because each tri-state field borrows a value the loader already rejects as its “inherit” sentinel (⚑ `wanderRadius` and `respawnTicks` cannot use 0: it is a real authored value for both); C3 closed it out: `manual-tiled-editor.md`, `tools/tiled/verify.sh` (a headless real-Tiled round-trip anyone can re-run), and the bookkeeping — backlog §58 answered by a different editor rather than by fixing the in-game one, `plan-content-tooling.md` C2 re-scoped to the in-world-only case, and roadmap §4’s *“external editor vs custom JSON — biggest unknown”* flag answered **both****: author `api/zones/world.json` in **Tiled** via a checked-in JS extension that `read`s *and* `write`s the file natively — no intermediate `.tmj`, no converter, `world.json` stays the single source of truth. 3 PO rulings (Tiled over LDtk · native round-trip over a converter · all six arrays). ⭐ The feasibility answer is that **none of Aura's world is a tilemap** — every array is freeform floats with no grid and no snapping anywhere — so only the *object* layer matters, and there Tiled wins decisively: LDtk entities support neither rotation (#841) nor flipping (#978), which would leave the 537-piece `terrain` array, i.e. the parked pain, uneditable. Targets **backlog §58** (terrain: no select/move/rotate) and makes `plan-content-tooling.md` C2 largely obsolete for terrain+props; ⚑ **D7 of that plan is NOT overturned** — bulk placement stays AI-side in `scripts/world-*.py`. ⚑ **Schema NONE at every layer.** Its whole risk is **D6, byte-stability**: `ZoneModel.getZoneAsJSON()` is the canonical serializer and the extension must reproduce it exactly, or every alternating Tiled/in-game save is a 260 KB diff. Six measured landmines the writer must respect (§4.2), of which two are silent-corruption class: the **tri-state pointers** (absent `wanderRadius` = inherit, explicit `0` = force stationary — an always-present Tiled property rewrites ~226 mobs) and **talkers carrying no respawn keys at all** (absent parses to 0 = respawn every tick). ⚑ `terrain.size` is a **half-extent**, and terrain **array order is paint order**. ⭐ **C0 measured all three unknowns headlessly** (`tmxrasterizer` ships with Tiled): SVG textures load as-is so C2 loses its rasterization step · the rotation/origin/flip mapping is a pure anchor change, same handedness and same flip-before-rotate order as PixiJS (formulas in §11) · and the feared `.json` extension collision is a non-issue — Tiled falls through to our reader, so `world.json` opens directly. ⚑ The one caveat: a **project-local `extensionsPath` does NOT load** (relative, subdir and absolute all fail headlessly), so `tools/tiled/install.sh` runs once per machine


### Execution-step records (the build order, oldest first)

- **archive-block2-survival-removal.md** — Block 2 (roadmap items 1+2): survival systems, crafting, inventory, item wire protocol removed; single resource established. Complete 2026-07-04
- **plan-skill-system.md** — the skill system: Phases 1–9 migration record **plus** the combination-system design and the skill wire protocol. Complete 2026-07-05 — still the reference for how skills work
- **plan-item11-hp-resist-variance.md** — absolute HP / resistances / damage tags / stat variance; decisions A1–A3, B1–B7. Complete 2026-07-06 — the damage-tag substrate is live
- **plan-effect-foundations.md** — effect-vocabulary scaling: decisions F1–F10 (stay in Go, no scripting for effects; primitive-first growth) + the candidate-effect cost map. Complete
- **plan-world-zones.md** — world & zones first slice: in-game editor, rectangular single-Space world, server-authoritative `zone.json`; decisions A–D. Complete 2026-07-08
- **plan-mob-depth.md** — mob depth & totems (step 2): 9 chunks (totem → flee → aggro/threat → steering → patrol → companion → taunt → support mobs → encounter-controller spine). Complete 2026-07-12 — the threat/encounter spine reference
- **plan-atmosphere-recovery.md** — atmosphere & recovery (step 3): regen combat gate → campfires → darkness & light → death state + campfire respawn. Complete 2026-07-13
- **plan-skill-vocab.md** — skill-vocabulary fill (step 4): shield/lifesteal/execute/crit/berserker/dash, cast-time + interrupt, tick-rate seam. Complete; §4.3 holds the crit v1→v2 rework
- **plan-npc-teaching.md** — unlock-source systems (step 5): teaching NPCs, one-way speech, zone-editor `npc` mode. Complete 2026-07-15
- **plan-sim-harness.md** — the balancing / what-if explorer (pre-step-6 gate): headless ECS runner, TTK/TTD/kills-per-hour batteries, guardrail asserts. Complete — *the tool itself is live; drive it via the `run-simharness` skill*
- **plan-content-zones12.md** — the content pass (step 6) for Zones 1+2: capability baseline, geography/story/mob/ability plan, systems-coverage table, chunks C0–C8. Complete 2026-07-21
- **plan-rebrand-cleanup.md** — rebrand to "Aura" + Berryhunter cleanup (step 7), `aa509d95`. Complete 2026-07-21 — records which Berryhunter references are *deliberately* kept
- **plan-accounts-schema.md · plan-accounts-implementation.md · plan-accounts-frontend.md** — **accounts & persistence (step 8a)**, the three-doc set: designed 2026-07-30, built across chunks 0 · 1a · 1b · 1c · 2 · 3 · 4, live on the playtest server, **CLOSED 2026-08-04**. Read schema (the DDL and why each shape) → implementation (§0 tech stack: pgx/v5, hand-written SQL, `golang-migrate` as a library; save triggers, snapshot mechanics, the sacrifice transaction, native Go auth) → frontend (anonymous-first join, register vs. login, 3 slots, soft-delete; **its §10a holds every chunk ledger**). ⚑ Read them for *why*, not current state — with one exception that IS current: **§8's ruling box in implementation.md — the step closed WITHOUT backups**, deliberately (PO 2026-08-04, the live server is a testing ground and infinite persistence is not the goal yet), and the durability items are the one part of this plan still owed. `plan-accounts-password-reset.md` stays live in `docs/`
- **plan-ascension.md** - **the character-sacrifice loop; COMPLETE 2026-08-11**, persistence's first consumer and the last v1 scope item pulled in by the 2026-07-19 intermission ruling. A max-level character is spent at a world stone and its **bloodline** (the character SLOT, not the account) keeps one picked reward forever. Built as C1 `dee6a9c0` (the atomic sacrifice transaction + the catalog loader) → C2a `4674cf00` (the stone, the ceremony's channel, the confirm) → C2b `a64f5b96` (the bloodline on character select) → C3 `b995061b` (the memorial + the eight-entry seed catalog), plus two follow-ups: the row tooltip `0497417a` and the ceremony's own channel effect `5b014c17`. **Schema impact NONE at every layer, no migration, across the whole plan.** 31 PO rulings D1–D31; superseded ones live in its §10 and nowhere else (D13 cut the point economy; D18 put conditions back). ⚑ Three threads outlived it and are NOT in this doc: **leaving the world without a reload** (`plan-leaving-the-world.md`), the **quest-row reward tooltip** (an open PO call, §13.9), and §8's **deferred cluster** (point economy, cross-life counters, per-faction ascensions, cosmetics), revivable whole. ⚑ Its catalog is a content directory of its own (`api/ascension/`), the ninth in `contentSources`
- **plan-ascension-sites.md** - **COMPLETE 2026-08-11**: many ascension stones, each owning its own price and its own reward list, so the village stone asks for level 30 while the one at the front asks for level 25 plus a finished *Thin the Orc Line* and pays out three rewards where the village pays eight. Successor to `plan-ascension.md`, which shipped the loop against exactly one stone at one price. Built as C1 `509321e6` (the site owns its price - C1a moved `RowSource` onto the NODE, C1b moved the price) → C2 `d66ca9f3` (an unmet gate renders LOCKED and NAMED instead of vanishing, opt-in per row) → C3 `350701c9` + `f86420f5` (the site owns its rewards - C3a the plumbing, behaviour-neutral, C3b the content). ⭐ **Half of it was already authorable** before a line was written: node `conditions` are a list and the vocabulary already covered `minLevel`, `quest_at_stage`, `bloodline_ascensions` and `kills_this_life`, so the plan was mostly about removing three blockers - the Go authority that overrode the content, the single global catalog behind an interface that never learned which actor was asking, and a gate the player could not read. Ten PO rulings (**D1 retires the archived plan's P1**, max level as the entry price; **D5** refuses a catch-all reward list outright). **Schema impact NONE at every layer**, no migration, across the whole plan. ✅ **The PO's own in-game check passed 2026-08-12**; §7's leftovers (a price visible from outside talking range, the lore write both stones owe) live in CLAUDE.md's open items

### Fix / cleanup records

- **plan-code-health.md** - **COMPLETE 2026-08-14, all 7 chunks in three days**: the prophylaxis pass off the 2026-08-12 three-sweep audit - deleted what nobody should restyle, collapsed what every change would otherwise pay N times, fixed what was measurably wrong, pinned what drifted silently. C1 dead-code sweep `ca34800b` (~550 lines; its CI step REVERSED same day - **NO CI BY CHOICE**, the standing PO ruling) · C2 live frontend defects `beeba7c0` · C3 the server's `stepMillis` 33.0→derived `6f0c08df` · C4 the pinning batch `b5a88221` · C5 frontend duplication closure `38e36153` · C6 the Theme.ts ↔ variables.less pinned token pair `a46b30a5` · C7 the absorbed `research-code-quality.md` §11.5 backend batch (`deleted_` reserved, the harnessdb DSN guard, `-serve` refusing XP flags, the 201-outranks-the-session fix, the dead pre/post-update systems). ⭐ Codified the four-tier value hierarchy (derive > serve > pin > never a bare "SYNCED" comment). **Schema NONE in every chunk.** ⚑ Still current: its §Ownership holds the boot-once offender ranking `plan-leaving-the-world.md` picks up
- **plan-reconnect-token.md** — reconnect-token persistence (`a8e82851`): reload restores the character via `sessionStorage` + append-only `Join`/`Accept` fields
- **plan-input-jitter.md** — dropped movement inputs → held-state model (`cb7f011f`): instrument-first, overturned the TCP-HoL hypothesis; server coasts, client re-sends "stopped" on release
- **plan-render-jitter.md** — walking micro-resets → buffered snapshot interpolation (`0e504c22`/`8a29a75c`/`c5064732`): tickrate align + `RENDER_DELAY_TICKS=2`, freeze-not-extrapolate
- **plan-entitytype-validation.md** — backlog §27.2.1 (`c3938be7`): the mob `EntityType` name-fallback validated at load, turning a live crash-at-first-spawn into a boot error
- **plan-resource-decay-prune.md** — backlog §26 (`ee9d42e9`+`a2ab90b5`): the dead resource/placeable/decay layer removed. Its §13 holds two process lessons (hidden webpack `require`s; rebuild **both** sides after content deletions)
- **plan-item-system-removal.md** — backlog §28 (`b9d01d33`+`2f933634`+Chunk 3): the dead Berryhunter item system removed in 3 chunks — backend registry, frontend scaffolding, then the wire-enum prune that **pinned explicit `EntityType`/`StatusEffect` values** so no future removal renumbers a survivor. Its §13 Chunk 3 records a plan audit that caught a broken repoint target before it shipped
- **plan-unlock-attribution.md** — unlock source attribution (`2bfee286`): `EntityMessage.kind=Unlock` labels every unlock from the 4 grant sites
- **plan-entity-model.md** — **the Actor model** (backlog §31; `cf9a10c7` 1a + `ee01ccdb` 1b + `0be771bd` 2 + `ba124ceb` 3a + `6368b2e5` 3b-i + `ef4355f1` 3b-ii + `759ddfb6` R1/R2/R5 + R4; **complete 2026-07-29**). Converged the player/mob/NPC stat model onto one Actor + optional capabilities, replaced the *inferred* roles with an authored `role` discriminator, and deleted `model/npc` — an NPC is now an ordinary actor carrying an `interaction` block. ⭐ Read it for the reframe: §31 read as *"three types should be one"*, but the code said the inverse — **`Mob` was ALREADY the universal entity doing five jobs, signalling which one by lying about its numbers** (`speed: 0` = structure, `velocity > 0` = follower), and that single defect was behind all five gaps. Measured outcome: `Mob` went 66 → 67 fields while absorbing a whole type, and **a talking follower needs zero new code**. 7 PO decisions; its §8b holds the latent traps that arm when quest-style content is authored, and it spawned `plan-faction-flips.md` (L2) and `backlog.md` §39
- **plan-faction-flips.md** — **runtime allegiance** (`ec73634e` the seam + `216f733b` calm + `153c0032` charm + `3b1b3ef6` the in-world teachers; **complete, every §8 question closed 2026-07-29**). Closed `plan-entity-model.md` L2 by replacing one undefined setter (`SetFaction`, whose `^f.Bit()` was an *undefined destination*, not a wrong value) with three defined verbs, then shipped **calm** and **charm** on top of it. Read it for **D2** — one accessor answering three questions (`owner` = *whose level* / *whose credit* / *whose signals*) is invisible until a caller needs them answered differently — and for **L-O**: skill-level JSON has no `DisallowUnknownFields`, so a faction allowlist must be *mandatory* per effect type or a typo reads as unrestricted. Hands one thing forward: `backlog.md` §39
- **plan-pre-accounts-hygiene.md** — **config truth + wire vestiges**, the last chunk before step 8 (`c183ce12` + `50a1e5c9`), run as **two sessions split at H1a** so that *"the other four changed nothing"* stayed a live check rather than a claim. Closed backlog §35 tier 1 and §30. ⚑ **The whole chunk came out byte-identical, H1a included** — the harness's 4× chase-margin drift turned out to be *latent*, because no battery scenario ever makes a mob approach, so TTK 6.67 s / TTD 8.70 s stand. Its §11 pins the four battery commands and the guardrail output's run-to-run map-ordering noise
- **plan-quests.md** — **the quest system** (backlog §42 + GDD §8 "Quests & the Journal"; designed 2026-07-29, built 2026-07-30 in six chunks: `d45ba07c` P + `d3b89328` C1 + `2a3b137d` C0 + `2dc6973a` C2 + `604f3f4d` C3 + `395177e4` C4; **complete 2026-07-30**). Journal-carried multi-stage quests with **no markers, ever**, riding the interaction container the Actor model already shipped. ⭐ Read it for the load-bearing shape: **branching is dialogue-shaped** — objective stages auto-advance off *lifetime* counters (so credit is retroactive and quests can never disagree with XP, D3/D4), while several rows on several NPCs can move the same stage to different next stages with different rewards, which is why "two NPCs finish the quest" is content and not a feature (D9, and `wolves-on-the-road` is the proof). Its traps are worth the read even outside quests: the ledger had to join the **death/reconnect stash** or every death wiped it (L11), the D17 banner notifier must be installed by the *owning* player because the ledger outlives the player struct, **terminality is derived rather than authored** (hence a two-phase cross-validation pass), and abandon + a mid-quest `grant_xp` is a loopable faucet (L10). §15 records what authoring the first four quests taught about the container itself
- **plan-conversation-journal.md** — **the conversation & journal pass** (planned 2026-07-30, built the same day in four chunks: `d23670d7` Q1 + `1dfb57d8` Q2 + `49b49857` Q3 + `3ccadb5e` Q4; **complete 2026-07-30**): the 13 items the PO's in-game walk of quest chunk C4 raised, every one verified against the running game before planning. What it left behind: `blockedLine` deleted (a greyed row is simply inert), **a quest row is shown iff its ledger op would succeed** (`Ledger.CanApply` — the show-rule that made Accept vanish while sibling questions stay, and then let Q4 author quest rows with zero `quest_at_stage` gates), talking in combat un-gated, server-composed objective lines with authored `tracker` overrides (`{n}/{m}` substitution), the two-pane journal with remembered selection, every conversant on R1's tree shape, the lamp quest simplified to the Lantern aura's only source (L5b), and Damage as a creation-seeded level-1 milestone. Its §2 is the measurement record: what looks like a content bug and is not, and what looks systemic and is not
- **plan-conf-duplication.md** — **backlog §35 tiers 2–5** (`e7531444` C1+C2 · C3+C4 2026-07-30; **complete 2026-07-30**). Every tuning value got one authored home: env confs shrunk to genuine deltas over a now-total Go defaulting layer, unknown conf keys warn at boot, the tier-3 Go literals are pinned by tests, `spacedName()` died in favour of the served `/mobs` `displayName`, `ActivationRejection` became a pinned wire enum, and `api/shared-constants.json` pins the pip/ring/tier bit tables + viewport/tickrate from both languages. Read it for the C1.3 finding — a *rounded* JSON restatement split the fleet at float32 in production's favour — and for the deliberate-mirror rule: pinned, not unified

- **plan-feel-pass-2.md** — **what came back from the R1–R3 checklist** (planned 2026-08-01, **built 2026-08-02, all five chunks N1–N5 in one session; complete**): the second PO feel pass on the priced catalog — the R-series landed, so what came back was presentation, one live bug and two design questions, ruled into 5 decisions and 5 chunks. What it left behind: the Focus bar's denominator is total effective HP (a shield larger than the pool no longer paints the whole bar — `ShieldBarMath`, shared by all three bars incl. the mob overhead the plan had missed) · tooltip cost lines grouped by charge trigger + one shared `Ticks every` line (sum-of-rounded, never rounded-sum) · **dots leech** (`tickBuffEvents` had no `Lifesteal` field — no dot had ever leeched; read live at each burn tick off the post-credit caster, D1) · **quest progress counts per stage entry** (`Progress.KillBase`/`TalkBase`, reversing `archive/plan-quests.md` D3 in place; the baselines are persisted state recorded in `plan-accounts-schema.md` ahead of 8a) · the ring pulse + HUD metronome with the `BeatDetector` switch-reset guard. Its §6 holds the deliberate parkings (Damage's dead levels + the guard test → backlog §37)

- **plan-numbers-feel-pass.md** — **the PO feel pass on the numbers rewrite** (**✅ RUN 2026-08-01; archived 2026-08-05**): the seven areas to judge by hand (the premise, the free floor, the point economy, the retuned set, damage types, tooltips, Discipline). It stays the **checklist of record** — what was asked and why; the replies and their triage are in `plan-resource-costs-feedback.md`, archived beside it. Read it for the framing that made the pass work: *a value that reads wrong is a finding, not a bug* — the checklist asks "does this read the way it was designed to read", never "is this number correct"
- **plan-resource-costs-feedback.md** — **what came back from the numbers rewrite, and the plan it became** (2026-08-01; **✅ COMPLETE — the whole R1 → R4 schedule executed, archived 2026-08-05**): the PO **feel pass** (the premise works — costs read as a decision; 11 items) plus a **technical review** of the cost system (9 findings, 2 fixed in session). Triaged into **R1** what a cost *says* (`b2116ba3`) → **R2** what "landed" *means* (`47074d63`) → **R3** one beat, one price (`f4ab5bc9`) + the follow-up `194036c8`, then **R4** the downtime design session → `plan-downtime.md`, both of whose chunks shipped and were PO-verified. ⭐ Read it for the eight rulings in §5 (pay for work done · pay to ignite · keep the 1-HP floor and make it visible · unify at the damage beat · First Aid free · **the resource is named Focus**, with "aura" left to mean the field around you) and for **§3.10 — why the order was load-bearing**: throughput-neutral re-pricing is **3× at a level-1 pool**, so R2 before R1 would have shipped a silent difficulty spike. Its sharpest finding is that **"landed" was three different rules**, so a shield aura charged a proximity tax that at cap outran tapered regen — and the re-check against HEAD **corrected §3.2**: three costed effects author `targetsSelf`, so they charged full price with *nobody nearby at all*
- **plan-downtime.md** — **R4: downtime agency + a baseline Recall** (designed 2026-08-03, **both chunks built and PO-verified the same day — C1 `ec389164` deployed + PO-verified · C2 `3910e536` + a PO hand pass; complete, archived 2026-08-05**). A new **baseline-utility** class: Recall and a placeable **mini-campfire**, both always-present HUD buttons outside the cooldown slots and outside the spellbook, both held from level 1. Recall free and cooldown-less (the 10 s interruptible cast is the only brake; `recall.json` retired, id 28 burned). The camp: 5 s channel → a 15 s fire healing everyone standing in it + a dim light deliberately narrower than Lantern's, one per player, replace-on-place, charge-fed with cap `1 + ⌊level/7⌋`, refilled by the same dwell that rebinds your spawn — **purely per-session, the database untouched, which is how it answers backlog §32 without a migration**. 9 PO rulings, 10 landmines; ledgers in §9. ⭐ Read it for **D5's premise being wrong in both halves** and the survey catching it before the first edit (there is no owner→summon index anywhere — one-totem-at-a-time is a *content convention* — and `OwnedEntities`/`doFuneral` is a vestige with no writers); for **replacement being `SetTTLTicks(1)`, not `RemoveEntity`** (removing an entity from inside `fireUtility` splices `SkillSystem.entities` while `Update` ranges it); for **a def referenced only from Go being invisible to boot validation**; and for the PO retune's real lesson — **shrinking the sprite alone was the wrong half of the ask**, the heal radius had to follow it or the camp looked half-size while healing over a full footprint. ⚑ Two harness traps worth carrying: `.width` on a PIXI mob container measures the **aura ring**, not the sprite, and a harness that needs editing when a value is retuned has stopped being evidence (its charge assertions were rewritten to be relative). Every number is [PLACEHOLDER]

- **plan-zone-editor-structure.md** - **COMPLETE 2026-08-16: C1 `327f3828` + C2 `82ccbd4d` + C3 `e9a0894c`, designed 2026-08-12** - the spawn editor's five unlike things (wolf, farmer, ascension stone, barricade, proving-grounds Dodo) got a **derived** category (`kindOf` off `interaction` + `role`, no content field) read by the picker's `<optgroup>`s, per-bucket marker colour and capability-gated controls; C2 made the ascension stones editable (respawn tri-state - a talker authors NO respawn keys, and the 17 respawn-free `world.json` spawns were exactly the 17 interaction carriers); C3 then **retired the legacy roster outright** - the 10 `legacy: true` defs, `proving-grounds.json` (395 spawns), 3 factions, 5 mob skills, the smoke encounter and 7 `EntityType` wire values (the plan's one wire change; survivors keep their explicit numbers). ⭐ Durable traps: the export is a field WHITELIST (L1/L7), `String(undefined)` blanks a number input and surfaces as a misleading refusal at Update (L2), `require.context` is untestable under vitest so every pure rule lives in `ZoneModel.ts` (L3), capability never the bucket (Wanderer talks AND walks, L4), and a shared test-fixture file (`smoke_test.go`) can make one deletion break a sibling's compilation
- **plan-mob-levels.md** — **✅ COMPLETE: C1 `975e5c4c` + C2 `f1d6eebc` + C3 `0c6eca22` (2026-08-05), and its CONTENT half discharged by `plan-world-replacement.md` C2 the next day** — backlog §38 closed. A zone spawn point may author an absolute `level`; the mob stands at it, with HP, damage and kill-XP all following because all three derive live from `Mob.Level()` (`owner ?? spawnLevel ?? curveLevel`). ⚑ **Its three landmines each cost a chunk to learn**: **L3** — C1 must not reach a live zone alone, because the nameplate and difficulty tint still read the species *catalog* until C2 puts the effective level on the wire, so an override silently lies; **L7** — the zone editor's `getZoneAsJSON` is a field WHITELIST, so hand-authoring `level` was unsafe until C3 named it there and a save silently deleted the override; and C3's most load-bearing test was the *negative* one — **a blank field must export NO key**, without which one edited spawn becomes a 485-line diff freezing every inherited level into a copy. ⚑ Two views of one value with two refresh disciplines: plate **text** is written once per species (`setMobId` early-returns) while the **tint** recomputes per frame, so a `setLevel` that only stores the number leaves the text catalog-fed forever *and the tint correct* — a half-fix that looks right in-game
- **plan-world-replacement.md** — **the world re-placement pass** (roadmap step 2 of the XP chain; designed + built 2026-08-06 in three chunks — C0 `7055aad4` the honest plate · C1 `3df461a8` the decisions + archetype rule · C2 the re-placement — and **closed the same day by the PO in-game walk: "feels very good, I like it"**, the seven reshaped speeds accepted provisionally). All **423 combat spawns** of `world.json` carry a decided `level`, **27 stand under a different species**; `scripts/world-place.py --check` holds the authored table, `scripts/world-regions.py` the total-partition region map (11 rectangles, 423/423). ⭐ Read it for the finding that reframes the whole pass (§3.10): **the rung is nearly cosmetic next to the species** — a 5-rung band is 1.57× wide while the roster's HPx spans 0.64→8.18, a 9× range, so *which species stands in a region* sets difficulty by an order of magnitude more than which rung it takes — and for **D6, the archetype rule** (the Wolf is the unit; above 1.5× its HP, pay with speed ≤0.8× or damage ≤0.8×; **7 species reshaped, GiantSpider exempted with cause**, two rival formulations measured and rejected). Its durable traps: **an assert that cries wolf on correct content gets deleted, not fixed** (a patroller is a POLYLINE, not a disc; region claims don't survive a viewport that spans a seam); a plate carries the **display name, with spaces**; and a world-wide content pass **breaks harnesses that encode "the world doesn't do that yet"** — three of them, one invisible until the webpack-bundled zone JSON was rebuilt. Hands forward to `xp C2`: the high half at **1.8–2.1× a standard at-level fight** (low half 0.7–1.0×, part bought by **D12**'s HP-heavy predator family) · **no battery measures mob speed** (the walk was the instrument) · levels 21–30 empty by **D5** · later per-species tuning of speed/xpFactor is cheap JSON, HP/damage re-prices placements (**L6**)
- **plan-generic-kill-quests.md** — **nine generic single-species kill quests, one per quest-poor NPC (planned AND both chunks built+verified 2026-08-07, one day)**: targets **measured within ~22u of each giver** against the re-placed world, so proximity bought level-appropriateness for free — 10 of 14 placed conversants now offer a quest. 4 PO rulings D1–D4 (all ten givers minus the Dog · one-shot · XP-only at the L9 half-level rule ½×300×1.2^(L−1)). C1 zone-1 side (Farmer boars 180 XP — the first two-offer giver · Lamplighter dire wolves 370 · Wanderer kobolds 450, the giver actually beside the nest · Miner spiders 930) + C2 zone-2 side (Shaman dire wolves 930 · Emberkeeper bandits 1250, **the first human-target kill quest, not yet PO-seen in play** · VillageHealer alpha wolves 1900 · CityGuard bears 2300 · FrontCaptain elite orcs 4800). Content-only, schema impact NONE. ⭐ Its two harness lessons: `E` resolves to the NEAREST interactable and three of five world fires stand within ~1u of a conversant (flight C3's E-at-the-fire steals the talk); and **Escape closes the JOURNAL, not just the map** — the §9 remedy that said otherwise is superseded by §10's L2
- **plan-cc-and-retaliation.md** — **CC immunity, a retaliate passive, and the game's first hard stun** (designed 2026-08-07 from one PO ask; all three chunks built 2026-08-07/08 and committed `9da1e0d4`, complete). C1 the immunity gate — an authored `factors.ccImmune`, **required at tier ≥ elite by a boot error**, gating every CC door on `Mob` · C2 **FrostShield**, the first passive in the game with a *runtime trigger* rather than an equip-time scalar fold (Troll drop, slows anything that damages you 10 % → 30 % for 5 s) · C3 **Paralyze**, the first hard stun (GiantSpider drop, holds the nearest mob 3 s). 11 PO rulings D1–D11. **Schema impact NONE at every layer.** ⭐ Read it for **two rulings that overturned the plan's own recommendations, both found by pricing them against the code rather than arguing**: **D6** — "ride the existing `slowPayload`" looked cheaper than a dedicated stun payload, but `appliedBit()` is compile-enforced and cast suppression needs a duration, so *every* option needed a payload and the cheap-looking branch merely added a **second entry with its own timer**; and **D5** — a rising slow fraction was warned against, then measured to sit at or under the Slow aura's own fraction rank for rank. ⭐ And for **the pin that was green and asserted nothing**: the "dots keep burning on a stunned target" test survived the mutation that moves the gate above `tickBuffEvents`, because its fixture carried no `Stunned()` — *a pin asserting something is NOT suppressed must first prove its subject is suppressible.* Its durable traps: a **stale `frontend/dist` is invisible to vitest** (a tooltip case green in unit tests and absent from what the server serves); **`api/skills/` has a `mobs/` subdirectory**, so a flat glob mis-reported the free id and collided at load; and **a wolf at melee parks and stops moving on its own**, which makes "the stunned mob stops moving" unreadable as an in-game observable. Hands forward three watch items, none of them a chunk: a mob stunning a *player* (needs the inert player CC direction), **silent immunity** (no pip lights), and **a stun being indistinguishable from a slow on the wire** — a backlog §39 dependency, the second buff now waiting on that widening after `lifestealPayload`
- **plan-xp-formula.md** — **the kill-XP formula** (the XP chain's bookends; C1 `a03b95ff` 2026-08-05 · C1.5 + C2 2026-08-06, **closed by the PO in-game: "feels much better"**, `a5a7928b`). Kill XP became a computed, level-relative award — anchored to the RECIPIENT's level, per participant, WoW-Classic-shaped: `base(P) × mod(Δ) × tier × xpFactor`, base 20 × 1.2^(P−1) (~15 kills/level), gray distance **WoW's own `5 + ⌊P/10⌋`**, taper truncated by the boundary at ~24–30% (`taperStretch 1.15`), tiers 1/2.5/5. ⭐ Read §12.2 for the finding that dissolved its hardest open question: **D8 ("green must pay meaningfully") existed because the shipped formula had collapsed WoW's TWO distances into one** — vanilla's gray boundary cuts the linear taper while it still pays 20–45%, and restoring that shape (D13) cost Go+conf only, no wire, no client. ⭐ §12.3/§12.4 for the same-day reversal pair: the wide band D14 tuned to a *recorded* requirement was **falsified by twenty minutes of real play** (D18) — and the PO's in-game boundary ("at 12, not even a level-6 mob") matched WoW's gray level to the digit. Durable traps: a taper knob and a boundary knob are ONE decision (wide+rich measurably inverted the economy — risk-free deep farming out-earned at-level play); a fallback is only safe on a field whose absence differs from its zero (L6's four legacy JSON names); a whole-object conf guard cannot protect independently-meaningful fields. D16 retired the Session-⑥ kite rule as applied content; §11's roster table and §12.1's width measurements stay as the evidence record
- **plan-flight-paths.md** — **campfire-to-campfire fast travel** (part 2, closes the other half of backlog §41 with **option 2**, flight paths over a campfire teleport network; designed 2026-08-04, **fully shipped 2026-08-05**: C1 inside world-map C2 · `bc01a45c` C2 · `bcfb4faf` C3 · `fc000765` C4 · **C5 cut**). Dwell at a fire to discover it, press `E` there to open the map, two presses to fly — straight line at 2.8× walk, zoomed out, aura off, abilities locked. ⭐ Read it for **three rulings that each made the work smaller by removing a case rather than deferring it**: **D13** — takeoff removes the flyer's shapes from `phy.Space`, so non-interaction and snapshot invisibility are structural via §54's two invariants instead of a per-system suppression checklist whose completeness is unprovable; the **C3 feel pass** — moving the trigger to `E` at the fire made an `M`-opened map read-only, which *deleted* the not-at-a-fire refusal instead of giving it a voice; and **D17** — C5 was cut outright once the pass showed flight read well without it (**YAGNI applied to a plan document**). ⭐ **D16 is the one to read for judgement**: the plan assumed world-visibility and map-visibility were one fact and specified a roster filter from §2 onward; they are **two** facts — the world asks *can this reach me*, the map asks *where is everyone* — so a flyer is unseeable on the ground and still a dot crossing the map. C4 became a ruling, a pin test, and eleven doc corrections. **"Two channels carry the same fact" is a claim to verify, not to assume.** Its durable traps: a **sentinel compared against a value that is also the sentinel** (the `E` badge shipped suppressed at every lonely fire and the harness went green, because a conversant stands within ~1.5 units of four of the five fires); a **constant set once from a rate that later gains a faster mode** (the camera's `movementSpeed × 2`); and **queued-but-not-yet-consumed state surviving a cancel verb** (`CancelCast` left the press queues). Hands forward: the **occluded landing dot** (marker sizing) and the speed 2.8× / viewport 1.2× [PLACEHOLDER]s
- **plan-world-map.md** — **the world map & minimap rework** (fast travel part 1, closes half of backlog §41; designed and built 2026-08-04 in three chunks: `f09d99d0` C1 + `6c0888ff` C2 + `106585c4` C3; **complete, live, mobile-verified 2026-08-04**). Today's minimap became the **docked state of one map module**, with a full-screen state on `M` / a HUD button / a minimap tap; then discovered campfires with per-character persistence; then a ~1 Hz `PlayerRoster` message putting every online player on both states. ⭐ Read it for the three surveys that were wrong and what each cost: **§4.2's "no fog of war"** and **§8's "no schema change"** were both reversed by PO rulings mid-plan (the second pulled `plan-flight-paths.md` C1 wholesale into C2, so part 2 now restarts at *its* C2), and **§2's "other characters already appear on the minimap"** was simply false — `visibleOnMinimap` is `false` in `Character`'s constructor and only the local `Player` flips it true, which is what made C3 *smaller* than billed and dissolved its own landmine 6. Its durable traps: **`Welcome` cannot carry per-character data** (sent on connect, before Join, from one shared message), a marker layer must be inserted **by index** and sits **between** the icon layers, and **the map and the world have deliberately OPPOSITE draw orders** — a map marker ranks by information value, a world sprite by physical sense. ⚑ Twice a bug shipped to the PO while **every harness leg passed**, which is why depth is now asserted as stage indices; and a harness that assumed it was alone on the dev server went red because the PO was playing on it. Hands forward: the marker sizes/colour [PLACEHOLDER], and your own dot being invisible while you stand at your bound fire

- **plan-playtest-feedback.md** - **⛔ CLOSED + archived 2026-08-24 (collection-doc restructuring)** - the rolling playtest-feedback collection, rounds 2-10, retired as intake in favour of `docs/feedback.md`. Its open items were redistributed at closing: auto-walk → backlog §59 · round-9 UI items → `plan-ui-pass.md` · Pass 2 + CallForAid combos → backlog §40 · environmental hints → backlog §60 · three small items → `feedback.md` rows · the July feel-checks DISCHARGED by PO ruling (a month of play is the feel pass). *(historical: chunk P presence-XP `d45ba07c`, Swift movement `a29fe986`, rounds 3-6 + filler batches all shipped from here; ledgers inside)*
- **plan-intermission-triage.md** - **⛔ CLOSED + archived 2026-08-24 (collection-doc restructuring)** - the PO's 22 post-C7 items, now ALL accounted for (the closing banner adjudicates the stale claims: items 7+15 shipped `e8b67289`, item 10 became ascension). Residue redistributed: the `null.split` repro → backlog §29 · readability residue → `plan-ui-pass.md` · wildfire feel pass DISCHARGED · day/night deactivation already a CLAUDE.md standing lock. *(historical: sessions ①② `2c155a68`/`dad7c42d`, crit rework v2, night readability `6afbee84`, wolf-line drops `f9a64db5`)*
- **plan-numbers-rewrite.md** - **⛔ CLOSED + archived 2026-08-24 (collection-doc restructuring)** - Pass 1 of the numbers rewrite, complete since 2026-08-03; the one watch item that kept it live (does the free floor stay free under augmentation?) moved to backlog §37. *(historical: C1+C2 `40d9b204`, 16 decisions D1-D16, landmines L1-L16, retunes R1-R4 all PO-verified)*
- **plan-ui-polish.md** - **⛔ CLOSED + archived 2026-08-24 (collection-doc restructuring)** - the step-8b UI-polish slice; chunk 1 (skill tooltips + `GET /skills`) shipped `ae51d8b5` 2026-07-21, and the whole §Deferred checklist + tooltip debt moved to `plan-ui-pass.md`. Its chunk-1 ledger stays the record of the ruled tooltip conventions. *(historical: PO-verified ×2 same day)*
- **plan-immune-feedback.md** - **the floating "Immune" label, COMPLETE 2026-08-24 in one chunk** (`9fb3859d`; planned 2026-08-18; PO in-game pass same day, "tested works"): a hit that lands and is fully mitigated (scripted invulnerability, or resistances multiplied out to zero) stamps a per-tick `immune_hit` one-shot at the two `takeDamage` choke points and the client prints a grey "Immune" where the damage number would have been - only when no damage landed that tick (D9). A gate-key miss stays silent (D1: "not a valid target" is not "immune"), god writes nothing (D6), and - the gap the plan missed - a 0-HP hit is a no-op, not immunity, so both stamps gate on `damage.HP > 0`. **DB schema NONE, wire YES** (one appended bool on Mob + Character, both binding regens). The harness lesson: the Rockfall the plan named sits in the Dark Tunnel, where darkness suppression swallows the label - `immune-feedback.mjs` runs on the outdoor Bramble wall instead. Cadence accepted as measured (one label per ~1.2 s at aura beat); the CC-immunity follow-up stays a CLAUDE.md watch item
- **plan-portal-spells.md** - **the group-logistics pair, COMPLETE 2026-08-18, both chunks in one day** (C1 `564f62c8` · C2 `a0bbc80e`; PO in-game check passed the same day): **Open Portal** (wind-up cast leaves a doorway standing beside the caster for 30 s; E + "Step through." delivers ANY player to the CASTER's bound campfire) and **Pull Through** (the same cast places the doorway AT that campfire and delivers to wherever the caster is standing at step-through). ⭐ PO-ruled shape: wind-up + TTL, never a sustained channel - cast state rides own-player-only wire fields (D29), so nobody but the caster could perceive a channel, and no sustain/cancel hook exists. It was cheap because the cast machinery, the `spawn`/FireTotem TTL path, the memorial stone's inert-conversant template and the Ground+SetPosition teleport recipe all shipped already; the new Go is one **`travel_to` GrantKind** (the four existing kinds could not move a player) with two destination modes, and one **`spawn_at_anchor`** effect type for the remote placement. ⭐ Read §11 for **the anchor-gate pair, which is the general rule**: `spawn`'s `requiresAnchor` is an opt-in because anchor-free content shares that type (FireTotem) and the anchor is only its DESTINATION, while `spawn_at_anchor` carries the gate implicitly because the anchor IS the placement - the key is off its allowlist so even `false` hard-fails at boot. ⚑ **D8, the landmine that shaped the content**: the client synthesizes a campfire interact offer that OVERRIDES the server's, so the portal lands on a 2.5 u ring outside every bind circle or it loses every E press to the flight map. Also carries C1's escaped-defect finding (`backend/pkg/api/` is gitignored, so embedded-content tests are honest only after `cp-defs`) and the harness lessons a portal-shaped script needs: `IsGod` short-circuits the pricing site, E presses get swallowed by the throttled rAF edge trigger, and a leg block that outlives the 30 s TTL reads exactly like a broken refusal. **Schema NONE at every layer.** Left open, and moved to `CLAUDE.md`: §10 item 13, what the pair should cost, plus their unlock placement (both are cheat-only)
- **plan-effect-types.md** - **new effect types round 1; COMPLETE 2026-08-17, all four chunks in two days** (C1 `e2261f6f` · C2 `6d79c40c` · C3 `1846dfe6` · C4 `dfdd7281`, off the ability matrix's §7 hole list, cheapest first): **C1 vulnerability** (pure `resist_aura` content, `resistFactor > 1` + `targetsEnemies`; the one real fix was the buff store's strongest pick, lowest-wins and therefore inverted for curses - now furthest-from-unity) · **C2 `retaliate_damage` + Retribution** (the retaliate spine's flat and percentage reflects, both fully ATTRIBUTED by D4 ruling: XP, kill credit, threat, mitigation) · **C3 invulnerability** (generic `instant_resist` + the wildcard resist tag at factor 0; D7's `buffLifetimeMatchesInterval` pricing lever - charged every cycle at base cadence, refresh-free under Haste) · **C4 ally speed** (new `speed_aura` + target flags on `speed_burst`; the caster's exclusion is structural, and the ring took the LAST free `aura_category` bit in the pip's green). **Schema NONE at every layer, all four chunks.** ⭐ Read §9's ledgers for the recurring lesson the plan minted: widening a value's domain breaks every consumer that assumed the old one, and RENDERERS are consumers too (C1's "−-20%" tooltip; C4's speed ring having no color). Recorded-not-built: per-skill point costs (parked with backlog §37); the remaining §7 holes (silence, cleanse, knockback, ground-targeting, conditional triggers, stealth) stay open in the matrix

### Playtest / deploy records

- **plan-playtest1-feedback.md** — first external playtest (2026-07-22) triaged into Passes A/B/C; fully executed (its last open items 1c+1d landed in `2bfee286`). Superseded as the intake point by the live `plan-playtest-feedback.md`; the design themes it spun off (aura differentiation, tutorial/onboarding, quests) are still open and partly picked up there
- **plan-phase0-deploy.md** — Phase-0 deploy runbook, **never executed as written**; superseded by the live `plan-playtest-deploy.md`. Kept for the runbook detail

### Pre-decision research & captures

- **plan-test-world.md** - **⛔ DROPPED 2026-08-22, never executed**, superseded by `plan-release-map.md`: the big opt-in test map (`api/zones/testworld.json`) whose job was to show every feature at once. Kept for its §1, the only survey anyone has done of what bounds a map - **F1** one zone / one `phy.Space` / no transitions · **F2** mob level per species (⛑ since moved: a spawn may author its own `level`, so the 13-17 rung hole is no longer a cliff) · **F3** every mob ticks regardless of players, so size is capped by mob count, with `world.json` measured at 46.8 spawns / 74.9 props / 51.8 textures per 1000 sq units · **F4** the Warlord encounter hardcoded to `zone.ID == "world"`. ⛔ Its D1-D8 do **not** transfer; §3.1-§3.2 are inputs to the release map, not decisions
- **archive-session-log.md** — verbatim snapshot of CLAUDE.md's old migration-status changelog chain (pre-2026-07-12 history)
- **archive-content-zone1-capture.md** — the 2026-07-09 content capture (was `plan-content-zone1.md`), absorbed into the `content-*.md` set; keeps two resolved-conflict rationales (turnips=harvest-mobs, peasant onboarding)
- **archive-scripting-audit.md** + **archive-scripting-options.md** — data-vs-Go audit and scripting/expression-layer options (decided → `plan-effect-foundations.md`)
- **archive-combo-questions.md** — resolved Phase-9 combo question catalog (rationale record)
- **archive-combat-pacing-recovery.md** — combat pacing/recovery research + decision banner; every decision it carried was executed as step 3. *Was `research-combat-pacing-recovery.md` until 2026-07-21*
