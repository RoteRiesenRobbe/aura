# Status history — archived CLAUDE.md `## Status` entries

Verbatim copies of the `## Status` bullets that used to live in `CLAUDE.md`,
moved here 2026-08-03 when the section was collapsed to its cap (last completed
+ at most 2 prior entries — see the `chunk-wrap` skill). **These are historical
snapshots as written at the time**: "open"/"next"/"pending" claims in them may
since have been closed. The authoritative full ledgers remain the plan-doc
§-banners each bullet points at; the authoritative current state is
`CLAUDE.md ## Status`.

Entries are in the order they appeared (newest first, as of 2026-08-03).

*(2026-08-06: `xp-formula` **C1.5** took the cap slot; the `WORLD RE-PLACEMENT C0` bullet below moved here verbatim.)*

- **Prior: WORLD RE-PLACEMENT C0 — THE HONEST PLATE** ✅ `7055aad4` 2026-08-06, headless-verified — **the client's frozen copy of the gray rule is deleted, so a nameplate's colour is now a FUNCTION of what the kill pays.** `Welcome` carries `gray_base`/`gray_step` (appended at slots 6–7, both binding sets regenerated together) and the boundary is derived. Measured in one world, one run, at player level 18 (`ZD(18)=8`): the isolated **Marauder (cL12) plates GREEN and pays 222 XP** while the **cL2 Boar beside it plates gray and pays 0** — the deleted −5 rule grayed **both**. ⛑ **The tempting wiring is the wrong one and a codec round-trip cannot tell them apart**: `core.Config(config)` is already threaded in, but `curve.Normalized` falls back **per field**, so a conf omitting the block (the live server's) pays 5/6 while the raw block reads **0/0** — shipping raw hands the client `ZD=0` and grays every mob below it. The wire reads `mob.KillXPConfig()`, and the discriminating pin had to start at `g.welcomeMsg`. *When a value has a normalizing accessor and a raw source, a test downstream of the choice cannot see the choice.* ⛑ **That pin also holds the BOOT ORDERING** — the Welcome is marshalled **once**, at construction, so this is only correct because `SetKillXP` runs before `NewGameWith`; a reorder would ship defaults to every client with the boot log still printing the right numbers. ⛑ **The vitest table found a real off-by-one at `ZD=0`**: `Δ <= -ZD` reads `Δ <= 0` there and grays the *at-level* mob, because the server only consults the gray distance on its below-you branch. Found because the oracle is an independent mirror of `curve.Modifier` asserting the **biconditional**, not a restatement of the client's arithmetic. ⛑ **Gray branches FIRST, not as green's lower edge** — `grayBase` is a conf-only knob, and a narrow band pushes the boundary up into "even", where a green lower edge leaves **yellow** plates paying nothing. ⛑ **No client-side fallback pair**: it would re-create the copy being deleted, and the pre-Welcome window is structurally empty. ⛑ **The harness derives its expectations from `backend/conf.json`** — a hardcoded table would be a **third** frozen copy; an early draft hardcoded "the Marauder pays" and went red the moment the band narrowed, *a frozen expectation about the gray rule inside the script written to prove the client no longer holds one*. ⚑ **The strongest evidence is the conf-only second run**: `grayBase: 2` + restart, **no rebuild** → the same Marauder plates gray and pays 0. A boundary that tracks conf cannot be a constant. ⚑ `backend/conf.json` is **gitignored** — `git checkout` will not restore it. ⚑ **Nothing was tuned**: D8's A-vs-B stays with `xp C2`. Verified: `tsc` clean · **vitest 235/235** (8 new, the ZD-0 case **proven RED first**) · `go build`/`go vet` clean · full Go suite **53 packages, 0 FAIL** · `db-test` green uncached vs `aura_test` · boot `-content ../api` 15 factions/87 skills/65 mobs/3 milestones/10 recipes/4 quests/5 props/777 props/485 spawns/5 campfires, **0 panics** · **`hygiene-wire-prune` clean** (637 sprites — the mandatory `.fbs` gate) · **`c0-honest-plate` 6/8 + `c0-narrow` 6/8, 0 FAIL, 0 console errors** (NEW, registered in the coverage map) · **`npc-portraits` 4/4 plate-less**. ⚑ **Two pre-existing reds, both proven against HEAD**: `sys.TestDwell_TakeoffDropsAnInProgressCount` is **nondeterministic** (clean HEAD 4/5 fail, this tree 1/5 — unowned, new), and **`c2-mob-level` scores 4/7 with or without C0** — its CONTROL Stag (spawn 172, still authored) is not alive at run time; **venue rot in that script**. **Schema impact: DB NONE · FlatBuffers YES** (two appended scalars) **· content JSON NONE.** Full ledger: `docs/plan-world-replacement.md` §12 C0.

---

*(2026-08-06: world-replacement **C1** took the cap slot; the `MOB LEVELS C2` bullet below moved here verbatim.)*

- **Prior: MOB LEVELS C2 — THE NAMEPLATE STOPS READING THE CATALOG** ✅ `f1d6eebc` 2026-08-05, headless-verified — **an overridden mob's plate now tells the truth, which discharges L3: a per-spawn level may reach a live zone.** Measured at the real surface: a Stag placed at `level: 25` plates **"Stag 25" in red** beside an untouched Stag of the *same species* at **"Stag 1" in yellow** — one world, one run, two levels. `Mob.level:ushort` appended at slot **24** (nothing renumbered, both binding sets regenerated together), `Level()` added to `model.MobEntity` exactly as C1 predicted, and `codec/mob.go` encodes the **effective** level. ⛑ **The plate text is written ONCE and the tint is not, so the obvious `setLevel` ships a half-fix that looks 50 % correct**: `setMobId` early-returns on an unchanged id and stamps the text, while the level arrives on a *later field of the same snapshot* — a setter that merely stored the number would leave every plate catalog-fed **forever** and the tint still *right*, because the tint recomputes per frame off a cached difference. *General shape: when two derived views of one value refresh on different disciplines, the lazy one hides the staleness and the eager one disguises it.* Both now route through one `effectiveLevel()`, and the harness asserts them **separately**. ⛑ **The snapshot field is `mobLevel`, deliberately not the existing character-only `level` slot** — reusing it would make `isDefined(entity.level)` newly true for every mob and silently widen what the character path sees (§35 in the making). ⛑ **Encoding the EFFECTIVE level buys the summon case free**: an owned mob with `SetSpawnLevel(25)` goes on the wire at its *owner's* 12; the raw override would have plated an unoverridden mob **"Stag 0"**. ⛑ **§9's wire-0 → catalog fallback needed no test — it is the first-frame path every mob traverses**, so every green plate entered through it. ⛑ **The verify probe needs REVERT-BEFORE-REBUILD, not just revert**: `cp-defs` would bake a throwaway zone `level` into the binary; verified it never fired. ⛑ **§8.2's gray-tint seam went hypothetical → live** — client and server finally read the *same* number, so all that is left is `DIFFICULTY_BANDS` (−5) vs `ZD(P)=5+⌊P/6⌋`, diverging above player level ~6. Owned by nobody. Verified: full Go suite **53 packages, 0 FAIL** (incl. `db-test` vs real Postgres) · `go build`/`go vet` clean · **3 new codec tests, all proven RED first** · `tsc` clean · vitest **225/225** · boot `-content ../api` 15 factions/87 skills/65 mobs/3 milestones/10 recipes/4 quests/5 props/485 spawns/5 campfires, **0 panics** · **`hygiene-wire-prune` clean** (645 sprites decoded — the mandatory `.fbs` gate) · **`c2-mob-level` 7/7** (NEW, registered in the coverage map) · **`npc-portraits` 4/4 plate-less** · **`chunk2-follower` 5/5 + 1 SKIP**, identical to C1's. **Schema impact: DB NONE · FlatBuffers YES** (one appended field) **· content JSON NONE** — the probe was reverted, C3 still owns the first real placements. Full ledger: `docs/plan-mob-levels.md` §11 C2.

---

*(2026-08-06: world-replacement **C0** took the cap slot; the `MOB LEVELS C1` bullet below moved here verbatim.)*

- **Prior: MOB LEVELS C1 — THE PER-SPAWN LEVEL OVERRIDE, SERVER-SIDE** ✅ `975e5c4c` 2026-08-05, headless-verified (no in-game pass is *possible* yet — L3: the nameplate still reads the species catalog until C2, so no overridden spawn may reach a live zone) — **a zone spawn point may now author `level`, and the mob standing there stands at it.** Pool, damage and kill XP all follow with **zero seam code**, exactly as §3 predicted: `MaxHealth()`, `PowerScale()`, `casterPowerScale` and the XP award already read the live `Mob.Level()`, whose precedence became `owner ?? spawnLevel ?? curveLevel`. Three production files (`world/zone.go`, `model/mob/mob.go`, `sys/mob.go`), ~11 new Go tests. ⛑ **This unblocks the sibling plan's C2**: it is the structural fix for the cL13–17 hole that made XP calibration a tune-against-noise exercise. ⛑ **L2's premise was VERIFIED, not inherited — and the cheap direction is worth recording too.** The plan argued owner and spawnLevel never coexist because summons are skill-spawned; but **charm targets world mobs, which are exactly the point-spawned ones**, so the claim was worth one grep. Charm binds `charmer`, deliberately never `owner`, and both `SetOwner` sites are skill spawns — so a charmed overridden mob keeps the *placement's* level. *C4's "two channels carry the same fact" lesson, applied before writing rather than after.* ⛑ **The `0` sentinel is a PINNED PAIR, not a coincidence**: `spawnLevel == 0` means "none" only because the loader rejects `level: 0` — both halves are tests, and `Level()` guards on `> 0` so a mob that never met the loader falls through instead of returning nonsense (the flight-C3 sentinel class, headed off at authoring time). ⛑ **The wiring line needed its own end-to-end test and my first two did not provide it** — both constructed `world.Spawn` directly and never reached `parseZone`, leaving the authored-JSON→mob chain covered as two disjoint halves; the seam test was **proven red** by deleting `level: s.Level`. *The silent-wiring class, which starts at the authored text or not at all.* ⛑ **§3.4 contains a promise the code cannot keep**: "the PO can author `level` by hand from C1 on" is **false through the editor** — `ZoneModel.toJSON` is a field whitelist, so opening and saving such a zone **silently deletes** the override. Harmless until C2 (L3), **C3 owns the fix**. ⛑ Every pin proven RED first (the L1 fill dropped, the precedence swapped, the carry deleted). ⛑ **C2's first edit is known**: `Level()` is not on `model.MobEntity`, and `codec/mob.go` encodes against the interface. Verified: full Go suite **53 packages, 0 FAIL** · `go build`/`go vet` clean · boot `-content ../api` 15 factions/87 skills/65 mobs/10 recipes/4 quests/5 props/485 spawns/5 campfires, **0 panics** · **`chunk2-follower` 5/5 + 1 SKIP, 0 console errors, 0 WebGL losses** — the one harness row this chunk owns ("owner/**level** plumbing"); the SKIP is the script's own documented tri-state (the companion dies at its deliberately hot venue), not a regression. **Schema impact: DB NONE · FlatBuffers NONE** (the plan banner's "FlatBuffers YES" is C2's field) · zone JSON may carry optional `spawn.level`, absent = byte-for-byte today. Full ledger: `docs/plan-mob-levels.md` §11 C1.

---

*(2026-08-05: mob-levels **C3** took the cap slot; the `XP FORMULA C1` bullet below moved here verbatim.)*

- **Prior: XP FORMULA C1 — KILL XP BECOMES LEVEL-RELATIVE** ✅ `a03b95ff` 2026-08-05, headless-verified (the two-ended in-game pass — gray farming at a cheated high level, tagging along at an endgame kill — is C2's, per §6) — **a mob kill is no longer worth one number to everybody.** The award is computed **per participant at that participant's own level** inside the existing fan-out (`base(P) × mod(Δ) × tier × xpFactor`), which closes both exploit ends at once: a carried level-3 is bounded to their own at-level pay × the +20 % up-cap × the tier, and a level-30 farming level-1 mobs earns **exactly zero**. ⚑ **`factors.experience` is GONE from all 65 defs** — replaced by the relative `factors.xpFactor` (absent → 1, `0` = pays nothing, Turnip 0.05 per **D4**), and the authored kph-calibrated numbers stop being a balance input. This is an economy retune, not a patch. ⚑ **L2 struck a THIRD time, at the conf seam — and my first fix scored the hazard as a PASS.** `SetKillXP` guarded the block as a whole (`Base > 0 && Growth > 0`), so a calibration pass writing the two knobs it is calibrating (`{base, growth}`) would have installed `grayStep 0` (**everything below your level pays nothing**) and `tierElite/tierBoss 0` (**every elite and boss pays nothing**) — silently, because the boot log printed the two fields that *were* set. Now normalized **per field**, with the resolved gray + tier values in the log. *General shape: a whole-object "is it configured" guard cannot protect a struct whose fields are independently meaningful.* ⚑ **The rename needed a POINTER tombstone even though the loader already rejects unknown keys**: 29 of 65 defs authored `"experience": 0`, which parses perfectly against a plain `uint32` and keeps meaning something. *A rename is only loud where the old value differed from the zero value.* ⚑ **L1 was PROVEN, not argued** — the `name → combatTarget` map of all 65 defs was captured **before** the sweep and diffed after: **65/65 identical** (36 prey, 29 not), which is order-sensitive and gone if you migrate first. Verified: full Go suite + `go build`/`go vet` · **~30 new Go tests** (§3.2's three worked examples transcribed, the per-participant split, the L3 participation pins, the tombstone, the tier census) · all three repo confs parsed and asserted **equal to the Go defaults** · vitest 225/225 + `tsc` clean · boot 0 panics · **`chunkP-presence` 6/6, 0 console errors — its measured award `0 → 42` IS the formula** (base(1) 40 × 1.05, a level-1 killing a level-2 wolf) · **`npc-portraits` 4/4 plate-less with 8/7/7/2 mob plates as the control**. **Schema impact: NONE.** Full ledger: `docs/plan-xp-formula.md` §10 C1.

---

*(2026-08-05: mob-levels **C2** took the cap slot; the `FLIGHT PATHS C4` bullet below moved here verbatim.)*

- **Prior: FLIGHT PATHS C4 — THE MAP KEEPS THE FLYER** ✅ `fc000765` 2026-08-05, **PO-verified in-game same day** (two clients, two browser profiles) — **the chunk INVERTED before a line of it was written.** Its whole scope was one line (`if p.Flying() { continue }` in `codec.RosterFor`), and the PO's answer to the pre-coding overview was that it must not exist. ⭐ **D16: the world and the map are DIFFERENT facts** — a flyer is unreachable and unseeable on the ground (D13, structural) **and stays a dot crossing the map**, because fires and the routes between them are what the map is *for*; it also becomes the only way to know someone is inbound. So **zero product code changed**: what shipped is the ruling, a pin test that fails if anyone adds the filter, and the correction of **eleven sites** that instructed them to — across all four layers of the status model, two of which (`CLAUDE.md` Next, the `verify` coverage map) load every session. ⚑ **The durable lesson: "two channels carry the same fact" is a claim to VERIFY, not to assume.** Landmine 2 enumerated the visibility channels correctly, then concluded they must agree — the world asks *can this reach me*, the map asks *where is everyone*. ⚑ **A negative pin needs a double that ANSWERS the question, not one that panics on it**: `rosterPlayer` embeds the interface, so the filter would have nil-panicked inside flatbuffers rather than naming D16 — and generated `Entries(obj,i)` **does not bounds-check**, so any leg reading an entry by index must assert `EntriesLength()` first. ⚑ **Measured, not assumed: the landed dot is OCCLUDED** (`r=3.5 px` under a `9.0 px` fire marker) — every arrival, for every observer, which is CLAUDE.md's standing own-dot open item reaching a second surface and giving the marker-sizing call a real consequence. Recorded as a diagnostic print, deliberately not a leg. Verified: full Go suite + `go build` · the three D16 pins **proven red** with the filter temporarily restored · **`c3-flight-client` 35/35, 0 console errors** (+3 legs; leg 5 — snapshot invisibility — still green *beside* 5b, which is the pair that matters) · boot 0 panics · no other harness re-run, reasoned: zero product lines changed. **Schema impact: NONE.** Full ledger: `docs/archive/plan-flight-paths.md` §10 C4.

---

*(2026-08-05: mob-levels **C1** took the cap slot; the `FLIGHT PATHS C3` bullet below moved here verbatim.)*

- **Prior: FLIGHT PATHS C3 — THE CLIENT FLIGHT EXPERIENCE** ✅ `bcfb4faf` 2026-08-05, **PO-verified in-game same day** — flight now runs end to end. Approach a discovered fire, **`E` lights over it**, `E` opens the map, two presses fly: the camera zooms out and hard-follows, the ability bar greys out and says why, an ETA counts down, and landing restores all of it. ⚑ **The feel pass reshaped the chunk, it did not just tune it — four more PO rulings:** the trigger moved from a bare map click to **`E` at the campfire**; a map opened with **`M` is read-only**, which **removes** the silent-refusal case instead of reporting it and so **shrinks C5** to the route overlay; a **second `E` closes** the map; and both knobs were cut — speed **4→2.8×**, viewport **2.5→1.75→1.2×**, the latter taking streamed area from ~6.25× to ~1.4× so **flight is no longer the mobile-perf cost §4.3 designed it as**. ⚑ **THE `E` BADGE SHIPPED SUPPRESSED AT EVERY LONELY FIRE AND THE HARNESS WENT GREEN ON IT**: the "hide while my own panel is open" guard read `offered === partnerId()`, and `partnerId()` is **0** when nothing is open — free while both operands moved together, wrong the moment the campfire offer made them diverge (`E` still worked; the key reads the tracked id, the badge the suppressed one). The harness could not see it because **a conversant stands within ~1.5 units of four of the five fires**, so every badge reading was taken beside one — now pinned by a leg scored first, at spawn, at the one clear fire. *General shape: a sentinel compared against a value that is also the sentinel.* ⚑ That same clustering made the first cut **unflyable at spawnpoint-2** (a conversant won the offer), so **the fire wins inside its bind radius** — 0.75 units against the 2.0 talk range, the tighter condition being the unambiguous intent; it is a **content** pattern and will recur. ⚑ **The flyer rendered UNDER props** — mobs are below characters deliberately (`6afbee84`) but `resources`/`bossMobs` are added after them, and altitude has no other cue, so a `flyers` layer now sits above the props and below darkness. ⚑ **The camera could not have kept up and it would have been misdiagnosed as flight speed** (`movementSpeed × 2` fixed in the constructor) — *a constant set once from a rate that later gains a second, faster mode*. ⚑ The ability lock has **four entry points**, so the guard belongs at the shared handlers. Verified: Go suite + `go build` · `tsc` clean · vitest **225/225** · **`c3-flight-client` 32/32, 0 console errors** — carrying **the two-client snapshot-invisibility leg C2 deferred** · `chunk3b-interact` 14/14 (the suppression fix touches NPC talk) · `c2-campfire-markers` 17/17 · `c1-world-map` 12/12 · boot 0 panics. **Schema impact: NONE.** Full ledger: `docs/archive/plan-flight-paths.md` §10 C3.

---

- **Prior: FLIGHT PATHS C2 — THE FLIGHT STATE MACHINE, SERVER-SIDE** ✅ `bc01a45c` 2026-08-05, headless-verified (the in-game pass waits for C3 — no client can send `StartFlight` yet) — `StartFlight = 12` + three appended `Character` fields, validation, the lerp, landing. ⚑ **The mechanism is D13, decided in this session's plan review: flight REMOVES the flyer from the physics space** (body+hand+aura out, viewport stays — the flyer still sees) instead of a per-system suppression checklist — non-interaction is structural via the two §54 invariants (`RemoveShape`'s instantaneous purge + `ForgetDeparted`, both fired at takeoff), and **snapshot invisibility arrives free, shrinking C4 to the roster filter** (⚑ *which C4 then inverted — D16 keeps the flyer on the map and built no filter at all*). The enumerated remainder is the players-list gates: input discarded whole, aura off synchronously, utilities refused, the dwell tracker skips flyers (a slow fly-over would otherwise discover AND rebind), disconnect stashes the destination (D14 — no live-position column exists, so crash semantics are unchanged). ⚑ Finding: **`CancelCast` does not clear the pending press queues** — a same-tick UseUtility+StartFlight would have started a Recall cast mid-air; takeoff clears both queues. `Ground()` is the ONE re-entry (landing + WARP-cancel), so a half-restored flyer is unrepresentable. Verified: Go suite + `db-test` vs real Postgres · **~28 new Go tests** (incl. the 7-case rejection table + real-second-player invisibility) · vitest 207/207 + typecheck · `hygiene-wire-prune` clean (the `.fbs` gate) · `c2-campfire-markers` 17/17 + `campfire-bind-persistence` 6/6 (re-run — the dwell tracker was refactored) · boot 0 panics. **Schema impact: NONE.** Full ledger: `docs/archive/plan-flight-paths.md` §10 C2.

---

*(2026-08-05: flight-paths **C4** took the cap slot; the `WORLD MAP C3` bullet below moved here verbatim. ⚑ Its closing line — a flyer being filtered off the roster — is exactly what C4 then ruled AGAINST (D16): the map keeps the flyer.)*

- **Prior: WORLD MAP C3 — THE PLAYER ROSTER** ✅ `106585c4` 2026-08-04, PO-verified in-game same day — a new **`PlayerRoster = 7`** message carries every live character at **1 Hz**, and other players are dots on both map states. **The map is feature-complete for part 1** (D6's self + fires + players all drawn). ⚑ **The plan's §2 survey was WRONG and it made the chunk smaller**: it recorded that other characters already appear on the minimap, but `Character` sets `visibleOnMinimap = false` and only the local `Player` flips it true — nobody but you has ever been drawn. So the roster is the **only** source, not a second one, and **landmine 6 dissolves** into "skip your own id". Assembled **once**, marshalled **once**, the same bytes to everyone — a design requirement, not an optimisation, because part 2's flyer filter must have one home (the Go test pins the *same backing array*, which an equality assertion would not). ⚑ **PO ruling: the map's draw order is `terrain → props → other players → you → campfires`** — *"the campfire is still the most important information the map can provide"* — which **inverts C2's** fires-below-the-people rule. ⚑ **The world is NOT part of it**: building it to match was tried, bounced back from a PO screenshot within minutes, and reverted to byte-identical (`6afbee84` stands). A map marker ranks by information value; a world sprite by physical sense, and one harness leg now asserts **both** orders so nobody tidies them into agreement. ⚑ Its live consequence, **still open**: you respawn at your bound fire, so your own dot is invisible under that fire's marker until you walk off it. Verified: Go suite + `db-test` vs real Postgres · vitest **207/207** · **`c3-player-roster` 15/15** (run with a real third player online — the PO — after three `dots === 1` legs went red on exactly that) · `c1-world-map` 12/12 · `c2-campfire-markers` 17/17 · `hygiene-wire-prune` clean (the `.fbs` gate) · `filler-batch` back to its 1 recorded pre-existing failure · boot 0 panics. **Schema impact: NONE.** Full ledger: `docs/archive/plan-world-map.md` §10 C3.

---

*(2026-08-05: flight-paths **C3** took the cap slot; the `WORLD MAP C2` bullet below moved here verbatim.)*

- **Prior: WORLD MAP C2 — CAMPFIRE MARKERS, AND DISCOVERY BECOMES PERSISTENT** ✅ `6c0888ff` 2026-08-04, PO-verified in-game **and DEPLOYED LIVE** same day (schema 1 → 2, 14 characters intact; the dump was pulled off-box **and restore-tested**) — discovered fires are drawn on **both** map states, the bound one ringed. ⚑ **It absorbed `plan-flight-paths.md` C1 whole**: the PO ruled mid-chunk that discovery persists **per character**, which was that chunk's entire scope — so this ships migration **`000002_character_campfires`** and **reverses `plan-world-map.md` §8's "no schema change in this plan"**. The reversal cost nothing: the question was asked before any code existed. Discovery joins the *existing* exactly-at-threshold dwell firing as a fourth consequence of one act; the set is **connection state with four seams** (ticket · reconnect stash · death's re-add · disconnect) and a missed seam loses discoveries **silently**, because the next save writes the shrunken set back. Two `GameState` **one-shots** carry it — ⚑ `Welcome` *cannot*, being sent on connect, before Join, from one message shared by every client. Save **inserts** rather than replaces (discovery is monotonic). Also closes backlog **§53**, whose filed fix was insufficient: a bare minimap clear is a one-way loss, so it clears **and rebuilds**. ⚑ **Shipped a bug to the PO and got it back** — markers sat *under* the ~777-icon prop layer, so a fire's visibility depended on how wooded its surroundings were, and **every leg still passed while they were invisible**; the harness gained a stage-depth assertion. Verified: Go suite + `db-test` vs real Postgres · vitest 199/199 · **`c2-campfire-markers` 17/17** · `c1-world-map` 12/12 · `hygiene-wire-prune` clean (the `.fbs` gate) · boot 0 panics. Full ledger: `docs/archive/plan-world-map.md` §10 C2.

*(2026-08-05: flight-paths **C2** took the cap slot; the `backlog §54` bullet below moved here verbatim.)*

- **Prior: backlog §54 — MOBS NO LONGER CHASE PLAYERS WHO HAVE LEFT** ✅ `71735371` 2026-08-04, PO-verified in-game same day — the long-standing "disconnect and the pack parks on the spot you vanished from, ignoring everyone" bug. **Three defects stacked, and the first two fixes each looked complete and changed nothing in-game.** ① no hook severed a mob's references when its target left (`Mob.ForgetEntity`, driven from the removal fan-out that already carried `EndCharm`); ② `MobSystem.Remove` **returned early on the mob branch**, and a player's placed **camp is a mob** that `doFuneral` removes ALIVE — which is what the reported screenshots actually showed; ⭐ ③ the real one: **`Space.RemoveShape` left dangling collision references**, and the tick order makes them readable — `MobSystem` acquires at priority **20**, `PhysicsSystem` rebuilds at **0**, `NetSystem` notices the dead socket at **−100**, so a player removed at the bottom of tick N is still in every sensor's set at the top of tick N+1. Instrumented, the whole failure is **29 ms — one tick**; the clear worked and one stale sensor read undid it, permanently, because a ghost never fails a liveness check. ⚑ **The purge must sweep ALL shapes, not the removed shape's own set** — overlap is recorded per direction, so a sensor records a body while the body records nothing back; the first attempt missed every one-way watcher and still failed in-game. Verified: Go suite vs real Postgres · each new test confirmed to fail without its fix · headless ghost audit **2 confirmed ghosts on the buggy build → clean on the fixed one** · `chunk2-calm` 6/7 + `chunk2-roles` 1/3 **proven identical at HEAD**. **Schema impact: none.** Full ledger: `docs/backlog.md` §54.

- **Prior: WORLD MAP C1 — THE MAP GETS A SECOND STATE** ✅ `f09d99d0` 2026-08-04, PO-verified in-game same day — the minimap is now the **docked state** of one map module; `M` / a HUD button / tapping the minimap open a **full-screen** state over the running world. ⚑ **One Application, reparented, never a second renderer** — `createMinimapIcon()` returns a single ViewContainer and a display object lives in one stage, so a second app would mean changing `IMiniMapRendered`; it also keeps the GL context count at two. Terrain **bakes once** into a single sprite (frame pinned to the world rect, not container bounds). Mid-chunk PO additions: **session fog of war** (reverses §4.2's recorded "no fog"; AOI-sized reveal, no schema change) and click-away dismissal. Overlay is z-index 102 but **stays translucent** — panels showing *through* is fine, panels *on top* was the bug. Found + filed **backlog §53** (⚑ its first diagnosis — "player-relative coords" — was WRONG; corrected `9a054b56`, closed in C2). Verified: vitest 190/190 · `c1-world-map.mjs` 12/12 0 errors · `filler-batch` 1 fail proven identical at HEAD · boot 0 panics. **Still open: the mobile real-device pass.** Full ledger: `docs/plan-world-map.md` §10.

*(2026-08-04, second move of the day: backlog §54 — the disconnect ghost-chase
fix — took the cap slot, so the compressed `backlog §46` bullet dropped off
`CLAUDE.md`. It is **not** re-copied here: its full pre-collapse banner is
already the last entry in the 2026-08-03 batch below, and the compressed line
carried nothing the full one does not.)*

*(2026-08-04, third move of the day: world-map **C2** — campfire markers +
discovery persistence — took the cap slot, so the compressed `R4 C2` bullet
dropped off `CLAUDE.md`. Like `backlog §46` above it is **not** re-copied here:
its full pre-collapse banner is already the first entry in the 2026-08-03 batch
below, and the compressed line carried nothing the full one does not.)*

*(Moved 2026-08-04 when world-map C1 took the cap slot.)*

- **R4 C1 — RECALL BECOMES A BASELINE UTILITY** ✅ `ec389164` 2026-08-03, deployed + PO-verified in-game same day — every character holds a free, cooldown-less **Recall button** from creation (new baseline-utility class outside the cooldown slots and the spellbook; the 10 s interruptible cast is the whole brake). `recall.json` deleted (id 28 burned), `UseUtility = 11` pinned, spellbook restore now warn-skips retired ids. Found pre-existing bug: `#registrationNag` covers the mobile ☰ sheet (still open, needs a PO call). Full ledger: `docs/archive/plan-downtime.md` §9.

- **⭐ Last completed: R4 C2 — THE MINI-CAMPFIRE** ✅ `3910e536` 2026-08-03 — the second and last chunk of `docs/archive/plan-downtime.md` (full ledger there, §9), which **closes the plan's build work**: every character now also holds a **Camp button**, a charge-fed placeable fire. Press → a 5 s channel → an aligned, owned, unkillable structure that **heals everyone standing in it** for 15 s, emits a dim light (r2.0, Torch-band, well under Lantern's 4.0 so it can never substitute for the light aura, D6), protects nothing, cannot be bound to and cannot refill charges. **Charges are purely per-session (D3)** — refilled to a level-derived cap by the same dwell that rebinds the spawn point, spent at channel completion, zeroed by death, carried through an F5 by the reconnect stash and **by nothing else: the database is untouched, which is what answers backlog §32 without a migration**. **Shipped:** `camp.json` (id 65) + `camp-aura.json` (id 138) · `UtilityKind.Camp = 2` · `ActivationRejection.NoCharges = 4` · `camp_charges:ubyte` as `GameState` id 23 — the CAP is deliberately NOT a second wire field, both languages derive it from the level through a new `campChargeCap` block in `api/shared-constants.json`, pinned by both fixture tests · the charge store on `*player` + the refill hook beside the bind · `applyCamp` + a `map[client-UUID]entityID` replace index · the Camp button, its `2/3` counter and its greyed-at-zero state, desktop + mobile (Camp closest to the thumb, Recall a step up, Talk above both — PO 2026-08-03). ⚑ **D5's premise was wrong in BOTH halves** and the survey caught it before the first edit: there is no owner→summon index anywhere (one-totem-at-a-time is a *content convention*), and `OwnedEntities`/`doFuneral` is a vestige with no writers — so the ruling stood but the mechanism was new work. ⭐ **Replacement is `SetTTLTicks(1)`, not `RemoveEntity`** — removing an entity from inside `fireUtility` splices `SkillSystem.entities` while `Update` ranges it, the §27.1 skip-a-survivor class — and the index is keyed by **client UUID** so it survives death and respawn (the `anchors` precedent). ⚑ **A def referenced only from Go is invisible to boot validation**: nothing resolves `camp.json` at boot (no zone spawn, no `spawnMob` effect), so a rename would turn the button into a log line nobody reads — pinned by a content test. ⚑ **`campExpirable` is a structural assert**, the R2/R3 silent-wiring class, so a capability test pins the real `*mob.Mob` against it. ⚑ **Two harness findings:** `DAMAGE` takes a whole PERCENT and writes the pool **directly, through godmode**, while godmode skips `updateVitalSigns` — which turned the heal check from a noisy A/B into a clean **isolation** (**+48 % in camp vs +0 % control**, same 8 s window); and **open ground is prop-free, not MOB-free** — `(-23, 14)` is the documented clean tile for *pacing*, and a level-1 character standing there through the 20 s warp settle **died before the first press**, so GOD is not optional in a script that stands still out there. **Verified:** full Go suite `-count=1` vs real Postgres (⚑ `store`/`accounts` need **`AURA_TEST_DB_URL`** — without it they SKIP silently in milliseconds, which reads exactly like a fast green run) · **two mutation runs, each reddening exactly its own test** · vet/gofmt clean · vitest 155/155 · tsc/prod build · sim battery byte-identical (TTK 6.67 s / TTD 8.70 s) · boot **87 skills/15/65 mobs/10/4/5 props/5 campfires, 0 errors 0 warnings** · new **`r4-camp.mjs` 24/24** · reruns green: `hygiene-wire-prune`, `r4-recall-utility` 12/12, `campfire-bind-persistence` 6/6, `chunk4-persistence` 16/16, `mobile-interact` ALL, `mobile-layout` green **except the pre-existing leg-7 `#registrationNag` bug** C1 found (still open, still needs a PO call). The mobile stack was probed directly — Camp 701–771, Recall 619–689, tiles from 782, right edge, no overlap. ⭐ **PO hand pass the same day produced two changes, both taken.** ① **The cap dropped from `3 + ⌊level/10⌋` to `1 + ⌊level/7⌋`** — 1 charge at level 1, 2 from 7, 3 from 14 — because three at level 1 meant a character never ran out, which makes the walk back to a fire optional and the whole loop invisible. One constant per language plus the fixture; the formula's shape did not move. ⚑ Two Go tests went red and both were right to: the authored-role census, and `TestReconnect_CarriesCampCharges`, which spent the character's ONLY charge and would have asserted a carried **0 — the same value a broken carry produces** (it levels up first now). ② ⭐ **The camp got its own wire `EntityType` (`Camp = 75`) so it could be drawn half-size** (30 px against the campfire's 60): it had been borrowing `Campfire`'s type, so the client could not tell them apart. ⚑ **The cheap alternative was rejected twice over** — sizing the sprite off the wire `radius`, which already differs (0.25 vs 0.3), is only a 17 % difference AND would make ART depend on a PHYSICS value, the "role inferred from an incidental number" defect the actor model's chunk 2 spent itself removing. `Mobs.Camp` deliberately does NOT subclass `Campfire`: a camp can never be bound to, so it must never grow the dwell bind ring. ⚑ The harness's charge assertions were rewritten to be **relative** (parse `n/m`, level to the ceiling, loop until empty) — a cap that has been retuned once will be retuned again, and a harness that needs editing for a tuning change has stopped being evidence. ⭐ **③ The sprite alone was the wrong half of that ask** — shrinking only the art left the camp healing over a full campfire's footprint, i.e. *looking* half-size while not being it, which is worse than either extreme; the heal radius went **1.5 → 0.75** with it (light stays 2.0, deliberately wider than the heal, as the permanent fire's 7.0 is wider than its 1.5). ⚑ **Its floor is the PLACEMENT OFFSET**: `applyCamp` puts the camp 0.8 units from the placer and the query is collider-vs-collider, so 0.75 reaches a 0.25-radius body with 0.2 to spare — below ~0.6 the placer stops being healed by their own camp. `r4-camp.mjs` stayed 24/24 with the heal at +48 %, which is the check that mattered. ⚑ **Two harness traps in the retune:** the step-out-and-back that reproves the refill must WARP ONTO THE FIRE, not onto the recorded home — home is the character's *jittered spawn point*, and rounding that to a whole-unit warp target lands outside the 0.75-unit bind radius, so the refill silently never fires and reads as broken (one red run); and **`.width` on a PIXI mob container measures the AURA RING, not the sprite** — the first size check reported camp and campfire both at 423 px, and only a side-by-side screenshot showed the camp correctly at half size. **Every number is [PLACEHOLDER]** (channel 150 ticks, TTL 450, cap `1 + ⌊level/7⌋`, sprite 30 px), and there is no hotkey (D1's ruling).
- **Prior: backlog §46 — ONE AUTHENTICATOR** ✅ `2be3a3dd` 2026-08-03 — the server could be told who you are **two** ways: the `aura_session` cookie, and the anonymous secret riding **every** request as `X-Aura-Anonymous-Secret`. That forced `resolveCaller` to arbitrate ("the JWT wins"), and that rule is where the 2026-08-02 two-tab logout bug lived. The secret is now spent **once** at **`POST /api/session/anonymous`**, the way a password is. **Shipped:** `resolveCaller` a single cookie branch · character creation calls the new `issueSession`, so a brand-new player leaves already signed in · the login discard is **told** its target instead of re-reading the raw header mid-request (that out-of-band read is what let it soft-delete a live character) · the header left `Access-Control-Allow-Headers`, and `AnonymousSecretHeader` stays as a **tombstone** · `loadbot`/`authbench` carry the cookie by hand — ⚑ `net/http/cookiejar` will not send a `Secure` cookie over plain http even to localhost, where browsers exempt it. ⚑ **The secret still never expires, deliberately** — it is a **recovery** credential, the only key to an unregistered account; what collapses is its *exposure* (two requests in its lifetime instead of every proxy log). ⚑ **Two progress-loss hazards, the first CREATED by this change:** `AccountFlow`'s *"a stored secret is dead by definition"* was true only while the header rode every request, and would have destroyed an anonymous account on every cold boot — the boot path now exchanges **first** and forgets only on an explicit `session_expired`; and pre-existing, `login()` forgot the secret unconditionally while `discardAnonymous` defaults to false, i.e. the server deliberately **kept** the account and the client threw away its only key. ⚑ **The header test was pinning the TEST, not the code** (the harness simply stopped sending it, so restoring the branch reddened nothing) — it now has an explicit stale-client mode. ⚑ **A panicking test kills the whole test binary**, so every test after it never runs, which made one mutation look three times narrower than it was. **Verified:** Go **33/33** vs real Postgres · **four mutations, each reddening exactly its own test** · vitest **163/163** (+9) · tsc/prod build · boot **0 errors 0 warnings** 86/15/64/10/4/777/485 · **`chunk2-accounts` 24/24** (no 401 filter, so the clean console is meaningful) · **`chunk4-persistence` 16/16**. ⚑ **Three box traps, none caused by the change:** the test DB stuck **dirty** with a complete schema (the inverse of the documented trap — a killed run) · the **embedded content was stale** since R4 C1, so 20 content tests failed on `skill "Recall" not found` (`backend/pkg/api/` is gitignored, so it has no history to notice — run `cp-defs`) · ⭐ **a stale `aurad.exe` beside the extensionless `aurad`, which Windows PATHEXT launches instead** — a new variant of the stale-server trap, producing a boot panic against correct code. **§48 (the reconnect token) is next**, and it is really *"there is one join path"*, not *"delete a token"*.
- **Prior: R4 C1 — RECALL BECOMES A BASELINE UTILITY** ✅ `ec389164` 2026-08-03, **deployed live + PO-VERIFIED IN-GAME same day** (follow-up same day: the mobile button moved to the RIGHT edge under where Talk shows up — the left half of a phone is the joystick; Talk stacks one step above it) — the first chunk of `docs/archive/plan-downtime.md` (full ledger there, §9): every character now holds a free, cooldown-less **Recall button** from creation — a new **baseline-utility class** outside the 3 cooldown slots and outside the spellbook (D1), its 10 s damage-and-movement-interruptible cast the entire brake (D7). **Shipped:** `recall.json` DELETED (id 28 burned) + both teach rows removed · `skills.UtilityKind`/`UtilityDef` as Go literals (deliberately not catalog content) · `UseUtility` pinned at **`ClientMessageBody = 11`** with a `UtilityKind` wire **enum**, cross-language pin test · utility casting state on `SkillComponent` (`CastingUtility` + `PendingUtilities`, mirroring the slot vocabulary; `CancelCast` clears both, so every existing cancel site covers utilities by construction) · `cast_utility:ubyte` appended to `GameState`, riding the two existing tick fields · `#utilityBar` + `features/utilities/` (desktop panel between the loadouts; mobile a fixed round button above the tile row on the LEFT — the six-tile row is width-saturated and must not grow). ⚑ **Two plan-doc claims corrected by the survey:** D8's *"the prune cascades the teach rows"* is wrong (`pruneEmptyDestinations` spares authored-empty nodes as lore leaves — the row + `teachings` node had to be deleted together, in content), and Recall never had a *"fallback ladder"* — it is deliberately fail-closed on no anchor and stays so. ⚑ **The chunk-4 retired-skill rule covered the LOADOUT only** — the spellbook restore validated nothing, so ghost id 28 would have shipped on the wire and priced unfundable skill points forever; closed with a registry warn-skip in `sys/persist.go`, proven red first. ⚑ **Deleting a taught skill's JSON is a hard BOOT failure, not a red test** (teach grants resolve by name at load) — file + rows + the 87→86 registry pin must land as one edit. ⚑ **NEW pre-existing BUG found by the harness gate: `#registrationNag` covers the open mobile ☰ sheet** — it sits over the journal button (`elementFromPoint` probe; identical on a HEAD build), so journal-from-sheet is dead on phones; the accounts↔mobile merge class (§49's cousin), needs a PO call on where the nag lives on a phone, `mobile-layout.mjs` leg 7 legitimately red until then. **Verified:** full Go suite `-count=1` vs real Postgres · new component/sys/codec tests proven red first, completion-re-check mutation-tested · vitest 154/154 · tsc/prod build · boot **86 skills** (87−1)/15/64/10/4/777/485/5, 0 errors 0 warnings · new **`r4-recall-utility.mjs` 12/12** (bind → 37 u out → "Recall" cast bar → move-interrupt goes nowhere → completion lands 1.04 u from home → the crier no longer teaches) · `hygiene-wire-prune` clean · `chunk4-persistence` 16/16 · `chunkC4-quests` 37+1 SKIP · `mobile-layout.mjs` repaired in passing (it still joined via the deleted `#startForm`).
- **Prior: bugfix — held keys survive focus loss (round-8 item 3, bug half)** ✅ `c8163ad1` 2026-08-03 — `KeyboardManager` sweeps keys on blur/hidden + drops the queued events; the latent `ResetKey` preventDefault clobber closed with it; `focus-loss-sweep.mjs` 4/4 with a red HEAD control. The auto-walk feature half stays open. Full ledger: `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: bugfix — A SAVE THAT CAN NEVER SUCCEED** ✅ `c368ff1c` 2026-08-02 — **the live server wrote nothing to the database for 37 minutes**, the consecutive-failure counter at 78 unbroken from 1. **Trigger:** `cleanup-loadbots.sql` run against the live DB while the server still held those characters **stashed in the reconnect window** — disconnecting does not make the server forget a character (~10 min in `stashByToken`), so when the TTLs expired together the §2 session-expiry trigger enqueued a final save for each and every one hit a row that no longer existed. ⚑ **The stash window is invisible from the database**, which is what made this systemic rather than a typo: the script documented WHICH rows are safe to delete at length and said nothing about WHEN. ⭐ **Three causes compounded, and only the first is obvious:** ① the re-queue was **unconditional** (`ErrNoCharacter` can never succeed and was retried like a connection blip) · ② `failures` was **ONE GLOBAL COUNTER**, so one unwritable snapshot drove the backoff to its 30 s cap for everyone and pinned `Failing()` true for every live player's banner · ③ ⭐ **the backoff was a sleep in the run loop while `takeLocked` picks in Go's RANDOM MAP ORDER** — with 44 poison entries a healthy save had a **~1-in-45 chance of being attempted per 30 s cycle**, which is **why one deleted row became a total outage instead of 44 skipped writes**, and is the cause that reading only the re-queue line would miss. **Shipped:** ① **`persist.ErrGone`** wrapped by `store.SaveCharacter` (⚑ it lives in `persist` because that package depends on nothing else in aura and `store` already imports it — the arrow points the right way; ⚑ **scoped to the save path**, since the other three `ErrNoCharacter` sites answer HTTP where *terminal* means nothing; ⚑ terminal errors **deliberately do not count toward `Failing()`**) · ② **per-character queue entries** with their own attempts/backoff/give-up clock, and ⚑ **`takeLocked` SKIPS an ineligible entry rather than waiting on it** — that one word is the whole difference (`sync.Cond` → a `wake` channel, since the loop now needs a *timed* wait) · ③ **`abandonAfter` 30 min**, bounding an unclassified failure into one lost character loudly logged · ④ **bounded `writer.Close()` on shutdown** — ⚑ until now called only from tests, leaving the shutdown escape inside the retry ladder **unreachable code**. ⚑ **A newer snapshot supersedes in place and INHERITS the backoff** — staleness, never ordering; resetting the ladder on every forced save would hammer a sick database precisely when it can least answer. ⭐ **The finding worth carrying forward: a mutation that came back GREEN** — removing the `requeueLocked` handover reddened nothing, which showed the spin test pinned `Save`'s branch rather than the handover (whose only reachable window is a snapshot landing *while a failing write is in flight*), and its comment had been claiming a property nothing tested. **A green mutation run is evidence about the TEST, not the code.** **Verified:** Go **full suite** green (33 packages; `store` at 74 s against real Postgres, so the terminal-error assertion ran against a live DB) · **five mutation runs, each reddening exactly its own test** · `-count=8`/`-count=10` on the timing-sensitive tests · build/vet clean. ⚑ **`-race` unavailable on this box** (no gcc; `CGO_ENABLED=1` fails). ⚑ **`auth`'s `TestMissingAccountStillCostsABcryptCompare` flaked once under parallel suite load** — a wall-clock bcrypt timing comparison, passes in isolation; unrelated, but expect it. ⚑ **The operational half is not code:** the live server needed a **restart** (in-process queue, no admin surface; making the entries succeed would mean re-creating deleted rows whose accounts are gone too), and restarting sooner loses strictly less — the last good save predates the outage, so that loss is already banked. **Deliberately not done → `backlog.md` §51** (the transient second queue entry — a readability item, ruled not worth touching verified concurrency code for) · a queue-depth/oldest-entry-age log line when `Failing()`, which had to be *inferred* here · **a character-name content filter** for the spam registrations noticed the same day (`CS2SKINBARON dot GG` passes `ValidateCharacterName` legitimately — that is a charset guard, this is a content problem). Full ledger: `docs/plan-accounts-frontend.md` §10a.
- **Prior: bugfix — TWO TABS, ONE ACCOUNT** ✅ `99d15fd8` 2026-08-02 — the second reported bug against step 8a. **Reported:** *"logging in twice in two tabs reaches character selection; the logout button there says 'That request could not be understood'"*, then *"logout on the second tab kills the first tab's session and the game freezes."* ⭐ **The reframe: the second LOGIN is not the way in.** The session cookie is **browser-wide** and `sessionStorage` is per-tab, so a second tab reaches character-select **with no credentials typed at all** — a login guard closes the cross-browser case and cannot close the reported one. ⚑ **And "just forbid a second character-select" cannot work: the cookie identifies an ACCOUNT, not a TAB** — a per-tab rule needs a per-tab token invented for it, a TTL and a heartbeat (HTTP has no "I left this screen" event), and a take-over rule, i.e. `Stashed` vs `Connected` reinvented in a second registry — and two tabs can still press Delete in the same tick. **The list is a snapshot; make a stale snapshot self-correcting rather than forbidding a second reader.** **Shipped:** ① login refused while the account is in the world (**`Connected`, not `Live`** — a stashed session is a dropped socket and must stay loginable) · ② **logout is idempotent, checking the cookie FIRST** — the reported 400 was a second tab arriving with only a **leftover anonymous secret** after another tab's logout cleared the shared cookie, which `resolveCaller` resolved into a caller that had not come in on a token (`register()` now forgets the secret like `login()` does; `caller.viaJWT` is deleted) · ③ **the §6 discard no longer deletes a character out of the world** — a guest playing in tab 1 could log in from tab 2 and soft-delete the live character, releasing its name; it now ends that account's world session first (⚑ `handleDeleteCharacter` refuses exactly this — the discard went round it because it **re-resolves a second identity out of band, from a raw header**) · ④ **`playingCharacterId` + refresh-on-refusal**: the played character renders as **"In world"** with no Play/Delete, a panel warning, and **two presses** to log out; `bad_request`/`already_logged_in`/`character_playing` now **re-read the list and then say why** (the message goes on AFTER the refresh — `show()` clears the error slot). ⭐ **Then the PO's architectural call — *"semantically the char list should not be an auth"* — and the code agreed:** `GET /api/characters` also answered *who am I*, *have I progress*, *am I in the world*; `resolveIdentityQuietly()` fetched a whole list **from inside the world** and discarded the characters, and a cold load with nobody signed in was answered **401**, an error standing in for an ordinary answer. **New `GET /api/session`** — a pure read returning `hasAccount`/`registered`/`username`/`hasProgress`/`playingCharacterId`, **200 for "nobody"**; `/characters` lists characters and nothing else. ⚑ Its one side effect: a **stale identity clears the cookie**, because a dead cookie *shadows* a good anonymous secret and is unrefreshable anyway. ⭐ **The proof the split was real: both harnesses had their 401 filters REMOVED and still report 0 console errors.** **Verified:** Go **full suite** green · vitest **92/92** · typecheck/vet/gofmt/build clean · `-dev` boot **0 errors 0 warnings** (86 skills/15 factions/64 mobs/10 recipes/4 quests/777 props/485 spawns/5 campfires) · **`chunk2-accounts.mjs` 24/24** (+3, unfiltered) · **`chunk4-persistence.mjs` 15/15**. **Both new tests mutation-checked:** flipping `Connected`→`Live` fails **exactly** the stashed assertions and nothing else (the test pins the *word*), and forcing `playingCharacterId: 0` turns leg 4b red with `stillHere=false` — **the original bug reproduced verbatim**. ⚑ **Two harness traps, each producing a run that proved nothing: a second `aurad` from 14:30 held port 2000** (`taskkill //F //IM aurad` killed only one of two ⇒ persistence read **10/15 with the character at level 1**, indistinguishable from a save regression — use `Get-Process aurad | Stop-Process -Force` and check StartTime), and **a mutation that does not compile leaves the OLD binary serving**, so a suspiciously green mutation run means "read the build output" first. **Deliberately not done → `backlog.md` §46–§48:** ⭐ **§46 fold the anonymous secret into the session** (exchange it once instead of carrying it on every request — **PO says most likely next**; it deletes the JWT-wins precedence rule and gives anonymous accounts expiry + revocation for the first time) · §47 the *"Connection lost — reload to reconnect"* banner the other window still shows · §48 the reconnect token may be deletable outright. Full ledger: `docs/plan-accounts-frontend.md` §10a.
- **Prior: bugfix — the campfire bind survives a login** ✅ `3c9e17ba` 2026-08-02 — the first reported bug against step 8a, and **not a regression**: it is chunk 4's deliberately-deferred `home_campfire_id`, which §8 had parked on "the anchor object-identity work". The bind lived only in `ConnectionStateSystem.anchors`, keyed by **connection** — enough to survive death and reconnect, nothing else — so every cold login fell through to `defaultSpawnPosition()`, a random `startingSpawn` fire. **PO rulings:** ⭐ **authored object ids, explicitly chosen over a cheaper synthesized `"<zone>@<x>,<y>"` key "to prepare for the deferred object identity work"** · the id is spelled **`spawnpoint-N`, not `campfire-N`**, and its uniqueness is asserted **zone-wide**, so the next bindable object kind (waystone, bound totem) joins the namespace instead of starting its own · a deleted spawn point falls back to the zone's default spawn · a fresh bind is a **forced save trigger**. **Shipped:** `world.Campfire.ID` (8 fires seeded, non-empty + unique enforced in `Zone.Validate()`) · `sys.CampfireAnchor.ID` + `campfireByID`, with **`s.anchors` holding the id rather than a position** so an unresolvable id reads as *unbound* everywhere at once · `persist.CharacterState.HomeCampfireID` ↔ `home_campfire_id` (`"" ↔ NULL` both ways) · the join path **re-installs** the bind, not just the position · the zone editor mints and serializes ids. ⚑ **A second defect the harness found underneath it, predating this work: the dwell counter only reset when a player was near NO fire**, so moving straight from one bind radius into another kept counting the FIRST fire — and since the bind fires on the count being *exactly* at the threshold, it never fired again and the player stayed bound to a campfire they had left (reachable on foot wherever two radii touch, instantly with a warp). **Without this the whole feature read as broken:** the first harness run persisted `spawnpoint-1` for a character that had stood at fire 2 for twenty seconds, because it had bound to the fire it *spawned* at. ⚑ **The zone editor would have silently deleted every id** — `getZoneAsJSON` is a field **whitelist**, so one PO round-trip drops any field it does not name, which with mandatory ids means a zone that refuses to boot; caught by reading the serializer, pinned by the new `ZoneModel.test.ts`. **Verified:** Go **full suite** green (incl. the real-Postgres round trip with the new column) · vitest **92/92** (+5) · typecheck/vet/build clean · `-dev` boot **0 errors 0 warnings** (86 skills/15 factions/64 mobs/10 recipes/4 quests/777 props/485 spawns/5 campfires) · **new `campfire-bind-persistence.mjs` 6/6** (binds at the EASTERN fire on purpose — binding at a starting fire would pass either way — leaves to character-select, returns **0.6 units from the bound fire, 102 from the starting one**), `chunk4-persistence.mjs` **15/15**, `chunk2-accounts.mjs` **21/21**, `chunk2-roles.mjs` **3/3**, 0 webgl losses. ⚑ **One property is pinned in Go ONLY, deliberately: *does dying after a cold login return you to the bound fire* cannot be read from the browser** — `window.game.character` is not re-pointed at the respawned entity, so every post-respawn position read returns the PRE-DEATH position (20 s of polling never moved it while the server log showed the respawn landing correctly); a leg asserting on that reports a working product as broken, so it was removed and `TestColdJoin_SpawnsAtThePersistedSpawnPoint` covers the same map. ⚑ **Two harness traps recorded:** the death-screen **Respawn button cannot be clicked** (MouseManager preventDefaults `mousedown`, suppressing the synthetic click — dispatch `submit` on `#endForm`), and **every non-starting campfire has hostile company**, so a level-1 character respawning at one dies again in **74 ms**. **Plain-language summary: `docs/accounts/campfire-bind-fix.html`.** Full ledger: `docs/plan-accounts-frontend.md` §10a.
- **Prior: step 8a chunk 4 — SAVE & LOAD** ✅ `7639503b` 2026-08-02 (backend only — no wire, no frontend, no schema change) — **your progress survives leaving the world.** Level, experience, spellbook, loadout slots, active aura and the quest ledger all round-trip through Postgres. **This closes step 8a's code**; only §8's operational work (backups with a proven restore, off-machine storage, live provisioning) is left, and it is runbook work. Shipped: **`pkg/aura/persist`** (the `CharacterState` value struct + the async `Writer`, depending on **nothing else in aura**, which is what lets `store` write it, `auth` carry it and `sys` build it without any of the three depending on each other) · `store.SaveCharacter`/`LoadCharacterState` (the §4 transaction, `SET LOCAL synchronous_commit = off`, child tables fully replaced in FK order) · `quests.EncodeFlags`/`DecodeFlags`/`Ledger.Restore` (**three** flag rows, `Running` persisted not derived) · `sys/persist.go` (the five save triggers, the `CharacterSaves`/`PersistenceSink` seams, §5b's save-failure warning) · `cmd/aurad/shutdown.go` (SIGTERM flushes every live character, two bounded waits, a log line naming anything it could not save). **Verified:** Go **33/33 packages** (new: `persist`), DB rows included · vitest **87/87** · typecheck/vet/gofmt clean · `-dev` boot **0 errors 0 warnings** (86 skills/15 factions/64 mobs/10 recipes/4 quests/777 props/485 spawns/5 campfires) · **new `chunk4-persistence.mjs` 15/15** (level 3, 240 XP, Damage+Hardy, the equipped aura AND its active slot all survive a real leave-and-return) · `chunk2-accounts.mjs` **21/21** · `chunk3b-interact.mjs` **14/14** · `chunkC3-journal.mjs` **8/8 + 1 SKIP** · 0 webgl losses. **Decisions:** ⭐ **the load rides the PLAY TICKET and the game still gets no `*store.Store`** — the handover called this the chunk's biggest decision and ruling 11 had already answered it: the loop is one goroutine, and `/select` has read that character a moment earlier to prove ownership · ⭐ **one struct for both directions, round-trip IS the acceptance test** — `TestCharacterStateRoundTrips` (real Postgres) proves the SQL agrees with itself, `TestCharacterStateRoundTripsThroughAPlayer` proves the game agrees with the mapping; a field the builder writes and the restore ignores passes the first and fails the second · forced-save detection is a **revision counter** on `SkillComponent`/`Ledger`, not a per-tick rescan, and ⚑ **`NoteKill` deliberately does not bump it** (a kill counter is not memorable progress; bumping there would write on every mob killed) · **"if dirty" is a fingerprint inside the writer**, so the session-expiry trigger is free instead of a guaranteed duplicate · §5b's warning **rides the existing journal banner** (the client already renders that text verbatim) and waits **30 s**, because `Failing()` is true after ONE failed write and a database restart produces that routinely · **avatar/faction stay unwired** per §4 — neither has a runtime source. **⚑ Traps closed structurally:** the FK ordering is pinned by a test that fails either way round (**proven red** by swapping the two DELETEs) · an **empty spellbook means "never saved"**, so a brand-new character's component is left alone · **`active_aura_slot` NULL loads as −1, not slot 0** · a loadout slot naming a retired skill is **skipped, not fatal** · **`Ledger.Restore` does not cascade** (a load installs settled state; replaying D3's retroactive walk would re-fire banners for quests finished weeks ago) · the save UPDATE carries the alive check, so an autosave in flight cannot resurrect a just-deleted character's numbers. **⚑ Findings:** ⭐ **the chunk broke `harnessdb -cleanup` and ONLY the verification tail found it** — cleanup hard-deletes harness characters, and the moment they had spellbook rows the FK refused (the schema carries no `ON DELETE CASCADE` anywhere, deliberately); **every Go test and every browser harness was green while it was broken**, because nothing else deletes a character · ⭐ **JSONB is not a string column** — Postgres re-renders what it stored, keys reordered, so without `persist.CanonicalFlags` on *both* sides the round trip compares equal-but-different bytes AND the fingerprint calls every reloaded character dirty forever · ⭐ **`Running && Completed` is unreachable with an objective-only repeatable quest** (lifetime thresholds mean re-accepting completes it in the same call), so the test pinning it needs a **dialogue** first stage or it passes for the wrong reason; mutation-checked · ⭐ **chunk 3 left a real hole this chunk had to close: DYING FREED THE ACCOUNT'S SESSION SLOT** — death runs through `removeFromPlayers`, which stashes the session and forgets the identity maps while the socket is still open, so *die in one tab, open another, be in the world twice*; `handleDeath` now re-claims and re-registers after the fan-out, as it already did for the name, anchor and token · **a zero tick is a real tick** (`failingSince == 0` as "not failing yet" left a tick-0 failure permanently unwarned). **Deliberately NOT built:** the sacrifice transaction · `home_campfire_id` (**since CLOSED — see Last completed**) · `bloodline_unlocks` writes · a sticky HUD banner (the alert banner carries the sentence) · avatar/faction wiring. ⚑ **Known local limitation: the SIGTERM flush cannot be exercised on this Windows box** (`taskkill /F` is uncatchable and the process has no console for Ctrl-C); both halves are covered by Go tests and the live server is Linux under systemd. **Plain-language summary: `docs/accounts/chunk-4-summary.html`.** Full ledger: `docs/plan-accounts-frontend.md` §10a.
- **Prior: step 8a chunk 3 — the wire** ✅ `de8d6910` 2026-08-01 — the socket is authenticated: `Join` carries a **play ticket** and nothing else, **a join without one is refused**, and **logout closes the socket**. ⚑ Two findings outlive the chunk: ⭐ **`tsconfig` targets ES5, where subclassing `Error` breaks the prototype chain — so `instanceof ApiError` was ALWAYS false in the shipped bundle** while the vitest asserting it stayed green (esbuild compiles to modern JS), so **anything relying on `instanceof` must be proven in a real browser**; and ⭐ **chunk 3 silently broke RECONNECT** — the server began requiring a ticket to resume a stashed character and the client was never taught to send one, which arrived as two unrelated-sounding reports (*"F5 sometimes gets me stuck"*, *"settings forgot my account"*) and only the second was investigated. Full ledger + the PO hand-testing pass (eight defects): `docs/plan-accounts-frontend.md` §10a; summary `docs/accounts/chunk-3-summary.html`.
- **Prior: step 8a chunk 2 — the frontend** ✅ `de8d6910` 2026-08-01 — **aura got a front door:** the account screens (creation both mounts, character-select **slot cards**, login, register, delete dialog, HUD nag), an identity-scoped `features/accounts/`, `Account.ts` deleted, and `cmd/harnessdb` (seed + cleanup). ⭐ **The cookie gate was ANSWERED in a real browser (8/8):** a `Secure; httpOnly; SameSite=Lax` cookie **is** stored and replayed over `http://localhost`, and Lax survives webpack :2001 → aurad :2000 because **SameSite is per-*site* and ports do not count** — no https, no same-origin dev server. ⚑ **`localhost` and `127.0.0.1` ARE different sites** (mixing spellings silently signs the player out). ⭐ **The `hrnss_` prefix is a `-dev` flag, corrected twice mid-flight** — "allow it for accounts with no username" would hand the reserved namespace to **every new player**, since every new player is anonymous at exactly that moment. ⚑ **Findings, each of which presented as something else:** a **leading HTML comment silently deletes an entire screen** (`renderPartial` takes only `firstChild`, and the resulting null deref kills bundle init) · **`taskkill //F //IM aurad.exe` matches nothing** (the binary has no extension), so a stale server served old code through three debugging cycles · **`waitForSelector` defaults to VISIBLE while `.hidden` is `display:none`** (a 60 s hang while the product works) · **Playwright `route.fulfill()` pages cannot reach loopback** (Private Network Access ⇒ a bare "Failed to fetch" that reads like a CORS refusal) · **`game.audit_log` has an FK to `accounts`** that only bites once an account has logged in · **`.button` is NOT global**. All 19 harness scripts now enter the world through `.claude/skills/verify/lib/join.mjs`. **Plain-language summary: `docs/accounts/chunk-2-summary.html`.** Full ledger + the 11 rulings: `docs/plan-accounts-frontend.md` §10a + §10b.
- **Prior: step 8a chunk 1c — the eight endpoints** ✅ `9c19c5f2` 2026-08-01 — the eight HTTP/JSON endpoints, `pkg/aura/origins/` (one allowlist serving both `CheckOrigin` and CORS), and the transport hardening that had to arrive with them. **Closed `backlog.md` §43, both halves.** Its yield was **12 mutation runs, 11 caught**. ⚑ **Three findings still load-bearing:** `go test ./...` runs packages in **parallel against ONE test database**, so every DB-touching package must take the **Postgres advisory lock** in `store/storetest` (without it the suite reads as a broken migration) · **every `aurad` boot needs `AURA_DB_URL` AND `AURA_JWT_KEY`**, harness runs included · **a non-`-dev` boot with no `tlsHost`/`allowedOrigins` correctly WARNS** (it refuses every browser), so the standing "0 errors 0 warnings" check is a **`-dev` boot**. Full ledger + the §3a wire contract: `docs/plan-accounts-frontend.md` §10a + §3a; summary `docs/accounts/chunk-1c-summary.html`.
- **Prior: step 8a chunk 1b — auth & sessions** ✅ `31ebebcb` 2026-08-01 — **aura can tell who you are:** the `auth` package (bcrypt behind a `Gate`, HS512 JWTs carrying `token_generation`, the play-ticket TTL map, the two-axis throttle, the account-scoped session registry), pure Go, no HTTP surface. ⭐ Its yield was **six mutation runs**, and its two durable findings: **`Gate.do` needed an early `ctx.Err()` check** because when a free slot and a finished context are both ready Go's `select` picks at random, so a caller who had gone away still burned a hash half the time; and **deleting `WithValidMethods` leaves the alg-none forgery rejected but lets HS256 straight through**, so those two rows pin different things. **Decisions on record:** bcrypt **cost 11** (measured, not conventional — ⚑ extrapolates to **~0.9 s on the live vCPU**, re-measure while provisioning) · **`auth.Gate` with `DefaultGateSlots = 2`**, the only route to bcrypt, added on a PO question about login cost at 100 concurrent players (answer: **auth is not the constraint, the single-threaded game loop is, and it already breaks at ~60–70 clustered**) · ⚑ **a separate accounts DB was considered and REJECTED** — anonymous-first fuses account + credentials + character into one transaction on the hottest write, and `characters.account_id → accounts.id` cannot cross databases. Full ledger `docs/plan-accounts-frontend.md` §10a chunk 1b; summary `docs/accounts/chunk-1b-summary.html`.
- **Prior: step 8a chunk 1a — schema, migrations & the connection layer** ✅ `6d5cc695` 2026-07-31 — **aura has a database.** The whole `game` schema (8 tables) incl. `token_generation`, `golang-migrate` as a **library** with the SQL `go:embed`-ed so it applies at boot, and the `pgx/v5` + `pgxpool` layer; no game code touches it, which was the point. ⚑ Its durable traps: **`go mod tidy` silently raises the `go` directive** (1.22 → 1.25 unasked) so run **`go mod tidy -go=1.22`** and read the diff · a failed migration leaves `dirty` set **while the DDL has rolled back**, so the database shows *no schema at all* and it reads as "migrations never ran" · connection strings must be **percent-encoded** (psql's parser is lenient, Go's `net/url` is not) · **boot without a DB emits 1 deliberate WARN**, so the standing "0 errors 0 warnings" check means *with* `AURA_DB_URL` set, and **1c must flip unset-is-fatal**. ⚑ **This machine: `make` is not installed** (run `cp-defs`'s two lines by hand), local `conf.json` names no zone so `./aurad` needs **`-zone world`**, and `backend/pkg/api/mobs/.gitignore` holds `*.json` so the embedded mob content is **untracked** — a fresh clone cannot run `go test ./...` green until someone builds. Full ledger `docs/plan-accounts-frontend.md` §10a chunk 1a; plain-language summary `docs/accounts/chunk-1a-summary.html`.
- **Prior: MOBILE RENDER COST — the phone was asked for 3 Mpx a frame, and it starved the input clock ✅ `59dfe266` 2026-08-02, PO-VERIFIED ON A REAL PHONE + DEPLOYED LIVE same day** — PO report against the live mobile deploy (*"mobile performance and especially movement is quite laggy"*), with the open question of whether it was obvious or deep. **It was obvious, ONE root cause, two expressions in one file.** `Game.ts` took `resolution: window.devicePixelRatio` + `antialias: true` **unconditionally**; a phone reports DPR 3, so the canvas was a **1170×2532 backbuffer — 2.97 Mpx/frame with MSAA**, more pixels than a 1440p desktop monitor on a phone GPU. ⭐ **The measurement that named the cause: frame time is nearly LINEAR in pixel count** (~16 ms fixed + ~204 ms/Mpx) ⇒ **fill-bound, not JS-bound** — the per-frame JS is already a small constant and the `AuraRings`/`updatePlate` dirty checks all hold, which is what made this a one-knob fix rather than an optimisation pass. ⚑ **The reported symptom was MOVEMENT, and movement is a CONSEQUENCE, not a second bug:** `Controls`' Tock clock is `setTimeout`-based at 33 ms and so nominally independent of rendering, but it still needs the main thread and a saturated one starves it — measured input sends **tracked the frame rate 1:1** (**1.8/s at DPR 3, 10.4/s at DPR 1, against 30/s**), after which the server coasts and corrects, reading as lurching and rubber-banding *on top of* low fps. **Anyone chasing this from the input side — joystick, `INPUT_TICKRATE`, stop-tail, packet loss — would have found nothing wrong, because nothing is** (the drag reaches the joystick, `.nipple` spawns, and nipplejs sets its own `touch-action: none`, so both were checked and CLEARED). **PO ruling: cap at 2, antialias off on mobile.** Both knobs gate on the existing `isMobile()`, so **desktop is unchanged by construction** — off mobile the two expressions reduce *literally* to the old ones; ⭐ **`renderResolution()` is ONE definition read by both `init()` and the resize handler**, because a second `devicePixelRatio` at the resize site would silently drop the cap on the first orientation change — the exact drift class that handler exists to close. ⚑ **MSAA was near-pure cost** (geometry edges only, so in a sprite game it touched nothing but the vector `Graphics` — rings, bars, tier frames — while being paid for over the whole framebuffer): **−26 % on its own**. **Interleaved A/B** (one phone viewport at device DPR 3, `?desktop` vs `?mobile` in the SAME build so load cannot favour an arm): **2.96 → 1.32 Mpx, 559.4 → 230.9 ms/frame, 1.3 → 2.5 input sends/s = 2.25× fewer pixels, 2.42× faster frames, 2× the input rate**; ⚑ **read the ratios, never the absolutes** — headless Chromium rasterizes in software, so the fps figures are not device predictions. Confirmed on the **deployed** bundle: phone `780×1688 SAMPLES=0`, desktop control `1170×2532 SAMPLES=4`. ⚑ **Two near-misses worth keeping:** a single-context run reported **16.7 ms / exactly 60 fps**, a 14× beat on the prediction and the *blank-world* signature (§29) — real, but proven by screenshot before being believed, and the interleaved A/B is the number that survived (**a suspiciously good result deserves the same scepticism as a bad one**); and a pixel-readback probe called the canvas 100 % black because `preserveDrawingBuffer` is false — **the probe was broken, not the render**. **Verified:** `tsc` · 133 vitest · prod build · `ctxloss-warning clean` at the documented baseline (**5 GL contexts / 2 probe losses / 0 warnings** — this changes the renderer boot path) · `mobile-layout` all checks · `mobile-interact` 14/14 incl. its desktop control · `r1-focus-cost` 5/5 · live boot 0 errors 0 warnings, 87 skills/15 factions/64 mobs/10 recipes/5 props/4 quests/5 campfires. **⏳ Open — PO: *"works fine for now, needs some love"***: not a regression, a ceiling. Cheapest next turns in order — `MOBILE_MAX_RESOLUTION` 1.5 (one line, `[PLACEHOLDER]`) · the minimap is a **second WebGL context** rendering every frame (a driver context switch per frame for a 94×94 canvas) · `touch-action: auto` still on `html`/`body` (minor iOS double-tap-zoom arbitration on HUD taps) · only then anything structural. ⚑ **Not to be confused with `plan-server-performance.md`** — that is the *server's* player ceiling; this is client render cost and they share no mechanism. Full ledger `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: a MOBILE LAYOUT — the HUD stops covering the world on a phone ✅ 2026-08-02, PO-VERIFIED ON A REAL PHONE 2026-08-02** — one `html.mobile` class gates everything, `#leftColumn` *becomes* the menu sheet with no DOM surgery, scaling is one vmin-keyed root font size; its two real finds were INPUT bugs, not layout (read-only overlays swallowing joystick touches; the spellbook-inside-the-sheet making aura/cooldown equipping unreachable). Desktop invariance MEASURED (computed-state diff, identical but one hidden node). Plus the interact verb's tappable twin (`Interact.trigger()` the one definition, `mobile-interact` 14/14). Full ledger `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: ROUND-7 SESSION 2 — the remaining SIX intake items in one session ✅ `7c30b3e8` 2026-08-02** — every open round-7 item except totem tooltips (item 3, deliberately left queued — needs catalog/data design), rulings collected up front as choice prompts, built small → large; full ledger **`plan-playtest-feedback.md` §Round-7 session-2 ledger**, findings pinned inline per item. **Item 12** — turn-in prose re-differentiated, PO pick AGAINST the drop-the-tracker recommendation (4 content lines, trackers untouched so the quest harnesses hold by construction) · **item 6** — **per-caster dot streams** (`ApplyDot` keys on caster+HP; ⚑ the advertised "one-line key change" was TWO sites — `DueBuffEvents`' strongest-dot suppression had to become per-caster too, as an alloc-free nested scan because it runs per entity per tick under the idle-loop pins), and **HoTs stay last-writer-wins BY RULING** — the result matches WoW Classic exactly (per-caster dots, exclusive same-spell HoTs), which is the stated reference · **item 5** — Strong visible: `GameState.damage_factor`, the R1 pattern verbatim; `DerivedStats.DamageFactor()` is the ONE composition site (damage sites + codec both read it); new harness `r7-strong.mjs` proves 14 → 15 · **item 7** — blue cost numbers: `Character.cost_paid` + `costPayer.NoteCostPaid` (⚑ the R3 silent-wiring class — the capability test now pins *player MUST and *Mob must NOT satisfy costPayer, on real types) + floating kind `'cost'` 0x4D9EFF; ⚑ the health bar is NOT a witness for a cost — paying doesn't enter combat, so regen tops the pool back up; the blue number itself is the evidence (`r7-respec-cost.mjs`) · **item 10** — wording audit: every VALUE correct, ONE bug (Discipline's stat line rendered its raw JSON key — `costReduction` missing from STAT_LABELS; a vitest now walks every dispatched stat) + PO ruling: one −X% reduction vocabulary (Tough `Damage taken: −10%`, Discipline `All costs: −6%`) · **item 8** — respec RULED (free · blocked in combat · level-1 floor) and BUILT: `ClientMessageBody.Respec = 10` (pinned), `ResetSkillLevels()`, EquipSystem handler on the equip lock, two-press-arm Reset button; ⭐ **the refund is zero arithmetic — `SpentPoints` is derived**, the comment predicted respec. **Verified:** full Go suite + vet + gofmt · 133 vitest (+3) · tsc · prod build · alloc pins + guardrails · boot `-content ../api` 87 skills/15 factions/64 mobs/10 recipes/4 quests/777 props/485 spawns/5 campfires, 0 errors 0 warnings · harnesses one-at-a-time on fresh servers: `hygiene-wire-prune` (3 wire fields added) · `r7-strong` · `r1-focus-cost` 5/5 · `round4-tooltip` · `chunkC4-quests` 37+1 SKIP · new `r7-respec-cost` (respec 3→1, badge 27→29). ✅ **PO-VERIFIED IN-GAME 2026-08-02, all six items.** **Follow-up same day: a new character spawns with Damage PRE-EQUIPPED in aura slot 1, deliberately NOT active** (PO pick — the first press of "1" turns it on): a derived rule in `applyCreationMilestones` (a creation-seeded active aura fills the first free aura slot; passives stay spellbook-only; respawn/reconnect overwrite the loadout right after `New`, so only genuinely new characters keep it), pinned by test + live probe, ✅ **PO-VERIFIED IN-GAME 2026-08-02** `0e161de8`.
- **Prior: FIRST LIVE DEPLOY SINCE 2026-07-29 + INTAKE ROUND 7 ✅ 2026-08-02** — the N1–N5/R-series/quests stack went live at `aura-game.duckdns.org`, 12 items intaken, 6 shipped same day incl. the empty-destination prune `5f2925b9` (PO: *"no option that leads to nothing but Back"* — a derived rule in `present()`, no new condition vocabulary) and items 1/2/4/11 `3b415fa2` + 9 `fd6fe48e`; ⚑ the frontend server sends NO Cache-Control — a stale `index.html` can hide a deploy for hours (hard-refresh). Full ledger `plan-playtest-feedback.md` §Round-7 ledger.
- **Also 2026-08-02: the help panel — a placeholder tutorial ✅ `8cc3ef82`, PO-VERIFIED IN-GAME same day** — a circled `?` atop the zoom column opens "How Aura works": 12 short design-statement sections ordered by when a new player meets each mechanic, PO-edited draft (auras are target-capped, dying loses progress-XP, "no way to grief" cut as intent-not-fact). Frontend-only, 5 files, static HTML + a ~40-line visibility module on the journal overlay pattern; no hotkey by PO choice. Verified tsc · 132 vitest · prod build · 8/8 smoke. Full ledger `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: FEEL PASS 2 EXECUTED — N1–N5 in one session ✅ `14b35f98` 2026-08-02, ✅ PO-VERIFIED IN-GAME 2026-08-03 (all five chunks approved; ⚑ PO note: cost balancing continues over time — tuning stays open as an ongoing activity, not a scheduled chunk)** — shield-bar truth (`ShieldBarMath`, denominator `max(maxHealth, health+shield)`, HUD + both overhead bars) · one `Ticks every X` line with costs grouped by charge trigger · dots leech LIVE off the post-credit caster · quest baselines per stage entry (REVERSING quests-D3, persisted shape pre-recorded for 8a) · `BeatDetector` ring pulse + HUD metronome pip. 130 vitest (+19), sim battery byte-identical, TTK 6.67 s / TTD 8.70 s stand. A parallel prose session (`ac0f8a11`) coexisted cleanly. Full per-chunk ledgers **`docs/archive/plan-feel-pass-2.md` §8**.
- **Prior: the cooldown re-slot exploit — a cooldown belongs to the SKILL, not the slot ✅ `abdb5673` 2026-08-01** — `CdTicks` DELETED for `SkillComponent.cooldowns` (map, absent key = ready; deleted rather than mirrored — a mirror is the §35 drift class); the in-combat equip lock STAYS as a design choice (PO); unslotted cooldowns keep ticking on purpose (a park-to-freeze map would be a worse exploit than the one closed). Full ledger `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: R3 — ONE BEAT, ONE PRICE ✅ `f4ab5bc9` 2026-08-01** (+ `194036c8`) — every multi-effect aura put on ONE damage beat with magnitude and cost divided by the ratio (throughput-neutral), dots re-priced ×3, entry prices for FireWard/Slow/Rejuvenation, First Aid free, and **`lifesteal_burst` a new effect type end to end** (Bloodthirst — still dev-only, and its placement is an open PO call). ⚑ Its durable landmine is R2's in a new hat: **the whole feature was built, dispatched and tested GREEN while the real `*player`/`*Mob` types lacked the methods** — self-buff cooldowns reach the caster by *structural* assert, so a missing method fails silently and completely; `sys/self_buff_capabilities_test.go` now guards it. ⚑ The follow-up closed the R1/R2 discrepancy R3 surfaced: the cost line printed a tick cadence for effects R2 had made charge per target-entry, so it now prints the **charge trigger**, derived by both languages from `api/shared-constants.json` instead of each restating it. Full ledger `docs/archive/plan-resource-costs-feedback.md` §8.
- **Also 2026-08-01, after R3: the tooltip's next-level preview is GATED ON AFFORDABILITY ✅ `66646743`** (PO ask, frontend only, 3 files + 1 harness; full ledger `docs/plan-playtest-feedback.md` §Ledgers). Round 4 gave every scaled value a `current → next` preview whenever the skill was below its cap — a different condition from *the player can act on this*: a level-1 character holds **zero** points (`TotalSkillPoints` is `(level−1) × perLevel`), so from creation to the first ding every hovered skill advertised an upgrade it could not buy, and the same returns permanently once the last point is spent. ⭐ **The whole feature is ONE preview cap** — every `→` renders through `prog()`, so `previewMax = showNextLevel ? def.maxLevel : level` gates damage, cost, radius, targets, cadence, cooldown and cast time at once, **and a new effect type inherits it without knowing it exists**; it is a *rendering* cap only, so the subtitle still reads `def.maxLevel` and the cooldown's summed per-cast cost still measures its slope across the real range. **PO ruling: the trigger is AFFORDABILITY, not possession** — levels cost 1–3 points on the D10 curve, so the preview now appears exactly when the `+` button is live (`skillPointCost(maxLevel, level+1)` against the live count, the rule `updateSpellbook` already greys on). The count is **pushed** from `updateSkillPointsDisplay` (SkillTooltip cannot import HUD back) and starts at 0 — no preview beats a wrong one. ⚑ **The wiring is invisible to vitest by construction**: `formatSkillTooltip` takes the flag as an argument, so **a HUD that never pushed the count would leave all 111 unit tests green with the feature dead on screen** — hence a new `round4-tooltip` leg watching it. ⚑ **And that harness needed a real repair, the same failure mode as the R1/R2 cost wording**: its radius check compares rendered TEXT to prove radius does not ride the character curve, and its own XP cheat is what turns the preview on — so an unmoved radius (`Radius: 2.5` vs `Radius: 2.5 → 2.6`) reported as **moved**. **A harness that asserts on rendered text is broken by any change to what is rendered, not only by a change to what it measures.** **Verified:** 111 vitest (+4) · `tsc` · prod build · `round4-tooltip` all legs · `r1-focus-cost` 5/5. ⚑ **Two red runs were a SECOND CLAUDE SESSION on the same machine** restarting `aurad` on port 2000 underneath this one (dead XP cheat, missing Discipline, a stray `equip Swift`) — `AURAD_CONF=<copy with another server.port>` gives a private server and the harnesses all take the URL.
- **Prior: R2 — what "landed" MEANS ✅ `47074d63` 2026-08-01** — D8 said *"an aura pays only when its effect lands"* and the code implemented THREE rules; there is now **one**, and it means **work done**. `Buffs.Apply{Resist,Slow,Dot,Hot,Shield}` **return** the new-vs-refresh answer they always computed internally, with **no lookup added anywhere**; a refresh at the same strength is not work and is not charged — you pay to ignite, not to keep burning. **Shield keeps a sustain cost** (its pool IS a work signal), the **instant twins were deliberately NOT swept up** (D9 — a cooldown pays on cast, hit or whiff), and `noteHarmDealt` stayed on *reached anyone*. ⚑ Its durable landmine: the appliers reach targets through RUNTIME `UserData.(slowable)` asserts, so **widening an interface breaks test doubles SILENTLY** — eight recorders stopped satisfying their interfaces with no compile error, reading exactly like a regression in the feature under test. Full ledger `docs/archive/plan-resource-costs-feedback.md` §8.
- **Prior: R1 — what a cost SAYS ✅ `b2116ba3` 2026-08-01** — costs render in **absolute Focus** (`roundHP(fraction × maxHealth × costFactor)`, the effective-percentage arithmetic **deleted, not extended**), **`GameState.cost_factor` new on the wire** so Discipline is visible for the first time, **the resource is named FOCUS** (the PO took the widest option — no "HP" left in the UI at all), plus F10 and F3. ⚑ **It reverses the 2026-07-29 "percentage alone" sweep ruling**, marked superseded in `plan-numbers-rewrite.md` §7 and in the sweep bullet below. ⚑ **§3.11's cross-language pin, both directions**: `api/shared-constants.json`'s `hpRounding` case table — *round half up, floored at 1* — asserted by `vitals.HP` in Go and by the exported `roundHP` in the client, **each proven red against a real drift**. ⚑ **The floor is now visible rather than explained, and it makes a working passive look broken**: Immolate at CL30 reads `7 → 9 Focus`, `7 → 8` with Discipline — the level-1 endpoint stays 7 because `6.96 × 0.94` still rounds up, so **a cost-reduction check reading only the first number scores a working passive as invisible**. Full ledger `docs/archive/plan-resource-costs-feedback.md` §8.
- **Prior: the NUMBERS REWRITE (Pass 1) — C1 + C2 ✅ `40d9b204` 2026-07-31/2026-08-01** — caps `{1, 5, 10}`, a cap-relative point curve, resource costs on everything outside the free five, the retune and D4's damage-type/gate-key split; **16 PO decisions, 16 landmines**. Full ledger `docs/plan-numbers-rewrite.md` §7. ⚑ **Three findings bind anything that touches costs, and R2/R3 touch exactly that:** `vitals.HP` floors any positive cost at **1 HP** (L15), so a cost's real price is set by its **cadence** and `tickInterval` defaults to **1** — a cost on a `slow_aura`/`resist_aura` is 30 HP/s on a level-1 pool; **only the SEVEN dispatched appliers can be charged** (L16), so a cost on a passive or `light_aura` is inert while reading as priced; and **the batteries cannot see an aura cost at all** (`TTD` is by construction the idle-player run — measure in `-chain`, A/B against a cost-stripped copy). ⭐ **The PO feel pass RAN 2026-08-01 and the premise LANDED** (*"resource cost changes feel good actually"*, PO switching auras mid-fight in response to the drain) — the model is off trial; what remains is tuning, presentation and seams. Checklist `docs/archive/plan-numbers-feel-pass.md` · findings + the R1–R4 plan `docs/archive/plan-resource-costs-feedback.md`. ⚠ **`chunk3-charm.mjs` is still red** — 6–8/9 across four runs, always legs needing the charmed pet alive (the accepted D9 fragility), and the HEAD baseline hung, so it is **INCONCLUSIVE, not cleared**. ⚑ Every number is [PLACEHOLDER].
- **⭐ Last design session: STEP 8a — ACCOUNTS & PERSISTENCE — DESIGNED IN FULL** ✅ `728f7ef1` 2026-07-29→31 (docs only, no code) — four new docs, all indexed in `docs/README.md` and pointed at by roadmap step **8a**: **`docs/plan-accounts-schema.md`** (the DDL) · **`plan-accounts-implementation.md`** (save/load, failure modes, native Go auth) · **`plan-accounts-frontend.md`** (the flow the player clicks through) · **`plan-accounts-password-reset.md`** (split out, runs last — it is the only part needing outbound email, which aura has none of). ⚑ **Step 8 was split into 8a/8b** because it bundled two unrelated roadmap items and made *"is step 8 done?"* unanswerable. ⭐ **The shape: anonymous-first.** A player creates a character and plays with **no signup at all** — an anonymous account is minted behind them and stored as a `localStorage` secret; registering later just *adds credentials to that same row*, so progress is never lost by signing up. **Auth is native Go** (bcrypt + JWT HS512); the third-party Java fork the PO offered (`proehr/mindcraft-backend`) was **read in full and rejected** with evidence — its `changePassword` did `findByPassword(oldPassword)`, a plaintext lookup against a salted column (impl §7 carries the review table). ⚑ **The same defect was then reproduced in this very schema and caught in review** — `anonymous_secret` and the reset token were marked "hashed like a password", which cannot work: **a salted hash cannot be a lookup key.** Hence the schema's §"Hashing: lookup keys vs. verifiers" — **SHA-256 for lookup keys, bcrypt for verifiers** — the one section to read before touching any secret column. **The 2026-07-30 rulings (14, closing every askable question):** **play ticket** for `Join` — ownership is proven over authenticated HTTP at `/select`, the socket carries no credential, because a bare `characterId` on an unauthenticated socket is unverifiable and ids are `BIGSERIAL` · cookies **`httpOnly; Secure; SameSite=Lax` with the `CheckOrigin` allowlist in the SAME chunk** (else CSWSH protection is accidental — backlog §43) · **silent session refresh** (never a forced logout mid-fight) · **one live session per ACCOUNT, not per character** — the old per-character wording permitted all three characters at once in three tabs · **`token_generation` MOVED from the reset plan into 8a chunk 1a**, because logout that only clears a cookie is not revocation and silent refresh without it is an immortal session · credential rules final (3–32 / ≥8 + special + blocklist; ⚑ the special-char rule runs *against* the NIST reasoning the doc argued from — tension recorded, not hidden) · failed-login = **progressive delay both axes, no lockout** (a lockout is a griefing tool against a known username) · **the bootstrap path DELETED** — creation always returns to character-select, which auto-selects the first character (⚑ scoped to *"just created"*, **not** *"exactly one character"*, or the player is locked out of Delete and Logout) · **spawn resolution ladder** — `home_campfire_id` is nullable and stays NULL until a character *dwells* at a fire, so NULL is a legitimate persisted value falling through to the **existing** `defaultSpawnPosition()`; rung 4 is a per-character **default spawn point**, later keyed by race/faction · expired ticket → **silent retry once** · **chunk 1 split 1a/1b/1c** · absolute session cap **skipped, gap accepted**. **Also recorded:** a pre-existing **disconnect-to-escape exploit** (frontend §12 — disconnecting removes your entity instantly; harmless today, a real problem once there is progress to protect), and **backlog §43** — `CheckOrigin` accepts every origin and both catalog endpoints serve wildcard CORS, **both of which persistence turns into vulnerabilities** and must be fixed before the first cookie is set. ⚑ **A readiness sweep then added `plan-accounts-implementation.md` §0 "Tech stack"** — the plan was fully specified at the level of *schema and behaviour* and **silent on how Go reaches the database and where the database lives**, which is what chunk 1a needs first: **`pgx/v5` + `pgxpool`, not `database/sql`** (the schema is Postgres-only by construction — CITEXT, JSONB, partial unique indexes), hand-written SQL in a `store` package (`sqlc` considered, deferred at ~20 queries), the **three** sites that get a transaction, **`golang-migrate` as a LIBRARY with `go:embed`** so the single-binary deploy stays true, **Postgres as an OS package on the same VPS bound to loopback** (Docker rejected — there is none in the deploy path today; managed rejected on cost + a hop per autosave), the concrete **throttle mechanism** (⚑ the delay must apply *after* the dummy bcrypt compare or it reintroduces the timing oracle; ⚑ a restart resets every throttle — accepted), and a **`game.audit_log` table recording successes only** (⚑ never failed logins — an attacker generates those at will, which would make the audit trail an amplification target). ⚑ **Provisioning is one-time manual server work and must NOT enter `deploy.sh`**, whose whole virtue is being rsync + restart. **Open:** avatar/faction defaults (blocked on `plan-avatar-system.md`), §32 charge survival + §41 discovery scope (deliberately deferred), and the reset plan's own five.
- **Prior: hygiene sitting — gofmt drift · §27.3.6 · §27.3.4** ✅ `e1ed7468` + `af7925d0` + `389f3935` 2026-07-31 — three low-effort items committed separately; **§27.3 fully closed**. Durable: `gofmt -l pkg/` misses `cmd/` (36 files, not 34), and the authored-key table's `gatedDamageTags` row was wrong until checked against the mapper (⚑ that key no longer exists — the numbers rewrite D4 replaced it with `gateKey`). Full ledgers `docs/backlog.md` §27.3.4/§27.3.6.
- **Prior: backlog §33 — heal-over-time can pre-hot** ✅ `dc21ad81` 2026-07-31 — `HealthRatio() < 1` deleted from `applyHotAura`, KEPT on `applyHealAura`; the gate had been blocking RE-application, so a HoT stopped topping up the target it had healed. Knock-on: §25 A#2 SUPERSEDED. Full ledger `docs/backlog.md` §33.
- **Prior: backlog hygiene — §25 A#1 + A#3 and §27.2.8** ✅ `1bfd6677` 2026-07-31 (full ledgers `docs/backlog.md` §25 and §27.2.8). Zero behaviour change, sim battery byte-identical. `fireCooldown`'s if-chain → one `switch` (⚑ the survey said **eight** branches; there were **twelve**), plus `instantQueryCircle`/`queryInstantTargets`, `threatEntryFor`, `pruneDeadThreat` and `forEachCredited`. ⚑ **Durable lessons:** a §-banner and the prose beneath it can disagree (A#4 was already done); **a cleanup on an extension point that is actively growing should be scheduled, not left to a passing visit**; and **`pkill -f aurad` matches the invoking shell's own command line and kills it** — use `pkill -x aurad`.
- **Prior: conversation chunk Q4 — the content restructure** ✅ `3ccadb5e` 2026-07-30 — R1 tree shape on every conversant, zero `quest_at_stage` gates on quest rows, the Miner dropped so the lamp turn-in is Lantern's only source, Damage a silently-seeded level-1 creation milestone. Closed and archived `plan-conversation-journal.md` (full ledger `docs/archive/plan-conversation-journal.md` §6).
- **Prior: conversation chunk Q3 — the two-pane journal** ✅ `49b49857` 2026-07-30 — selection by quest ID in `JournalModel`, remembered across close/reopen (PO ruling); fallback first running → first completed → empty; sizing top `7vh` + `max-height: 60vh` + `@journal-left-reserve: 26rem`, no third copy of the strip's geometry. Full ledger `archive/plan-conversation-journal.md` §6.
- **Prior: conversation chunk Q2 — server-composed objective tracking** ✅ `1dfb57d8` 2026-07-30 — R2 end to end: `QuestProgress.objectives` appended last, L4 cache-on-`Progress` (`Snapshot()` never composes), `{n}/{m}` tracker override, three PO rulings (current stage only · override wins · mechanism only — Q4 authors the trackers). Full ledger `plan-conversation-journal.md` §6.
- **Prior: conversation chunk Q1 — the dialogue system** ✅ `d23670d7` 2026-07-30 — `blockedLine` gone at every layer (locked rows inert at both ends), ⭐ the `CanApply` show-rule extracted FROM the ledger ops (L3 — a quest row is shown iff its op would succeed; the predicates avoid `progressOf()`, which creates the entry it looks up), all three combat gates deleted (the entity-model R2/D21 tests **inverted, not deleted**), the client-only "Leave." row (`leave()`, never `take()`), scrollbar padding. Harnesses rewritten with it: conversation 31/31 · interact 14/14 · C4 28/28+1 SKIP · journal 17/17. Full ledger `plan-conversation-journal.md` §6.
- **Prior: quest chunk C4 — the first authored quests** ✅ `395177e4` 2026-07-30 — **closed `archive/plan-quests.md`** (P → C1 → C0 → C2 → C3 → C4 in one day; full ledger §15). Four quests, seven conversants, **zero engine code** — the C1–C3 vocabulary held. ⚑ Its durable findings live in `manual-content-authoring.md` § 6: a quest-conditional greeting hijacks the greeting (every quest node needs a row back to `root`); a quest row's `next` must name a node visible *before* the row is taken; rewards only ride a turn-in row; and a `Ledger` on a registry that skipped `CrossValidate` reads every dialogue stage as terminal (the test helper reproduces the boot sequence). PO calls: quest rewards may source drop-only skills (Taunt/Slow/Lantern) · XP "punchy, about half a level each", pinned by test against the Session-⑥ band flag.
- **Prior: quest chunk C3 — the wire + the journal** ✅ `604f3f4d` 2026-07-30 — the D14 minimal-projection `/quests` catalog, the ledger on `GameState` every tick, `EntityMessageKind.Journal` and `ClientMessageBody.AbandonQuest` pinned at birth, a centered-overlay journal panel on **`J`** + a HUD button, and abandon by clicking the row. ⚑ Its durable trap is L11's shadow: **the D17 banner notifier is installed by the OWNING player, not at ledger construction** — the ledger outlives the player struct across death/reconnect, so a callback captured at birth would fire banners into a closed client and the banner would simply stop existing after the first death with nothing failing. Also: `Ledger.Snapshot()` sorts, and the sort is load-bearing (Go randomises map order, and an unsorted projection would rebuild the abandon rows ~30×/s). It also **repaired the flaky D22 Wanderer harness leg** — `pinBadgedActor()` ran after the panel opened, but the badge is suppressed for the panel's own actor, so the pin never matched and the measurement fell back to "largest mover" (the camera). Full ledger `archive/plan-quests.md` §14.
- **Prior: quest chunks C0 + C2 — interaction hardening, then the dialogue vocabulary** ✅ `2a3b137d` + `2dc6973a` 2026-07-30 — the N1 both-ends fix + the L4 ≤254 guard, then the three quest grant kinds, `quest_at_stage`, and the loader cross-validation. ⚑ Its durable rulings: **option-as-atomic-row** for turn-ins with the **ledger op FIRST** (that ordering is the transaction — it is what stops a re-clicked turn-in paying twice), and **terminality is derived, not authored**, which is why `quests.CrossValidate` had to be a two-phase post-load pass. Full ledgers `plan-quests.md` §12 + §13.
- **Prior: quest chunk C1 — the lifetime ledger + kill/talk events** ✅ `d3b89328` 2026-07-30 — lifetime `killCounts`/`talkedTo`/walked-path ledger on the player, `NoteKill` at the `rewardPlayer` fan-out (participation not XP amount), `MobID` keys + the duplicate-id boot guard, L11's five-site death/reconnect stash carry, the `QUEST` cheat. ⚑ Its durable finding: **go:embed rejects a pattern matching nothing**, so the quests embed is `*`, not `*.json`. Full ledger `plan-quests.md` §11.

- **Prior: chunk P — presence-counts XP attribution (Pass 3 item 1, quest D15's prerequisite) ✅ `d45ba07c` 2026-07-30** — a live player with an active aura on, in `presenceRadius` of a mob that is in combat AND already holds ≥1 participant, joins the participant map as one ordinary participant class (P2 closed the NPC-battle watch-farm); harness `chunkP-presence.mjs` 6/6. Open: the PO in-game checklist (§Chunk P plan), per the standing per-bug model. Full ledger `plan-playtest-feedback.md` §Chunk P ledger; the two harness lessons (wait for HUD sync, never fixed sleeps · `Torch` is a passive, the light aura is `Lantern`) are pinned in the verify skill.
- **Prior: the quest design session + its code review** ✅ `8d6440f6` + `e9676836` 2026-07-29/30 (docs only) — D1–D17, 15 landmines, chunks P/C0–C4, **all now built and archived** (`docs/archive/plan-quests.md`). ⭐ The reframe worth keeping: **branching is DIALOGUE-shaped** — objective stages auto-advance off *lifetime* counters, while several rows on several NPCs can move the same stage to different next stages with different rewards, which is why two-NPC turn-ins and the later **camps** concept are content rather than features (D9). The review's biggest catch was L11 (the ledger must join the death/reconnect stash or every death wipes it), and its restatement of D4 is the invariant that outlives the plan: **quest credit = XP credit at the same event, whatever the attribution rule is.** Full record in the archived plan + `MEMORY.md` → project_quests.
- **⭐ Prior design session: backlog §35 (conf duplication, tiers 2–5) — NOW FULLY EXECUTED AND ARCHIVED** — planned `22c51b52` 2026-07-29 → **`docs/archive/plan-conf-duplication.md`** (D1–D3 PO-ruled · 11 landmines · **C1 + C2 ✅ `e7531444` · C3 + C4 ✅ 2026-07-30 — see Last completed**); §35 stays the findings record. **The three rulings:** ① tier 2 = **shrink the three environment confs to their real deltas** — no overlay loader; absent-key-resolves-to-Go-default is already the live pattern (`conf.docker.json` runs on it), and the shrink retires the live-server-drift *class* (`devops/conf.json`'s missing `mob`/`combat` blocks) instead of patching instances; both `conf.default.json` copies stay FULL (the embedded one is the fresh-boot file) · ② unknown conf keys get a **boot warning**, not `DisallowUnknownFields` (`_`-prefix exempt — three files use `_comment`, one stashes `_walkingSpeedPerTick`; a hard fail would block deployed/local boots) · ③ tier 5 = **per-item serve/generate** — `spacedName()` dies in favour of the already-served `/mobs` `displayName`, `ActivationRejection` becomes a real FlatBuffers enum with values pinned at today's ordinals (§28 discipline; today a Go renumber shows the wrong client message with nothing failing), and the pip/ring/tier-rank bit tables + viewport/tickrate get **one shared fixture asserted by BOTH a Go test and a vitest test**. ⚑ **The survey's new finding, and the plan's ordering constraint (L1): the Go defaulting layer is NOT total** — `player.healthGainTick` and `player.walkingSpeedPerTick` are copied raw (`core/gameconf.go:18-19`) with no fallback anywhere, so a conf omitting the player block yields 0-regen 0-speed players; C1 closes that BEFORE any file shrinks, then adds the resolved-equality drift test (`{"game":{}}` vs `conf.default.json` through the full normalization ⇒ "the default conf is a pure restatement of the Go defaults" becomes a red-test invariant). Also surfaced: a fifth tier-5 mirror the survey missed (`Mobs.ts:48` `TierRank`). Deliberate mirrors (mob speed pair L7, the two `combatRegenGraceTicks`, the two 0.2 margins) are **pinned, not unified**. Two stale status lines corrected during the survey — see the code-health bullet.
- **Prior design session: runtime allegiance — ALL THREE CHUNKS NOW SHIPPED** ✅ `074d1a53` 2026-07-28 (docs only) — produced `docs/archive/plan-faction-flips.md` (D1–D5, later D6–D14; landmines L-A–L-O). ⭐ **Its reframe is the durable part:** `SetFaction`'s `^f.Bit()` was not a wrong *value* but an **undefined destination** — correct for `aligned`, undefined for everything else — so the fix was named verbs, not a lookup. Three findings sized it: the restore direction needs no registry (`MobDefinition.AggroMask` **is** the authored mask), there is **no runtime faction registry** to look up anyway (`factions.Registry` is boot-only), and ⚑ the overwrite was **load-bearing** — a registry-derived "fix" would have silently stripped harm rights from every companion, totem and campfire. Full ledger `docs/archive/plan-faction-flips.md`.
- **Prior design session: chunk 3b-ii planned in full** ✅ `8a95e579` 2026-07-27 (docs only) — §6b.9–6b.19, 9 PO decisions **D15–D23**, 6 landmines **L21–L26**; the reframe (the panel is a conversation TREE browser) and every ruling are now **shipped** — see Last completed. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior design session: chunk 3b planned and SPLIT (2026-07-27, docs only)** ✅ `85afcdb1` — 7 decisions **D8–D14**, 5 landmines **L15–L19**, and the split that let **3b-i ship on its own** (`6368b2e5`). ⚑ **3 of its rulings are now superseded by the session above** — D11 and D14 by D18 (the trigger enum is deleted, not authored), D13 by D19 (the reply moves into the panel) — all annotated in place in §6b.0, so read §6b.9 before implementing anything from §6b.0. Its two durable findings: **`E` was already the cooldown-slot-2 hotkey** (L15, moved to `R`), and the client had **no way to know who is interactable**, resolved as one `GameState` field rather than a new message (L16). Full ledger `docs/archive/plan-entity-model.md` §6b/§11.
- **⭐ Entity-model design session — backlog §31 resolved into a plan (2026-07-26, DOCS ONLY, no code)** ✅ `d3904307` — new **`docs/archive/plan-entity-model.md`** ("the Actor model"), indexed in `docs/README.md`. **7 PO decisions · 5 chunks · all of it runs BEFORE step 8.** **⭐ CODE-AUDITED 2026-07-26** (4 parallel line-level sweeps, corrections folded into the plan doc, tagged "code audit" there): the reframe held line-for-line; **4 material drifts fixed** — `DerivedStats` has **6** fields not 5 and `Derived.Resistances` is a **4th player-only stat** (deferred out of 1a, composition-with-authored-resists question recorded) · summon **output already tracks the owner's level LIVE** via `casterPowerScale` (`skills.go:382`) while HP is spawn-frozen ⇒ 1b must decide spawn-vs-live `Level` (recommendation: live) · NPC inventory is **16 entries not 14** (2 legacy proving-grounds Sages, keep-or-drop = open question 6) · L5 resolved: nameplate + tier frame gated **free**, the **health bar is ungated** (`initHealthBar` unconditional, `Mobs.ts:113`) so 3a adds the gate or the PO accepts bars. Also: 1a needs curve-threading into the mob layer (no `curve.Curve` retained, only the float), and `client.fbs`'s union is **unpinned/positional** — append-only by discipline alone. ⭐ **The reframe that did the work:** §31 reads as *"three types should be one"*, but traced against the code it is the inverse — **`Mob` is ALREADY the universal entity, doing five jobs, and it signals which job by lying about its numbers.** 10 defs use `speed: 0` to mean "structure" (plus a dummy `aggroRadius: 0.1` on all 10, and `collisionLayer: 32` / a `resistances` wildcard where that wasn't enough); 4 use `velocity > 0` to mean "follower"; and the one role with **no number to hide behind** — teacher — had to become a separate statless type. **Role is inferred from incidental values instead of authored, and that single defect is behind all five gaps.** **2 findings that shrink the work:** ① **gap 3 is already ONE curve** — `definitions.go:294` and `player.go:253` both call the same injected `curve.Curve.F()`; the only difference is that a mob's is frozen at registry load and **`*Mob` has no `Level()` accessor**. ② **the WoW-NPC target is 3 pillars done, 1 missing** — `api/factions/human_army.json` already ships `hostileTo: ["orc"] + friendlyToPlayers: true`, so **`ArmySoldier` IS the NPC the PO described** (fights orcs on sight, ignores players, unattackable by them); it simply cannot talk, because the thing that talks is a different Go type with no HP. Faction *reaction* and *unattackability* are done; only the **interaction layer** and the **type split** are missing. **Target: one Actor core + optional capabilities** (`Controlled`/`Brained`/`Conversant`/`Owned`/`Rewarding`/`Perishable`, via the already-proven `acting any` structural assert), explicitly **NOT** a god struct — and **`role` and capabilities are ORTHOGONAL**: an NPC is not a role, it is a creature/structure carrying an `interaction` block, so a teaching guard that fights bandits needs no new type and no new branch. **⚑ Sequencing principle on record: Go types are free to change; content JSON and the persisted schema are not** ⇒ authoring vocabulary → persistence vocabulary → Go structure. **The 7 rulings:** ① mob passives scale **identically** to player ones — one formula `effective = base × (1 + derived)`, where `factors.*` **is** the base and passives **are** the multiplier, which dissolves gap 2 along with gap 1 · ② **levels dynamic for every actor** — collapses `SummonPower × owner.PowerScale()` **and** `RaiseMaxHealth` into `Level = owner.Level`, and unblocks the parked `Autoattack` · ③ **NPCs merge in this pass**, `model/npc` deleted (content is at its smallest it will ever be — 14 NPCs, one line each) · ④ **only live players persist** ⇒ step 8's schema is barely constrained; it just needs the character record written in **Actor vocabulary** (`level`/`faction`/`loadout`/`spellbook`/`xp`), not as a player-struct dump · ⑤ conversations start with an **interact key in range** — no world targeting, so the GDD "no targeting" pillar stands; proximity-opens-panel rejected because NPCs standing among mobs would pop panels mid-fight · ⑥ define the **full** interaction container (nodes/conditions/options/**typed grant list**) but author only the degenerate one-node `teach_skill` case ⇒ gossip trees, quest offer/accept, vendors and the journal become later *additive content*, not a schema migration · ⑦ **all three chunks, then step 8.** **Chunks:** **1a ✅** one derived-stat formula (three factor methods on `DerivedStats`, both model types call them — `acting any` is the wrong tool here, they know their own type) + `game.mob.walkingSpeedPerTick` + `Level()` on Mob — **no numbers move, and sim byte-identity IS the acceptance test** · **1b ✅** dynamic levels + summon-scaling collapse — **numbers DO move**, harness before/after + PO sign-off · **2 ✅** authored `role` (`creature`/`structure`/`follower`), retiring the two inferences and the 10 dummy aggro radii — **and the sim harness, which read `speed: 0` as *turret*: new landmine L9** · **3a ✅** NPC merge + interaction schema — `model/npc` deleted, `zone.npcs` folded into `spawns`, health bars on NPCs **accepted** (so L5 needed no gate) · **3b** interact verb + dialogue panel — **PLANNED 2026-07-27 and SPLIT into 3b-i (the verb) + 3b-ii (the panel)**, see the ⭐ banner above. **⚑ 3 landmines worth knowing before anyone touches it:** mob speed is `0.055 × factors.speed` vs the player's `0.05`, so a naive "convergence" makes **every mob in the game 9 % slower** — mirror the name and unit, keep the value (the `game.mob.healthGainTick` precedent); **`SetFaction` nukes the authored aggro mask** (`mob.go:559` → `^f.Bit()`, "aggro everything that is not me") — audit: **TWO callers, not one** (`spawnSummon` + campfire placement `aurad.go:157`; the `mob.go:554` "first caller" comment is stale), inert in effect today but **directly blocking** any charm / side-switch / quest-turns-hostile; and **NPCs change wire path in 3a** — they ride the *Resource* path today (`Resources.ts`), as Actors they ride the *Mob* path — audit resolved the exposure: nameplate (`combatTarget`) and tier frame (rank 0 invisible) are gated **for free**, only the **health bar is ungated** and needs a 3a gate (screenshot still required). 5 more landmines + the per-chunk test strategy in the plan doc §8/§9. **Knock-on: Pass 1a.2 (resource costs) is UNBLOCKED** — ruling ① removes the three numeric player-only stats (`Derived.Resistances` stays player-only by deliberate deferral, is not a `validStat`, and does not block it), so its cost-reduction passive becomes an ordinary sixth `validStat`; re-schedule it once Chunk 1 lands.
- **⭐ Round-6 PO design session — 3 topics, all resolved (2026-07-26, DOCS ONLY, no code)** ✅ `2e2e2809` — full write-up `docs/plan-playtest-feedback.md` §Intake round 6 (4 items), the resource ruling in **`docs/gdd.md` §3 Consumption**, the collision analysis in **`docs/backlog.md` §34**. ① **Resource costs — the design intent behind the numbers, which is what Pass 1a.2 was actually missing.** PO ruling: the resource's double meaning is *the design* — one value is both "possibility of actions" and "time left to die", and spending survivability to gain power **is** the combat game. **⚑ Two constraints now bound every cost value: a permanently FREE baseline** (the base damage aura, at any resource level — *"there is never no option left"*) **and everything above it gated behind spending.** ⚑ **The free baseline REPLACES the 1-HP clamp as the death-spiral protection** — `sys/skills.go:706` already stops a cost killing its caster, but relying on it is wrong under this ruling because it makes the ability *silently stop working*; the clamp stays a backstop. Not new scope (GDD §3 + round-2 decision 2 already said costs go on auras and cooldowns) — `selfDamageHP` is **live on `HealParams` today** and the tooltip already renders `Costs you: …`, so 1a.2 generalizes a live field. **4 heal rules must survive that generalization** (never-kill clamp · cost rides `casterPowerScale` or it goes free as the pool grows ~26× · mobs pay nothing · GOD skips) and a **cost-reduction passive is engine-new: the sixth `validStat` and the first that modifies an *input*.** ⚠ **Sequencing deviation on record** — PO wants costs first / retune later, but Pass 1b will re-touch every number authored in the cost pass. ② **Orc Warlord grunt waves never reach the fight** — diagnosed as **distance-vs-sensor, not behaviour**: wave-mouth→warlord-home is **7.57 units**, `OrcGrunt.aggroRadius` is **5.4**, and with no waypoints/wanderRadius the grunt walks back to its spawn point and stands there. **3 options sanctioned** (raise the radius ~9 · move the anchor in the editor · seed threat at spawn, ~6 lines in `warlord.go`); spawn-at-boss and a new scripted-move seam **explicitly NOT taken**. ⚑ **Threat-seeding is marginal ALONE** — grunt speed is ~1 unit/s, so it closes into its own sensor in ~64 ticks against a 90-tick leash, and that breaks if the anchor ever moves out. ③ **Collision ✅ DECIDED: mob-vs-mob SOFT SEPARATION** — extend `blockerRepulsion` to nearby mobs at **low weight**, no hard blocking, **no player↔player** (rejected against GDD §9 "no griefing by design"). The gap it closes: `steering.go:87` queries `AppendCircleStatics` — **statics only** — so mobs have zero awareness of each other, which *is* the clump. ⚑ Watch the `steerSide` detour latch: tuned against *stationary* blockers, so low weight is what keeps mob repulsion from tripping it. ④ **NEW BUG surfaced while chasing ③** — `selectTargets` (`sys/targeting.go:108`) has **no target persistence at all**: it rebuilds and re-sorts every tick, so a `maxTargets: 1` aura re-picks ~30×/s and **smears damage across a clump instead of killing anything**. Fix is **per-selector** stickiness (right for `nearest`, wrong for `lowest_health` heals). **⚑ Hard collision would NOT have fixed it** — ~19 wolves still fit inside a level-1 Damage aura with collision fully enforced, which is why ③ does not close ④. Not scheduled, blocks nothing.
- **Prior: §35 C3 + C4 — the pins and the mirrors, closing the conf-duplication plan** ✅ `e7e14c71` 2026-07-30, **PO-VERIFIED IN-GAME 2026-07-30** — Go-literal pins vs `conf.default.json` (red proven) · `spacedName()` deleted for the served `displayName` · `ActivationRejection` a pinned wire enum (⚑ import the enum FILE, not the `AuraApi` barrel — the barrel breaks vitest from client-data) · `api/shared-constants.json` asserted by both languages (⚑ const enums cannot be enumerated). The pass re-raised the summon-tooltip intake item (`plan-playtest-feedback.md` §Rolling filler). Full ledgers `docs/archive/plan-conf-duplication.md` §7.
- **Prior: §35 C1 + C2 — total defaulting + shrink-to-deltas · unknown-key boot warning** ✅ `e7531444` 2026-07-29 — full ledgers `docs/archive/plan-conf-duplication.md` §7; the durable findings: a *rounded* JSON restatement (`mob.healthGainTick`) split the fleet at float32 in production's favour, absent `server.port` bound a random port, and `encoding/json` applies case-insensitive matches so the unknown-key walker must accept them.
- **Prior: R4 — the last two interact-badge defects** ✅ `a4225e8b` 2026-07-29 — closed and **archived** `plan-entity-model.md`: the badge now measures `actualShape` while riding `Mob.shape`, and a fading corpse withdraws its badge. Includes the first full 15-harness sweep (4 rotten repaired, 1 deleted); the venue/cluster/atomic-probe harness gotchas live in the verify skill + `docs/archive/plan-entity-model.md` §11.
- **Prior: Swift becomes a movement cooldown** ✅ `a29fe986` 2026-07-29 — the empty movement slot filled with a new `speed_burst` effect type, the movement twin of `tick_rate`; **`Buffs.MovementFactor()` is the one place the movement axis composes**, and it made **players slowable for the first time** (`SlowFraction()` had exactly one reader) and took **the last `applied_effects` pip bit**. Full ledger `docs/plan-playtest-feedback.md` §Ledgers.
- **Prior: `plan-faction-flips.md` chunk 3 — charm** ✅ `153c0032` 2026-07-29 — a wolf can be told whose side it is on: the nearest eligible mob joins the player side as a full companion, keeps its own level, credits its kills to the charmer, and reverts to its authored allegiance on expiry or when the charmer leaves the world. Two skills, `CharmBeast` (id 63, wildlife) and **`BindElemental`** (id 64, `charm-elemental.json`, elemental) — **the PO tuned the second differently on purpose**, and it needed zero Go changes, which is L-L's proof that both the faction list *and the numbers* are data. Its durable finding: **one accessor answering several questions is invisible until a caller needs a different answer** — `owner` was really three (*whose level am I* / *who gets my credit* / *whose signals do I follow*), now split, with the stat path byte-identical across a charm. ⚑ **The pet is focused by its former packmates and can die in ~8 s** — D9 working as ruled, PO-accepted 2026-07-29, and the first thing a playtester will report. All three of the plan's skills got in-world teachers in `3b1b3ef6` (Hermit @10, Emberkeeper @15), so **none is cheat-only any more**. Full ledger `docs/archive/plan-faction-flips.md` §9.
- **Prior: `plan-faction-flips.md` chunk 2 — calm, the disengage cooldown** ✅ `216f733b` 2026-07-29 — a wolf can be told to **stop**: no faction flip, it drops the live aggro link, takes everything in the radius, and **any** damage ends it (the calmer's own aura included). Its two durable findings: **calm is a `Buffs` payload, not a `Mob` field** (the pip is derived from the store, and `buffPayload` is closed so a new kind cannot compile without deciding its pip), and **L-O — skill-level JSON has no `DisallowUnknownFields`**, so a faction allowlist must be **mandatory** per effect type or a typo reads as unrestricted. Also built the **D8 seam** chunk 3 reused unchanged. Full ledger `docs/archive/plan-faction-flips.md` §9.
- **Prior: `plan-faction-flips.md` chunk 1 — the allegiance seam** ✅ `ec73634e` 2026-07-28 — **`SetFaction` is GONE**, replaced by three *defined* verbs (`Align()` / `RevertFaction()` / `EnlistUnder(model.Allegiance)`) plus a reflection tombstone guarding the general-setter SHAPE from returning under any name. Its finding is **L-N**: the summon path has a mob-caster leg that is unreachable in content but **pinned by a shipped test**. The durable lesson: **a faction and its aggro mask are a pair, and every place that takes one without the other is a defect.** Full ledger `docs/archive/plan-faction-flips.md` §9.
- **Prior: pre-accounts hygiene SESSION 2 — H1a, the chase margin** ✅ `50a1e5c9` 2026-07-28 (backend, 3 files) — the part priced as *"the only one that moves a number"*. ⭐ **It moved nothing** — two Go defaults disagreed 4× (`gameconf.go` 0.2 vs `mob.go`'s fallback 0.05, copied into `sim/world.go`), both now 0.2, and **TTK 6.67 s / TTD 8.70 s STAND** as the standing baseline. ⚑ Its durable findings: **no battery scenario ever makes a mob approach**, so the drift was real *and* latent (the harness was one `-distance 3` away from modelling a different game); a test taking a default **pins a number nobody chose**; and **three guardrail log lines reorder run to run** (Go map iteration — diff that output raw and it looks like a regression). Deliberately NOT unified: the two independent 0.2s are backlog **§35 tier 3**, still open. Full ledger `docs/archive/plan-pre-accounts-hygiene.md` §11.
- **Prior: pre-accounts hygiene SESSION 1 — H2 → H1b/H1c → H3 → H4** ✅ `c183ce12` 2026-07-28 (backend + frontend + wire + config + docs, 25 files) — the four parts that were supposed to change nothing, run together so *"everything else is byte-identical"* stayed a live check; it held. The L12 collision guard (constrains **authoring**, not policy), the embedded config hand-synced + a drift test, both bare `33`s → `SERVER_TICKRATE`, and the wire prune (`Vec3f` + `Resource.capacity`/`stock`, `aabb` renumbered and pinned). ⚑ Its durable finding: there are **five** tracked conf files and the fifth is `devops/conf.json`, the live server's — **also drifted** (no `mob`/`combat` block ⇒ live runs on Go fallbacks), recorded not fixed. Full ledger `docs/archive/plan-pre-accounts-hygiene.md` §11.
- **Prior: post-review chunks R1 + R2 + R5** (2026-07-28, backend + frontend + content + docs, 40 files) ✅ `759ddfb6` — a **six-dimension code review of the whole entity-model range** (`cf9a10c7~1..HEAD`) ran first; its findings are `docs/archive/plan-entity-model.md` **§8b**, the chunks are **§10b**. ⭐ **The architecture held, measured rather than asserted:** `Mob` went **66 → 67 fields** while absorbing an entire type (`model/npc`, −122 lines) and the Conversant capability added **zero** data (it is a pointer chase into the shared definition, so 500 wolves carry no conversation bytes); there is genuinely **one** derived-stat implementation with six call sites and no entity-kind branch; `acting any` did not spread (three sites, all pre-existing); and **role and capabilities are orthogonal under test** — a **talking follower needs zero new code**, traced end to end. **R1 — pin + prune (zero behaviour change):** all four FlatBuffers unions **pinned** (`ClientMessageBody`/`ServerMessageBody`/`AnyEntity`/`Player`), matching the §28 enum contract — regenerating **both** binding sets gave a **zero diff**, which is the proof the window was still free; ⚑ **this corrects the 3b-ii banner's "L16 unexercised" claim** — true of the server union, but **3b-i DID extend `ClientMessageBody`** without pinning either. Also `RoleNames()` over the one table (**`follower` was missing from the simharness explorer**, a live drift under a comment claiming no second list could exist), the dead `skills.Registry` thread out of `world` (one vestige wearing four hats: a signature, an import, a 4-method fake, 35 call sites), `DisallowUnknownFields` on the mob loader — **the last content loader without it**, which is why a retired key had needed a hand-written tombstone — `MaxHealthFactor()` → **`PoolFactor()`** (two same-named methods meant `1+passive` and `curve×passive`; `sys/skills.go` already schedules `*Mob` satisfying `healCaster`, and the obvious implementation would have silently dropped the curve), 3 unreachable `blockedLine` strings, the dead frontend `maxHealthPerOwnerLevel` tooltip branch, 8 stale comments, and both content manuals. ⚑ **`DisallowUnknownFields` found a live bug on its first run:** `catalog_test.go` authored `baseMaxHealth` at the ROOT where it is declared inside `factors`, so three fixture mobs had been loading with a zero base pool. **R2 — combat withdraws the OFFER:** `sense()` stamped the badge unconditionally while `refreshConversations` dropped the session on `InCombat`, so for the whole ~3.3 s window the key cap stayed lit over an actor that refused to talk — and the key is edge-triggered, so holding it does not retry. ⚑ **The fix belongs in the offer, not the client:** `handleInteracts`' own contract is that range enforcement is *one comparison against the value the client was told*, and combat was a second gate violating it — withdrawing the offer needs **zero client change**. Ambient lines stay ungated on purpose (D18 — the crier still calls out mid-fight). **This is the direction the harness reports as SKIP** (no cheat can stamp player combat), so the 3 Go tests are the only eyes on it. **R5 — summon output live:** the last half-frozen term — 1b made the pool and f(L) read the owner live but left `SummonPower` stamped at spawn, so a companion outliving a ding stayed permanently behind an identical one summoned a moment later, against ruling ②. `Mob` now stores the authored `powerPerOwnerLevel` **rate** and evaluates it against `Level()`; `SpawnParams.PowerAt()` **deleted** — computing the multiplier at the spawn site is what froze it. ⚑ **A summon SPAWNED at a level is unaffected** (only 1.40 → 1.45 across a 9→10 ding), which is why every battery stays byte-identical and only a targeted test can see it. **Verified:** `go build`/`vet`/`test ./...` clean, guardrails + alloc `-count=2`, frontend typecheck + **44 vitest** + prod build; boot **both ways** 0 errors 0 warnings 0 panics — 83 skills/15 factions/64 mobs/777 props/485 spawns/5 campfires. **⭐ Sim battery BYTE-IDENTICAL after each of the three chunks** (default · `-chain` · `-levels` · `-content ../api` roster; TTK 6.67s / TTD 8.70s), diffed against a pre-R1 worktree. **R4 shipped 2026-07-29** and closed the plan — the two review-found badge defects (anchor measured off the whole shape subtree; badge riding a fading corpse), see Last completed. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 3b-ii — the conversation panel ✅ `ef4355f1` 2026-07-28, **talking became a TREE you browse** — `present()` (pure) + `applyGrant()` (one grant, validated on its own merits) replaced `evaluate()`, the whole personalised tree rides `GameState`, and clicking a row teaches **that row and nothing else**. Harness-verified 28/28, 1 deliberate SKIP (*being hit closes the panel* — no cheat can stamp player combat; **R2 has since fixed the other half of that same gap**). ⚑ Its durable trap: the panel re-rendered ~30×/s because the tree is re-sent every tick by design — fixed with a view-signature check, and the R1 review re-verified that check is sound. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 3b-i — the interact verb ✅ `6368b2e5` 2026-07-27, **talking became something a player DOES**: the sensor only *offers* the conversation (`GameState.interactable_entity_id`) and an `Interact` message accepts it, so range enforcement is one comparison against the value the client was told. Harness-verified 15/15. ⚑ Its two durable traps — **L20 stamp-before-drain** and *a fixed walk duration cannot reach these actors* — both still bind. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 3a — the NPC merge ✅ `ba124ceb` 2026-07-27, **`model/npc` is GONE** — a teaching NPC is an ordinary actor carrying an `interaction` block, placed as an ordinary spawn; the zone editor's NPC mode deleted (teachings are JSON-only content now), 14 defs + faction `townsfolk`, −800 lines net. **⚑ Its one-NPC pilot found a latent wire gap:** `Mob.radius` had been in `server.fbs` since the beginning and the server never wrote it — invisible for the life of the project because every mob sprite sizes from `GraphicsConfig`, until NPCs, which size from the wire. Harness-verified 6/6 + `npc-portraits.mjs`. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 2 — the authored `role` discriminator ✅ `0be771bd` 2026-07-27, **the chunk where a mob stopped implying what it is with a stat value**: `creature`/`structure`/`follower` authored and validated through one `mobs.ParseRole` table, retiring the `speed <= 0` = turret and `owner != nil && velocity > 0` = follower inferences plus the 10 dummy `aggroRadius: 0.1`. Sim battery byte-identical incl. the kite row (L9 held). **its FOLLOWER half is verified** (harness `chunk2-follower.mjs`, PO in-game 2026-07-29) (see Open PO checks). Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 1b — dynamic levels + the summon-scaling collapse ✅ `ee01ccdb` 2026-07-26, **the chunk where numbers move**, priced and PO-signed before the first edit — `MaxHealth()` fully derived (`baseMaxHealth × f(Level) × MaxHealthFactor()`), `Level()` reads the OWNER's level **live**, `Factors.MaxHealth` → `BaseMaxHealth`, `MobDefinition.PowerScale` + `RaiseMaxHealth` + `maxHealthPerOwnerLevel` all deleted. **Summon OUTPUT does not move at all** (every summon is `curveLevel: 1`); the delta is **HP, large by design** — a Companion goes 118 → **1605** at L30, a constant ~60 % of a same-level player's pool. Sim battery + preset roster byte-identical. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** `plan-entity-model.md` Chunk 1a — one derived-stat formula ✅ `cf9a10c7` 2026-07-26, **sim battery byte-identical** (TTK 6.67s / TTD 8.70s) — three factor methods on `DerivedStats` that `*Mob` now reads too (the 3 latent player-only stats closed), plus `game.mob.walkingSpeedPerTick` (**0.055 preserved**, L1 held) and `Level()` on `*Mob`. Full ledger `docs/archive/plan-entity-model.md` §11.
- **Prior:** chunk B — mobs now push each other apart while they move ✅ `8b045395` 2026-07-26, **PO-VERIFIED IN-GAME** (*"feels much better"*) — soft separation at `mobSeparationWeight` 0.45 [PLACEHOLDER] via new `phy.Space.AppendCircleDynamics`, kept out of the head-on latch, perpendicular fade so a chase does not weld the pack into single file. **⚠ Known limitation, by design: a STOPPED mob does not separate** — a settled ring keeps its arrival spacing; the tangential settle nudge is offered, not taken. Full ledger: `docs/plan-playtest-feedback.md` §Round-6 chunk B ledger.
- **Prior:** chunk A — a lost WebGL context now says so ✅ `6c8bde2e` 2026-07-26, **PO-verified in-game 2026-07-29** — one `webglcontextlost` listener on the **world** canvas → `[webgl] world context lost` + the red banner; **detection only, §29 stays open**. ⚑ A clean boot measures 5 GL contexts / 2 deliberate probe losses / **0 warnings**, so it does not cry wolf — re-run `ctxloss-warning.mjs clean` after any boot-path change. Full ledger `docs/backlog.md` §29.2.2.
- **Prior:** round-5 batch ✅ `f06b2161` 2026-07-26, **PO-VERIFIED IN-GAME** (all 6 items; the drummer's 50 %-speed retreat "reads fine" ⇒ decision 4's authored speed stands) — shield-aura tick indicator (`EffectTypeShieldAura` joins `HasVisibleTickCadence`, no frontend change) + pacifists flee when attacked with nobody to support (`modeFlee` below support, existing `moveAwayFrom`). Full ledger: `docs/plan-playtest-feedback.md` §Round-5 chunk ledger. **2 gotchas worth keeping:** `highestThreatTarget()` **prunes dead rows as it reads** (resolve once per tick), and **campfires/totems are pacifists too** — they reach `modeFlee` but are inert both ways (`auraAlwaysOn` early-return + `moveAwayFrom` refuses a zero-velocity mob), pinned by test.
- **Prior:** ⭐ **all three then-pending chunks PO-VERIFIED IN-GAME** in one session (2026-07-26): **round 3** `03b152f4`, **round 4** `eaae2e69`, **rolling-filler batch** `dab4dae0` — every checklist item passed; `RallyDrummer` pacifism **accepted** and the whole-point tooltip HP rounding **signed off**. That session raised the 2 round-5 items, both now shipped and verified above. Full intake + the 4 design decisions: `docs/plan-playtest-feedback.md` §Intake round 5.
- **Prior:** rolling-filler batch — 4 independent bug fixes ✅ `dab4dae0` 2026-07-26, PO-verified in-game (`DAMAGE <pct>` fraction-of-`MaxHealth` · floating numbers suppressed in unlit darkness, tested at the **entity** position not the label's · `CLEAR_MINIMAP_ON_DEATH` off + the own-icon leak it exposed · Ctrl +/− suppressed, `Ctrl+0` kept). Second use of the vitest infra. Full ledger + the reusable harness notes: `docs/plan-playtest-feedback.md` §Rolling-filler batch ledger.
- **Prior:** playtest round-4 — ability tooltips now scale with character level ✅ `eaae2e69` 2026-07-26, PO-verified in-game (whole-point HP rounding signed off). Brought the **first JS test infra** into the repo (vitest + jsdom; landmines under Frontend tests below). Full ledger: `docs/plan-playtest-feedback.md` §Round-4 chunk ledger.
- **Prior:** **playtest round-3 chunk — healer combat state + role-as-loadout DONE** (2026-07-25) ✅ `03b152f4` — **✅ PO-VERIFIED IN-GAME 2026-07-26** (all 4 checklist items; both ⚠ judgement calls below resolved **as shipped**). 11 files. `Mob.InCombat()` returned exactly `aggroTarget != nil` — it *was* the bug; now + a damage-recency window reusing the player's `combatRegenGraceTicks` name AND value (§31 convergence). `healer.go` → `support.go`, the latched `seekHealer` type flag gone, roles derived from aura **categories**. **2 ⚠ PO-judged 2026-07-26, both KEPT:** ① `RallyDrummer` carries `shield_aura`, so under the loadout rule it is a **pacifist** and no longer chases players — PO: shielding its own squad "seems fine"; ② **every** mob now stops regenerating ~3.3 s after any hit, so hit-and-run whittling works on anything — accepted. Full ledger: `docs/plan-playtest-feedback.md` §Round-3 chunk ledger.
- **Prior:** **code-health triage session — 2 chunks DONE** (2026-07-24, ledgers in `docs/backlog.md` §30/§27.3.3 and §27.2.3/§25 B) ✅ `f095514a` + `2ec03ee7` — `layers.placeables` vestige prune + `tickInterval` hard-fail, and 3 hardcoded Go constants → conf knobs with no behaviour change (`game.mob.healthGainTick` + a new `game.combat` block; ⚠ **authoring 0 restores the default, it does not disable** — open PO question). Spun off **new backlog §31**.
- **Prior:** §28 item-system removal **COMPLETE** — Chunk 3 wire-enum prune ✅ `8ed4ff4c` (explicit permanently-pinned enum values ⇒ no future removal can renumber a survivor; new `NpcPlaceholder` art; a PO-requested plan audit caught a self-deleting dev-only class being shipped as the NPC fallback sprite), Chunk 2 frontend scaffolding ✅ `2f933634`, Chunk 1 backend registry ✅ `b9d01d33`, all 2026-07-24 and PO-verified. EntityType name-fallback validation §27.2.1 ✅ `c3938be7`. Full ledgers: `docs/archive/plan-item-system-removal.md §13`, `docs/archive/plan-entitytype-validation.md`.
- **Recent chunks (newest first; full ledgers in the plan docs):** dead resource+placeable+decay prune §26 FULLY DONE ✅ `ee9d42e9`+`a2ab90b5` (Chunks 1+2 — emptied the item registry to `None` ⇒ §28 removal now trivial; Chunk 2 also fixed a Chunk-1 frontend-build regression — **rebuild frontend AND backend after content deletions**; `plan-resource-decay-prune.md`); render-jitter buffered-interp fix ✅ `0e504c22`/`8a29a75c`/`c5064732` (`plan-render-jitter.md`); input-jitter held-state fix ✅ `cb7f011f` (`plan-input-jitter.md`); unlock source attribution ✅ `2bfee286` (`plan-unlock-attribution.md`); idle-loop alloc fix ✅ `fe0044d0` + day/night DEACTIVATED ✅ `e648ab88` (`plan-intermission-triage.md`); playtest-1 Passes A/B/C ✅ — **plan fully executed** (`plan-playtest1-feedback.md`); F&F deploy LIVE ✅ `a7a2267d` → `https://aura-game.duckdns.org/` (`plan-playtest-deploy.md`); content pass C1–C8 + rebrand step 7 complete. Earlier chunks: roadmap.md "Execution order" + the plan-*.md §13 banners.
- **✅ Open PO in-game checks — ALL CLOSED 2026-07-29.** The PO's single deferred in-game pass happened: *"checked in-game, no issues found so far. Mark all as done. Future issues will be raised as individual bugs."* ⚑ **So the standing "⏳ PO check pending" state is retired across the board** — chunks 3a / 3b-i / 3b-ii, chunk 2's follower half, calm + charm, chunk A's WebGL banner and chunk B's soft separation are all signed off, and **nothing carries a pending-verification flag any more**. The working model from here is per-bug, not per-chunk: a new chunk gets its harness run and ships; anything wrong shows up later as its own reported bug. The harness evidence below stays on record as what was proven mechanically. **(Historic, for the reasoning:)** ⚑ **PO ruling 2026-07-27: all three were driven headlessly and pass; the PO's own in-game pass happens ONCE, after every open chunk has landed** — so do not re-ask per chunk, and do not treat these as blocking. **What the harness proved** (`.claude/skills/verify`, all runs 0 console errors / 0 webgl context losses, boot 83 skills / 15 factions / 64 mobs / 777 props / 485 spawns / 5 campfires, 0 errors 0 warnings): ① **chunk 3a, the NPC merge** (`ba124ceb`) — `chunk3a-npc-merge.mjs` 6/6 (Farmer teaches Harvest with the "Taught by: Farmer" banner · the Emberkeeper's ordered walk grants Torch@1 and **stops** at Ignite@7 · the grant-less ForestSign still speaks), plus the new **`npc-portraits.mjs`**: Farmer/Hermit/TownCrier/Emberkeeper all render at correct size (the scale-`[0,0]` wire gap has not returned), **health bars present** (D3 as accepted), **no nameplates** — while `Turnip 1`/`Boar 2`/`Stag 1` plates render in the same frames, so the gating is proven by an in-picture control rather than assumed. ② **chunk 2's FOLLOWER half** (`0be771bd`) — the gap with no eyes on it is now closed by the new **`chunk2-follower.mjs`**, which drives the aura panel with real clicks and exercises the summon path end-to-end for the first time: 6/6, companion gap **0.8 → 1.44 units** across a **9.2-unit** walk (it trails), and it **fights** — `−8`/`−6` on a Wolf and a Boar with its aura ring lit and **XP 0 → 70/300** while all three aura slots are Empty, so none of that damage was the player's. ③ **chunk 3b-i, the interact verb** (`6368b2e5`) — the new **`chunk3b-interact.mjs`** 15/15: approaching the Farmer teaches **nothing** and says nothing (the L18 check, asserted on the spellbook because a missing guard would still produce a plausible bubble), the `E` badge lights, the key teaches with the `Taught by: Farmer` banner, walking away puts the badge out, returning re-lights it and a second press skips the known grant to speak the lore line, the Emberkeeper's ordered walk runs on one press and stops at the Ignite@7 gate, and **`R` fires cooldown slot 2 while `E` no longer does**. ④ **chunk A's WebGL banner** (`6c8bde2e`) — `ctxloss-warning.mjs` PASS both ways: `clean` **0** warnings (it does not cry wolf on a normal boot, where pixi deliberately loses 2 probe contexts), `forced` emits the exact `[webgl] world context lost` line + the red banner. **What the harness deliberately does NOT cover, and is what the PO pass is FOR:** it runs headless at ~10 FPS, so it proves the mechanisms work and says nothing about whether anything *feels* right; and the **zone editor is untouched** (D1 — NPC mode gone, NPCs placed with the spawn tool, teachings JSON-only), which is manual. ⚑ **Two harness bugs found and fixed in that session, both of which FAKED a product failure** — pinned in the verify skill: a killed player nulls `Character.plate`, which is the documented way into the scene graph, so a mid-run death reads as a crash in the feature under test; and **`Cam Boundaries: On` clamps the camera at map edges, so the player is NOT drawn at screen centre** — a screen-space distance metric reported a correctly-following companion as fleeing (84px → 638px) and would have shipped a false regression report.

- **⭐ OPEN-QUESTIONS SWEEP — every open PO question in the live docs, ruled in one session (2026-07-29, DOCS ONLY, no code).** The whole standing backlog of *"waiting on the PO"* items was collected and put to the PO as choice prompts: **14 rulings**, plus 2 questions that turned out to be already answered by shipped work. **What it closed structurally:** `plan-faction-flips.md` has **no open items left** and is **archived**; `content-zone1.md`'s content-pass list is **retired**; the standing ⏳ in-game-check state is **retired** (see the bullet below); and **Passes 1a, 1b item 3 and Pass 3 item 1 are unblocked**. ⭐ **The two rulings that are bigger than the question asked:** ① *"should the charm pip show duration?"* was answered **wider** — the pip stays, but mob and player presentation needs **one comprehensive rework** (buffs/debuffs **with durations**, allegiance, faction, cast bars), now **`backlog.md` §39**, which also absorbs the six independently-anchored overlays the R4 badge bug lives in ⇒ **do not invest further in per-effect overlay art before it**; ② *"what are the raised skill caps?"* was answered **uneven, per-skill** — but with the note that the **skill level system itself needs reworking**, possibly folding in the augmentation concept (a damage aura gains slow *or* heal at level 10, player's choice), so **`backlog.md` §37 is now coupled to the caps ruling** and Pass 1a should author per-skill ceilings **without treating them as final**. **The other 12:** charm's ~8 s pack life **accepted as the price of D9** (numbers stay [PLACEHOLDER], re-opens only on a playtest complaint) · **R4 scoped down** to exactly the two review-found badge fixes, no longer waiting on a polish list (**shipped 2026-07-29**) · **Swift → a cooldown**, not deleted, not a weakened passive (it is the answer to the empty-movement-slot finding) · the freed drop slots **stay empty**, but **Wolf keeps its first-drop moment, now dropping Swift-as-cooldown** · XP credit = **presence counts** (an aura that never procced still earns; the only rule that cannot deny a support player XP by luck) · **damage types & resistances are authored WITH the Pass 1 retune** — today **no skill authors a `damageType` at all**, so every hit in the game is physical · shields trigger on **ally-in-combat**, not most-wounded (⚑ this **splits the support mode's entry condition by aura category**) · **no mode-thrash hysteresis** until flicker is actually observed · the `% of max HP` tooltip **keeps the percentage alone** (⚑ **REVERSED 2026-08-01 by feel-pass F6** — costs now render in **absolute Focus**; see R1) · **target stickiness stays unfixed** — the re-open trigger is **cost, not feel** (⚑ the damage-smear half is unchanged and is what a playtester would actually report) · soft separation **closed**: settled-ring spacing accepted and **`mobSeparationWeight` 0.45 stands as the value**, the tangential settle nudge **withdrawn** · conf `0` **means default, not disabled** (an explicit flag if a knob ever needs switching off) · crit stays **players-only** · **wiki generator KEPT**; replacement art and the eventual domain **both parked until v1**.
- **⭐ R4 RAN 2026-08-03 → `docs/archive/plan-downtime.md`** ✅ `5b4ee103` (design session, no code; 9 PO rulings): a new **baseline-utility** class — Recall + a placeable **mini-campfire** as always-present HUD buttons outside the cooldown slots and the spellbook; Recall free with **no cooldown** (the 10 s interruptible cast is the only brake, `recall.json` retired); charges refill at any real campfire, cap scales with level, **purely per-session — §32 answered: store nothing**. **C1 ✅ BUILT + DEPLOYED 2026-08-03 (see Last completed — it also closes round-8 item 1); C2 (the camp) is next.** The bullet below is the pre-session record.
- **Prior context — R4, the downtime design session (no code), then step 8a.** With N1 → N5 built (banner above), `plan-resource-costs-feedback.md` has exactly one open item: **R4 — the downtime agency loop** (*"downtime wasn't really fun and there was quite a lot of it"*), PO sketch = campfire-granted food charges, **raising out-of-combat regen explicitly rejected**; couples to `backlog.md` §32 and §36, is designed against a world where **First Aid is free**, and its direct input is the feel-pass finding that free First Aid *"does not remove downtime enough"*. ⭐ **R4 WAS WIDENED 2026-08-02 (intake round 8 item 1): it also owns a FREE, BASELINE RECALL** — PO ruling that *the way back to safety and the way back to fighting shape are one loop*, designed together, **both held by a level-1 character from the start**; out-of-combat regen may now **scale with level** (*"in charges or otherwise"*) without reversing the rejection of simply raising it. Recall today is `costFractionOfMax: 0.05` + a 10 s interruptible cast + a 5 min cooldown, and it is **taught** by the Town Crier and the Wanderer — whose `teachings` nodes hold *only* Recall, so removing the row empties both and the empty-destination prune then deletes their "Teach me something." rows entirely. ⚑ **§32 is no longer a "wants schema room in 8a" note** — 8a is code-complete, so a charge count is a **migration**, and R4 should answer *does a charge survive death* rather than defer it. Constraints + the five open questions: `plan-resource-costs-feedback.md` §2.3. **Parked with their measurements** (`docs/archive/plan-feel-pass-2.md` §6): the Damage aura's dead levels + blandness AND the catalog guard test (backlog §37 — it goes red on Damage the moment it is written) · the fire line's upside over free Damage (a next-play item) · a tutorial for entry pricing (no tutorial system exists; bigger call than a chunk) · Bloodthirst's overhead pip (§39). ✅ **The N-session owes nothing any more:** the PO by-eye pass on all five chunks PASSED 2026-08-03 (with the note that cost balancing continues over time), and the `chunkC4-quests` re-run was settled 2026-08-02 — 37+1 SKIP twice, §Round-7 ledger. **Round 7's open-item queue is CLEARED except one** (session 2, 2026-08-02, banner above): items 5/6/7/8/10/12 all shipped or ruled — only **totem tooltips (3, third raise)** remains, still needing catalog/data design (the `/skills` catalog serves only the caster's `spawn` effect).
- **⭐ OPEN INTAKE — round 8 (2026-08-02, against the accounts build): 3 items documented — item 3's BUG half ✅ SHIPPED 2026-08-03 (see Last completed), the rest open** (`plan-playtest-feedback.md` §Intake round 8). **Item 1 — free/baseline Recall — is not a fix, it is R4's scope** (bullet above). **Item 2 — quest-related extra info readable after the quest is done** (*"lampless traveller, you can still ask where the kobolds are"*): ⭐ **the window is the turn-in itself** — `root_lit` IS correctly gated to `completed` and a *fresh* talk opens on it, but the turn-in row carries no `next`, so the player **stays standing on node `lamp`** (`ConversationModel.update()` only re-enters at `entryNode` when the actor changes or the current node vanishes — `lamp` is unconditional and never vanishes), both quest rows correctly gone via `CanApply`, and the pure-navigation *"Where do they nest?"* row still there. ⚑ **Quest GRANT rows have a visibility rule (`CanApply`); pure info rows have none, and the vocabulary cannot express one** — conditions are AND-ed, the only sentinels are `not_started`/`completed`, and there is no negation, so *"while this quest is running"* today means duplicating the node once per running stage. **Recommended: a third sentinel `running`** (`ok && p.Running`, one arm on `Ledger.MatchesStage`'s existing switch) — then gating `lamp_where` empties `lamp`, and the 2026-08-02 empty-destination prune **cascades the "Do you have a task for me?" row off `root` for free**. ⚑ Audit all seven quest NPCs, but **do not gate pure lore** (Emberkeeper directions, Wanderer roads) — the rule is *a row that answers a question only a running quest asks*. **Item 3 — auto-walk + stop on focus loss**: today the game has **the bug and not the feature**. ⚑ `Key.isDown` is cleared only by `keyup`, which never arrives when the window loses focus, so held movement keeps streaming to the server; **there is no `blur`/`visibilitychange` handler anywhere in `input-system/`** (the client's only two are `Audio.ts` and `Chat.ts`). A `ResetKey` helper already exists ⇒ sweep the keys, **do not zero the movement vector** — `Controls`' deliberate stop-tail is fed by the held-key state, and zeroing the vector alone leaves the keys down and the tail un-armed. The two halves ship separately; *"unless auto-walk is enabled"* is one condition added later. Auto-walk itself still needs a keybinding, a cancel rule, and a mobile answer (the joystick has no held state to inherit).
- **Step 8a: accounts & persistence — CODE-COMPLETE** (`plan-accounts-frontend.md` §10a). Chunks 0 → 1a → 1b → 1c → 2 → 3 → 4 all shipped, plus the campfire-bind and two-tab bugfixes — see the banner at the top. ⚑ **What is left is §8’s OPERATIONAL work, and it was never chunked:** backups with a *proven* restore, off-machine storage, and the live-server provisioning half of chunk 0 — arguably runbook work rather than a code chunk, and **deciding which is what "is step 8 done?" needs**, since that exact ambiguity is why the step was split. Still open and deliberately outside 8a: the **sacrifice transaction** (§3 — persistence’s first real consumer, the mechanic does not exist in Go yet), **`bloodline_unlocks`** writes, avatar/faction defaults (blocked on `plan-avatar-system.md`), **backlog §32** (does a charge survive death) and **§41** (discovery scope), plus the reset plan’s own five, **backlog §46–§48** from the two-tab fix and **§51** from the save-writer fix. `plan-playtest-deploy.md` §Ops & security posture is a required input. ⚑ The scoped-filler queue is EMPTY.
- **Prior: the hygiene planning session** ✅ `ec0732e9` 2026-07-28 (docs only) — 4 PO decisions D1–D4 · 6 landmines L-H1–L-H6, and the §9.1 two-session split that made session 1's byte-identity claim a live check. Full ledger `docs/archive/plan-pre-accounts-hygiene.md`.
- **Step 8 detail (the bullet above is the pointer).** ⭐ **`plan-entity-model.md` IS COMPLETE — 1a ✅ → 1b ✅ → 2 ✅ → 3a ✅ → 3b-i ✅ → 3b-ii ✅** — which satisfies PO ruling 7 (*all three chunks, then step 8*). **R4 shipped 2026-07-29 and the doc is ARCHIVED** (`docs/archive/plan-entity-model.md`) — nothing in it is open. Its **§10 questions 2 and 8 are now closed too** (crit stays players-only; the companion aggro radius is the authored 3.5), leaving only the deliberately-out-of-scope ones — `Autoattack` as content, and the journal — plus the R3 latent-trap set in **§8b**, which arms the day quest-style content is authored. ⚑ **L2 is CLOSED — `docs/archive/plan-faction-flips.md` chunk 1 shipped 2026-07-28** (see Last completed). `SetFaction` no longer exists; `Align()` / `RevertFaction()` / `EnlistUnder()` are its three defined successors, and a reflection tombstone guards the general-setter shape from returning. **`plan-faction-flips.md` is COMPLETE and ARCHIVED** — chunk 2 (calm) and chunk 3 (charm) both shipped 2026-07-29, `3b1b3ef6` gave all three skills in-world teachers (Hermit @10 · Emberkeeper @15), and the 2026-07-29 sweep closed its last two design questions. It hands exactly one thing forward: **`backlog.md` §39**. ⚑ **Carry these three habits forward:** never assert on `Derived` (`recomputeDerived()` runs regardless of owner type, so a mob equipping Hardy has `Derived.MaxHealthBonus` correctly populated while the behaviour is absent — pin on HP pool / damage taken / distance moved); verify in the **sim harness** AND with **boot `-content ../api` against the pinned counts**, because `sim/world.go` feeds `NewMob` synthetic inline definitions and never loads authored content, so anything loader-side is invisible to sim byte-identity; and treat a **lost WebGL context as an INVALID harness run**, not a failure (backlog §29). **Then step 8 (accounts & persistence)** — ruling 4 (*only live players persist*) has largely de-risked it: mobs and NPCs always respawn from definitions, so the schema is barely constrained by the entity model and mainly needs the character record written in **Actor vocabulary**. Still open it as a **design session, not a schema session**, and ride **backlog §32** into it: *does a charge survive death?* is a persistence question and the one most likely to want schema room (recommendation on record: charges never transferable, which sidesteps the economy-creep problem the GDD puts outside v1). Then the **character-sacrifice loop** (triage item 10) as persistence's first consumer, and `plan-playtest-deploy.md` §Ops & security posture folded in — persistence is the security tipping point. **⭐ BOTH chunks approved to run before that design session are now DONE** (PO 2026-07-26): ① §29 option A ✅ `6c8bde2e` and ② **mob soft separation ✅ chunk B** `8b045395` (**PO-verified in-game 2026-07-26**; ① signed off 2026-07-29), neither blocks the design session. ① went first deliberately, and it paid off: a blank world now labels itself instead of reading as a regression in ②. **② is now fully CLOSED (PO 2026-07-29):** the stopped-mob limitation is **accepted** (a settled ring keeps its arrival spacing) and **`mobSeparationWeight` 0.45 stands as the value**, not merely as an unchallenged placeholder; the tangential settle nudge is **withdrawn, not deferred**. **Two more round-6 rulings 2026-07-26:** the **orc-grunt fix is the PO's, taken in the zone editor** (option B, move `wave-mouth` inside the grunt's 5.4 `aggroRadius` — currently 7.57; **no code chunk**, A and C not implemented), and **Pass 1a.2 (resource costs) now runs AFTER the entity design session, not before** — its cost-reduction passive would be the **sixth `validStat`** while **three of the current five are player-only** (§31 gap 1), and it is the first that modifies an *input*, so gap 1's ruling is an input to it. **Target stickiness is RULED, not merely unscheduled** (PO 2026-07-29): `selectTargets` keeps re-picking ~30×/s and **stays that way** — the re-open trigger is a **measured performance problem**, not feel. ⚑ Its behavioural half is unchanged and worth recognising if it is ever reported: a `maxTargets: 1` aura smears damage across a clump instead of killing anything (`plan-playtest-feedback.md` §Intake round 6 item 4). PO decisions on record: **2026-07-26 round 6 — the resource's action-economy/survivability duality is INTENTIONAL with a permanently free base damage aura; collision = SOFT SEPARATION, hard collision and player↔player both rejected; 3 grunt options sanctioned**; **2026-07-26 — `RallyDrummer` pacifism ACCEPTED, tooltip whole-point HP rounding SIGNED OFF**; round-3 2026-07-25 — no universal auto-attack (parked in §31), support set = Heal+Shield, pacifist healers ignore their attacker, survivors-like fork **CLOSED — stays MMO-lite**; round-4 2026-07-25 — tooltips show **absolute numbers**, and 2026-07-26 — add a real JS test runner rather than skip the frontend guard.
- **Then: step 8 — accounts & persistence** (roadmap item 3, planning session), then the character-sacrifice loop (triage item 10) as persistence's first consumer — extra-motivated since the live server wipes characters on every restart. **Backlog §31 is no longer a fold-in — it is `docs/archive/plan-entity-model.md` and it runs BEFORE this** (PO 2026-07-26). What survives into step 8 from it: the character record must be written in **Actor vocabulary** (`level`/`faction`/`loadout`/`spellbook`/`xp`), and ruling 4 fixes the scope — **only live players persist**, mobs and NPCs always respawn from definitions. **Also fold in backlog §32 (consumable cooldowns / spellbook charges)** — "does a charge survive death" is a persistence question, and it's the idea most likely to want schema room. **And backlog §36 (3 character slots, 3 bloodlines)** — PO idea 2026-07-29, a direct amendment to GDD §5's *account-wide* sacrifice reward; ⚑ it adds a **third persistence scope** (account-scoped state that outlives every character in a slot), so the top-level persisted record would be an **account**, not a character, from the first migration. **Fold in `plan-playtest-deploy.md` §Ops & security posture** when planning it — persistence is the security tipping point (cloud firewall, DB bound to localhost, daily backup + proven restore, DB-credential handling). Expect ad-hoc live-playtest triage in parallel. **Deferred:** step-8 audio half (combat SFX, PO-deferred 2026-07-21 — no placeholder assets); UI-polish later passes (`plan-ui-polish.md` §Deferred — popups ride the in-game announcement system); playtest-1 design rounds (`plan-playtest1-feedback.md` §Own planning rounds); the full Deferred/placeholder catalog lives in the respective plan docs.
- **Open PO calls — ✅ NONE OUTSTANDING as of the 2026-07-29 sweep.** The three long-standing ones are ruled: **replacement art** (mascot/splash/favicon) and the **eventual domain** are both **PARKED until v1** — neither blocks anything, berryhunter.io URLs and `aura-game.duckdns.org` keep working — and the **wiki generator is KEPT** (left in place unmaintained; revisit only if it becomes useful for player-facing content docs). PO continues manual zone-editor placements in parallel.
- **Code-health findings** (`docs/backlog.md` §27 / `docs/research-code-quality.md` §10) — the three called out 2026-07-24 are all **✅ FIXED test-first**: ① `MobSystem.Update` mutation-during-iteration (skipped/double-updated a survivor per dead mob/tick — collect dead in the loop, remove after; `f6fcfbad`, §27.1); ② drop-RNG determinism (**PO-ruled bug** — `NewMob` seeded per-mob RNG from the entity ID alone so a fresh server re-rolled the same HP variance + first drop for the Nth spawn every restart; now a per-process salt mixed with the ID randomizes per run while sim/guardrails stay deterministic; `b4b0e66d`, §27.2.2); ③ `definition.go` guard coverage evened out — `mapToEffectDef` `default:` + inert-config/radius guards (`eee10331`, §27.3). ④ §27.2.1 EntityType name-fallback (**was a live-server crash at first spawn**) validated at load now — `mobs.ResolveEntityType` + loader guard + `NewMob` panic (`c3938be7`, see Last completed). **§26 dead resource/decay prune ✅ FULLY DONE `ee9d42e9`+`a2ab90b5`** (Chunks 1+2) — emptied the item registry to `None`. **§28 item-system removal ✅ FULLY DONE 2026-07-24** (`docs/archive/plan-item-system-removal.md`, 3 chunks): C1 backend registry `b9d01d33` · C2 frontend scaffolding `2f933634` · C3 wire-enum prune `8ed4ff4c` (see Last completed) — **the wire enums now carry explicit, permanently-pinned values; a future removal is a one-line delete that leaves a gap.** **2026-07-24 triage session ✅ `f095514a`+`2ec03ee7`** (see Last completed): §30 items 2/3/4, §27.3.3, §27.2.3, §25 B and §25 A#4 all closed. **⭐ §29 DIAGNOSED 2026-07-26 + option A (detect + warn) ✅ SHIPPED 2026-07-26 `6c8bde2e`** (ledger §29.2.2 — a `[webgl] world context lost` log + red banner; **detection only, the trigger is still unknown and rendering is still dead, so §29 stays open**; ⚑ if a smoke run goes blank, look for that log line first — its absence now means the cause is NOT a context loss). **The diagnosis: it is a LOST WEBGL CONTEXT, and the `null.split` is PixiJS's error *reporter* crashing on the way to reporting it** (`docs/backlog.md` §29.1): on a lost context every WebGL getter returns `null`, so `generateProgram` reads `LINK_STATUS` as null, concludes the program failed, and `logPrettyShaderError` dies on `gl.getShaderSource(shader).split("\n")` — destroying the real diagnostic, which is why 4 sightings gave no lead. The throw escapes the rAF callback ⇒ **the render loop stops** ⇒ blank world with a healthy HUD, websocket and server ticks. **Reproduced deterministically** via `.claude/skills/verify/ctxloss-repro.mjs`: a mid-boot loss gives **exactly the observed three errors** (count = how many shader programs the dying frame still had to build; 1 in steady state). ⚑ **Two old leads are now wrong:** the 40 000 ms frame time was a *consequence* (a dead ticker), not starvation — so CPU throttling is the wrong lever (24 throttled cold loads, incl. fresh-`aurad`-restart runs, all clean; worst real frame gap 902 ms); and **the scene-graph probe cannot see this** (3/24 children before and after the world vanishes), so "errors separable from the black world" is unsafe — only pixels detect it. Also normal-and-not-suspicious: the client makes 5 GL contexts at boot and **pixi deliberately loses 2** (capability probes). PixiJS only auto-restores losses *it* forced, so a real loss is never restored; a hand `restoreContext()` revived the minimap (its own Application) but not the world. **5th sighting landed organically in the same session** on the restored *minified* bundle, unthrottled: 3 errors, identical stack, all at t≈0.9 s, worst rAF gap 420 ms — and **the minimap rendered while the world did not** (own `Application`, own context), which is the fastest visual tell. Trigger (what drops the context) still unknown; all 5 sightings are headless runs, none player-facing ⇒ it is a **harness-reliability problem first** — ~1 in 6 smoke runs can lose to it and it looks like the change under test. **No fix applied — 5 options + a recommendation in §29.2 (A: detect + warn, ~20 lines) await a PO call.** **⭐ §31 — one entity, many roles: ✅ DESIGNED 2026-07-26 → `docs/archive/plan-entity-model.md`** (5 chunks, scheduled ahead of step 8 — see the ⭐ banner at the top for the reframe, the 7 rulings and the landmines). §31 itself **stays the findings record** and is still the place to read *why*: 3 of 5 derived stats applied only in player code paths (latent — 0 mob defs equip any of the 5, re-verified by effect **type** not name), `model/npc` statless so every stat-bearing "NPC" is really a mob, and gap 5's role-as-a-type in AI (✅ mostly shipped in round 3, `03b152f4`). **Both former blockers are now ruled:** mob passives scale **identically**, and the NPC question is answered by the merge. **⚠ Its collision with step 8 is resolved, not deferred** — ruling 4 (*only live players persist*) means the schema is barely constrained; it just needs Actor vocabulary. **⭐ NEW §34 — entity-vs-entity collision: considered, NOT taken** (round 6, see the ⭐ bullet above). Records why hard collision is only a *mask* edit (the broadphase already pairs dynamic-vs-dynamic; **there is no client-side collision and no client-side prediction**, so server-side movement changes have no rubber-banding problem), the A/B/C decomposition, and **2 engine landmines if it is ever revisited** — co-located equal-radius circles **never separate** (`resolveCircleThomas` returns a zero vector ⇒ same-species mobs spawned on one point weld together) and push-out is **undamped instantaneous position correction**. Re-opens only if soft separation is overwhelmed at scale, and only for mob↔mob. **⭐ NEW §38 — one species, many levels (a per-SPAWN level override), PO idea 2026-07-29: NOT possible today.** Level is authored per *species* (`MobDefinition.CurveLevel`) and `world.Spawn` carries none, so every Wolf in the world stands at the same level. ⭐ **Entity-model chunk 1b already did the hard half** — `baseMaxHealth` is authored at the **baseline** and `MaxHealth`/`PowerScale` derive from `Level()` **live**, so HP and damage would scale off a per-instance level for free. ⚑ **The real costs are elsewhere:** the nameplate and its level tint read `curveLevel` from the **catalog** (per species, static), so this needs a **wire field** and a client change; and **XP does not scale** (`factors.experience` is flat per species), which collides head-on with the Session-⑥ XP band lock. 5 open questions recorded — absolute vs offset vs per-zone band, whether XP must move, the CC axis (skill levels are separate and do **not** scale), sim-harness visibility, and whether the wolf family then becomes levels of one species. **⭐ §27.3 is now FULLY CLOSED** — findings 1–4 and 6 all fixed (§27.3.4 + §27.3.6 shipped 2026-07-31, see Last completed), and 5 is the one explicitly ruled not worth acting on. **§27.2 keeps 4–7 open, all watch items** — ⚑ and **§27.2.6 needs re-surveying before anyone acts on it**: entity-model 1b deleted `MobDefinition.PowerScale` and R5 re-shaped `SummonPower` into an authored rate, so the finding names read sites that have moved. **Still open, unscheduled:** §25 **D + E only** (⚑ A#1 + A#3 and §27.2.8 shipped 2026-07-31, and **A#2 is SUPERSEDED — §33's ruling the same day means heal and hot no longer share a predicate, so the hot-side copy was deleted rather than deduped**; the "C/D" in older banners was stale — C, the `applySlowAura` faction gap, was FIXED 2026-07-25), and **round-6 item 4** (`selectTargets` has no target persistence — real defect, PO-ruled to stay unfixed, re-opens on measured cost; `plan-playtest-feedback.md` §Intake round 6). **§30 is FULLY DONE** — item 1 (`Resource.capacity`/`stock`) closed 2026-07-28 as hygiene H4 `c183ce12`; the "§30 item 1 still open" pointer in older banners was drift. **§35 is FULLY DONE** — C1 + C2 ✅ `e7531444` 2026-07-29 · C3 + C4 ✅ 2026-07-30 (ledgers `docs/archive/plan-conf-duplication.md` §7, plan archived) — see the ⭐ bullet at the top and Last completed.
- **Standing locks & gotchas:** growth **1.12 × maxLevel 30**; regen **0.00033 ≈1%/s × taper 1.0→0.4** FINAL; §11 no-pity + campfire **0.12** + heal cost **10 % → 2 % of max HP across its 10 levels** FINAL (the numbers rewrite converted the old absolute 10−2/level to a fraction; it was already exactly 10 % of the pool); **the base damage aura stays FREE at every resource level** (round-6 ruling — no cost curve may ever leave a player with no action; GDD §3); drop + milestone tables are **TUNING-OPEN** (milestones: Damage@L1 seeded at creation · **Discipline@L5** · Haste@L7); density = mob visible per ⅔-screen window; downtime 10 s + chain 20; guardrail asserts in `cmd/simharness/guardrail_test.go`. New mobs: inherit the Session-⑥ XP rule (facetank kph, else kite ×0.5) and **must** author tier + baseline (raw `maxHealth` hard-fails). Day/night cycle is **OFF** (`DAY_CYCLE_PRESENTATION_ENABLED=false`, bug unfixed — don't re-enable without collapsing the ~25 per-layer filter passes). Per-chunk placeholder values live in their plan docs. **Dev cheats:** GOD, WARP `<x·120> <y·120>` (1-unit granularity — land on a whole unit), SPEED, XP, SKILL, ANNOUNCE, THREAT, QUEST (dump / `ACCEPT <id>` / `ABANDON <id>` / `ADVANCE <id> <from> <to>`). `make -C backend build` runs `cp-defs` (reverts embedded `backend/pkg/api/` from `api/`); boot `-content ../api` for content iteration (schema/Go changes still need the rebuild).

- **MOB LEVELS C3 — THE EDITOR CAN AUTHOR A LEVEL, AND SAVING NO LONGER DELETES IT** ✅ `0c6eca22` 2026-08-05, headless-verified — **L7 is discharged, which ends the plan's build work: all three chunks are in.** The spawn tool has a *Level* field (blank = inherit) and `getZoneAsJSON` **names** it, so an override survives the save that used to eat it. Measured on the real export path: a Wolf placed at 15 exports `"level": 15`, the same Wolf with a blank field exports **no `level` key at all**, and none of `world.json`'s 485 spawns gained one. ⛑ **The blank-field leg is a SEPARATE assertion from "the level round-trips", and it is the one protecting the other 485 spawns** — both go green with a serializer that writes a default; only absent-stays-absent goes red, and without it one edited spawn becomes a 485-line diff that freezes every inherited level into a copy. ⛑ **`populateSpawnControls` is a SECOND instance of the same silent-loss class**: Update reads the *input*, so a field left blank on selection writes the blank back over the level — the whitelist ate it on save, this would eat it on edit. Own harness leg. ⛑ **The editor refuses FRACTIONS, which the loader's rule does not cover** — `Spawn.Level` is a `*int`, so a `2.5` fails `json.Unmarshal` at boot instead of the friendly `>= 1`. *An input mirroring a validator must also mirror the parser underneath it.* ⛑ **Beyond the plan's letter, vetoable: the map marker says `Wolf L15`** — only when overridden (a bare `Wolf` otherwise, L6); asserted as the pair, 1 × vs 122 ×. ⛑ **Three harness attempts died at SETUP, both causes now in the script header**: **`&textures` mounts the editor, not `&develop`** (a `&develop`-only URL leaves every `#zoneEditor_*` id out of the DOM — reads exactly like "the field was never added"), and `window.game` is the six-member console façade with no `player`/camera. ⛑ **`elementFromPoint` over open world returns the `#inputAreas` overlay, not the canvas**, and **a `page.click` on a control inside a `hidden` group HANGS rather than fails**. ⛑ **The editor's zone JSON is webpack-BUNDLED, not fetched** — a hand-authored level needs `npm run build`, the *opposite* of C2's server-restart probe. **Placement is deliberately NOT in this chunk**: no `api/zones/*.json` changed; roadmap step 2 (the world re-placement pass) owns the content. Verified: `tsc` clean · **vitest 227/227** (2 new, the round-trip pin **proven RED first**) · `go build`/`go vet` clean with **no Go file changed** · boot `-content ../api` 15 factions/87 skills/65 mobs/3 milestones/10 recipes/4 quests/5 props/777 props/485 spawns/5 campfires, **0 panics** · **`c3-zone-editor-level` 7/7, 0 console errors** (NEW, registered in the coverage map) · no other harness, reasoned: no wire, no server, no `.fbs` — **`hygiene-wire-prune` not required**. **Schema impact: DB NONE · FlatBuffers NONE · content JSON NONE.** Full ledger: `docs/plan-mob-levels.md` §11 C3.
